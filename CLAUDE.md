# CLAUDE.md

Godot 4 のゲーム「ポモドーロヒーローズ」。**日本語で答えること。**

このファイルは毎回読まれる。**細かい仕様はここに書かず、下の表のファイルを読むこと。**

---

## 最初に読むもの

| ファイル | 何が書いてあるか | いつ読むか |
|---|---|---|
| `AGENTS.md` | フォルダ構造・命名規則・数値管理・翻訳キー・開発ルール | **毎回。着手前に必ず** |
| `NEXT_STEPS.md` | 次の1タスクだけ。これだけ読めば着手できる | **毎回** |
| `PROJECT_STATUS.md` | 全体の現在地・溜まっている宿題・過去の事故 | タスクの位置づけが要るとき |
| `EXEC_◯◯.md` | 直前タスクの決定事項・完了条件・実施結果 | 同じ領域を触るとき |
| `PLAN_◯◯.md` | 領域ごとの決定事項の台帳 | 仕様を決めるとき |

`docs/` 配下は**指示されたもの以外読まない・書き換えない。**

---

## このプロジェクトで何度も事故った点（最重要）

### 1. ドキュメントの「実装済み」を信じない

**PLANやNEXT_STEPSの記述と実コードのズレが6回続いている。** 「対応済み」と書かれた関数が空実装だった、呼んでいるはずの関数を呼んでいなかった、など。

**必ず `grep` で関数の中身を見てから判断する。** 読んだ結果ドキュメントが間違っていたら、それを報告する（勝手に直さない）。

### 2. 編集したら `grep` で当たったことを確認する

差し替えを当てたつもりで当たっておらず、「戦闘だけ反映されない」で1タスク溶かしたことがある。

**編集直後に `grep -n "<新しい関数名>" <ファイル>` が0件でないことを確認する。**

### 3. `MasterDataLoader` が返す数値は必ず `float`

JSONから読んだ値は `3.0` で来る。**`int()` で包み忘れるとセーブに `"atk": 55.0` と書かれ、戦闘の計算と表示に `.0` が乗る。**

### 4. 状態にマスターデータを複製しない

セーブが持つのは **IDと数字だけ**。性能値はマスターから毎回引き直す。

- 育成の `stats` … レベル由来の素の値だけ。研究・装備の補正は都度合成
- 装備の個体 … `item_id` と `grade` だけ。性能は `equip_stats` × 等級係数で毎回計算

**代償として、リリース後にIDを改名できない**（`item_id` / `recipe_id` / ノードID）。改名すると加算が黙って消える。

### 5. 再描画に `await` を持たせない

1回の操作で2本のシグナルが飛ぶ画面がある。`queue_free()` + `await process_frame` にすると再描画が並走して**行が二重に並ぶ**。

**`remove_child()` してから `queue_free()` する。**

### 6. 状態を変える前に全部の判定を終える

`equip_instance()` / `forge_equipment()` / `start_craft()` / `purchase_shop_item()` は全部この形。**途中で状態を触ってから弾くと壊れる。**

### 7. インデントはタブ

`.gd` はタブ。スペース混在でパースエラーになる。
### 8. 装備の個体は add_to_inventory() だけが作る

新しい入手経路を足すときは、必ず add_to_inventory() を通す。
直接 inventory を書くと装備が個体にならず静かに消える。
grep -n "_create_equipment_instance" autoload/game_manager.gd の
呼び出し元が1箇所であることを保つ。
---

## 触ってよいもの・悪いもの

| 対象 | 可否 |
|---|---|
| `scenes/` `scripts/` `autoload/` `resources/balance/` `localization/ja.csv` | 指示されたファイルのみ可 |
| `addons/` | **禁止** |
| `docs/` | **指示されたもの以外禁止** |
| 新しいフォルダの作成 | **人間の承認が要る** |
| `*.tres` の直接編集 | 生成スクリプト経由。走らせるのは人間 |

**`ja.csv` は UTF-8（BOMなし）。** BOMが付くと1行目が `\ufeffkeys` になり全滅する。編集後はGodotで再インポートが要る（人間の作業）。

---

## 実行環境の制約（2026-08-18・実測で更新）

**Godotはヘッドレスで起動できる。ただし画面は見えない。**

```powershell
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=area
```

- **`Start-Process` ＋ `-RedirectStandardOutput` / `-RedirectStandardError` で実行する。** 直接叩くと出力が1行も返らない（GUIアプリのため）
- **読むときは `[System.IO.File]::ReadAllLines(path, UTF8)`。** `Get-Content` は既定ANSIで日本語が化ける
- **人間が遊んだあとの出力パネルは `user://logs/godot.log` に残る。** 貼ってもらわずに読む。**保持は5本なので、読む前に自分でヘッドレスを走らせないこと**（1本押し出されて消える）
- **`tests/debug_boot.tscn` は編成・スキル枠・ステージ・撃つ合図まで自動で走る**（1回13秒）。**シナリオは `SCENARIOS` に1行足す。シーンを増やさない**

**できないこと**

- **画面の絵は取れない。** ヘッドレスは描画がダミーで、`--write-movie` はアクセス違反で落ちる
- **表示・色・ボタンの活性・レイアウトは人間しか見られない**
- `.tres` の再生成・`ja.csv` の再インポート・窓ありでの操作は**人間に依頼する**

**書き方**

- 「動きました」と書かない。**取れたものだけを根拠にする**
- 完了条件は「ログ」「ファイル」「画面」の3つに分けて書く。**同じことを2箇所に書かない**（誰が取るかは `AGENTS.md`「誰が取るか」）
- **人間に渡す前に、ヘッドレスで事前チェックを1回通す。** ロード時の赤・黄はそこで潰す
- UIから到達できない項目は人間の確認項目にしない（別枠に「将来コードを変えたときに見る項目」として書く）

---

## 止まる条件

- 1つの症状に対して**2手まで**。3手目に進まず報告する
- **1ファイルへの書き込みが2回失敗したら中止する**（方法を変えても回数に数える）
- 切り分けのために本番コードを書き換えない
- 原因を「環境の問題」と結論づけない。**観測した事実だけ報告する**
- 同じ箇所を3回以上直すことになったら、実装を止めて設計を見直す

---

## 仕事の進め方

- **1タスク = 1つの通し。** 「ついでにこれも」をやらない。宿題は `PROJECT_STATUS.md` に足す
- 大きい変更は**先に方針を出して合意を取る**。合意なしにファイルを書き換えない
- 検証用のもの（`debug_instant`・0Gスロット・`weapon_debug_blade`）は**リリース前に消す前提**。増やしたら宿題に書く
- コミットメッセージは日本語。`feat(equipment): 武器スロット・装備の着脱・戦闘への反映` の形
