class_name DungeonConfig
extends Resource

# 難ダンジョン（段階17-a・PLAN_HARD_DUNGEON.md）のバランス数値。
#
# Balance に登録し、Balance.dungeon.bag_initial_slots のように参照する。
#
# 【なぜ FloorConfig と分けたか】
# FloorConfig はシナリオ（floor_1..5）のつまみ。難ダンジョンは別枠の遊びで、
# 台帳も別（PLAN_HARD_DUNGEON.md ／ PLAN_SCENARIO_MAP.md）。同居させると
# 「どちらの数値を触っているのか」が Inspector の1画面から読めなくなる
# （AGENTS.md「1つのConfigに別領域の数値を混ぜない」）。
#
# 【何をここに置き、何を dungeon.json に残すか】
#   ここ           … 全ダンジョン共通の「調整つまみ」。1次元の配列と数値だけ
#   dungeon.json  … ダンジョンごとの「形と中身」。層のノード数・battle_pool・
#                   boss・ノード種ごとの戦利品表・通貨の額
# ⚠ 同じ数値を2箇所に書かない。迷ったら「ダンジョンを1本増やしたときに
#   書き足すことになるか」で決める。書き足すなら dungeon.json。
#
# ⚠⚠ ここの数値は全部「仮置き」（決定14・§6-B）。
#   ⚠ 遊ぶ前に当てられるものは1つも無い。遊んでから測って直す（段階17-g）。

# --- 層のノード出現比（段階17-a） ---
#
# ⚠ 添字が層番号 − 1。⚠ 配列の長さがそのまま層数になる。
# ⚠ 4本とも同じ長さにすること（E133 が見張る）。
# ⚠ shop の欄は無い。ショップはボスを倒した先だけ（決定15・§5-0）。
#   ⚠ 層に shop を置かないこと。置くと「潜るか降りるか」の決断の場所が2つになる。
#
# 層1 … 入口。必ず戦闘（合流点が1ノードなので、ここで分岐は作れない）
# ⚠ rest を厚くしすぎないこと。休憩は「その層の戦闘・レリック・宝箱を諦める」
#   ことがコスト（§4-9-1）なので、選び放題だとコストが消える。
#
# ⚠⚠ 段階20-a で 8 本 → 25 本に伸ばした（⚠ 1階＝25層。⚠ 人間の決定26）。⚠ 全部仮置き。
#   ⚠ 長さを変えるときは dungeon.json の layers も同じ長さにすること
#     （⚠ 揃っていなくても clampi で末尾に落ちるので黙って動く＝W31 が鳴く）。
# ⚠⚠ 段階19-b で chest の1本を足した（4本目）。⚠ 足すときは
#   get_dungeon_layer_weights() / _dungeon_loot_chance_pct() / E133 の3箇所も一緒に。
#
# ⚠ 5層ずつの区切りで書いてある（⚠ 台帳の「区画＝5層」に合わせた並び）。
#   ⚠ 地帯（バイオーム）を入れるときは、⚠ この並びを地帯ごとにまとめ直すことになる。
# ⚠ 層1 … 入口。必ず戦闘（⚠ 合流点が1ノードなので、⚠ ここで分岐は作れない）。
@export var layer_weight_battle: Array[int] = [
	100, 60, 55, 55, 50,
	55, 50, 50, 55, 50,
	50, 50, 55, 50, 50,
	50, 55, 50, 50, 50,
	55, 50, 50, 55, 55,
]
@export var layer_weight_relic: Array[int] = [
	0, 20, 20, 15, 20,
	15, 20, 15, 20, 15,
	20, 15, 20, 15, 20,
	15, 20, 15, 20, 15,
	15, 20, 15, 15, 15,
]
@export var layer_weight_rest: Array[int] = [
	0, 5, 10, 15, 15,
	15, 15, 20, 10, 20,
	15, 20, 10, 20, 15,
	20, 10, 20, 15, 20,
	15, 15, 20, 15, 15,
]
@export var layer_weight_chest: Array[int] = [
	0, 15, 15, 15, 15,
	15, 15, 15, 15, 15,
	15, 15, 15, 15, 15,
	15, 15, 15, 15, 15,
	15, 15, 15, 15, 15,
]

# --- 分岐の密度（段階19-d） ---
#
# ⚠ 上の層の1ノードから、下の層の「持ち分の窓」の外へ何個ぶん伸ばすか。
#   ⚠ 0 … 一本道になる（分岐が消える）
#   ⚠ 1 … 2択（⚠ 19-d より前の実装がこれ。⚠ 既定）
#   ⚠ 2 … 3択（⚠ 全ルート数が跳ね上がる。⚠ scenario=dungeon の総当たりが重くなる）
# ⚠⚠ 接続そのものは決め打ちのまま（乱数を入れない）。⚠ 乱数にすると
#   「ボスに着かないルート」が低確率で生まれ、⚠ 再現できない事故になる。
@export var branch_spread: int = 1

## 1つのマスから出る通路の上限（段階19-f）。
##
## ⚠⚠ 人間の指摘「⚠ そんなに入り組ませないでほしい　ルートを」。
##   ⚠ branch_spread だけだと、⚠ 上の層より下の層のマスが多いときに
##     1マスから3本も4本も出て、⚠ 線が交差して読めなくなる。
## ⚠ 1 にすると一本道になる（⚠ branch_spread と同じ副作用）。
## ⚠⚠ 到達性のほうが優先。⚠ この上限で切った結果「入ってくる線が0本のマス」が
##   出たときは、⚠ 上限を超えてでも1本足す（_connect_dungeon_layers）。
@export var max_edges_per_node: int = 2

# --- 区画（合流しないエリア。段階20-b・人間の決定28） ---
#
# ⚠⚠ 人間の裁き：「⚠ 1区間は5層まで　⚠ 1回に3個　⚠ 入り口は3　⚠ 中で分岐してもいい」。
#
# ⚠⚠ 「合流しない」のは**区画と区画のあいだ**。⚠ 区画の中では合流してよい
#   （⚠ 外の台帳 §3-2 の図がそうなっている：⚠ 入口1・出口1の箱で、中は分岐して合流する）。
#   ⚠ 一度入った区画から隣の区画へは移れない＝⚠ 5層ぶんをまとめて賭ける重い判断。
# ⚠ 区画の入口と出口はそれぞれ1ノード。⚠ 外との接続は2点だけ。

## 1つの区画が何層か（⚠ 入口1層 ＋ 中 ＋ 出口1層）。⚠ 人間の裁き＝5。
## ⚠ 3 未満にすると「中」が無くなり、⚠ 分岐できる場所が消える。
@export var segment_layers: int = 5

## 1階にいくつ区画を置くか。⚠ 人間の裁き＝3。
## ⚠ 0 にすると区画が消えて、⚠ 全部が合流ありの層になる（＝19-f までの形）。
## ⚠ segment_layers × ここ が層の総数を超えると、⚠ 入り切らないぶんは置かれない（W34）。
@export var segment_count: int = 3

## 区画の入口で何択にするか。⚠ 人間の裁き＝3。
## ⚠⚠ ここだけ max_edges_per_node を超える（⚠ 区画を選ぶのが一番重い判断だから）。
@export var segment_choices: int = 3

## 区画の「中」の層に何ノード置くか。⚠ 中で分岐してよい（人間の裁き）。
## ⚠ 1 にすると区画の中が一本道になる。
@export var segment_inner_nodes: int = 2

# --- 通路の効果（段階19-c-2・人間の決定24・台帳 §5-3-1） ---
#
# ⚠ 何が出るか（罠3通り・宝箱・資源の重み）は dungeon.json の `edges`。
#   ⚠ ここに置くのは「どれくらいの通路に効果が付くか」と「どれだけ削るか」だけ。
# ⚠⚠ 逃げ道は保証しない（⚠ 人間の決定：「全部ペナルティもあり」）。
#   ⚠ 「分岐に1本は無害を混ぜる」処理を足さないこと。

## 効果が付く通路の割合（％）。⚠ 5本に1本＝20（人間の決定。⚠ 仮置き）。
## ⚠ 100 にすると全部の通路に何か起きる＝⚠ 差が読めず「選ぶ」ことにならない。
## ⚠ 0 にすると通路が全部ただの線に戻る（⚠ W33 が鳴く）。
@export var edge_effect_chance_pct: int = 20

## 罠（HP）で、戦闘時 MAX HP を素の MAX HP の何％ぶん削るか。
## ⚠ 基準は「素の MAX HP」（⚠ ポーション・蘇生と同じ流儀＝§4-3 / 決定13）。
## ⚠⚠ 罠では脱落させない（⚠ 最低1は残す）。⚠ 死ぬのは戦闘だけ（§4-4-2）。
##   ⚠ 見えない通路を通っただけで全ロストになるのは、⚠ 一貫原則ではなく理不尽。
@export var edge_trap_hp_pct: int = 12

## 罠（通貨）で失う一時通貨。⚠ 0 未満にはならない（add_dungeon_currency が守る）。
@export var edge_trap_currency: int = 20

## 罠（鞄）で落とす個数。⚠ 鞄が空なら何も起きない。
@export var edge_trap_bag_count: int = 1

## 資源の通路で拾う一時通貨。⚠ 素材のほうは dungeon.json の表から引く。
@export var edge_resource_currency: int = 18

# --- たいまつの目盛り（段階19-c-2・人間の決定25・台帳 §5-3-2） ---
#
# ⚠⚠ 等級0 を「何も見えない」にした。⚠ ランは等級1 を持って始まる。
#   ⚠ 実際に遊ぶ範囲の数字は1つも動いていない（⚠ 開始1層先／買える3回／上限5層先）。
#   ⚠ 入れた理由は台帳 §5-3-2 の2つ（⚠ 罠を避ける経路を組む価値 ／ 将来の落ちる先）。

## ランに入るとき最初から持っているたいまつの等級。
## ⚠ 0 にすると入口から何も見えない（⚠ 店はボスの先にしか無いので、フロア1が全部手探りになる）。
@export var torch_initial_grade: int = 1

# --- 戦利品（§5-2・決定：宝箱は「ノード種」に紐づける） ---
#
# ⚠⚠ シナリオ側（FloorConfig.chest_chance_pct）と作りが違う。あちらは「移動」に
#   紐づくので、層構造だと歩数がどのルートでも同じ＝どの分岐を選んでも報酬の総量が
#   動かない（FLOOR_GAMEPLAY_CURRENT.md §2-B・実測 1.99 個/周）。
#   ⚠ こちらは最初からノード種に紐づける。⚠ 移動に紐づけ直さないこと。
#     戻すと休憩場所のコストも、たいまつを買う理由も同時に効かなくなる。
#
# ⚠ 添字はノード種ではなく「そのノードで抽選する確率（％）」。
#   実際に何が出るかは dungeon.json の loot[kind]。
@export var loot_chance_battle_pct: int = 100
@export var loot_chance_relic_pct: int = 0
@export var loot_chance_rest_pct: int = 0
@export var loot_chance_boss_pct: int = 100

## 宝箱のマスで戦利品を引く確率（％。段階19-b）。
## ⚠ 100 以外にしないこと。⚠ 「開けたのに空だった」は、⚠ 一貫原則の
##   「安全は犠牲でしか買えない」ではなく、⚠ ただの理不尽になる
##   （⚠ Roguebook の知見「不公平だと感じると離脱する」）。⚠ E137 が 0 を見張る。
@export var loot_chance_chest_pct: int = 100

# --- 鞄（§4-1） ---
#
# ⚠ 個数制限方式（コンセプト文書）。ポーションも鍵も戦利品も一律1枠。
#   ⚠ 重み付けを入れないこと（タルコフの煩雑さを持ち込まないという決定）。
# ⚠ 溢れたぶんは拾えない（＝床に置く）。⚠ 自動で捨てる先を作らないこと。

## 鞄の初期枠。⚠ 8 は仮置き（未決5。コンセプト文書自身が「テストプレイで実測」と書いている）。
@export var bag_initial_slots: int = 8

# --- 1ラン（決定15・§5-0） ---
#
# ⚠⚠ 段階20-a：⚠ 人間の決定26「1ラン ＝ 3階 × 25層 ＝ 75層」。
#   ⚠ 未決1-c（フロアは何枚まで潜れるか）が閉じた。⚠ 上限なしをやめた。

## 1ランで潜れる階の数（＝フロアの枚数）。⚠ 3階＝75層（決定26）。
## ⚠ 0 以下にすると1階も潜れない。⚠ 大きくすると1ランが長くなりすぎる
##   （⚠ 台帳の指摘：⚠ 「2時間潜って全部失う」が離脱に直撃する）。
@export var max_floors: int = 3

## フロアが1枚深くなるごとに、一時通貨の入手が何％増えるか。
## ⚠ 「深く潜る＝より良い戦利品」（§2 の表）を数値にしたもの。
@export var currency_growth_pct_per_floor: int = 25

# --- ポーションと休憩（段階17-c・§4-3 / §4-9・決定19） ---
#
# ⚠⚠ 基準は全部「素の MAX HP」（`get_effective_stats().hp`）。
#   ⚠ 「戦闘時 MAX HP」を基準にしないこと。⚠ 削れているほど戻る量も減って坂が急になり、
#     脱落したキャラは 0 の何％でも 0 のままで永久に戻らない（決定13 と同じ理由）。
# ⚠ 効き方は GameManager の1本（use_dungeon_item / apply_dungeon_rest）だけが読む。
#   ⚠ 画面側（17-d）でここを読んで計算し直さないこと。

## 回復ポーション1個で、戦闘時 MAX HP を素の MAX HP の何％ぶん戻すか。
## ⚠ 決定19（HP回復と MAX HP回復を1件に統合した）の 30%。⚠ 仮置き。
## ⚠ 素の MAX HP を超えては戻らない。
@export var potion_heal_pct: int = 30

## 蘇生したとき、戦闘時 MAX HP を素の MAX HP の何％にするか（決定13）。
@export var revive_max_hp_pct: int = 50

## 蘇生したときの HP を、上の戦闘時 MAX HP の何％にするか（決定13。素の 25% になる）。
## ⚠ ここだけ HP ＜ 上限になる。⚠ 復帰直後は弱者狙いAI（§4-5）に狙われる位置＝意図どおり。
@export var revive_hp_pct: int = 50

## 休憩ノードで、戦闘時 MAX HP を素の MAX HP の何％ぶん戻すか。
## ⚠ ポーションより強くしてある。⚠ 休憩は「その層の戦闘・レリック・戦利品を諦める」のがコスト（§4-9-1）。
@export var rest_heal_pct: int = 40

## 休憩ノードで、脱落したキャラを何人まで戻すか（§4-9 の蘇生2本目）。
## ⚠ 0 にすると休憩から蘇生の口が消える。⚠ 消すならポーション以外の蘇生手段が無くなる。
@export var rest_revive_count: int = 1

# --- たいまつ（§4-7・段階17-e） ---
#
# ⚠ 読む側（GameManager.get_dungeon_reveal_layers / is_dungeon_node_revealed）と
#   同じ回に入れた（AGENTS.md「欄だけ足して実装しない」）。
# ⚠⚠ 値段は dungeon.json の shop に書く（⚠ 表＝JSON・つまみ＝Config）。
#   ⚠ ここに値段を置かないこと（⚠ シナリオ側の FloorConfig.torch_prices とは作りが違う）。

## たいまつの等級ごとに「何層先まで中身が見えるか」。
## ⚠ 添字が等級（0 から）。⚠ 配列の長さ − 1 がそのまま上限の等級。
## ⚠ シナリオ側（FloorConfig.torch_reveal_layers）と同じ数字だが、⚠ 別の欄にしてある
##   （⚠ 器が別。⚠ 片方を調整したときにもう片方が黙って動かないため）。
## ⚠⚠ 段階19-c-2 で先頭に 0 を足した（⚠ 等級0＝何も見えない・決定25）。
##   ⚠ ランは torch_initial_grade（＝1）で始まるので、⚠ 開始時に見えるのは1層先のまま。
@export var torch_reveal_layers: Array[int] = [0, 1, 2, 3, 5]

# --- レリック（段階17-e-2・人間の決定：⚠ 表はシナリオ側と共有する） ---
#
# ⚠ 中身（`relics.json` の12件）は共有する。⚠ ここに置くのは「何件から選ぶか」だけ。
# ⚠ 候補はノードごとに固定（⚠ 種で引く。⚠ GameManager.get_dungeon_relic_choices）。

## レリックのマスで何件から選ぶか。
@export var relic_choice_count: int = 3
