# EXEC — **素材の4段階＋装備の等級10**

前提は `docs/NEXT_STEPS.md` §1-1（候補「作業場の作り直し」から順番が変わった経緯は §0）。
直前の回は `docs/02_exec/EXEC_STATUS_UI.md`。決定台帳は `docs/GAME_DESIGN.md` 2章「素材はすべて4段階で揃える」・6-2・6-3。

⚠ **この回は戦闘を1行も触らない。** ⚠ **触るのは素材のIDと、等級の上限と、そこにぶら下がる入手経路だけ。**
⚠ **人間が画面で見て変わるのは「素材の欄が3行から12行になること」と「等級が3で止まらなくなること」の2つ。**

---

## 0. 人間が決めたこと（**本文と矛盾する場合はこちらが優先**・2026-08-22）

### 0-0. ⚠ 順番が変わった経緯

| # | 決めたこと | 内容 |
|---|---|---|
| **1** | ⚠ **作業場は後回し** | ⚠ **設計役の推奨は「作業場の作り直し」だったが、⚠ 人間が「装飾を先にやる」＋「全素材を4段階に」を選んだ結果、⚠ 3つの通しに割れた**：⚠ **①素材の4段階 → ②装飾システム → ③作業場の廃止** |
| **2** | ⚠ **今回は①だけ** | ⚠ **装飾素材が4段階なので、⚠ 装飾より先に段階を入れないと後からIDを改名することになる**（⚠ **`CLAUDE.md` 4番と同型の事故**） |
| **3** | ⚠ **作業場が来たときは「廃止だけ」** | ⚠ **中間素材も作らない。⚠ `recipes.json` は空になる**（⚠ **③の回の決定。⚠ 今回は実行しない**） |
| **4** | ⚠ **廃止で落ちるキューは何もしない** | ⚠ **`_normalize_crafting_queue()` が黙って落とすまま**（⚠ **③の回の決定。⚠ 今回は実行しない**） |

### 0-1. ⚠ この回の決定

| # | 決めたこと | 内容 |
|---|---|---|
| **A** | **系統の数** | ⚠ **既存の3系統だけ＝12件。⚠ 鍛冶・建築・育成を各①〜④へ。⚠ 装飾素材は足さない**（⚠ **使い道が0の素材を作らない。⚠ ID規則がここで確定するので、②の回で足しても改名は起きない**） |
| **B** | **ID** | ⚠ **全部 `_1`〜`_4` に揃える。⚠ 既存2件を改名する**（⚠ **未リリースなので改名できるのは今だけ**） |
| **C** | **使い分け** | ⚠ **等級10化も今回一緒にやる**（⚠ **`MAX_EQUIPMENT_GRADE` を 3 → 10。⚠ 「等級帯 → 要求段階」の4分割を本当に成立させる**） |
| **D** | **上位段階の入口** | ⚠ **ステージで段階をずらす ＋ ショップ**（⚠ **下位→上位の変換は作らない。`GAME_DESIGN` 2章**） |
| **E** | ⚠ **鍛冶のコストは式ではなく数値** | ⚠ **設計役の初稿（式1本）は覆された。⚠ 理由は人間の指示「バランスの整える難易度が違う。⚠ 1個だけ直したいときに式では書けない」。⚠ 決定5と同じ「配列1本」の形に揃える** |
| **F** | ⚠ **分解は半分しか返さない** | ⚠ **現状は「基礎3 ＋ 上げるのに払った全額」＝ 完全に元に戻る（無損失）。⚠ 人間の指示「ある程度素材は消えるのは当然」。⚠ 返却率は `.tres` に持ち、⚠ 実測で数字1つ直せるようにする** |
| **G** | ⚠ **`EquipmentConfig` を新設する** | ⚠ **設計役の推奨（`CharacterConfig` に間借り）は覆された。⚠ 置き場としては新設が正しい。⚠ 代償として `class_name` の登録と `.tres` の作成・枠の割り当てが人間の作業になる**（§3-C・§7-B） |

---

## 0-2. ⚠ 設計役が自分で決めたもの（**人間が見ていない決め・要確認**）

| # | 決めたこと | なぜそうしたか |
|---|---|---|
| **1** | ⚠ **ID規則は `<系統>_material_<1..4>`** | ⚠ **既存の `forging_material_1` がこの形。⚠ `state_keys.gd:274` のコメントが「リリース後に改名できないため、ここを `forging_material` にしないこと」と名指しで段階を見越している。⚠ 既存の番号付き1件に他2系統を合わせるほうが、⚠ 番号付き1件を改名するより影響が小さい** |
| **2** | ⚠ **翻訳キーは `ui_res_<item_id>` のまま**（12行） | ⚠ **`AGENTS.md`「素材名は `"ui_res_" + material_id` で引く」。⚠ 規則を変えない。⚠ `.gd` を1行も触らずに済む＝Ziva に渡せる**（§9） |
| **3** | ⚠ **～～式1本にする～～ → 覆された。⚠ 数値9個の配列にする**（決定E） | ⚠ **初稿の理由は `game_manager.gd:117-119` の警告「4〜10の必要素材量は…勘で置くと全部やり直しになる」だった。⚠ 人間の判断は「式にすると1個だけ直せない」。⚠ 実測が来たときに直すのが1箇所である点は配列でも同じ**（⚠ **`.tres` の1行**）。⚠ **残り7個が勘であることは式でも数値でも変わらない。⚠ §10 の宿題に残す** |
| **4** | ⚠ **～～`CharacterConfig` に間借りする～～ → 覆された。⚠ `EquipmentConfig` を新設する**（決定G） | ⚠ **初稿の理由は「新しい `class_name` はエディタを1回通すまで登録されず、参照側が Parse Error で落ちる」（前回1タスク溶けた形）。⚠ 人間の判断で置き場を優先した。⚠ 代償として §3-C の順番の制約が生まれる**（⚠ **`balance.gd` の `@export` を書いた瞬間に Autoload が落ちるため、⚠ 実装を前半・後半に割る**） |
| **5** | ⚠ **等級帯→段階の対応は `Array[int]` 1本で持つ**（`forge_material_tier_min_grades = [1, 4, 7, 10]`） | ⚠ **「等級10を4分割（刻みは調整）」（`GAME_DESIGN` 2章）。⚠ 表をJSONに置くと6本目のマスターファイルになる**（⚠ **宿題3で「`summons.json` を分けたのは設計役の判断」と既に指摘されている。⚠ ファイルを増やさない**）。⚠ **鍛冶で使う段階は「上げた先の等級」で引く。⚠ この刻みだと 2〜3→① / 4〜6→② / 7〜9→③ / 10→④ で、⚠ 4段階が全部使われる** |
| **6** | ⚠ **分解の戻りを `int` から `Dictionary` に変える**（`{material_id: count}`）。⚠ **量は決定F により「その段階で払った量 × `dismantle_refund_ratio`」（切り捨て）** | ⚠ **`GAME_DESIGN` 資源表が「装備の分解（⚠ 等級帯で段階が決まる）」と書いている。⚠ 等級7の装備を分解して段階①だけ返すと、⚠ 払った上位素材が消える。⚠ 払った段階ごとに返す**（⚠ **`get_dismantle_refund()` の戻り型が変わる。⚠ 呼び元は `equipment_screen.gd` の1本だけ**）。⚠ **切り捨てにするのは、⚠ 端数を切り上げると「1つ上げてすぐ分解」で素材が増える経路ができるため** |
| **7** | ⚠ **`E118`（赤）を新設**：⚠ **マスターが参照する `material_id` が `items.json` に無い | ⚠ **この回の主目的が改名。⚠ 改名漏れは「加算が黙って消える」形で出る**（`CLAUDE.md` 4番）。⚠ **現状 `MasterDataLoader` は skills / summons / basic attacks しか検証していない。⚠ `stages.json` / `shop.json` / `research.json` / `recipes.json` の素材参照は無検証で、⚠ 12件に増やすこの回が一番危ない** |
| **8** | ⚠ **`debug_boot` に `kind: "report"` を1つ足す**（⚠ **シーンは増やさない**） | ⚠ **この回は戦闘を触らないので、⚠ 既存19シナリオを全部回しても何も見えない。⚠ 「記録が無いものは検証できない」（`NEXT_STEPS`）。⚠ 等級1〜10のコスト・分解の戻り・段階の対応を `print` で出す枝を1つ作る。⚠ `tests/` は `print` が出口（`NEXT_STEPS` §4）** |
| **9** | ⚠ **`initial_state_config.tres` の2行は人間に頼む** | ⚠ **`CLAUDE.md`「`.tres` の直接編集は生成スクリプト経由。⚠ 走らせるのは人間」。⚠ Inspector で2行直すほうが速いので、⚠ 生成スクリプトは作らない**（§7-B） |
| **10** | ⚠ **`stages.json` の段階の割り**：⚠ **`stage_1`＝①のみ ／ `stage_2`＝①＋② ／ `stage_3`＝②中心＋③少量** | ⚠ **決定D「ステージで段階をずらす」。⚠ ステージが3本しか無いので、⚠ ④はステージから落とさない。⚠ ④の入口はショップだけになる**（⚠ **§8 に「ステージが増えたら④を落とす」を残す**） |
| **11** | ⚠ **研究のコストは `construction_material_1` に読み替えるだけ**（⚠ **段階を上げない**） | ⚠ **決定3（作業場の回）で「研究用素材は作らない」と決まっている。⚠ 研究を段階②以上に上げると、⚠ 研究用素材が入ったときにもう一度書き直すことになる** |
| **12** | ⚠ **`GRADE_STAT_RATIO` は 0.25 のまま触らない** | ⚠ **`game_manager.gd:124` のコメントが「等級10でも 1 + 0.25*9 = 3.25倍で止まる」と既に10で試算済み。⚠ この回で数値の意味は変わらない** |
| **13** | ⚠ **`W` は使わない。⚠ `E` は E118 の1件だけ** | ⚠ **E117 / W17 まで使用済み（W6・W7 は欠番）。⚠ 素材の参照漏れは黄では弱い**（§0-2 の7） |

---

## 1. いま何がどうなっているか（**実コードで確認済み・2026-08-22**）

### 1-1. 素材が触れる場所は**6箇所だけ**

| # | 場所 | 中身 |
|---|---|---|
| **A** | `resources/balance/master/items.json` | ⚠ **素材3件**（`training_material` / `construction_material` / `forging_material_1`）。⚠ **全16件中の3件** |
| **B** | `resources/balance/master/stages.json:5,17,29` | ⚠ **`stage_1/2/3` の `rewards.materials` に3件とも入っている** |
| **C** | `resources/balance/master/shop.json:5,13,30,38,46` | ⚠ **5枠**（`training` ×2 / `construction` ×2 / `forging_1` ×1） |
| **D** | `resources/balance/master/research.json:7,16,26,35,44` | ⚠ **5ノード全部が `cost_material_id: "construction_material"`** |
| **E** | `scripts/utils/state_keys.gd:274-275` | ⚠ **`.gd` 側で素材IDを直書きしているのはここ1行だけ**（`ITEM_FORGING_MATERIAL_1`） |
| **F** | `resources/balance/initial_state_config.tres:10-12` | ⚠ **`starting_materials` に2件**（`construction_material: 5` / `training_material: 50`） |

⚠ **`localization/ja.csv` は3行**（21 / 130 / 197行目）。
⚠ **`recipes.json` も素材IDを持つが、⚠ 作業場は③の回で全部消える。⚠ 今回は改名にだけ追従させる**（⚠ **消さない。§4**）。

### 1-2. ⚠ 等級10の器は**もう書いてある**

| # | 事実 | 実コード |
|---|---|---|
| **G** | ⚠ **上限だけが 3 で止めてある** | ⚠ **`MAX_EQUIPMENT_GRADE: int = 3`**（`game_manager.gd:119`）。⚠ **コメントは「第1弾は3で止める」** |
| **H** | ⚠ **ステータスの係数は10で試算済み** | ⚠ **`GRADE_STAT_RATIO = 0.25` /「等級10でも 1 + 0.25*9 = 3.25倍で止まる」**（`game_manager.gd:121-124`） |
| **I** | ⚠ **枠が開く等級が既に5と10** | ⚠ **`PART_SLOT_GRADES: Array[int] = [5, 10]`**（`game_manager.gd:129`）。⚠ **`get_open_part_slot_count()` のコメントが「第1弾は上限が3なので常に0。⚠ 器だけ先に作っている」** |
| **J** | ⚠ **等級の表示は `%d` で刷っている** | ⚠ **`ui_equipment_grade,等級%d`（`ja.csv:212`）。⚠ 10でも崩れない** |
| **K** | ⚠ **鍛冶のコストは定数の掛け算1行** | ⚠ **`FORGE_COST_PER_GRADE * (grade + 1)`**（`game_manager.gd:1486`）。⚠ **素材IDは `FORGE_MATERIAL_ID` の直書き1本**（`:132`） |
| **L** | ⚠ **分解の戻りは `int` 1本** | ⚠ **`get_dismantle_refund()` が `int` を返し、⚠ `add_material(FORGE_MATERIAL_ID, refund)` で段階①に全額戻している**（`:1543-1576`） |

⚠ **つまり決定C（等級10化）で本当に決め直すのは「4〜10のコスト」と「等級帯→段階の対応」の2つだけ。**
⚠ **ステータス・枠・表示は触らなくても10で成立する。**

### 1-3. ⚠ **コードに書かれた警告**（**決定Cに正面からぶつかる**）

> ⚠ **等級の上限。第1弾は3で止める。**
> ⚠ **4〜10の必要素材量はバランスの計算道具ができてから決める（勘で置くと全部やり直しになる）。**
> — `game_manager.gd:117-119`

⚠ **決定3（`NEXT_STEPS` 0-2）で「バランスの実測はだいぶ後」と決まっているので、⚠ この回のコストは必ず勘になる。**
→ ⚠ **逃げ方は §0-2 の3（式1本にする）。⚠ 「勘で置いた7個の数値」ではなく「勘で置いた係数1つ」にする。⚠ 実測が来たら直すのは1箇所。**
⚠ **既定値は現状と一致させる**（`base * grade`・`base = 4`）。⚠ **等級1〜3の挙動がこの回で変わらないことを §6 で測る。**

### 1-4. ⚠ 素材の欄は**画面に全件そのまま並ぶ**

⚠ **`base_screen.gd:70-73` の `_init_materials()` が `MATERIALS` のキーを全部回して1行ずつ作っている。**
→ ⚠ **3行が12行になる。⚠ 並びは `items.json` の `sort_order` ではなく Dictionary のキー順**（⚠ **＝入った順。⚠ 段階がばらばらに並ぶ**）。⚠ **これは §7 の人間の確認項目にする**（⚠ **崩れるかどうかは画面でしか分からない**）。

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ 改名漏れは「黙って消える」

⚠ **`get_instance_stats()` の中のコメントが名指しで書いている**（`game_manager.gd:1292-1293`）：
> ⚠ **items.json から消えたIDを装備したまま。⚠ 装備は外れないが加算されない。⚠ リリース後にアイテムIDを改名しないこと。**

→ ⚠ **今回は「まだリリース前だから改名する」回。⚠ だからこそ E118（§0-2 の7）を先に入れてから改名する。⚠ 順番を逆にしない。**

### 2-2. ⚠ セーブの `MATERIALS` のキーも旧IDのまま残る

⚠ **`save_slot_0.json` を持っている状態で改名すると、⚠ 旧キーの素材が画面に残り続ける**（⚠ **`_init_materials()` はキーを全部回すため、⚠ `ui_res_construction_material` が翻訳表に無くなってキー文字列が画面に出る**）。
→ ⚠ **開発用セーブしか無いので、⚠ 対処は入れない。⚠ 人間には「§7 の前にセーブを消す」と伝える**（§7-B）。⚠ **`AGENTS.md`「マスターデータと状態を同期する型」の対象外**（⚠ **素材は進捗ではなく所持数**）。

### 2-3. ⚠ `MasterDataLoader` が返す数値は必ず `float`

⚠ **`CLAUDE.md` 3番。⚠ 段階の対応で引いた要求量を `int()` で包み忘れると、⚠ 鍛冶のコストが `12.0` と表示される。**

### 2-4. ⚠ 状態を変える前に全部の判定を終える

⚠ **`forge_equipment()` は `CLAUDE.md` 6番の形で書かれている**（`:1526` に `# --- ここから状態を変える ---` の区切りがある）。⚠ **段階の判定を足すときは、⚠ 必ずこの区切りより上に書く。**

### 2-5. ⚠ 新しい `class_name` を作る回である（**前回1タスク溶けた形**）

⚠ **決定G で `EquipmentConfig` を新設する。⚠ ヘッドレスでは登録されない**（`NEXT_STEPS` §4）。
→ ⚠ **参照する側（`balance.gd` の `@export`）を、⚠ 人間がエディタを通すまで書かない**（§3-C-1）。
→ ⚠ **`Node` 型 ＋ `has_method()` で逃げない。⚠ `@export var equipment: Resource` にも落とさない**（`AGENTS.md`「エラーを理由にルールを緩めない」）。
⚠ **確かめ方**：`grep -c "EquipmentConfig" .godot/global_script_class_cache.cfg`（⚠ **前半は 0 のまま／後半に入る前に 1以上**）。

### 2-6. ⚠ `ja.csv` の再インポートはエディタを通すまで走らない

⚠ **12行足しても `ja.ja.translation` が古いままだと `tr()` はキーをそのまま返す**（`NEXT_STEPS` §4）。
→ ⚠ **人間に頼むのは1回。⚠ この回は新しい `class_name` を作らないので、⚠ 頼むのは `ja.csv` の再インポートだけ。**

---

## 3. 実装（ファイル別）

### 3-A. `resources/balance/master/items.json` … 素材を12件にする

| 系統 | ID | 表示名（`GAME_DESIGN` 2章の語） |
|---|---|---|
| 建築 | `construction_material_1` 〜 `_4` | ⚠ **木・石・鉄・金** |
| 鍛冶 | `forging_material_1` 〜 `_4` | ⚠ **下級・中級・上級・超級** |
| 育成 | `training_material_1` 〜 `_4` | ⚠ **`GAME_DESIGN` に段階名が無い。⚠ 設計役が置く**（§3-F） |

- ⚠ **`sort_order` は「系統 → 段階」の順に 0〜11 を通しで振る**（⚠ **建築 0-3 ／ 育成 4-7 ／ 鍛冶 8-11**）
- ⚠ **`stamina_potion` を 10 → 12 へ動かす**（⚠ **鍛冶③と重なるため。⚠ 装備の 20〜61 は動かさない**）
- ⚠ **`forging_material_1` は既存のIDをそのまま使う**（⚠ **改名しない。⚠ ①の1件だけ改名しないのが正しい**）
- ⚠ **`construction_material` → `construction_material_1` ／ `training_material` → `training_material_1` を改名**
- ⚠ **インデントはタブ**（⚠ **既存 `items.json` はタブ**）

### 3-B. `scripts/utils/state_keys.gd` … 定数

- ⚠ **`ITEM_FORGING_MATERIAL_1` はそのまま**（⚠ **IDが変わらないため**）
- ⚠ **段階②〜④の定数は足さない**（⚠ **`.gd` から名指しで引くのは鍛冶の①だけ。⚠ 残りはマスターとConfigの数値で引く。⚠ 使わない定数を12件並べない**）

### 3-C. `resources/balance/equipment_config.gd` … **新規**（決定G）

`class_name EquipmentConfig extends Resource`

```
@export var max_equipment_grade: int = 10
@export var forge_material_tier_min_grades: Array[int] = [1, 4, 7, 10]
@export var forge_cost_by_grade: Array[int] = [8, 12, 16, 20, 24, 28, 32, 36, 40]
                                       # 上げた先の等級 → 2  3   4   5   6   7   8   9  10
@export var dismantle_refund_base: int = 3
@export var dismantle_refund_ratio: float = 0.5
```

- ⚠ **`forge_cost_by_grade` の先頭2つ（8 / 12）は現状の `FORGE_COST_PER_GRADE * (grade + 1)` と同じ値**（⚠ **等級1〜3の挙動がこの回で動かないことを §6-C で測る**）
- ⚠ **配列の長さが `max_equipment_grade - 1` と合わない場合は、⚠ 最後の値を使い回さず E118 とは別の赤を出す…のではなく、⚠ 起動時に `push_error` を1本出して末尾の値で埋める**（⚠ **黙って落とすと「等級9から上がらない」が無音になる**）
- ⚠ **`Expression` は使わない**（決定E）。⚠ **`CharacterConfig.level_up_cost_formula` の形はここには持ち込まない**

#### ⚠ 3-C-1. **実装の順番**（⚠ **`class_name` の鶏と卵**）

⚠ **`balance.gd` に `@export var equipment: EquipmentConfig` を書いた瞬間、⚠ `class_name` が未登録だと `Balance`（Autoload の1番）が Parse Error で落ち、⚠ ゲームが起動しなくなる。**
⚠ **しかも `.tres` を割り当てる枠は、その `@export` を書かないと Inspector に現れない。**

→ ⚠ **実装を2段に割る。⚠ 人間を止めるのは1回のまま。**

| 段 | 誰 | 中身 | ヘッドレス |
|---|---|---|---|
| **前半** | 設計役 | ⚠ **3-A / 3-B / 3-E / 3-F / 3-G / 3-H / 3-I / 3-J** ＋ ⚠ **`equipment_config.gd` を置くだけ**（⚠ **誰も参照しない**） | ⚠ **全部通せる**（§5-1〜3・§6-E・§6-F） |
| **中** | ⚠ **人間・1回** | §7-B | — |
| **後半** | 設計役 | ⚠ **`balance.gd` の `@export` ／ 3-D ／ 3-K** | ⚠ **その後に通す**（§6-A〜D） |

⚠ **前半のあいだ `game_manager.gd` は既存の定数のまま動かす。⚠ 中途半端に `Balance.equipment` を読ませない**（⚠ **`Balance` にプロパティが無く実行時に赤が出る**）。

### 3-D. `autoload/game_manager.gd` … 定数をConfigへ移し、段階で引く

| 関数 | 変えること |
|---|---|
| `get_forge_cost()` | ⚠ **返す `material_id` を「上げた先の等級」から段階で引く。⚠ 量は `forge_cost_by_grade` から引く。⚠ `int()` で包む**（§2-3） |
| `forge_equipment()` | ⚠ **上限を `MAX_EQUIPMENT_GRADE` から `Balance.equipment.max_equipment_grade` へ。⚠ 判定の位置は変えない**（§2-4） |
| `get_dismantle_refund()` | ⚠ **戻り型を `int` → `Dictionary`（`{material_id: count}`）。⚠ 等級1の基礎ぶんは段階①、⚠ 上げたぶんは払った段階へ。⚠ どちらも `× dismantle_refund_ratio` の切り捨て**（決定F） |
| `dismantle_equipment()` | ⚠ **`add_material()` を段階ごとに呼ぶ。⚠ `print` に段階別の内訳を出す** |
| **新設** `get_forge_material_tier(grade: int) -> int` | ⚠ **等級 → 段階（1〜4）。⚠ 「同じ形の判定」を1本に寄せる**（`NEXT_STEPS` §2-2）。⚠ **鍛冶・分解・②の回の装飾が全部ここを通る** |

- ⚠ **`MAX_EQUIPMENT_GRADE` / `FORGE_COST_PER_GRADE` / `DISMANTLE_REFUND_BASE` / `FORGE_MATERIAL_ID` の4定数は消す**（⚠ **`AGENTS.md` の数値管理ルール。⚠ 定数とConfigの二重管理を残さない**）
- ⚠ **`GRADE_STAT_RATIO` と `PART_SLOT_GRADES` は触らない**（§0-2 の12・§1-2 の I）

### 3-E. `scripts/systems/master_data_loader.gd` … **E118**

| # | 出すもの |
|---|---|
| **E118** | ⚠ **マスターが参照する `material_id` / `item_id` が `items.json` に無い**（⚠ **赤**） |

- ⚠ **見る先は `stages.json` の `rewards.materials` ／ `shop.json` の `item_id` ／ `research.json` の `cost_material_id` ／ `recipes.json` の `inputs` / `outputs`**
- ⚠ **`initial_state_config.tres` は見ない**（⚠ **`.tres` はマスターではない。⚠ `MasterDataLoader` の担当外**）
- ⚠ **1件ごとに1本出す。⚠ 重複を潰さない**（⚠ **どのファイルのどの行で漏れたかが消える。`EXEC_STATUS_UI.md` §3-E と同じ理由**）
- ⚠ **ロード時の出力に `items validated: N entries, X errors` の行を1本足す**（⚠ **`skills validated` と同じ形。⚠ 0件で通ることを §5 で確かめる**）

### 3-F. `localization/ja.csv` … **12行**（⚠ **Ziva に渡せる部分**）

⚠ **既存3行を書き換え、⚠ 9行足す**（⚠ **差し引き +9行**）。

| キー | 値 |
|---|---|
| `ui_res_construction_material_1` 〜 `_4` | ⚠ **木材 / 石材 / 鉄材 / 金材** |
| `ui_res_forging_material_1` 〜 `_4` | ⚠ **鍛冶の欠片 / 鍛冶の塊 / 鍛冶の結晶 / 鍛冶の極** |
| `ui_res_training_material_1` 〜 `_4` | ⚠ **修練の証 / 鍛錬の証 / 練達の証 / 極意の証** |

- ⚠ **`ui_res_construction_material`（21行）／ `ui_res_training_material`（130行）／ `ui_res_forging_material_1`（197行）の3行を消さずに書き換える**（⚠ **`forging_material_1` はIDが変わらないので値だけ据え置き**）
- ⚠ **UTF-8（BOMなし）。⚠ CR を混ぜない**（⚠ **`NEXT_STEPS` §4。⚠ `awk '/\r/{n++}'` で数える**）
- ⚠ **再インポートは人間**（§2-6）

### 3-G. `resources/balance/master/stages.json` … 段階をずらす（§0-2 の10）

| ステージ | 落ちる段階 |
|---|---|
| `stage_1` | ⚠ **①のみ**（3系統） |
| `stage_2` | ⚠ **① ＋ ②少量** |
| `stage_3` | ⚠ **②中心 ＋ ③少量** |

⚠ **④は落とさない**（⚠ **ステージが3本しか無い。⚠ §8 に残す**）。
⚠ **トップレベルのインデントだけ半角スペース2つ**（⚠ **`NEXT_STEPS` §4。⚠ このファイルだけ例外**）。

### 3-H. `resources/balance/master/shop.json` … ④の入口

- ⚠ **既存5枠のIDを段階①に読み替える**
- ⚠ **3系統 × ③④ の6枠を足す**（⚠ **④はここが唯一の入口。⚠ 高額**）。⚠ **`daily` が6枠 → 12枠になる**（⚠ **画面が縦に伸びる。§7-C の 27 で見てもらう**）
- ⚠ **`shop.json` は `daily` しか持っていない**（⚠ **状態には `weekly` / `monthly` があるが、⚠ マスターに定義が無い。⚠ この回では足さない**）
- ⚠ **`payout_type` / `count` / `item_type` は既存の書き方を踏襲する**（`AGENTS.md`）

### 3-I. `resources/balance/master/research.json` … 読み替えだけ

- ⚠ **5ノードの `cost_material_id` を `construction_material_1` へ。⚠ 段階は上げない**（§0-2 の11）

### 3-J. `resources/balance/master/recipes.json` … 改名への追従だけ

- ⚠ **`inputs` / `outputs` の素材IDを新IDへ。⚠ レシピは1件も消さない・足さない**（⚠ **廃止は③の回。決定3**）

### 3-K. `tests/debug_boot.gd` … `kind: "report"` と `materials` シナリオ（§0-2 の8）

| 出すもの |
|---|
| ⚠ **等級1→2 〜 9→10 の9行**：⚠ **上げた先の等級 / 段階 / 素材ID / 要求量** |
| ⚠ **等級1・5・10 の装備を分解したときの戻り**（⚠ **段階別の内訳**） |
| ⚠ **`items.json` の素材12件**（⚠ **ID / `sort_order` / `tr()` の戻り**） |

- ⚠ **`SCENARIOS` に1行足す。⚠ シーンもスクリプトも増やさない**（`debug_boot.gd:16`）
- ⚠ **`kind` の枝は今 `KIND_SCREEN` と `KIND_BATTLE` の2本**（`:501`）。⚠ **`KIND_REPORT` を足すときは、⚠ `:505` のコメント「⚠ 片方だけ直して片方を忘れた」を読むこと**
- ⚠ **状態を書き換えても保存しない**（`debug_boot.gd:495-497`）。⚠ **分解の確認のために作る個体は、⚠ `SaveManager` を呼ばずに捨てる**
- ⚠ **`tr()` は静的関数から呼べない**（`AGENTS.md`）。⚠ **`debug_boot` は `Node` なので `tr()` で良い**

### 3-L. 触らないファイル

⚠ **`battle_formula.gd` ／ `skill_resolver.gd` ／ `skill_runtime.gd` ／ `skill_schema.gd` ／ `status_registry.gd` ／ `battle_session.gd` ／ `unit.gd` ／ `battle_controller.gd` ／ `unit_view.gd` ／ `status_chips.gd`**
⚠ **本番スキルの JSON 全件 ／ `characters.json` ／ `enemies.json` ／ `parties.json` ／ `summons.json` ／ `main_theme.tres`**
⚠ **`workshop_screen.gd`**（⚠ **③の回。⚠ 素材IDの直書きが無いことは確認済み**）

---

## 4. 変えないもの

- ⚠ **戦闘の数値**（⚠ **この回で damage が1つも動かないこと。§6-D**）
- ⚠ **`GRADE_STAT_RATIO = 0.25`**（⚠ **等級10で 3.25倍。§1-2 の H**）
- ⚠ **`PART_SLOT_GRADES = [5, 10]`**（⚠ **②の回で使う**）
- ⚠ **等級1〜3の鍛冶コスト**（⚠ **式の既定値で現状と一致させる。§6-C**）
- ⚠ **レシピの件数**（⚠ **廃止は③の回**）
- ⚠ **`recipes.json` の廃止 ／ 中間素材 ／ 装飾**（⚠ **全部この回の外**）
- ⚠ **`ja.csv` の `ui_status_ch_*` 45行**（⚠ **前回の検証用。⚠ 触らない**）

---

## 5. 完了条件 — **§0 事前チェック**（⚠ **設計役・ヘッドレス**）

1. ⚠ **全シナリオ（既存19 ＋ 新規1）で `ERROR:` / `SCRIPT ERROR:` / `Parse Error` が1行も出ないこと**
2. ⚠ **`items validated: 24 entries, 0 errors`**（⚠ **素材12 ＋ 消耗品1 ＋ 装備11。⚠ E118 が0件＝改名漏れが無い**）
3. ⚠ **`skills validated: 79 entries, 0 errors, 1 warnings`**（⚠ **黄1本は `skill_dbg_dot_odd` の端数。⚠ 従来どおり。⚠ この回で増えないこと**）
4. ⚠ **前半の終わりに `grep -c "EquipmentConfig" .godot/global_script_class_cache.cfg` が 0 であること**（⚠ **まだ誰も参照していない＝落ちない**）。⚠ **後半に入る前に 1以上であること**（⚠ **人間がエディタを通した証拠。§7-B**）

## 6. 完了条件 — **ログ / ファイル**（⚠ **設計役が読む**）

### 6-A. 段階の対応

5. ⚠ **`scenario=materials` で、⚠ 等級1→2 〜 9→10 の9行が出ること**
6. ⚠ **段階の割り当てが `2〜3→① / 4〜6→② / 7〜9→③ / 10→④` であること**（⚠ **4段階が全部使われている**）
7. ⚠ **要求量に `.0` が付いていないこと**（⚠ **`int()` 包み忘れ。`CLAUDE.md` 3番**）

### 6-B. 分解

8. ⚠ **等級1の分解が段階①だけを返すこと**（⚠ **基礎3 × 0.5 = 1**）
9. ⚠ **等級10の分解が①②③④を全部返すこと**（⚠ **払った段階ごとに戻る**）
10. ⚠ **どの段階の戻りも「その段階で払った量の半分（切り捨て）」であること**（⚠ **決定F。⚠ 上げてすぐ分解して素材が増える経路が無いこと**）

### 6-C. ⚠ 等級1〜3が今回で動いていないこと

11. ⚠ **等級1→2 が 8個・2→3 が 12個であること**（⚠ **現状の `FORGE_COST_PER_GRADE * (grade + 1)` ＝ 4×2, 4×3 と一致。⚠ 配列の先頭2つが現状を再現している**）
12. ⚠ **どちらも段階①であること**

### 6-D. 戦闘の数値が動いていないこと

13. ⚠ **`area` 3/4/4/3 ／ `recast` 2/8 ／ `mitigate` 200/300/200/120 ／ `pierce` 194/200 ／ `lineup` 99.4 ／ `atk_mult` 200→400 ／ `pool` の `zone` 12件 ／ `dot_react` の `react` 6件**
14. ⚠ **`save_slot_0.json` が存在しないまま／書き換わらないこと**

### 6-E. ファイル

15. ⚠ **`ja.csv` が +9行（308+48=356 → 365行）。⚠ BOM が無いこと。⚠ CR が0件であること**
16. ⚠ **重複キーが0件であること**（⚠ **既存の `ui_common_yes` / `ui_common_no` の2重を増やしていない**）
17. ⚠ **`items.json` の素材が12件。⚠ `sort_order` に重複が無いこと**

### 6-F. ⚠ 足した検証が本当に出るか

18. ⚠ **E118 を実測する**：⚠ **`stages.json` の素材IDを1件だけ壊し、⚠ そのIDで赤が1本出ることを見る。⚠ 戻して0件に戻ることも見る**
19. ⚠ **`research.json` でも同じことを見る**（⚠ **見る先が4ファイルあるので、⚠ 1ファイルだけ通っていて他が素通り、を潰す**）

## 7. 完了条件 — **画面**（⚠ **人間だけ**）

### 7-A. ⚠ 先にやってもらうこと

⚠ **`EXEC_STATUS_UI.md` §7（12項目）が未実施なら、⚠ そちらが先。**
⚠ **スキル名は `強撃` / `横薙ぎ` / `聖光`**（⚠ **EXEC 側の「力の一撃」「聖なる光線」は実データに存在しない。⚠ ズレ17件目・未修正**）。

### 7-B. ⚠ この回の前にやってもらうこと

| # | やること |
|---|---|
| **B-1** | ⚠ **`save_slot_0.json` を消す**（⚠ **旧IDの素材が残るため。§2-2**） |
| **B-0** | ⚠ **消したあと、⚠ `F4` のデバッグパネルで「素材を全種類」と「装備を全種類 1個ずつ」を押す**（⚠ **新規セーブでは装備を1つも持っておらず、⚠ 22〜25（鍛冶と分解）に到達できない。§12-3 の②**） |
| **B-2** | ✅ **済（設計役が直した）**。⚠ **人間の許可を取ったうえでの例外**（2026-08-22）：⚠ **`initial_state_config.tres` の `starting_materials` のキー2つを新IDへ。⚠ Godot 4 の Inspector は Dictionary のキー編集でキーを作り直す挙動があり、⚠ 値（5 / 50）の入れ直しが要るため。⚠ `uid` にも `ext_resource` にも触れていない。⚠ ヘッドレスで `materials={ "construction_material_1": 5, "training_material_1": 50 }` を実測して壊れていないことを確認済み** |
| **B-3** | ⚠ **`ja.csv` を再インポート**（⚠ **FileSystem で右クリック → 再インポート。§2-6**） |
| **B-4** | ⚠ **`equipment_config.tres` を作る**：⚠ **`resources/balance/` に 新規 Resource → `EquipmentConfig` → `equipment_config.tres` で保存**（⚠ **中身は空でよい。⚠ `@export` の既定値が効く**） |
| **B-5** | ⚠ **`balance.tscn` の `Equipment` の枠に `equipment_config.tres` を割り当てる**（⚠ **割り当てを忘れると `Balance.equipment` が `null` で全画面が落ちる**） |

⚠ **残っているのは B-1 / B-3 / B-4 / B-5。⚠ B-2 は済んでいる。**
⚠ **エディタを開くのは2回・人間の作業は1回分**（⚠ **1回目は「開いて閉じる」だけ＝`class_name EquipmentConfig` の登録。⚠ そのあと設計役が `balance.gd` に `@export` を書いて枠を作る。⚠ 2回目で B-1 / B-3 / B-4 / B-5 をまとめて**）。
⚠ **設計役の初稿は「1回で足りる」と書いていたが誤り。⚠ 枠は `@export` を書かないと現れず、⚠ その `@export` は登録後にしか書けない**（§3-C-1）。
⚠ **確かめ方**：`grep -c "EquipmentConfig" .godot/global_script_class_cache.cfg` が **1以上**。

### 7-C. ⚠ 見るもの

| # | 見るもの |
|---|---|
| **20** | ⚠ **拠点の素材欄に12行出ること。⚠ 画面から溢れていないこと**（⚠ **3行の前提で組まれている。⚠ §1-4**） |
| **21** | ⚠ **12行のどれにも `ui_res_` で始まる文字が出ていないこと**（⚠ **出たら `ja.csv` の漏れか再インポート漏れ。⚠ どの行かを報告してほしい**） |
| **22** | ⚠ **倉庫で装備を1つ選び、⚠ 鍛冶を押し続けて等級が10まで上がること**（⚠ **3で止まらないこと**） |
| **23** | ⚠ **等級が4に上がるところで、⚠ 要求される素材の名前が「鍛冶の欠片」から「鍛冶の塊」に変わること**（⚠ **段階が繰り上がる最初の点**） |
| **24** | ⚠ **等級10で鍛冶ボタンが「最大」になって押せなくなること** |
| **25** | ⚠ **等級10の装備を分解すると、⚠ 4種類の鍛冶素材が全部増えること。⚠ ただし上げるのに払った量より少ないこと**（⚠ **半分。決定F。⚠ 増えた数を報告してほしい。⚠ §6-B と突き合わせる**） |
| **26** | ⚠ **研究の解放に要る素材が「木材」と表示されること**（⚠ **段階①。⚠ 名前が変わっただけで解放できること**） |
| **27** | ⚠ **ショップに④の枠（金材・極意の証・鍛冶の極）が出ていること。⚠ 買えること。⚠ 12枠が画面から溢れていないこと**（⚠ **6枠の前提で組まれている**） |
| **28** | ⚠ **`stage_1` をクリアすると①だけが増え、⚠ `stage_3` をクリアすると②と③が増えること**（⚠ **報酬画面で見える**） |
| **29** | ⚠ **育成のレベルアップに要る素材が「修練の証」のままであること**（⚠ **`CharacterConfig.level_up_material_id` の改名漏れ。⚠ ここだけ `.tres` に旧IDが残る可能性がある**） |

⚠ **29 は `.tres` の値。⚠ B-2 と同じ場所で直すことになるかもしれない。⚠ 設計役が §5 の時点で読めるので、⚠ 事前に潰しておく**（⚠ **潰せていたら 29 は「変わっていないこと」の確認だけになる**）。

---

## 8. 将来コードを変えたときに見る項目

- ⚠ **ステージが増えたら④をどこから落とすか**（⚠ **今はショップだけ。§0-2 の10**）
- ⚠ **4〜10のコストを実測で決め直す**（⚠ **式の係数1つ。§1-3。⚠ バランスの回で必ず戻ってくる**）
- ⚠ **等級帯の刻み `[1, 4, 7, 10]` が妥当か**（⚠ **`GAME_DESIGN` は「刻みは調整」と書いている**）
- ⚠ **装飾素材を足すとき、⚠ 同じ規則（`decor_material_1..4`）で足せるか**（⚠ **②の回。⚠ 足せなければ規則が間違っている**）
- ⚠ **研究用素材が入ったら、⚠ 研究のコストを建築素材から移すか**（⚠ **③の回。§0-2 の11**）
- ⚠ **素材欄の並びを `sort_order` にするか**（⚠ **今は Dictionary のキー順。§1-4**）

---

## 9. Ziva に渡せる部分

⚠ **ある。⚠ `localization/ja.csv` の12行（§3-F）。**
⚠ **`.gd` を1行も触らない。⚠ ただし `items.json` の改名（§3-A）が先に入っていないと、⚠ 入れたかどうかが画面にもログにも出ない。**
→ ⚠ **順番は「`items.json` と `.gd` → `ja.csv`」。⚠ 同時に渡さない。**

⚠ **`stages.json` / `shop.json` / `research.json` の数値の割り（§3-G / 3-H / 3-I）も JSON だけだが、⚠ 渡さない。**
⚠ **理由：⚠ 改名と数値の割りを同時に別々の手が触ると、⚠ E118 が出たときにどちらの手が原因か分からなくなる。**

---

## 10. 終わったあとに足す宿題

- ⚠ **解消：`construction_material` の取り合い**（⚠ **宿題。⚠ 研究が段階①、⚠ 拠点は段階②以上を使えるようになった**）
- ⚠ **一部解消：等級の上限が3**（⚠ **10になった。⚠ ただしコストは実測前**）
- ⚠ **NEW：4〜10のコスト7個が勘**（⚠ **`forge_cost_by_grade` の3〜9番目。⚠ バランスの回で必ず直す。§1-3**）
- ⚠ **NEW：分解の返却率 0.5 が勘**（⚠ **`dismantle_refund_ratio`。⚠ 同じ回で直す**）
- ⚠ **NEW：`Balance` の枠が9本になった**（⚠ **`EquipmentConfig`。⚠ 割り当て漏れで全画面が落ちる形が1つ増えた**）

---

## 11. ⚠ 実施結果 — **前半**（**2026-08-22・設計役がヘッドレスで実行して判定**）

⚠ **前半（§3-C-1 の表）は全部通った。⚠ 人間の1回（§7-B）待ち。**

| 項目 | 結果 |
|---|---|
| 5-1 全シナリオで赤が1行も出ない | ✅ **19シナリオで `red=0`**（`training` は窓あり専用のため除く） |
| 5-2 `items validated` | ✅ **`24 entries, 0 errors`**（素材12 ＋ 消耗品1 ＋ 装備11） |
| 5-3 `skills validated` | ✅ **`79 entries, 0 errors, 1 warnings`**（黄1本は `skill_dbg_dot_odd` の端数＝従来どおり。⚠ 増えていない） |
| 5-4 `EquipmentConfig` が未登録 | ✅ **前半では誰も参照していないので落ちない**（⚠ **後半に入る前に登録が要る**） |
| 6-D-13 戦闘の数値 | ✅ **`mitigate` の `attack_type: true` が `200 / 300 / 200 / 120`**（⚠ **`battle_last.jsonl` から実測。⚠ 従来と一致**） |
| 6-D-14 セーブ | ✅ **`save_slot_0.json` は存在しないまま** |
| 6-E-15 `ja.csv` | ✅ **356 → 365行（+9）。⚠ BOM なし・CR 0件**（`od` / `awk` で確認） |
| 6-E-16 重複キー | ✅ **素材キーの重複0件** |
| 6-E-17 `items.json` | ✅ **素材12件・`sort_order` の重複0件**（⚠ **`stamina_potion` を 10 → 12 へ移した**） |
| 6-F-18 E118 が本当に出るか | ✅ **§11-2** |
| 6-F-19 別ファイルでも出るか | ✅ **§11-2** |

### 11-1. ⚠ 実装中に踏んだこと

**① ⚠ `sort_order` が `stamina_potion` と衝突した。**
⚠ **素材を 0〜11 に並べると、⚠ 既存の `stamina_potion`（10）と鍛冶③が重なる。⚠ ポーションを 12 へ動かして避けた**（⚠ **装備の 20〜61 は動かしていない**）。

**② ⚠ `shop.json` は `daily` しか持っていない。**
⚠ **状態には `weekly` / `monthly` があるが、⚠ マスターに定義が無かった。⚠ ④の枠は `daily` に置いた。⚠ 報告のみ・勝手に足していない。**

**③ ⚠ `items.json` の件数が想定と違った。**
⚠ **設計役は「16件」と書いていたが実際は改名前で15件だった**（⚠ **素材3 ＋ 消耗品1 ＋ 装備11**）。⚠ **§5-2 を 24 に直した。**

### 11-2. ⚠ 足した検証が本当に出ることを確かめた

| 壊し方 | 出たもの |
|---|---|
| ⚠ **`stages.json` の `construction_material_1` を `construction_material_X` に** ＋ ⚠ **`research.json` の1件を `construction_material_ZZZ` に** | ✅ **`items validated: 24 entries, 2 errors`**。⚠ **`E118 stages.json (stage_1)` と `E118 research.json (res_cap_1)` が1本ずつ。⚠ ファイル名と文脈（ステージID・ノードID）が出た** |
| 戻したあと | ✅ **`0 errors` に戻った** |

⚠ **2ファイルを同時に壊したのは、⚠ 「1ファイルだけ通っていて他が素通り」を潰すため**（§6-F-19）。⚠ **`shop.json` と `recipes.json` は未実測**（⚠ **同じ `_report_missing_item()` を通る枝**）。

---

## 12. ⚠ 実施結果 — **後半**（**2026-08-22**）

⚠ **§5・§6・§7 が全部通った。⚠ このタスクは完了。**

⚠ **§7（画面）の結果**（**人間が実施・2026-08-22**）：

| # | 結果 |
|---|---|
| **20** | ❌ → ✅ **一度落ちた**（⚠ **横に際限なく広がった**）。⚠ **`GridContainer` 4列 ＋ 0を出さない に直して、⚠ 4列×3行で収まった**（§12-3 の①） |
| **21** | ✅ **`ui_res_` / `ui_equipment_` がそのまま出ている箇所は無い** |
| **22〜25** | ✅ **§12-4 のログで確認**（⚠ **等級10まで鍛えられ、⚠ 分解で4種類とも戻った**） |
| **26** | ✅ **研究が新IDで解放できた**（§12-4） |
| **27** | ✅ **ショップ12枠が出て、⚠ 溢れていない** |
| **28〜29** | ✅ **§12-4 のログで確認** |

| 項目 | 結果 |
|---|---|
| 5-1 全シナリオで赤が1行も出ない | ✅ **19シナリオ ＋ 新規 `materials` で `red=0`** |
| 5-2 `items validated` | ✅ **`24 entries, 0 errors`** |
| 5-3 `skills validated` | ✅ **`79 entries, 0 errors, 1 warnings`**（従来どおり・増えていない） |
| 5-4 `EquipmentConfig` の登録 | ✅ **`global_script_class_cache.cfg` に1件**（人間がエディタを1回通した） |
| 6-A-5 等級2→10 の9行 | ✅ **9行出た**（§12-1） |
| 6-A-6 段階の割り当て | ✅ **2〜3→① / 4〜6→② / 7〜9→③ / 10→④。⚠ 4段階が全部使われている** |
| 6-A-7 `.0` が付いていない | ✅ **全部 `int`** |
| 6-B-8 等級1の分解 | ✅ **`{ "forging_material_1": 1 }`**（基礎3 × 0.5 の切り捨て） |
| 6-B-9 等級10の分解 | ✅ **①②③④の4件すべて返った** |
| 6-B-10 半分（切り捨て） | ✅ **等級10：払った合計 216 → 戻り 109**（⚠ **216×0.5=108 ＋ 基礎1**）。⚠ **段階ごとにも一致**（§12-1） |
| 6-C-11 等級1→2 が8・2→3 が12 | ✅ **`x8` / `x12`**（⚠ **この回より前と同じ値**） |
| 6-C-12 どちらも段階① | ✅ **`forging_material_1`** |
| 6-D-13 戦闘の数値 | ✅ **`mitigate` 200/300/200/120 ／ `dot_react` の `react` 6件** |
| 6-D-14 セーブ | ✅ **`save_slot_0.json` は存在しないまま** |
| 上限で止まる | ✅ **等級10 で `forge_equipment()` が `false`** |

### 12-1. ⚠ 実測（`scenario=materials`）

```
--- 鍛冶（上限=等級10）---
  等級 2 へ  段階1  forging_material_1  x8      等級 7 へ  段階3  forging_material_3  x28
  等級 3 へ  段階1  forging_material_1  x12     等級 8 へ  段階3  forging_material_3  x32
  等級 4 へ  段階2  forging_material_2  x16     等級 9 へ  段階3  forging_material_3  x36
  等級 5 へ  段階2  forging_material_2  x20     等級10 へ  段階4  forging_material_4  x40
  等級 6 へ  段階2  forging_material_2  x24
--- 分解（実際に鍛えてから戻す）---
  等級 1 → { forging_material_1: 1 }（合計 1 / 払った合計 0）
  等級 5 → { forging_material_1: 11, forging_material_2: 18 }（合計 29 / 払った合計 56）
  等級10 → { forging_material_1: 11, forging_material_2: 30,
             forging_material_3: 48, forging_material_4: 20 }（合計 109 / 払った合計 216）
```

⚠ **段階ごとの突き合わせ**：①20→10（＋基礎1＝11）／ ②60→30 ／ ③96→48 ／ ④40→20。⚠ **全部ちょうど半分。**

### 12-2. ⚠ 実装中に踏んだこと

**① ⚠ `Balance.equipment` の割り当て忘れが「静かな壊れ方」をした。**
⚠ **枠が `null` のとき、⚠ `get_max_equipment_grade()` が 0 を返し、⚠ 画面には「等級0・打ち止め」とだけ出て理由が分からなかった**（⚠ **`SCRIPT ERROR` は出るが、⚠ 何を直せばいいかは書かれていない**）。
→ ⚠ **`_equipment()` を1本の口にして、⚠ `null` のとき「`balance.tscn` の Equipment の枠に `equipment_config.tres` を割り当てること」と赤で1回だけ言うようにした。⚠ 以降は既定値へ落ちる。**

**② ⚠ `.tres` をエディタ外でリネームすると `uid` が古いパスを指したままになる。**
⚠ **`balance.tscn` の `ext_resource` に `uid` を書くと、⚠ Godot は `path` ではなく `uid` を見る。⚠ `.godot` の uid キャッシュが古いパス（`equipment_conig.tres`）を指していて、⚠ `Parse Error: referenced non-existent resource` になった。**
→ ⚠ **`uid` を書かず `path` だけにした**（⚠ **`shop_config.tres` / `research_config.tres` / `workshop_config.tres` の3つが既にこの形**）。⚠ **人間が次にエディタで保存すると `uid` が付き直る見込み。**

**③ ⚠ `ITEM_TYPE_MATERIAL` の定数が無かった。**
⚠ **`items.json` の `item_type: "material"` を型で絞る側が今回初めてできたので、⚠ `state_keys.gd` に1行足した**（⚠ **`ITEM_TYPE_EQUIPMENT` の隣**）。

**④ ⚠ `get_dismantle_refund()` の戻り型を変えたので、⚠ 呼び元の表示が合わなくなった。**
⚠ **`warehouse_screen.gd:137` が `%d` に Dictionary を渡す形になった。⚠ `get_dismantle_refund_total()` を足して合計だけ出すようにした**（⚠ **内訳は分解したときの `print` に出る**）。

**⑤ ⚠ 鍛冶ボタンに素材名を足した。**（⚠ **指示書に無い変更**）
⚠ **`鍛える(8)` のままだと、⚠ 等級4で要求が段階②に変わったことが画面から分からない**（⚠ **§7-C の 23 が確認できない**）。⚠ **`鍛える(鍛冶の塊 16)` の形にした。⚠ 装備画面の素材所持数も4段階ぶん並べた。**

### 12-3. ⚠ 人間が §7-C で見つけたもの（**2026-08-22**）

**① ⚠ 拠点の素材欄が横に際限なく広がった**（⚠ **§7-C の 20 が落ちた。⚠ 予想どおりの箇所**）。

⚠ **`base_screen.tscn` の `MaterialsDisplay` が `HBoxContainer` だった**（⚠ **3件の前提**）。⚠ **12件を横一列に足すので器が伸び続ける。**
→ ⚠ **人間の決定（2026-08-22）：⚠ `GridContainer`（4列）にする ＋ 所持数 0 の素材は出さない。**
- ⚠ **`base_screen.tscn`：`HBoxContainer` → `GridContainer` / `columns = 4`**
- ⚠ **`base_screen.gd`：`@onready` の型も `GridContainer` へ**（⚠ **`Node` に落として逃げない。`AGENTS.md`**）
- ⚠ **`_init_materials()` で `<= 0` を飛ばす。⚠ 手に入った時点で `_on_material_changed()` が作る**（⚠ **既存の枝をそのまま使う。2本目を書かない**）

**② ⚠ 「分解する場所が無い」と報告されたが、⚠ 器は在った。**

⚠ **`warehouse_screen.gd:133` の「素材にする(N)」が、⚠ 倉庫 → 持ち物タブの装備の個体1つずつに付く。⚠ セーブを消した直後は装備を1つも持っていないため、⚠ ボタンが出る対象が無かった。**
→ ⚠ **`F4` のデバッグパネル（`tests/debug_overlay.gd`）の「装備を全種類 1個ずつ」で用意できる。⚠ §7-C に手順として書き足した。**
⚠ **穴ではない。⚠ ただし「新規セーブから §7-C の 22〜25 に到達する経路が `F4` しか無い」ことは事実**（⚠ **作業場で作ると30分待ちになる**）。⚠ **§10 の宿題に足した。**

### 12-4. ⚠ 実機ログ（**人間が窓ありで実行・2026-08-22**）

⚠ **`godot.log` を人間が貼った。⚠ 設計役のヘッドレス実測と数値が1つも食い違わなかった。**

| 見たこと | ログの根拠 |
|---|---|
| 鍛冶が等級1→10まで通る | ⚠ **`cost=8 / 12 / 16 / 20 / 24 / 28 / 32 / 36 / 40`**（`eq_1` と `eq_11` の2個体で同じ） |
| 段階の繰り上がり | ⚠ **`forging_material_1` ×2 → `_2` ×3 → `_3` ×3 → `_4` ×1** |
| **分解** | ⚠ **`refund={ "forging_material_1": 11, "forging_material_2": 30, "forging_material_3": 48, "forging_material_4": 20 }`**（⚠ **§12-1 の予測と完全一致**） |
| 研究が新IDで解放できる | ⚠ **`unlock_research_node('res_cap_1') -> true (cost construction_material_1 x20 ...)` 5件とも** |
| 素材12種が扱える | ⚠ **`add_material('forging_material_4', 999)` ほか12件** |
| 赤が0本 | ⚠ **`ERROR:` / `SCRIPT ERROR:` が1行も無い** |

⚠ **おまけ：等級5で `slots=1`、⚠ 等級10で `slots=2` が開いた**（`PART_SLOT_GRADES = [5, 10]`）。
→ ⚠ **次の回（装飾）で刺す枠が、⚠ 既に開くようになっている。⚠ `get_open_part_slot_count()` のコメント「第1弾は上限が3なので常に0」は、⚠ この回で嘘ではなくなった。**

⚠ **`SaveManager] save_game` が走っているので、⚠ 以降 `save_slot_0.json` は新IDで存在する**（⚠ **§6-D-14 は設計役のヘッドレス実行についての条件。⚠ 人間が遊べばセーブは当然できる**）。
⚠ **`鍛える(8)` のままだと、⚠ 等級4で要求が段階②に変わったことが画面から分からない**（⚠ **§7-C の 23 が確認できない**）。⚠ **`鍛える(鍛冶の塊 16)` の形にした。⚠ 装備画面の素材所持数も4段階ぶん並べた。**
- ⚠ **NEW：④の入口がショップだけ**（⚠ **ステージが3本しか無い**）
- ⚠ **NEW：素材欄の並びが Dictionary のキー順**（⚠ **12件になって初めて見えた**）
- ⚠ **NEW：`get_dismantle_refund()` の戻り型が `Dictionary` に変わった**（⚠ **②の回で装飾の分解を足すときに同じ形にする**）
