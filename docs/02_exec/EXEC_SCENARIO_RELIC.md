# 実行指示書：**段階14-d（レリック・戦闘系）**

**作成日：2026-08-25**

**台帳は `docs/01_plan/PLAN_SCENARIO_MAP.md` §5-2。** **前は `EXEC_SCENARIO_MAP_SCREEN.md`（14-c）。**

⚠ **作ったのは `PLAN_SCENARIO_MAP.md` §9-1 の 6・18・24・27 の**4件**。**
⚠ **フロア系レリック（宝箱の率・たいまつ・休憩・ショップ）は入れていない**（⚠ **決定11 で「あと」に回した**）。

---

## 1. ⚠ 一番大きかったこと：**パッシブの器をそのまま借りた**

⚠ **`relics.json` を `MasterDataLoader` が `_cache_skills` にマージする。⚠ パッシブと同じ辞書。**
⚠ **戦闘画面は `unit.passive_ids` に足すだけ。⚠ `get_effective_stats()` も `skill_schema.gd` の効果の解釈も触っていない。**

### 1-1. ⚠ ただし検証器には手を入れた（**報告事項**）

⚠ **`skill_schema.gd` の検証は「パッシブはキャラに紐づく」前提だった。⚠ そのままだと赤が36本出た。**

| 何が出たか | どうしたか |
|---|---|
| ⚠ **`user_character_id` が無い**（E3） | ⚠ **`relic_scope` を持つエントリでは要求しない** |
| ⚠ **`unlock_level` が数値でない**（E4） | ⚠ **同上**（⚠ **レリックはレベルで解放されない**） |
| ⚠ **知らない欄がある: `relic_scope`**（E26） | ⚠ **`SKILL_FIELDS_KNOWN` に1行足した** |

⚠ **例外の判定は `relic_scope` の**有無だけ**。⚠ IDの綴りで見分けていない。**
⚠ **代わりに `E130` を1本足した**（⚠ **`relic_scope` が `party` / `single` のどちらでもない。⚠ 綴りを間違えると「全員に効くつもりが1人にも効かない」が無音で起きるため**）。
⚠ **⚠ 最初 `E128` と書いて番号がぶつかっていた**（⚠ **`E128` は `research.json` の検証が使用済み・`E129` は `workshop`。⚠ `NEXT_STEPS` §2-7 が「`E130` から」と書いてあったのに見落とした。⚠ 2026-08-25 に `E130` へ直した。⚠ 次は `E131` から**）。

⚠ **これは `AGENTS.md`「エラーを理由にルールを緩めない」に触れる形なので、⚠ 人間に報告する。⚠ 緩めたのではなく、⚠ 検証器が知らなかった種類を教えたという理解で進めた。**

---

## 2. 入ったもの（**9ファイル**）

| ファイル | 中身 |
|---|---|
| ⚠ **`resources/balance/master/relics.json`** | ⚠ **新規。⚠ 12件（3人用6 / 1人用6）。⚠ マスター8本目** |
| `master_data_loader.gd` | ⚠ **`PATH_RELICS` / `_cache_relics` / `get_all_relic_ids()` / `get_relic()`。⚠ `_cache_skills` へのマージ** |
| `skill_schema.gd` | ⚠ **`FIELD_RELIC_SCOPE` と E3 / E4 の例外、⚠ E128** |
| `game_manager.gd` | ⚠ **`get_floor_relics()` / `is_single_relic()` / `roll_relic_choices()` / `take_relic()` / `get_floor_relic_passives()`** |
| `battle_controller.gd` | ⚠ **`unit.passive_ids` にレリックを足す1行** |
| ⚠ **`scenes/adventure/floor_relic_select.tscn` / `.gd`** | ⚠ **新規。⚠ 3択 →（1人用なら）誰に付けるか** |
| `floor_map.gd` / `.tscn` | ⚠ **レリックのノードから選択画面へ ／ 所持レリックの1行** |
| `ja.csv` | ⚠ **35行**（⚠ **レリック12＋画面9＋状態のマスの漢字13＋1**） |
| `debug_boot.gd` | ⚠ **`scenario=floor` にレリックの節 ／ `LAYOUT_SCENES` に1行 ／ 再インポートの合図を差し替え** |

### 2-1. ⚠ 設計役の決め

- ⚠ **`get_battle_passives()` と1本にまとめない。** ⚠ **あちらは「レベルで解放された恒久のパッシブ」で育成画面のスキル枠にも出る。⚠ 混ぜるとそこにフロアのレリックが並ぶ**
- ⚠ **持ち物一覧は専用画面にせず、⚠ マップ画面の1行にした**（⚠ **フロア中はここが唯一の一覧なので常に見えていること**）
- ⚠ **3択**（`CHOICE_COUNT`）。⚠ **すでに持っているものも候補に入れる**（⚠ **同じレリックは重ねてよい＝§5-2-5**）
- ⚠ **選ばずに出られない。⚠ 踏んだら必ず1つ取る**（⚠ **行き止まりを作らない**）

---

## 3. 完了条件（**ログ**・**全部通った**）

| # | 結果 |
|---|---|
| **L-1** | ✅ **`relics.json` 12件（3人用6 / 1人用6）** |
| **L-2** | ✅ **⚠ `get_skill()` で引けないレリック = **0件**（⚠ **引けないと戦闘で無音で消える。⚠ ここが今回いちばん危なかった**） |
| **L-3** | ✅ **3人用は3人全員に、⚠ 1人用は指定した1人にだけ並ぶ** |
| **L-4** | ✅ **1人用に `character_id` を渡さない／編成に居ないIDを渡すと `false`。⚠ 所持は増えない** |
| **L-5** | ✅ **`abandon_floor()` で所持が **0件** に戻る**（⚠ **フロア内限定＝§5-2-2**） |
| **L-6** | ✅ **`skills validated: 106 entries, 0 errors`**（⚠ **94 → 106。⚠ レリック12件ぶん**） |
| **L-7** | ✅ **`floor_relic_select.tscn` 最小 **452 x 212**（基準 1280 x 720）** |
| **L-8** | ✅ **`KIND_BATTLE` 21本すべて 赤0** |
| **L-9** | ✅ **30シナリオの赤の平常値は不変**（`unlock` 1 ／ `workshop` 2 ／ ほか0） |

### 3-1. ⚠ 黄の平常値が **13本増えている**（**再インポート待ち**）

⚠ **`ui_status_ch_status_relic_*` の13行が `ja.csv` に入ったが、⚠ 再インポートが済むまで `tr()` がキーをそのまま返すため、⚠ 「状態のマスの漢字が翻訳表に無い」の黄が13本出る。**
⚠ **再インポートが済めば **1本**（`skill_dbg_dot_odd`）に戻るのが正解。**
⚠ **合図は `scenario=passives` の `ja.csv の再インポート:` の1行**（⚠ **キーを `ui_status_ch_status_relic_thorns` に差し替え済み＝ズレ37 の手順**）。

---

## 4. 完了条件（**画面**）… ✅ **2026-08-25・人間が7項目とも確認済み**

⚠ **`F4` の「画面を全部解放」を押してから。**

| # | 何をする | 何が見えれば正解 | 結果 |
|---|---|---|---|
| **R-1** | フロアに入り、マップを見る | ⚠ **「レリック: なし」の行がある** | ✅ |
| **R-2** | レリックのマスを押す | ⚠ **3つ並ぶ。⚠ 名前の横に「3人全員」か「1人だけ」と出る** | ✅ |
| **R-3** | 「3人全員」のものを取る | ⚠ **マップに戻り、⚠ 上の行に名前が出る** | ✅ |
| **R-4** | 「1人だけ」のものを取る | ⚠ **誰に付けるか3人から選ぶ画面になる。⚠ 選ぶとマップに戻り、⚠ 名前の後ろに(キャラ名)が付く** | ✅ |
| **R-5** | そのあと戦闘に入る | ⚠ **キャラの頭上の状態のマスに漢字が出る** | ✅ |
| **R-6** | ⚠ **フロアを降りて入り直す** | ⚠ **「レリック: なし」に戻っている** | ✅ |
| **R-7** | レリックのマスを2回踏む | ⚠ **2つとも持っている（重ねられる）** | ✅ |

### 4-1. ⚠ `godot.log` で裏が取れたこと（**設計役が読んだ**）

- ⚠ **赤 **0本**・黄 **1本**（`skill_dbg_dot_odd`）＝ `ja.csv` の再インポートが済んでいる**（⚠ **状態のマスの漢字の黄13本が消えた**）
- ⚠ **`take_relic('relic_sharp_eye', 'char_priest')` … 1人用に誰に付けるかが渡っている**（R-4）
- ⚠ **`take_relic('relic_banner_of_war', '')` … 3人用は `character_id` が空**（R-3）
- ⚠ **所持 1件 → 2件 … 重ねられる**（R-7）
- ⚠ **`abandon_floor()` のあと入り直すと所持が1件から始まる … フロア内限定が効いている**（R-6）

### 4-2. ⚠ 人間から出た要望（**宿題62**）

⚠ **「コンポーネントでホバーしたら出てくるモーダルを後で作ったほうがいい」**（2026-08-25）。
⚠ **§5 の申し送り5（選択画面に効果の説明が出ない）への答え。⚠ レリック専用の解決ではなく、⚠ 使い回せる部品として作る。**
⚠ **`PROJECT_STATUS.md` の宿題62 に落とした。⚠ この段階ではやらない。**

---

## 5. ⚠ 申し送り

| | 中身 |
|---|---|
| ⚠ **1** | ⚠ **`ja.csv` の再インポートが要る**（35行）。⚠ **済むまで黄が13本多い** |
| ⚠ **2** | ⚠ **レリックの効果量は勘の数値**（⚠ **atk +15% / def +8 など）。⚠ 測っていない＝宿題50 と同じ形** |
| ⚠ **3** | ⚠ **フロア系レリック**（宝箱の率・たいまつ・休憩の回復量・ショップの割引）は **14-f の後**（決定11） |
| ⚠ **4** | ⚠ **`relic_thorns` の `react: took_damage` は戦闘で実際に跳ね返るところを見ていない**（⚠ **ヘッドレスでは撃たせていない。⚠ 人間が R-5 で見るのが唯一の確認**） |
| ⚠ **5** | ⚠ **選択画面に効果の説明が出ない**（⚠ **名前と適用範囲だけ）。⚠ 何が起きるか分からないまま選ぶことになる。⚠ 説明文の欄をどこに持つかは未決** |
