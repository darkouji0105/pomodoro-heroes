# EXEC — **状態のUI**（味方はスキル列の中・敵はHPバーの上・色付きのマス＋漢字1文字）

前提は `docs/NEXT_STEPS.md` §5（宿題20）。直前の回は `docs/02_exec/EXEC_SILENT_HOLES.md`。
検証の道具は `res://tests/debug_boot.tscn`。

⚠ **この回で器は増えない。** ⚠ **戦闘の数値は1つも動かない。** ⚠ **増えるのは「見えること」だけ。**
⚠ **段階1〜6で積んだ状態が、いま画面に1つも出ていない。** ⚠ **人間はログでしか異常に気づけない。**

---

## 0. 人間が決めたこと（**本文と矛盾する場合はこちらが優先**・2026-08-22）

| # | 決めたこと | 内容 |
|---|---|---|
| **1** | **束** | ⚠ **次の回は「状態のUI」にする。⚠ バランスの実測はもっと後**（⚠ **作業場の作り直しが未着手なので、測る対象が作り直しになる。`PROJECT_STATUS.md` 794章の前提がまだ生きている**） |
| **2** | **出す場所** | ⚠ **味方＝スキルアイコンの横に、一人ずつ専用のものを置く** ／ ⚠ **敵＝敵のHPバーの上** |
| **3** | **見た目** | ⚠ **色付きのマス ＋ 漢字1文字**（⚠ **画像素材が1枚も無いため**） |
| **4** | **色** | ⚠ **3つだけ。`dot`（`heals: false`）＝赤 ／ `dot`（`heals: true`）＝緑 ／ `buff`＝青。⚠ デバフも青に含める**（⚠ **器に「良い／悪い」の欄が無い。⚠ 分けるのは次の回**） |
| **5** | **範囲** | ⚠ **味方も敵も出す。⚠ 種類は `buff` / `dot` / `react` の3つ全部** |

---

## 0-1. ⚠ 設計役が自分で決めたもの（**人間が見ていない決め・要確認**）

| # | 決めたこと | なぜそうしたか |
|---|---|---|
| **1** | ⚠ **味方の欄は「各キャラのスキルボタン列の左側」に、⚠ 縦並びで置く**（⚠ **人間の指示・2026-08-22。⚠ 初稿は「列の中・名前の下」だったが覆された**） | ⚠ **理由は人間の指示どおり「スキルボタンはいずれ絵だけになる」から**（⚠ **今は文字幅を食っているが、⚠ 絵になれば列が細くなり、⚠ 左に1本置いても幅が破綻しない**）。⚠ **実装は、キャラごとに `HBoxContainer` を1枚かぶせ、⚠ 左に縦のマス帯・右に既存の `VBoxContainer`（名前＋ボタン）を入れる** |
| **2** | ⚠ **敵の欄は `ShieldBar` のさらに上**（`y` は `-62 〜 -50`） | ⚠ **`HpBar` は `-40〜-32`、`ShieldBar` は `-48〜-41` を既に使っている**（`unit_view.tscn`）。⚠ **「HPバーの上」に素直に置くとシールドと重なる** |
| **3** | ⚠ **`react` は青**（`buff` と同じ） | ⚠ **人間の決定4が「色は3つだけ」。⚠ 4色目を足すと決定に反する。⚠ 漢字で区別する** |
| **4** | ⚠ **状態を引く口を `StatusRegistry.entries_for(unit_id)` の1本にする**（⚠ **新設。中で `_applies_to()` を通す**） | ⚠ **`_applies_to()` は「状態が効くか」の唯一の判定**（`NEXT_STEPS` §3）。⚠ **UIが `_entries` を自分で回すと2本目の判定になり、⚠ オーラ（`host: point`）が画面に出ない／出続ける事故が起きる。⚠ この器で3回踏んでいる形** |
| **5** | ⚠ **`UnitView` に `StatusRegistry` を渡さない。⚠ `battle_controller` が entry の配列を渡す** | ⚠ **既存の `set_shield()` と同じ形**（`unit_view.gd:80` のコメントが「ビューは `BattleUnit` しか知らない。器を知るとリトライで古い参照を握る」と名指しで禁じている） |
| **6** | ⚠ **毎フレーム作り直さない。⚠ 「署名」（`instance_id` を並べた文字列）が変わったときだけ組み直す** | ⚠ **`query()` は参照を返すので引くのは軽いが、⚠ `ColorRect` と `Label` を毎フレーム作り直すと重いうえに文字がちらつく。⚠ 残り時間のバーだけは毎フレーム更新する** |
| **7** | ⚠ **作り直しは `remove_child()` → `queue_free()`** | ⚠ **`CLAUDE.md` 5番。⚠ `await process_frame` を持たせるとマスが二重に並ぶ** |
| **8** | ⚠ **漢字は `ja.csv` の `ui_status_ch_<status_id>` から引く**（⚠ **JSON に欄を足さない**） | ⚠ **JSON に `icon_char` を足すと、⚠ 同じ `status_id` を2つのスキルが付けるときに食い違う**（`status_edbg_dot` が実際に2ファイルから付いている）。⚠ **翻訳表なら `status_id` ごとに必ず1行。⚠ `.gd` を触らず追記できる＝Ziva に渡せる** |
| **9** | ⚠ **翻訳表に行が無いときは `？` を1文字出す** | ⚠ **`AGENTS.md` は「キーが無ければキー文字列がそのまま返るのを許容する」と書いているが、⚠ ここは 16px のマスなので `ui_status_ch_status_holy_burn` がはみ出して画面が崩れる。⚠ 漏れが見える（`？`）という目的は保たれる。⚠ しかも下の W17 でロード時に件数が分かる** |
| **10** | ⚠ **W17（黄）を新設し、`ja.csv` に行が無い `status_id` を出す** | ⚠ **`？` だけだと、⚠ 漏れているのか本当にその状態なのかが画面から区別できない。⚠ 48件すべてに行を入れて「0件で通る」状態にし、⚠ §6-F で壊して確かめる**（⚠ **0件で通る検査は生きているか分からない**） |
| **11** | ⚠ **マスの数を決め打ちにしない。⚠ 件数に応じてマスの大きさを変える**（⚠ **人間の指示・2026-08-22。⚠ 初稿は「6個まで・溢れたら `＋N`」だったが覆された**） | ⚠ **1〜4件は 16px ／ 5〜8件は 12px ／ 9件以上は 8px。⚠ 上限は「件数」ではなく「置ける場所の大きさ」で決まる**（味方＝列の高さ ／ 敵＝`Body` の幅 64px の2倍まで）。⚠ **それでも入りきらないときだけ、⚠ 最後のマスを `＋N` にする**（⚠ **`＋N` は事故の逃げ道であって、⚠ 通常は出ない**） |
| **12** | ⚠ **条件が偽の状態（`active: false`）は半透明で出す**（消さない） | ⚠ **消すと「条件で切れた」のか「寿命で消えた」のかが画面から区別できない。⚠ 段階3の後半②で作った条件の唯一の見え方になる** |
| **13** | ⚠ **`E` は使わない。⚠ `W` は W17 から** | ⚠ **E117 / W16 まで使用済み。⚠ W6・W7 は欠番のまま。⚠ この回は JSON の欄を1つも足さないので赤の追加は無い** |

---

## 1. いま何がどうなっているか（**実コードで確認済み・2026-08-22**）

| # | 事実 | 実コード |
|---|---|---|
| **A** | ⚠ **状態は画面に1つも出ていない** | ⚠ **`unit_view.gd` が持つのは `Body` / `HpBar` / `ShieldBar` / `NameLabel` の4つだけ。⚠ 状態は `F3` パネルと `P` キー（`print`）でしか見えない** |
| **B** | ⚠ **画像素材が1枚も無い** | ⚠ **`assets/images/` は `.gitkeep` の1ファイルだけ** |
| **C** | ⚠ **状態の表示名が翻訳表に1行も無い** | ⚠ **`ja.csv` に `ui_status_` で始まる行が0件** |
| **D** | ⚠ **良い／悪いを示す欄が器に無い** | ⚠ **`kind` は `buff` / `dot` / `react` の3つ。⚠ 攻撃力ダウンも防御ダウンも `kind: buff`（`value` が負なだけ）。⚠ 介入だけの `buff`（復活・免疫・シールド）は `stat` すら持たない** |
| **E** | ⚠ **表示に要る値は entry に全部ある** | ⚠ **`status_id` / `kind` / `heals` / `duration_sec` / `elapsed` / `stack` / `max_stack` / `active` / `host` / `host_unit_id`**（`status_registry.gd:292-340`） |
| **F** | ⚠ **`StatusRegistry` に「状態が増えた／消えた」のシグナルが無い** | ⚠ **シグナルは `effects_applied` の1本だけ**（`status_registry.gd:30`）。⚠ **UI は自分で引きに行くしかない** |
| **G** | ⚠ **`query()` は参照を返す**（軽い） | ⚠ **`snapshot()` は `duplicate(true)` なので毎フレーム呼べない**（`status_registry.gd:1454`） |

### 1-1. ⚠ **本番データで実際に出る状態は3件だけ**（**§7 を書くために全部確認した**）

| 状態 | 誰が付ける | 誰に付く | 種類 | 見え方 |
|---|---|---|---|---|
| `status_power_slash_atk_up` | ⚠ **剣士 `skill_power_slash`**（Lv1・CD6秒・`instant`） | ⚠ **自分** | `buff`（`atk +6` / 8秒 / `refresh`） | ⚠ **青のマス。8秒で消える** |
| `status_wide_sweep_guard` | ⚠ **剣士 `skill_wide_sweep`**（Lv1・`charge`） | ⚠ **自分** | `buff`（`def +20` / `until: charge_end`） | ⚠ **ためている間だけ青のマス。離すと消える** |
| `status_holy_burn` | ⚠ **僧侶 `skill_holy_ray`**（Lv1・CD12秒・全体） | ⚠ **敵全員** | `dot`（6秒 / 2秒周期 / `independent` / `max_stack: 5`） | ⚠ **赤のマスが最大5個** |

⚠ **本番の敵（`enemy_slime` / `enemy_wolf` / `boss_slime_king`）は `skills` を1つも持たない。**
→ ⚠ **敵が味方に状態を付けることは本番では起きない。⚠ 味方の欄に出るのは剣士の2件だけで、⚠ 弓兵と僧侶の欄は空のまま**（⚠ **これは事故ではない。§7 に明記する**）。

⚠ **`react`（購読）を持つ本番スキルは0件。⚠ オーラ（`host: point`）も本番に0件。**
→ ⚠ **人間の決定5「3種類全部」は器としては満たすが、⚠ 本番ステージでは `react` と `point` は画面に出ない。⚠ 見るのは検証用シナリオ（`§6`）のほう。**

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ 画面の完了条件は「その配置で本当に起きるか」を確かめてから書く

⚠ **3回続けて外している**（後衛の召喚は狙われない／反射を持つキャラが前衛でない／味方どうしのオーラは相対距離が変わらない）。
→ ⚠ **§1-1 で本番データを全部引いた。⚠ §7 はこの3件だけで書く。⚠ それ以外を人間の確認項目にしない。**

### 2-2. ⚠ 再描画に `await` を持たせない

⚠ **`CLAUDE.md` 5番。⚠ `remove_child()` してから `queue_free()`。**

### 2-3. ⚠ 「状態が効くか」の判定を2本目にしない

⚠ **UI が `_entries` を自分で回すと、⚠ オーラ（`host: point`）の出入りが `_applies_to()` と食い違う。**
→ ⚠ **`entries_for()` の1本を通す**（§0-1 の4）。

### 2-4. ⚠ `UnitView` は死ぬと `hide()` する

⚠ **`_process()` が `is_alive()` で `hide()` して `return` している**（`unit_view.gd:99`）。⚠ **マスの更新をこの `return` より下に書くと、⚠ 死んだ瞬間に更新が止まる**（復活したときに古いマスが出る）。

### 2-5. ⚠ 幅を先に決める

⚠ **`Body` は64px。⚠ マスを16px にすると4個で本体と同じ幅になる。⚠ 6個で96px。⚠ 中央揃えにすること**（左揃えにすると本体とずれる）。

---

## 3. 実装（ファイル別）

### 3-A. `scripts/systems/status_registry.gd` … 引く口を1本足す

```
func entries_for(unit_id: String) -> Array
```

- ⚠ **`_entries` を回し、⚠ `_applies_to(entry, unit_id)` が真のものだけ返す**
- ⚠ **返すのは参照**（`query()` と同じ。⚠ `duplicate(true)` しない）
- ⚠ **`active: false` も返す**（⚠ 半透明で出すため。§0-1 の12）
- ⚠ **並びは `_entries` の順**（＝付いた順）。⚠ **並べ替えない**（順が揺れるとマスが入れ替わってちらつく）

⚠ **`_applies_to()` を UI から直接呼ばない。⚠ この関数の外に判定を書かない。**

### 3-B. `scenes/adventure/status_chips.gd` … **新規**（マスの帯）

`class_name StatusChips extends BoxContainer`

⚠ **`HBoxContainer` ではなく `BoxContainer` にする。** ⚠ **`vertical` を切り替えるだけで縦にも横にもできる**（⚠ **味方＝縦・敵＝横。⚠ 2つのクラスに分けると、色と漢字の決め方が2箇所になる**）。

| 関数 | 中身 |
|---|---|
| `setup(is_vertical: bool, max_px: float) -> void` | ⚠ **向きと、⚠ 入る長さ（味方＝高さ・敵＝幅）を1回だけ渡す** |
| `set_entries(entries: Array) -> void` | ⚠ **署名（`instance_id` を `,` で連結）が前回と同じなら何もしない。⚠ 違えば組み直す** |
| `_rebuild(entries: Array) -> void` | ⚠ **`remove_child()` → `queue_free()` の順。⚠ `await` を持たせない** |

- ⚠ **マスは `ColorRect` ＋ 中央の `Label`（漢字1文字）**
- ⚠ **辺の長さは件数で決める**（§0-1 の11）：**1〜4件＝16px ／ 5〜8件＝12px ／ 9件以上＝8px**。⚠ **文字はその 6割**
- ⚠ **色は3つだけ**：`kind == "dot"` かつ `heals == false` → 赤 ／ `kind == "dot"` かつ `heals == true` → 緑 ／ それ以外（`buff` / `react`）→ 青
- ⚠ **色と3段の大きさは `const` で持たず `AdventureConfig` から引く**（`AGENTS.md` の数値管理ルール。⚠ **`unit_view.gd:26` が同じ理由でポップの色を `Balance.adventure` から引いている**）
- ⚠ **`.tres` は触らない**（⚠ **`adventure_config.gd` に `@export` を足すだけで既定値付きで Inspector に出る。⚠ 同ファイルのコメントが名指しでそう書いている**）
- ⚠ **`active == false` なら `modulate.a = 0.4`**
- ⚠ **`max_px` に入りきらないときだけ、⚠ 最後のマスを `＋N` にする**（⚠ **通常は出ない。§0-1 の11**）
- ⚠ **`tr("ui_status_ch_" + status_id)` の戻りがキーと同じなら `？`**（§0-1 の9）

⚠ **置き場所は `scenes/adventure/`。**（⚠ **`AGENTS.md` の「1画面だけで使うものは画面フォルダ」。⚠ 戦闘画面でしか使わない。⚠ 新しいフォルダは作らない**）

### 3-C. `scenes/adventure/unit_view.tscn` / `.gd` … 敵の欄

- ⚠ **`StatusChips` を子に足す**（`y` は `-62 〜 -50`・中央揃え。§0-1 の2）
- ⚠ **`set_status_entries(entries: Array) -> void` を1本足す**（⚠ **`set_shield()` と同じ形。⚠ `StatusRegistry` を持たせない**）
- ⚠ **`_process()` の `is_alive()` の `return` より上で更新しない**（§2-4）。⚠ **更新は `battle_controller` から呼ぶ**

### 3-D. `scenes/adventure/battle_controller.gd` … 味方の欄と、毎フレームの配布

- ⚠ **`_build_skill_buttons()` で、⚠ キャラごとに `HBoxContainer` を1枚かぶせ、⚠ 左に `StatusChips`（縦）・右に既存の `VBoxContainer`（名前＋ボタン）を入れる**（§0-1 の1）
- ⚠ **`_status_chips_by_unit_id` に控える**（⚠ **`_skill_buttons` に混ぜない。⚠ あちらはボタンの配列**）
- ⚠ **`_process()` で、⚠ `_all_units()` を回して `entries_for()` の結果を配る**（味方は列の帯へ・敵と召喚は `UnitView` へ）
- ⚠ **`_all_units()` を通す**（⚠ **味方 → 敵 → 召喚。⚠ 新しい配列を足したらここだけ直す**）

⚠ **`_build_skill_buttons()` はリトライで作り直される。⚠ `_status_chips_by_unit_id` も同じ場所で必ず捨てること**（⚠ **古い `BattleUnit` を掴んだままになる**）。

### 3-E. `scripts/systems/master_data_loader.gd` … W17

| # | 出すもの |
|---|---|
| **W17** | ⚠ **効果の `status_id` に対応する `ui_status_ch_<status_id>` が翻訳表に無い**（⚠ **黄。赤にしない**） |

⚠ **`TranslationServer.translate()` を使う**（⚠ **静的クラスから `tr()` は呼べない。`AGENTS.md`**）。
⚠ **同じ `status_id` が2つのスキルから付いている場合、⚠ 黄は2本出る**（⚠ **`status_edbg_dot` が該当**）。⚠ **重複を潰す仕組みは入れない。⚠ 検証はスキル1件ずつ独立に走る形で、⚠ ここに横断の状態を持たせると「どのスキルで漏れたか」が消える。**

### 3-F. `localization/ja.csv` … **48行**（⚠ **Ziva に渡せる部分**）

⚠ **`ui_status_ch_<status_id>` を48件。⚠ 値は漢字1文字。**
⚠ **UTF-8（BOMなし）。⚠ 編集後の再インポートはヘッドレス起動で自動的に走る**（⚠ **`AGENTS.md` の「人間が再インポート」は実際と違う。宿題1・未修正**）。

### 3-G. `tests/debug_boot.gd` … 検証用シナリオ

| シナリオ | 見るもの |
|---|---|
| ⚠ **`status_ui`（新規）** | ⚠ **`buff` / `dot`（ダメージ）／ `dot`（回復）／ `react` の4種類が同時に乗ること。⚠ 色の分岐が全部通ること** |
| ⚠ **`status_ui_over`（新規）** | ⚠ **1体に7件以上積んで `＋N` が出ること**（⚠ **`status_dbg_atk_stack` は `independent` で撃つたびに増える**） |

⚠ **`SCENARIOS` に2行足す。⚠ シーンを増やさない。**
⚠ **ヘッドレスは絵を出さない。⚠ このシナリオで取れるのは「entry がその瞬間に何件あったか」だけ**（§6）。

### 3-H. 触らないファイル

⚠ **`battle_formula.gd` ／ `skill_resolver.gd` ／ `skill_runtime.gd` ／ `skill_schema.gd` ／ `battle_session.gd` ／ `unit.gd`**
⚠ **本番スキルの JSON 全件 ／ `stages.json` ／ `enemies.json` ／ `characters.json` ／ `main_theme.tres`**

⚠ **`status_registry.gd` は `entries_for()` の追加だけ。⚠ 既存の関数を1つも変えない。**

---

## 4. 変えないもの

- ⚠ **戦闘の数値**（⚠ **この回で damage が1つも動かないこと。§6-E**）
- ⚠ **色は3つ**（人間の決定4）
- ⚠ **`_applies_to()` が唯一の判定**
- ⚠ **`UnitView` は器を知らない**
- ⚠ **`main_theme.tres`**（⚠ **触ると全画面の `ProgressBar` と `Label` が変わる**）

---

## 5. 完了条件 — **§0 事前チェック**（⚠ **設計役・ヘッドレス**）

1. ⚠ **全シナリオ（既存18 ＋ 新規2）で `ERROR:` / `SCRIPT ERROR:` / `Parse Error` が1行も出ないこと**
2. ⚠ **`skills validated` が `0 errors`。⚠ 黄は1本のまま**（⚠ **`skill_dbg_dot_odd` の端数だけ。⚠ W17 が0件であること＝48行が全部入っていること**）

## 6. 完了条件 — **ログ / ファイル**（⚠ **設計役が読む**）

### 6-A. 引く口

3. ⚠ **`entries_for()` が `_applies_to()` を通っていること**：⚠ **`aura` シナリオで、⚠ 範囲に入った味方の件数が `ev: "zone"` の `enter` / `leave` と一致すること**
4. ⚠ **`aura_follow` でも同じであること**

### 6-B. 4種類が揃うこと

5. ⚠ **`status_ui` で、⚠ 同じ瞬間に `buff` / `dot`(damage) / `dot`(heals) / `react` の4件が乗っていること**（⚠ **`status_add` の4行を秒で突き合わせる**）
6. ⚠ **`status_ui_over` で、⚠ 1体に7件以上積まれた瞬間があること**

### 6-C. 数値が動いていないこと

7. ⚠ **`area` 3/4/4/3 ／ `recast` 2/8 ／ `summon` の `x` が −60/−120 ／ `mitigate` 200/300/200/120 ／ `pierce` 194/200 ／ `lineup` 99.4 ／ `atk_mult` 200→400 ／ `pool` の `zone` 12件**
8. ⚠ **`dot_react` の `react` が6件のままであること**
9. ⚠ **`save_slot_0.json` が変わらないこと**

### 6-D. 翻訳表

10. ⚠ **`ja.csv` が48行増えていること。⚠ BOM が付いていないこと**
11. ⚠ **重複キーが無いこと**（⚠ **`ui_common_yes` / `ui_common_no` が既に2行ずつある。⚠ 増やさない**）

### 6-E. ⚠ 足した検証が本当に出るか

12. ⚠ **W17 を実測する**（⚠ **`ja.csv` から1行消して、⚠ その `status_id` 名で黄が1本出ることを見る。⚠ 戻して0件に戻ることも見る**）

## 7. 完了条件 — **画面**（⚠ **人間だけ**・⚠ **§1-1 で本番データを引いて裏を取ってある**）

⚠ **編成は既定のまま**（僧侶・弓兵・剣士）。⚠ **スキル枠は、剣士＝`力の一撃` と `横薙ぎ`、僧侶＝`聖なる光線` を選ぶこと**（⚠ **この3つ以外では状態が1つも出ない。§1-1**）。⚠ **ステージは `stage_1`。**

| # | 見るもの |
|---|---|
| **13** | ⚠ **剣士の`力の一撃`を押すと、⚠ 剣士のスキルボタンの左に青いマスが1つ出る**（漢字1文字・縦並び） |
| **14** | ⚠ **そのマスが8秒あとに消える**（⚠ **ウェーブが変わっても消えない。⚠ 消えるのは寿命**） |
| **15** | ⚠ **剣士の`横薙ぎ`を押している間だけ、⚠ 別の青いマスが下に増える。⚠ 指を離すと消える** |
| **16** | ⚠ **僧侶の`聖なる光線`を撃つと、⚠ 敵全員のHPバーの上に赤いマスが出る** |
| **17** | ⚠ **同じ敵に撃ち直すと、⚠ 赤いマスが横に増える**（⚠ **最大5個。⚠ 6個目は出ない**）。⚠ **5個になったときにマスが一段小さくなること**（⚠ **§0-1 の11。⚠ 4個までは 16px・5個からは 12px**） |
| **18** | ⚠ **赤いマスが出ている間、⚠ その敵のHPが2秒おきに減る**（⚠ **マスと毒の周期が一致していること**） |
| **19** | ⚠ **弓兵と僧侶の列は空のまま**（⚠ **これは正しい。⚠ 本番の敵はスキルを持たないので、⚠ 味方に状態が付く経路が無い**） |
| **20** | ⚠ **敵が死ぬと、⚠ その敵のマスも一緒に消える**（⚠ **マスだけ画面に残らないこと**） |
| **21** | ⚠ **マスがユニットの中央に揃っていること。⚠ 名前ラベル・HPバー・シールドバーのどれとも重なっていないこと** |
| **22** | ⚠ **リトライを押しても、⚠ 前の戦闘のマスが残っていないこと** |
| **23** | ⚠ **スキルボタンの文字が読めること**（⚠ **左に帯を足したぶん、⚠ ボタンの幅が狭くなる。⚠ 人間の指示どおり「いずれ絵だけになる」前提だが、⚠ 今の文字が折り返して読めなくなっていないかを見る**） |
| **24** | ⚠ **漢字が `？` になっているマスが1つも無いこと**（⚠ **出たら `ja.csv` の漏れ。⚠ どの状態で出たかを報告してほしい**） |

---

## 8. 将来コードを変えたときに見る項目

- ⚠ **良い／悪いで色を分けるか**（⚠ **今回は「色は3つだけ」。⚠ 分けるなら器に欄が要る**）
- ⚠ **`react` と `buff` が同じ青のままでよいか**
- ⚠ **オーラ（`host: point`）のマスが、⚠ 範囲を出るときに本当に消えるか**（⚠ **本番データに `point` が0件なので、⚠ 検証用シナリオでしか見えない**）
- ⚠ **マスの3段（16 / 12 / 8px）の刻みが妥当か**（⚠ **`max_stack` が5を超える状態を足したときに見直す**）
- ⚠ **スキルボタンが絵になったら、⚠ 左の帯の幅を見直す**（⚠ **人間の指示の前提**）
- ⚠ **残り時間をマスに出すか**（⚠ **今回は出さない。⚠ 16px に数字は入らない**）

---

## 9. Ziva に渡せる部分

⚠ **ある。⚠ `localization/ja.csv` の48行（§3-F）。**
⚠ **`.gd` を1行も触らない。⚠ ただし `.gd` の側（3-B の `？` と 3-E の W17）が先に入っていないと、⚠ 入れたかどうかが画面にもログにも出ない。**
→ ⚠ **順番は「`.gd` → `ja.csv`」。⚠ 同時に渡さない。**

---

## 10. 終わったあとに足す宿題

- ⚠ **解消：状態のUIが無い**（宿題20）
- ⚠ **一部解消：オーラの範囲が画面に描かれない**（宿題16。⚠ **効いている相手にマスは出るが、⚠ 範囲そのものは描かれない**）
- ⚠ **NEW：`ui_status_ch_*` の45行は検証用。⚠ リリース前に消す**（⚠ **宿題18の一覧に足す**）
- ⚠ **NEW：色が3つしかない**（⚠ **デバフが青。⚠ 分けるなら器に欄が要る**）
- ⚠ **NEW：本番スキルに `react` と `host: point` が0件**（⚠ **器はあるがプレイヤーには一度も見えない**）

---

## 11. ⚠ 実施結果（**2026-08-22・設計役がヘッドレスで実行して判定**）

⚠ **§5・§6 は全部通った。⚠ §7（画面）は人間の実施待ち。**

| 項目 | 結果 |
|---|---|
| 5-1 全シナリオで赤が1行も出ない | ✅ **19シナリオで `赤=0` / `exit=0`** |
| 5-2 検証の件数 | ✅ **`79 entries, 0 errors, 1 warnings`**（黄1本は `skill_dbg_dot_odd` の端数＝従来どおり）。⚠ **W17 は0件** |
| 6-A-3 `entries_for()` が `_applies_to()` を通る | ✅ **`aura` の `zone` が `enter` 3 / `leave` 1。⚠ `party_1` が出て入り直している** |
| 6-A-4 `aura_follow` も同じ | ✅ **`enemy_1_2` の `enter` / `leave` が1本ずつ** |
| 6-B-5 4種類が同時に乗る | ✅ **`t=6.58` に4件が生存**：`react`(party_0) ／ `buff`(party_2) ／ `dot`(party_0・赤) ／ `dot heals`(範囲・party_0/1/2 が `enter`)。⚠ **`party_0` は1人で3色を持つ** |
| 6-B-6 1人に7件 | ✅ **`party_2` に7件**（`status_dbg_atk_stack` ×5 ＋ `status_dbg_atk_refresh` ＋ 範囲の `st_dbg_pool_heal`） |
| 6-C-7 既存の数値が動いていない | ✅ **`area` 3/4/4 ／ `recast` 2/8 ／ `summon` の間隔 60 ／ `mitigate` 200/300/200/120 ／ `pierce` 194 ／ `lineup` 99.4 ／ `atk_mult` 200→400（同じ `enemy_1_2`）／ `pool` の `zone` 12件 ／ `shield` 200/170/5×4 ／ `reflect` 200/100** |
| 6-C-8 `dot_react` の `react` が6件 | ✅ **6件のまま** |
| 6-C-9 セーブ | ✅ **`save_slot_0.json` は存在しないまま** |
| 6-D-10 `ja.csv` が48行増えた | ✅ **308 → 356行。⚠ BOM なし・CR なし（`od` で確認）** |
| 6-D-11 重複キーが無い | ✅ **0件**（⚠ **既存の `ui_common_yes` / `ui_common_no` の2重も増やしていない**） |
| 6-E-12 W17 が本当に出るか | ✅ **§11-2** |

### 11-1. ⚠ 実装中に踏んだこと

**① ⚠ `_applies_to()` は `active` が偽だと `false` を返すので、⚠ そのままでは §0-1 の12（条件が偽の状態を半透明で出す）が書けなかった。**

⚠ **判定を2本に増やす（＝この器で3回踏んでいる形）のを避けるため、⚠ `_applies_to()` を2つに割った。**
- ⚠ **`_hosts()`** … 宿主一致・範囲内だけを見る。⚠ **新しい `host` を足すときに直すのは引き続きここ1箇所**
- ⚠ **`_applies_to()`** … `active` のゲート ＋ `_hosts()`。⚠ **中身は変えていない**

⚠ **`_hosts()` を直接呼んでよいのは `entries_for()`（＝画面）だけ。⚠ 効くかどうかを判断する側は必ず `_applies_to()` を通す**（コメントに明記した）。
⚠ **EXEC §3-H は「`status_registry.gd` は `entries_for()` の追加だけ」と書いていたので、⚠ これは指示書からの逸脱。**

**② ⚠ 新しい `class_name` はヘッドレス実行では登録されない（1タスク分の足止め）。**

⚠ **`battle_controller.gd` が `Parse Error: Could not find type "StatusChips"` でロードできず、⚠ 戦闘が始まらないまま `GIVE_UP_SEC`（180秒）まで回った。**
⚠ **`.godot/global_script_class_cache.cfg` に `StatusChips` が0件。⚠ `class_name` はエディタ起動時にしかスキャンされない**（`AGENTS.md`「エラーを理由にルールを緩めない」に載っている形そのもの）。
→ ⚠ **`Node` 型 ＋ `has_method()` で逃げる回避はしていない。⚠ 人間にエディタを1回通してもらって解消した。**

**③ ⚠ `ja.csv` の再インポートは、ヘッドレス実行では走らない。**

⚠ **48行足したのに W17 が49件出た**（＝1件も引けていない）。⚠ **`ja.ja.translation` が古いままだった**（`ja.csv` 13:00 に対し 10:54）。
→ ⚠ **`NEXT_STEPS.md` §2-1 の「`ja.csv` はヘッドレス起動で自動的に再インポートされている」は、⚠ 少なくとも新しいキーには当てはまらない**（⚠ **報告のみ。勝手に直していない**）。

**④ ⚠ `--check-only --script` は Autoload を読まない。**

⚠ **`Identifier not found: Balance` / `GameManager` / `SceneManager` が出るが、⚠ これはチェックモードの性質であって構文エラーではない**（⚠ **`Parse Error` は1件も出ていない＝構文は通っている**）。⚠ **構文だけ先に確かめたいときに使える。**

### 11-2. ⚠ 足した検証が本当に出ることを確かめた

| 壊し方 | 出たもの |
|---|---|
| ⚠ **`ja.csv` の48行を Godot が読んでいない状態**（再インポート前） | ✅ **W17 が48本**（`skills validated: 79 entries, 0 errors, 49 warnings`）。⚠ **`status_id` の名前とキー名が1件ずつ出た** |
| 再インポート後 | ✅ **`0 errors, 1 warnings` に戻った**（黄1本は従来の端数） |

⚠ **1行だけ消して試す形にはしていない。⚠ 48件が一斉に出て一斉に消えたので、⚠ 「行が引けないと必ず出る」ことはこれで確かめられている。**
⚠ **ただし試したのは「翻訳が読み込まれていない」状態で、⚠ 「行だけ無い」状態ではない**（⚠ **コードから見ればどちらも `translate()` がキーを返すので同じ枝**）。

### 11-3. ⚠ ヘッドレスを24回走らせた
