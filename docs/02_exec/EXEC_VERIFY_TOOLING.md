# EXEC — **検証の道具を入れ替える**（⚠ ゲームの中身は作らない回）

**`docs/NEXT_STEPS.md` §1 そのもの。** ⚠ **段階5（`phases[]` / `recast`）はこの回のあと。**
⚠ **本番コード（`.gd` / `.json` / `.tres` / `ja.csv`）を1行も触らない。** 触るのは `AGENTS.md` だけ（しかも**貼るかは人間の判断**）。

---

## 0. 人間が決めたこと（**本文と矛盾する場合はこちらが優先**・2026-08-18）

| 決めたこと | 内容 |
|---|---|
| **完了条件の型** | ⚠ **3章のまま**（「どこを見るか」で分ける決定は変えない）。**各章に「誰が取るか」を1行足し、先頭に §0「事前チェック」を新設する** |
| **ヘッドレスの扱い** | ⚠ **設計役が毎回使ってよい。** 走らせる前の**ログ退避も自動でやる**（人間に断らない） |
| **Ziva の担当** | ⚠ **無し**（§4）。⚠ **一度「検証の実行も Ziva に」と決めたが、同じ日に取り消した** |
| **次のタスク** | ⚠ **「操作の要らないデバッグ起動シーン」（`res://tests/`）を別タスクで作る**（§4-1）。⚠ **段階5より先にやるかは、そのとき決める** |
| **MCP** | ⚠ **放置。** 設定は残すが、使わない前提で書く（§1-8） |

### 0-0. ⚠ 取り消した決定（**記録として残す**・2026-08-18）

一度「**検証の実行も Ziva にやらせる**」と決めたが、**同じ日に人間の判断で取り消した。**

- ⚠ **設計役が出した懸念**：`AGENTS.md` 306行目「画面を見て確認する種類の完了条件を、実装役に検証させない。**実装役はヘッドレスでそれを再現しようとして、必ず時間を溶かす**」（実例：モーダルの検証で $0.66 を溶かして実装が中断）
- ⚠ **取り消した理由**：Ziva に渡しても**往復が「人間 → Ziva」に移るだけで消えない。** ⚠ **そもそも Ziva がゲーム内のボタンを押せるかが未検証**で、押せなければ全部やり直しになる
- → ⚠ **代わりに §4-1（操作の要らないデバッグ起動シーン）を次のタスクにした。** こちらは**往復を減らすのではなく無くす**

---

## 0-1. ⚠ 設計役が自分で決めたもの（**人間が見ていない決め・要確認**）

**この章は人間が目を通していない。違うと思うものがあれば貼る前に言うこと。**

| # | 決めたこと | なぜそうしたか |
|---|---|---|
| **1** | ⚠ **`--quit-after 300`（300フレーム）を固定値にする** | 実測4秒。`_ready()` の連鎖（`GameManager` → `MasterDataLoader` → `SceneManager`）が終わるのに足りる。⚠ **短すぎると検証が走り切る前に落ちて「0 errors」に見える** |
| **2** | ⚠ **ヘッドレスで見るのは「ロード時」だけと決める** | 戦闘に到達するにはボタンを押す必要がある。⚠ **入力を再現する仕組みを作ると `AGENTS.md` 306行目の事故になる。** 戦闘中の赤は `godot.log`（人間が遊んだあと）で読む |
| **3** | ⚠ **`godot.log` の退避を手順に固定する** | ⚠ **設計役がヘッドレスを走らせると、人間のログが1本押し出されて消える**（§2-1・実測）。**先に退避すれば、順番を間違えても失われない** |
| **4** | ⚠ **「画面」の章は人間のまま。Ziva にも渡さない** | ⚠ **Ziva もゲーム内のボタンは押せない**（§4-0 で最初に確かめさせる）。⚠ **「Ziva なら押せるはず」を前提に書かない** |
| **5** | ⚠ **`AGENTS.md` は追記のみ。既存の3章の表を書き換えない** | ⚠ **「どこを見るか」で分ける決定は変えない**（`NEXT_STEPS` §2-3）。**表を書き直すと、決定そのものを変えたように読める** |
| **6** | ⚠ **MCP の設定（`.claude.json`）を消さない・直さない** | ⚠ **今の結論は「MCP が無くても足りる」であって「MCP が壊れている」ではない。** 観測したのは「ツールが1つも見えない」だけ |
| **7** | ⚠ **セーブの退避は毎回やらない**（初回だけ） | ⚠ **ヘッドレス起動でセーブが書かれないことを実測した**（§1-3）。毎回コピーすると、事故ったときに「いつのセーブか」が分からなくなる |
| **8** | ⚠ **`godot_console.exe` を探しに行かない** | Steam 版には無い（`Godot Engine` フォルダに `.exe` は1つだけ）。⚠ **リダイレクトで取れているので、探す理由が無い** |

---

## 1. 実測で分かったこと（**2026-08-18・すべて実行した結果。推測は1つも無い**）

### 1-1. ⚠ Godot は設計役から起動できる（**`CLAUDE.md` の「起動できない」が事実と違っていた**）

```
D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe
バージョン: 4.7.1.stable.steam.a13da4feb
```

⚠ **ただし「直接叩く」形では出力が1行も返らない。** GUI アプリなので stdout がコンソールに繋がらない。

| 形 | 結果 |
|---|---|
| `& '...\godot.windows.opt.tools.64.exe' --version` | ⚠ **無音。終了コードすら空** |
| `Start-Process -RedirectStandardOutput/-RedirectStandardError` | ✅ **取れる** |

### 1-2. ⚠ ヘッドレスの実行（**この1行がこの回の成果物**）

```powershell
$exe = 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe'
Start-Process -FilePath $exe -NoNewWindow -Wait -PassThru `
  -ArgumentList '--headless','--path','d:\pomodoro-heroes','--quit-after','300' `
  -RedirectStandardOutput $out -RedirectStandardError $err
```

| | 実測 |
|---|---|
| 所要 | ⚠ **4秒** |
| 終了コード | `0` |
| 窓 | ⚠ **1つも出ない**（人間の画面を占領しない） |
| **stdout** | ⚠ **`[MasterDataLoader] skills validated: 59 entries, 0 errors, 1 warnings`** を含む通常ログ |
| **stderr** | ⚠ **警告・エラーだけが分かれて出る**（出力パネルより読みやすい） |

⚠ **読むときは `[System.IO.File]::ReadAllLines(path, [System.Text.Encoding]::UTF8)`。** `Get-Content` は既定 ANSI で**日本語が化ける**（`NEXT_STEPS` §5 と同じ罠が、ログの読み取り側でも出る）。

### 1-3. ⚠ ヘッドレス起動はセーブを書かない（**実測**）

`save_slot_0.json` は **14669 バイト / 2026-08-17 20:00:19 のまま**。
⚠ **ヘッドレスの起動時ログは `gold=100`（＝`initial_state` で初期化）で、そのあと `load_state` まで行かずに終わる。** **セーブを壊す経路が無い。**

### 1-4. ⚠ 画面の絵は取れない（**実測。ここは人間のまま**）

```
--headless --write-movie <file> → exit=-1073741819（アクセス違反）
ERROR: Parameter "t" is null.
   at: texture_2d_get (./servers/rendering/dummy/storage/texture_storage.h:110)
生成物: 0 バイト
```

⚠ **ヘッドレスは描画がダミーなので、絵のあるものは存在しない。**
⚠ **窓を出せば撮れるが、戦闘に到達するにはボタンを押す必要がある。** → **`AGENTS.md` の「画面」の章は人間のまま。**

### 1-5. ⚠ `user://logs/godot.log` は出力パネルの中身そのもの（**この回の一番大きい発見**）

`C:/Users/admin/AppData/Roaming/Godot/app_userdata/pomodoro-heroes/logs/godot.log`

⚠ **人間が普通に遊んだあと、設計役がそのまま読める。** 実際に 8/18 の段階4の検証回を読んで、以下が全部入っていることを確認した：

- `skills validated: 59 entries, 0 errors, 1 warnings`
- ⚠ **警告の本文と GDScript バックトレース**（`master_data_loader.gd:583` まで）
- ⚠ **`select_skill('char_debug_mix', 0, 'skill_dbg_area_narrow') -> true`** ＝ **スキル枠を押したかどうかが読める**（段階4で1回ぶん溶かした「押し忘れ」が、ログから判定できる）
- `change_scene` の履歴・`time_scale` の変更

→ ⚠ **段階4で溶けた6回のうち、1回目（「10 errors とだけ言われて本文が無い」）と2回目（本文の貼り直し）は、これで消える。**

### 1-6. ⚠ シーンを指定して起動できる／⚠ **ヘッドレスで戦闘が走る**（**2026-08-18・実測**）

⚠ **実行ファイルの引数に `res://...tscn` を渡すと、そのシーンから始まる。** `project.godot` のメインシーンを1文字も変えなくてよい。

```powershell
'--headless','--path','d:\pomodoro-heroes','res://scenes/adventure/battle.tscn','--quit-after','1800'
```

| | 実測 |
|---|---|
| シーン指定が効いた証拠 | ⚠ **`WARNING: [Battle] stage_id が渡されていないため stage_1 で開始する`**（`battle_controller.gd:94`）＋ `[BattleLog] ...` の行 |
| ⚠ **戦闘が進むか** | ✅ **進む。** 1800フレーム・**14秒**で `battle_start 1 / wave 2 / cast 7 / damage 7`（最終行 `t=11.14` の `damage`） |
| ⚠ **スキルは撃たれるか** | ❌ **撃たれない。** `cast` 7件は**全部が通常攻撃**。⚠ **スキルはボタンで撃つものなので、シーン側から `cast()` を呼ばないと出ない** |

⚠ **判定に使った合図に注意**：`training_screen.tscn` を指定した回は、**そのシーンが `print` を持たないため既定シーンの起動と区別がつかなかった。** ⚠ **「区別のつく合図があるシーン」で測ること**（段階4で `cast.targets` を使って判定を誤ったのと同じ形）。

### 1-7. ⚠ `--quit-after` で切ると `battle_last.jsonl` が途中で終わる（**実測で踏んだ**）

400フレームで切ったとき、`battle_last.jsonl` は **`battle_start` と `wave` の2行だけ**だった。⚠ **エラーは1つも出ない。**
→ ⚠ **行数で判定する完了条件を書くときは、`result` の行が出ているかを先に見ること。** ⚠ **`--quit-after` はあくまで保険で、本来は決着したらシーン側から `quit()` するのが正しい。**

### 1-8. ⚠ godot MCP は使えない（**そして要らない**）

⚠ **ツール一覧に godot 系が1つも無い**（4通りの語で検索して全部0件）。設定（`.claude.json` の `D:/pomodoro-heroes` → `mcpServers.godot`）は**書いてあるが、このセッションには届いていない。**
⚠ **godot MCP は Godot の実行ファイルを呼ぶラッパーなので、§1-2 が動く以上、間に挟む理由が無い。** §0-1 の6のとおり**設定は消さない。**

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ 設計役がヘッドレスを走らせると、人間のログが1本消える（**実測で踏んだ**）

`godot.log` は**起動のたびにローテートされ、保持は5本**。走らせる前は 08.28.59 / 08.36.37 / 08.48.21 / 08.50.42 ＋ `godot.log` の5本だったが、**ヘッドレスを1回走らせたら 08.28.59 が消えた。**

→ ⚠ **人間が遊んだ直後は、先に `godot.log` を退避してからヘッドレスを走らせる**（§0-1 の3）。**読みたいログを自分で押し出さないこと。**

### 2-2. ⚠ 「ログが読める」＝「画面を見た」ではない

⚠ **`CLAUDE.md`：「動きました」と書かない。** `godot.log` に赤が無いことは、**表示・ボタンの活性・レイアウトについて何も言っていない。**

### 2-3. ⚠ ヘッドレスで見えるのはロード時だけ

⚠ **戦闘中の赤・`battle_last.jsonl` の中身は、人間（または Ziva）が実際に戦わないと出ない。** ヘッドレスの「0 errors」を**戦闘の検証の代わりに使わない。**

### 2-4. ⚠ 赤が出たときの終了コードは未確認

⚠ **`skills validated:` に `errors` が乗ったときに終了コードが 0 のままかどうかは、まだ観測していない。**（今のデータは 0 errors なので試せない。⚠ **試すために本番データをわざと壊さない**。`CLAUDE.md`「切り分けのために本番コードを書き換えない」）
→ ⚠ **判定は終了コードではなく `skills validated:` の行の文字列で行う。**

### 2-5. ⚠ 静的な突き合わせは実機の代わりにならない（**この回で変わっていない**）

⚠ **段階4で、Python でロード時検証を真似て「OK」と言ったのに実機で10件の赤が出た**（`NEXT_STEPS` §5）。
→ ⚠ **§1-2 が使えるようになった今、Python で真似る意味は無い。真似ずに走らせること。**

---

## 3. `AGENTS.md` への追記案（**⚠ 貼るかは人間の判断。設計役は勝手に触らない**）

⚠ **既存の「完了条件は『どこを見るか』で3つに分ける」の表と本文は1文字も変えない**（§0-1 の5）。**その節の末尾に、下の枠内をそのまま足す。**

⚠ **足す前に `grep -n "事前チェック" AGENTS.md` が0件であることを確認すること**（`AGENTS.md` 299-302行「2回書かない」）。

```markdown
#### 誰が取るか（2026-08-18・実測で更新）

Godot は設計役から起動できる（`--headless`・4秒・窓は出ない・セーブは書かない）。
出力パネルの中身は `user://logs/godot.log` に残り、設計役が直接読める。
**「どこを見るか」で3章に分ける決定は変えない。変わったのは「誰が取るか」だけ。**

| 章 | 誰が取るか | 人間の仕事 |
|---|---|---|
| **§0 事前チェック**（新設） | 設計役（ヘッドレス） | **無し。人間に渡す前に終わっている** |
| **ログ** | 設計役（`godot.log` を読む） | **戦って終わるだけ。貼らない** |
| **ファイル** | 設計役（`battle_last.jsonl` 等を読む） | 無し |
| **画面** | **人間だけ** | **操作して見る** |

**§0 事前チェックは、人間に渡す前に設計役が必ず1回回す。**
ロード時の赤・黄・parse error はここで全部潰してから渡すこと。
**通っていないものを人間に渡さない。**

**画面の絵は取れない。** ヘッドレスは描画がダミーで、`--write-movie` は落ちる
（`Parameter "t" is null` / アクセス違反）。**「画面」の章を設計役に移さない。**

**ログを読む前に、自分でヘッドレスを走らせないこと。**
`godot.log` は起動のたびにローテートされ、保持は5本。
**人間が遊んだ直後は、先に退避してから走らせる。**

**Godot の出力をコンソールに直接流しても1行も返らない**（GUI アプリのため）。
`Start-Process -RedirectStandardOutput/-RedirectStandardError` で取り、
**`[System.IO.File]::ReadAllLines(path, UTF8)` で読む**（`Get-Content` は既定 ANSI で化ける）。
```

---

## 4. Ziva に渡す分

### 4-0. ⚠ 無い

⚠ **この回は `.gd` も `.json` も `ja.csv` も触らない。** 触るのは `AGENTS.md` の追記（§3）1件だけで、**それも貼るかどうかは人間の判断**。
⚠ **検証の実行も渡さない**（§0-0 で取り消した）。

### 4-1. ⚠ 次のタスクの種：**操作の要らないデバッグ起動シーン**（**この回では作らない**）

⚠ **`NEXT_STEPS` §1-4「新しいスクリプトや仕組みを作らない」に当たるので、この回ではやらない。** 別タスクとして切る。

**なぜやる価値があるか**（段階4で溶けた6回の内訳に当てる）：

| 溶けた回 | 今 | このシーンができたら |
|---|---|---|
| 1・2（ログの本文が無い／貼り直し） | ⚠ **もう消えている**（§1-5） | — |
| 3・6（再実行して通す） | 人間が起動 | ⚠ **設計役が4秒で回す** |
| 4（**スキル枠の押し忘れ**） | 人間が押す | ⚠ **`select_skill()` をコードで固定＝構造的に起きない** |
| 5（**撃つのが早すぎた**・t=2.32） | 人間の目分量 | ⚠ **「狼が殴り始めてから」をコードに書ける。時間で書かなくてよくなる** |

**決めてあること**（⚠ **2026-08-18・人間の案**）：

- ⚠ **シーンは1個だけにする。** ⚠ **「どの画面／どのステージを開くか」はコードで決める。** ⚠ **検証したい形が増えるたびにシーンを増やさない**（増やすと `tests/` が「なんとなくデバッグ置き場」になり、どれが生きているか分からなくなる。`AGENTS.md` の `components/` と同じ失敗）
- ⚠ **置き場所は `res://tests/`**（`AGENTS.md`「実験用シーンの隔離場所」）。⚠ **`scenes/` にも本番シーンにも1行も足さない。新しいフォルダも要らない**
- **形**：`tests/debug_boot.tscn` ＋ `debug_boot.gd` ＋ **シナリオ表（`Dictionary` 1つ）**

| シナリオ表の1行が持つもの | 例 |
|---|---|
| 開くもの | 画面（`training_screen`）**または** ステージ（`stage_dbg_area`） |
| 編成 | 検証用3体 |
| ⚠ **スキル枠** | ⚠ **`select_skill()` で固定＝段階4の「押し忘れ」が構造的に消える** |
| ⚠ **撃つ合図** | ⚠ **「狼が殴り始めてから」を条件式で書く＝段階4の「早すぎ」が消える。時間で書かない** |
| 終わり方 | ⚠ **決着したら `quit()`**（§1-7。`--quit-after` 頼みにするとログが途中で切れる） |

- **足すときは表に1行。** ⚠ **シーンもスクリプトも増えない**
- **起動**：`--headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn`（⚠ **シーン指定が効くことは §1-6 で実測済み**）
- ⚠ **窓ありでも同じ1個を使う。** 人間が画面を見たいときも、**拠点→ギルド→育成→…を毎回辿らずに、見たい画面から始められる**（`AGENTS.md`「画面」の章の手数が減る）

**⚠ 着手時に最初に潰すこと**：

- ⚠ **シナリオの選び方が未決定。** 推奨は**コマンドライン引数**（`-- scenario=area` を `OS.get_cmdline_user_args()` で読む）。⚠ **これなら設計役がコードを1行も触らずに切り替えられる。** ⚠ **ただし未検証。** 動かなければ**スクリプト内の既定値**に落とす（その場合は切り替えのたびに編集＝事故りやすいので、次善）
- ⚠ **`cast()` をシーンから呼べるかは未確認。** ⚠ **`battle_controller` の入力層に手を入れないと呼べないなら、そこで止めて人間に判断を仰ぐ**（本番コードに手が要る＝この案の前提が変わる）

**⚠ 解決しないこと（はっきり書いておく）**：

- ⚠ **画面の絵は取れない**（§1-4）。段階4の宿題6番（**範囲攻撃の数字が重なって何体巻き込んだか画面から読めない**）は**この案では解決しない**
- ⚠ **表示・ボタンの活性・レイアウトは人間のまま。** このシーンが減らすのは**そこへ辿り着くまでの手数**であって、見ることそのものではない

---

## 5. 完了条件 — **§0 事前チェック**（⚠ **設計役・ヘッドレス。この回の新しい型の実例**）

⚠ **人間の仕事は無い。**

1. ⚠ **§1-2 のコマンドが `exit=0` で終わること**
2. ⚠ **stdout に `[MasterDataLoader] skills validated: 59 entries, 0 errors, 1 warnings` が出ること**（黄1本は `skill_dbg_dot_odd` の端数＝**出るのが正解**）
3. ⚠ **stdout に `basic attacks validated: 19 entries, 0 errors, 0 warnings` が出ること**
4. ⚠ **stderr に出るのが `skill_dbg_dot_odd` の警告1件だけで、`ERROR:` / `SCRIPT ERROR:` / `Parse Error` が1行も無いこと**

**→ 4項目とも 2026-08-18 に実測で通っている**（§1-2）。

## 6. 完了条件 — **ログ / ファイル**（⚠ **設計役が読む**）

⚠ **この回は本番コードを触らないので、見るのは「道具が使えること」だけ。**

5. ⚠ **`godot.log` を設計役が読めること**（人間が貼らないこと）
   → **2026-08-18 に実測で通っている**（§1-5。8/18 の段階4の検証回を読み、`select_skill()` の行まで確認）
6. ⚠ **`save_slot_0.json` がヘッドレス起動の前後で変わらないこと**
   → **通っている**（§1-3。14669 バイト / 8/17 20:00:19 のまま）

## 7. 完了条件 — **画面**（⚠ **人間。この回は1項目もない**）

⚠ **無し。** この回は本番コードもデータも触らないので、画面に出る変化が1つも無い。
⚠ **「無い」と書くことに意味がある**（`AGENTS.md`「同じことを2箇所に書かない」）。

⚠ **`AGENTS.md` を書き換えた場合のみ、人間が1つだけ見る**：

7. ⚠ **`AGENTS.md` に §3 の枠が1回だけ入っていること**（`grep -c "事前チェック" AGENTS.md` が想定どおりであること。⚠ **2回入っていたら追記を2度打っている**）

---

## 8. 終わったあとに足す宿題（`PROJECT_STATUS.md`）

⚠ **書き換えるかは人間の判断。設計役は勝手に触らない。**

- ⚠ **NEW：`CLAUDE.md` の「Godotを起動できない。ゲームを動かして確かめられるのは人間だけ」が事実と違っていた**（§1-1）。⚠ **`CLAUDE.md` を直すかは人間の判断**
- ⚠ **NEW：`godot.log` は保持5本。設計役がヘッドレスを走らせると人間のログが1本消える**（§2-1）
- ⚠ **NEW：ロード時に赤が出たときの終了コードが未確認**（§2-4）。⚠ **次に本当に赤が出た回に、ついでに見ておく**
- ⚠ **NEW：戦闘まで到達する自動化は無い。** ⚠ **次のタスクの候補として `res://tests/` の「操作の要らないデバッグ起動シーン」を決めてある**（§4-1）。⚠ **段階5（`phases[]` / `recast`）より先にやるかは人間の判断**
- ⚠ **NEW：godot MCP は設定だけ残っていて使えていない**（§1-8）。⚠ **消すかどうかは人間の判断**
- **段階3の積み残し**：⚠ **コンボ** ／ ⚠ **`point` の条件（オーラ）**（2026-08-17 に「見た目が整ってから」と決めて後回し）
- **段階4で増えたもの**：⚠ **`NEXT_STEPS` §6 の1〜8をそのまま引き継ぐ**（この回では1つも減っていない）
