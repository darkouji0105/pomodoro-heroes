# EXEC — **立ち位置をずらす（重なりの解消）／射程を段に分ける**

前提は `docs/NEXT_STEPS.md` §1（このタスクの提起）・`docs/02_exec/EXEC_SKILL_AREA.md`（段階4・重なりが最初に出た場所）・`docs/02_exec/EXEC_SKILL_SPAWN.md`（段階6・召喚の重なり）。
検証の道具は `res://tests/debug_boot.tscn`。

⚠ **この回は器を1つも足さない。** 変えるのは**マスターデータの `attack_range` の値**と、**検証の道具（`tests/`）だけ**。
⚠ **`.gd` の本番コードは1行も触らない。**

---

## 0. 人間が決めたこと（**本文と矛盾する場合はこちらが優先**・2026-08-21）

| # | 決めたこと | 内容 |
|---|---|---|
| **1** | **やり方** | ⚠ **立ち位置（`y` / `x`）をずらすのはやめる。** ⚠ **代わりに `attack_range` を変えて、停まる `x` を型ごとに分ける** |
| **2** | **同じ型が複数体出るとき** | ⚠ **別にいい。** 重なったまま残す（ウルフ3体・スライム4体・召喚2体は同じ `x` に停まる） |
| **3** | **射程の値** | ⚠ **設計役が案を出して EXEC に載せる。** ⚠ **人間が EXEC を見て値を上書きする前提** |
| **4** | **段階4の巻き込み数** | ⚠ **変わってよい。** ⚠ **完了条件を「件数が一致する」から「新しい件数を実測して台帳に書き直す」に変える** |
| **5** | **召喚** | ⚠ **列（レーン）には入れない。** ⚠ **`y` をずらす仕組みそのものを作らないので、この決定は自動で満たされる** |

### 0-0. ⚠ 決定1・2の代償（**言ったうえで、決定どおりに作る**）

⚠ **`attack_range` はマスターデータの「型ごと」の欄なので、同じ型が複数体出ると必ず同じ `x` に停まる。**
⚠ **実際に起きる場面**（`stages.json` 実測）：

| ステージ | ウェーブ | 中身 |
|---|---|---|
| `stage_002` | 3 | ⚠ **ウルフ3体** |
| `stage_002` | 4 | ⚠ **スライム4体** |
| `stage_003` | 1 | ⚠ **ウルフ3体** |
| 段階6の検証 | — | ⚠ **`summon_dbg_guard` 2体** |

→ ⚠ **段階4の症状（範囲攻撃の巻き込みが読めない）は、型が混ざるウェーブでは解け、同型だけのウェーブでは残る。**
→ ⚠ **§10 の宿題に残す。**

⚠ **もう1つ。`attack_range` は戦闘の数値なので、変えた時点でバランスが動く。** ⚠ **特にボスとウルフは性格が変わる**（§2-1）。

---

## 0-1. ⚠ 設計役が自分で決めたもの（**人間が見ていない決め・要確認**）

**この章は人間が目を通していない。違うと思うものがあれば言うこと。**

| # | 決めたこと | なぜそうしたか |
|---|---|---|
| **1** | ⚠ **射程を「4つの段」に固定する：`60`（前衛）／ `180`（中衛）／ `300`（後衛）／ `420`（最後衛）。⚠ 120 ずつ空ける** | ⚠ **値を1件ずつ好きに決めると、次に敵を1体足す人が「何を避ければいいか」を知らない。** ⚠ **段にしておけば「同じ場面に出る型は別の段に置く」という1行のルールで済む。** ⚠ **120 なのは、体が 64×64 の `ColorRect` だから**（`unit_view.tscn`）。⚠ **60 だと接触して数字が読めない** |
| **2** | ⚠ **既に散っている値は動かさない**（`char_swordsman` 60 ／ `char_archer` 300 ／ `enemy_dbg_ranged` など 300 の5件） | ⚠ **バランスが動く件数を最小にする**（決定4で「変わってよい」とは言われたが、変える理由が無いものまで変えない） |
| **3** | ⚠ **同時に同じ場面へ出ない型は、同じ段に置いてよい** | ⚠ **段は4つしか無い。全13種類を別々の値にすると 60〜1500 まで伸び、射程の意味（近接か遠隔か）が消える。** ⚠ **判定は `stages.json` の同じ `wave_index` に居るかどうかで見る**（§3-3 に全ウェーブの突き合わせを載せた） |
| **4** | ⚠ **`summon_dbg_guard` を 400 → 420 にする** | ⚠ **味方側の段（60 / 180 / 300）に乗っていない中途半端な値だった。** ⚠ **420 にすると召喚が味方の最後衛になり、`char_debug_mix`（300）と 120 空く** |
| **5** | ⚠ **停まった `x` をログに出す口を `tests/debug_boot.gd` に足す**（`_dump_positions()`） | ⚠ **`battle_last.jsonl` に位置を持っているのは `spawn` の行だけ**（`battle_log.gd:181-190` で確認済み）。⚠ **`damage` にも `cast` にも `x` が無いので、今のままでは「散ったこと」を設計役が1つも観測できない。** ⚠ **本番の `BattleLog` に位置の出来事を足すと器が1つ増えるので、検証の道具側（`tests/`）に閉じた** |
| **6** | ⚠ **`_dump_positions()` は全シナリオで自動的に出す**（シナリオごとの設定欄を作らない） | ⚠ **欄を増やすと書き忘れる**（段階6で `characters.json` の候補一覧を書き忘れて赤を出したのと同じ形）。⚠ **出す合図は既にある「敵が動くのをやめた」（`_enemies_settled()`）と「決着」の2回** |
| **7** | ⚠ **シナリオ `lineup` を1本足す**（本番の味方3体を `stage_dbg_area` に出して、撃たずに位置だけ見る） | ⚠ **既存のシナリオは全部 `char_debug_*` の3体編成で、本番の `char_swordsman` / `char_archer` / `char_priest` が1度も並んでいない。** ⚠ **本番側の重なりを1回も観測しないまま渡すことになる** |
| **8** | ⚠ **段のルールを `AGENTS.md` にも `PLAN` にも書かない。この EXEC に書く** | ⚠ **`docs/` は指示されたもの以外書き換えない（`CLAUDE.md`）。** ⚠ **台帳へ載せるかは人間の判断** → §10 |

---

## 1. いま何がどうなっているか（**実コードで確認済み・2026-08-21**）

### 1-1. ⚠ 重なりの実体（**「同じ射程のユニットが同じ x に停まる」は本当だった**）

`battle_controller.gd:821-822`

```gdscript
var distance: float = abs(target.x - unit.x)
if distance <= unit.attack_range:
```

⚠ **完全に1次元。`y` はどこにも入っていない。**
⚠ **停まる `x` は「狙う相手の `x` ± 自分の `attack_range`」なので、⚠ 射程が同値のユニットは同じ相手を狙う限り必ず同じ `x` に落ちる。**

### 1-2. ⚠ 同値だった射程（**実データ**）

| ファイル | 同値の塊 |
|---|---|
| `characters.json` | ⚠ **6件中4件が `300`**（`char_archer` / `char_debug_status` / `char_debug_life` / `char_debug_mix`） |
| `enemies.json` | ⚠ **`50` が8件・`300` が5件** |
| `summons.json` | `400` が1件 |

⚠ **段階4の「巻き込みが読めない」の犯人は、検証用の味方3体が全部 `300` だったこと。**

### 1-3. ⚠ この回に効く実コードの事実（**4つ**）

| 事実 | 場所 | 影響 |
|---|---|---|
| ⚠ **`UnitView._process()` は `position.x` しか書かない** | `unit_view.gd:52-61` | ⚠ **`y` をずらす案なら2箇所で済んだ。⚠ 今回は使わないが、次に「見た目」をやるときの入口はここ** |
| ⚠ **位置を持つログは `spawn` の行だけ** | `battle_log.gd:181-190` | ⚠ **§0-1 の5の理由。⚠ 設計役が「散った」を取る手段が今は無い** |
| ⚠ **`target.range` × `attack_range` のクロス検証がある** | `master_data_loader.gd:636-646` | ⚠ **`target` に `range` が**書いてあるとき**だけ動く。⚠ 宿題15のとおり `range` はどのスキルにも書かれていないので今は眠っている。⚠ ただし「射程を**下げる**」方向は元々安全**（検証は「スキル射程が `attack_range` より短いと赤」なので、`attack_range` を下げると通りやすくなる） |
| ⚠ **`_enemies_settled()` が「全員が射程ぴったりに落ち着いた」を既に検出している** | `debug_boot.gd:402-421` | ⚠ **位置を出す合図を新しく作らなくてよい**（§0-1 の6） |

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ ウルフとボスは性格が変わる（**バランスの話。値は人間が上書きしてよい**）

⚠ **`enemy_wolf` は `spd: 80` の速い近接（射程 50）だった。⚠ 180 にすると「速く近づくが 180 手前で止まる」になる。**
⚠ **`boss_slime_king` は 70 → 300。⚠ 魔法型（`attack_type: magic`）なので後衛は不自然ではないが、⚠ 味方の剣士（射程 60）はボスまで歩くことになる。**

→ ⚠ **どちらも「絵として読める配置」を優先した案。⚠ 気に入らなければ値だけ差し替えればよく、他は1行も変わらない。**

### 2-2. ⚠ 変えるのは値だけ。欄も器も足さない

⚠ **`attack_range` の欄は既にある。⚠ 新しい欄・新しい検証（E / W）・新しい定数を1つも足さない。**
⚠ **E は E101 まで／W は W13 まで使用済みのまま、この回で1つも増えない。**

### 2-3. ⚠ JSON に追記したら改行コードを確かめる

⚠ **Windows の bash で `cat >>` すると追記分が CRLF になる**（`NEXT_STEPS` §5）。
⚠ **今回は「既存の行の数字を書き換える」だけなので追記しないが、`.bak` を触らないこと**（`characters.json.bak` / `enemies.json.bak` が残っている＝宿題5）。

### 2-4. ⚠ 編集したら `grep` で当たったことを確認する

⚠ **`grep -n "attack_range" resources/balance/master/characters.json` の値が意図どおりか、書いた直後に見る**（`CLAUDE.md` 2番）。

### 2-5. ⚠ ドキュメントの「実装済み」を信じない

⚠ **ズレが13回起きている。⚠ `grep` で中身を見てから判断する。⚠ 違っていたら勝手に直さず報告する。**

---

## 3. 実装（ファイル別）

⚠ **インデントはタブ。⚠ `.json` も既存ファイルはタブ。**

### 3-0. ⚠ 段の決まり（**この回で作るルール**）

| 段 | 値 | 意味 |
|---|---|---|
| **前衛** | **60** | 殴りに行く。相手に密着する |
| **中衛** | **180** | 一歩下がって戦う |
| **後衛** | **300** | 遠くから撃つ |
| **最後衛** | **420** | いちばん後ろ |

⚠ **ルールは1行：「同じウェーブに出る型は、別の段に置く」。**
⚠ **同時に出ない型は同じ段でよい**（§0-1 の3）。

### 3-1. `resources/balance/master/characters.json`

| ID | 今 | 変更後 | 段 |
|---|---|---|---|
| `char_swordsman` | 60 | ⚠ **据え置き 60** | 前衛 |
| `char_priest` | 250 | ⚠ **180** | 中衛 |
| `char_archer` | 300 | ⚠ **据え置き 300** | 後衛 |
| `char_debug_status` | 300 | ⚠ **60** | 前衛 |
| `char_debug_life` | 300 | ⚠ **180** | 中衛 |
| `char_debug_mix` | 300 | ⚠ **据え置き 300** | 後衛 |

⚠ **本番の味方3人は 60 / 180 / 300 に並ぶ**（`char_priest` の 250 → 180 だけが変更）。
⚠ **検証用の3体も 60 / 180 / 300 に並ぶ**（段階4の症状が直る場所）。

### 3-2. `resources/balance/master/enemies.json`

| ID | 今 | 変更後 | 段 | 同時に出る相手 |
|---|---|---|---|---|
| `enemy_slime` | 50 | ⚠ **60** | 前衛 | ウルフ・ボス |
| `enemy_wolf` | 50 | ⚠ **180** | 中衛 | スライム・ボス・`enemy_dbg_ranged` |
| `boss_slime_king` | 70 | ⚠ **300** | 後衛 | スライム・ウルフ |
| `enemy_dbg_react` | 50 | ⚠ **60** | 前衛 | （単独） |
| `enemy_dbg_followup` | 50 | ⚠ **60** | 前衛 | （単独） |
| `enemy_dbg_buff` | 50 | ⚠ **60** | 前衛 | `_heal` / `_dot` / `_revive` / `_recv` |
| `enemy_dbg_dot` | 50 | ⚠ **180** | 中衛 | `_buff` |
| `enemy_dbg_heal` | 50 | ⚠ **180** | 中衛 | `_buff` / `_recv` |
| `enemy_dbg_ranged` | 300 | ⚠ **据え置き 300** | 後衛 | ウルフ |
| `enemy_dbg_cond` | 300 | ⚠ **据え置き 300** | 後衛 | （単独） |
| `enemy_dbg_revive` | 300 | ⚠ **据え置き 300** | 後衛 | `_buff` |
| `enemy_dbg_immune` | 300 | ⚠ **据え置き 300** | 後衛 | （単独） |
| `enemy_dbg_recv` | 300 | ⚠ **据え置き 300** | 後衛 | `_heal` / `_buff` |

### 3-3. ⚠ 全ウェーブの突き合わせ（**同じウェーブに同じ段が2つ無いこと**）

⚠ **`stages.json` の全ウェーブを実際に読んで並べた。⚠ 括弧は変更後の段。**

| ステージ | ウェーブ | 中身 | 判定 |
|---|---|---|---|
| `stage_001` | 4 | スライム2（前）＋ ウルフ2（中） | ✅ 段が分かれた |
| `stage_001` | 5 | ボス（後）＋ —  | ✅ |
| `stage_002` | 2 | スライム2（前）＋ ウルフ2（中） | ✅ |
| `stage_002` | 3 | ⚠ **ウルフ3（中）** | ⚠ **同型3体＝重なる**（決定2で許容） |
| `stage_002` | 4 | ⚠ **スライム4（前）** | ⚠ **同型4体＝重なる**（決定2で許容） |
| `stage_002` | 5 | ボス（後）＋ スライム2（前） | ✅ |
| `stage_003` | 1 | ⚠ **ウルフ3（中）** | ⚠ **同型3体＝重なる** |
| `stage_003` | 4 | ウルフ2（中）＋ スライム3（前） | ✅ **段は分かれた**（同型同士は重なる） |
| `stage_003` | 5 | ボス（後）＋ ウルフ2（中） | ✅ |
| `stage_dbg_enemy_skill` | 3 | `_buff`（前）＋ `_heal`（中） | ✅ **直った**（今は両方 50） |
| `stage_dbg_condition` | 2 | `_dot`（中）＋ `_buff` 2（前） | ✅ **直った**（今は全部 50） |
| `stage_dbg_intervene` | 3 | `_heal`（中）＋ `_recv`（後）＋ `_buff`（前） | ✅ **3段に分かれた** |
| `stage_dbg_passive` | 1 | `_revive`（後）＋ `_buff`（前） | ✅ |
| `stage_dbg_area` | 1 | `_ranged` 2（後）＋ ウルフ2（中） | ✅ **段が分かれた**（同型同士は重なる） |

### 3-4. `resources/balance/master/summons.json`

| ID | 今 | 変更後 | 段 |
|---|---|---|---|
| `summon_dbg_guard` | 400 | ⚠ **420** | 最後衛 |

⚠ **味方側は `char_debug_status`(60) / `_life`(180) / `_mix`(300) / 召喚(420) の4段になる。**
⚠ **召喚が2体出ると、その2体は互いに重なる**（決定2で許容）。

### 3-5. `tests/debug_boot.gd`（**検証の道具。本番コードではない**）

⚠ **足すのは2つだけ。**

**① `Driver._dump_positions(session, label)`**

⚠ **生きているユニット全員を `x` の昇順に1行ずつ出す。⚠ チームごとに、隣との `x` の差も出す。**

```
[DebugBoot] 位置（合図）t=3.21
[DebugBoot]   party  char_debug_status  x=580.0  range=60   間隔=—
[DebugBoot]   party  char_debug_life    x=460.0  range=180  間隔=120.0
[DebugBoot]   party  char_debug_mix     x=340.0  range=300  間隔=120.0
[DebugBoot]   enemy  enemy_wolf_0       x=820.0  range=180  間隔=—
[DebugBoot]   最小間隔  party=120.0  enemy=0.0
```

⚠ **`最小間隔` の行が判定に使う値。⚠ `0.0` が出たら、その組は同じ x に居る。**
⚠ **召喚は `summon` として同じ形で出す**（`session.summon_units`）。
⚠ **`print` を本番コードに足さない**（`NEXT_STEPS` §5「`print` を増やさない」は `scenes/` `scripts/` `autoload/` の話。⚠ **`tests/` は検証の道具なので `print` が出口**）。

**② 呼ぶ場所は2箇所**

| 場所 | ラベル |
|---|---|
| ⚠ **`_signal_seen = true` にした直後**（`_step_fire()`） | `合図` |
| ⚠ **決着を検出した直後**（`_process()` の `_finished_sec < 0.0` の枝） | `決着` |

⚠ **シナリオごとの設定欄を作らない**（§0-1 の6）。

**③ シナリオ `lineup` を `SCENARIOS` に1行足す**

```
"lineup": 本番の味方3体（char_swordsman / char_archer / char_priest）を
          stage_dbg_area に出す。⚠ スキルは1つも割り当てない
	fire: [ {skill: "", gap: 2.0} ]   # ⚠ 撃たずに待つだけの行
```

⚠ **`skill: ""` の行が要る理由**：`fire` を空配列にすると、`_fired(0) >= size(0)` が最初のフレームで成立して**合図を待たずに敵を全滅させる**（`_process()` の最後の枝）。⚠ **位置が1回も出ない。**
⚠ **`stage_dbg_area` を使うのは、敵の `atk` が 1 だから**（本番の味方は `hp: 70〜120` なので、本番ステージだと位置を見る前に死ぬ）。
⚠ **シーンを増やさない。`SCENARIOS` に1行。**

### 3-6. 触らないファイル

⚠ **`.gd` の本番コード全件**（`scenes/` / `scripts/` / `autoload/`）
⚠ **`skills.json` 全件 ／ `stages.json` ／ `localization/ja.csv` ／ `project.godot` ／ `addons/`**
⚠ **`*.json.bak`**（宿題5。触らない）

---

## 4. 変えないもの

- ⚠ **戦闘の計算式**（`abs(target.x - unit.x)` の1次元のまま。2次元化は PLAN 16 の「担当外」）
- ⚠ **`GROUND_Y` と `y` 座標**（決定1で「ずらさない」。⚠ `unit_view.gd` を1行も触らない）
- ⚠ **`BattleLog` の出来事の種類**（`spawn` に `x` があるだけの状態を維持）
- ⚠ **器・欄・検証（E / W）の数**（§2-2）
- ⚠ **召喚の座標の規則**（`offset_x * (n+1)`・段階6の決定1）

---

## 5. 完了条件 — **§0 事前チェック**（⚠ **設計役・ヘッドレス。人間の仕事は無い**）

1. ⚠ **引数なしで起動すると、シナリオ一覧に `lineup` が出て `exit=0` で終わること**
2. ⚠ **stderr に `ERROR:` / `SCRIPT ERROR:` / `Parse Error` が1行も出ないこと**（全シナリオ：`area` / `recast` / `recast_expire` / `summon` / `summon_wipe` / `lineup` / 引数なし）
3. ⚠ **`skills validated: 61 entries, 0 errors, 1 warnings` と `basic attacks validated: 19 entries, 0 errors, 0 warnings` が変わらないこと**（⚠ **この回はスキルを1件も足さないので、61 のまま。⚠ 増減したら別のものを触っている**）

## 6. 完了条件 — **ログ / ファイル**（⚠ **設計役が読む**）

### 6-1. `-- scenario=lineup`（**本番の味方が散ったか**）

4. ⚠ **`合図` の位置の行に、`char_swordsman` / `char_archer` / `char_priest` の3体が出ていること**
5. ⚠ **その3体の `最小間隔 party` が **120.0** であること**（⚠ **`0.0` なら散っていない。⚠ 120 未満なら値を間違えている**）
6. ⚠ **`char_swordsman` の `x` が3体でいちばん大きく、`char_archer` がいちばん小さいこと**（＝剣士が前・弓が後ろ。⚠ **味方は敵に向かって `+x` 方向へ歩く**）

### 6-2. `-- scenario=area`（**検証用の味方が散ったか・段階4の症状**）

7. ⚠ **`合図` の位置の行で `最小間隔 party` が **120.0** であること**（⚠ **段階4ではここが `0.0` だった＝3体が重なっていた**）
8. ⚠ **`最小間隔 enemy` が **0.0 より大きいこと**（⚠ **`enemy_dbg_ranged` 2体と `enemy_wolf` 2体で、⚠ 段が2つに分かれる。⚠ 同型同士は `0.0` なので、⚠ この項目は「型をまたぐ最小差」を見る**）
9. ⚠ **巻き込み数を新しく実測して、この EXEC の §11 に書くこと**（⚠ **`narrow` / `wide` / `far` / `heal` の4件。⚠ 決定4で「変わってよい」。⚠ 段階4の 2 / 4 / 4 / 3 と一致しなくてよいが、⚠ **必ず実測値を書き残す**。次の回がここを基準にする）
10. ⚠ **`result` の行が出ていること**（途中で切れていない）

### 6-3. `-- scenario=summon`（**召喚が味方の段とぶつからないか**）

11. ⚠ **`spawn` の `begin` が2本出て、`x` が `召喚者の x − 60` と `− 120` であること**（⚠ **段階6の規則が壊れていないこと。⚠ `offset_x` は触っていないので変わらないはず**）
12. ⚠ **`決着` の位置の行で、召喚が生きているあいだの `summon` の `x` が、味方3体のどの `x` とも **一致しない** こと**（⚠ **召喚 420 と `char_debug_mix` 300 で 120 空く**）
13. ⚠ **`source_unit_id` が `summon_*` の `damage` の `amount` が `50` のままであること**（⚠ **射程を変えても発動者の分離が壊れていないこと**）

### 6-4. `-- scenario=recast` / `recast_expire` / `summon_wipe`（**壊していないこと**）

14. ⚠ **`recast` の `cast` が2本で `phase` が `0` と `1` であること**（⚠ **`damage` の値は 2 / 8 から**変わりうる**。⚠ 位置が変われば `distance` スケールが動くため。⚠ **変わった場合は §11 に実測値を書く**）
15. ⚠ **`recast_expire` に `recast` の `why: "expire"` が出ていること**
16. ⚠ **`summon_wipe` で `result` の `victory: false` が出ていること**（⚠ **召喚が生きていても敗北する＝段階6の決定2**）

### 6-5. セーブ

17. ⚠ **`save_slot_0.json` が走らせる前と1バイトも変わらないこと**（`debug_boot` は保存しない）

### 6-6. ⚠ 値が本当に効いているか（**無音で通るのが一番怖い**）

18. ⚠ **`char_priest` の `attack_range` を一時的に 300（＝`char_archer` と同値）に戻して `lineup` を走らせ、`最小間隔 party` が `0.0` になることを確かめる。⚠ 確かめたら必ず 180 に戻し、`120.0` に戻ることまで見る。**
    ⚠ **これをやらないと、「散ったように見えるが実は別の理由」を見分けられない。** ⚠ **本番コードは1行も触らない。**

## 7. 完了条件 — **画面**（⚠ **人間。窓ありで1回だけ**）

⚠ **ヘッドレスでは分からないものだけ。⚠ 合図で書く。時間で書かない。**

19. ⚠ **戦闘を始めて味方が歩くのをやめたとき、味方3体の四角が **重ならず横に並んでいる** こと**（⚠ **今は剣士だけ前に出て、弓と僧侶がほぼ同じ位置に居る**）
20. ⚠ **範囲攻撃（`skill_dbg_area_wide` 等）を撃ったとき、⚠ **巻き込まれた相手の数字が1つずつ読めること**（⚠ **重なって数字が1つに見えないこと。⚠ これが段階4で読めなかったもの**）
21. ⚠ **`stage_002` のウェーブ3（ウルフ3体）では、⚠ **3体が同じ位置に重なったままであること**（⚠ **決定2で許容した状態。⚠ 「直っていない」ではなく「そう決めた」ことの確認**）
22. ⚠ **ウルフが以前より手前で止まること、ボスが以前より遠くから撃ってくることが、⚠ **戦って違和感が無い範囲か**（⚠ **§2-1。⚠ 違和感があれば値を上書きしてよい。⚠ 変えるのは JSON の数字だけで、他は1行も変わらない**）

---

## 8. 将来コードを変えたときに見る項目（**UIから到達できない・人間の確認項目にしない**）

- ⚠ **敵の召喚が出たときに段が守られるか**（⚠ **敵の召喚スキルがまだ無い**・段階6 §8 から引き継ぎ）
- ⚠ **`target.range` を書いたスキルを足したとき、`attack_range` を下げた分だけクロス検証が緩くなること**（`master_data_loader.gd:636-646`。⚠ **宿題15 が片付くまで眠っている**）
- ⚠ **敵の型を新しく足したとき、同じウェーブの他の型と段がぶつからないか**（⚠ **ロード時に検出する仕組みは作っていない。§10 の宿題**）
- ⚠ **同型が複数体出るウェーブで、範囲攻撃の巻き込みが読めるようになるか**（⚠ **決定2で許容した。⚠ 読めるようにするには個体ごとの差が要る**）

---

## 9. Ziva に渡せる部分

⚠ **§3-1 / §3-2 / §3-4（JSON の数字を書き換えるだけ）は渡せる。⚠ ただし合計6ファイル中3ファイル・書き換えるのは9個の数字だけなので、⚠ 渡す手間のほうが大きい。**

⚠ **§3-5（`tests/debug_boot.gd`）は渡せない。** ⚠ **内部クラス（`Driver`）から外側の `const` を参照できない制約があり**（段階6 §11-1 の④）、⚠ **知らずに書くとパースエラーになる。**

---

## 10. 終わったあとに足す宿題（`PROJECT_STATUS.md`。⚠ **書き換えるかは人間の判断**）

- ⚠ **NEW：同じ型が複数体出ると重なったまま**（人間の決定2）。⚠ **`stage_002` のウェーブ3・4、`stage_003` のウェーブ1、召喚2体が該当。⚠ 直すには個体ごとの差（生成時のインデックス由来）が要る**
- ⚠ **NEW：段（60 / 180 / 300 / 420）はどこにも検証が無い**。⚠ **同じウェーブに同じ段の型を2つ置いても、赤も黄も出ない。⚠ 無音で重なる**
- ⚠ **NEW：段のルールが EXEC にしか書いていない**（§0-1 の8）。⚠ **`AGENTS.md` か `PLAN` に載せるかは人間の判断**
- ⚠ **NEW：`attack_range` を変えたのでバランスが動いた**（ウルフ 50→180 ／ ボス 70→300 ／ 僧侶 250→180）。⚠ **「バランスの実測」（`NEXT_STEPS` §4 の3番）はこの値を前提にする**
- ⚠ **NEW：位置を観測する口が `tests/debug_boot.gd` にしか無い**（§0-1 の5）。⚠ **本番の `BattleLog` には位置の出来事が無いままなので、⚠ 人間が遊んだログからは重なりを追えない**
- ⚠ **既存：見た目そのもの（絵・エフェクト）は手つかず**。⚠ **コンボが後回しになった理由（「見た目が整わないと検証が難しすぎる」）がこれで解けたかは、⚠ コンボに着手する時点でもう一度見ること**
- ⚠ **NEW：段が保証するのは味方側だけ**（§11-1 の②）。⚠ **敵は「味方がまだ歩いていた時点の位置」で停まり、一度停まると狙う相手を変えないので、⚠ 敵同士の間隔は射程の差にならない**（実測 54.6）。⚠ **直すには「射程に入ったら止まる」以外の停まり方が要る**
- ⚠ **NEW：`_enemies_settled()` の「動いた」判定がフレームレート依存**（§11-1 の③）。⚠ **`_all_settled()` だけ直した。⚠ 段階4の数字が動くので既存は触っていない**
- ⚠ **既存：検証用のものはリリース前に消す**（`char_debug_*` ／ `enemy_dbg_*` ／ `summons.json` ／ `tests/debug_boot`）

---

## 11. ⚠ 実施結果（**2026-08-21・設計役がヘッドレスで実行して判定**）

⚠ **§5・§6 は全部通った。§7（画面・19〜22）は未実施＝人間の担当。**

| 項目 | 結果 |
|---|---|
| 5-1 一覧に `lineup` が出て `exit=0` | ✅ 通った |
| 5-2 全シナリオで stderr に赤が1行も無い | ✅ 通った（`area` / `recast` / `recast_expire` / `summon` / `summon_wipe` / `lineup` / 引数なし。⚠ **黄は `skill_dbg_dot_odd` の1本だけ＝正解**） |
| 5-3 `61 entries, 0 errors, 1 warnings` ／ `19 entries, 0 errors, 0 warnings` | ✅ **両方とも変わらず** |
| 6-4 `lineup` に本番の味方3体が出る | ✅ 通った |
| 6-5 `最小間隔 party` が `120.0` | ✅ **`119.9`**（⚠ **移動が毎フレーム加算なので端数が出る。⚠ 段の差 120 と一致**） |
| 6-6 剣士がいちばん前・弓がいちばん後ろ | ✅ **弓 `600.0`（射程300）→ 僧侶 `720.3`（180）→ 剣士 `840.3`（60）** |
| 6-7 `area` の `最小間隔 party` が `120.0` | ✅ **`119.9`**（⚠ **段階4ではここが `0.0`＝3体が重なっていた。⚠ この回の主目的が果たされた場所**） |
| 6-8 `area` の `最小間隔 enemy` が 0.0 より大きい | ⚠ **`42.6`（型跨ぎ `54.6`）。条件は満たすが 120 ではない** → **§11-1 の②** |
| 6-9 `area` の巻き込み数を実測 | ✅ **§11-2 に記載。⚠ `narrow` が 2 → 3 に変わった**（決定4で許容） |
| 6-10 `area` に `result` が出ている | ✅ 通った |
| 6-11 `summon` の `spawn` が `召喚者 −60 / −120` | ✅ **召喚者 `496.19` に対して `436.19` / `376.19`。段階6の規則は壊れていない** |
| 6-12 召喚の `x` が味方のどの `x` とも一致しない | ✅ **召喚 `426.6` / `436.2` に対し味方は `600.5` / `720.2` / `840.1`** |
| 6-13 召喚の `damage` が `amount: 50` | ✅ **14本すべて `50`**（発動者の分離は無傷） |
| 6-14 `recast` の `phase` 0/1・`damage` 2/8 | ✅ **完全一致**（⚠ **位置が変わっても `distance` スケールは動かなかった**） |
| 6-15 `recast_expire` に `why: "expire"` | ✅ 通った（`t=7.95`） |
| 6-16 `summon_wipe` で `victory: false` | ✅ **`t=5.45`。`owner_death` も2本** |
| 6-17 セーブが変わらない | ✅ **`save_slot_0.json` は存在しないまま** |
| 6-18 壊すと検出できる | ✅ **§11-3** |

### 11-1. ⚠ 実装中に踏んだこと（**次に位置を測るとき用**）

**① ⚠ 「敵が動くのをやめた」は「味方が止まった」を保証しない。**

⚠ **初稿は既存の合図（`_enemies_settled()`）で位置を測っていたが、⚠ その時点で剣士（射程 60・`spd` 60）はまだ歩いている途中だった**（実測：`t=4.78` に `x=486.8`、本当の停止位置は `x=840.3`）。
⚠ **射程が短い者ほど遠くまで歩くので、いちばん遅く止まる。**
→ ⚠ **`_all_settled()`（味方・敵・召喚の全員）を別に足し、位置はそちらで測る。⚠ 既存の合図は1行も変えていない**（段階4がその合図で数字を取っているため）。

**② ⚠ 段（射程の差）が保証するのは「同じ相手を狙って落ち着いたとき」だけ。**

⚠ **敵側の型跨ぎの最小間隔は `54.6` で、段の差 120 になっていない。**
⚠ **原因は、敵が「味方がまだ歩いていた時点の位置」を基準に停まり、⚠ 一度停まると狙う相手を変えないので二度と動かないこと**（`battle_controller.gd:822`／狙う相手は死ぬまで変えない）。
⚠ **味方側が 119.9 ぴったりに揃うのは、味方が「ほぼ動かない敵」に向かって歩くから。**
→ ⚠ **これは射程の値では直せない。§10 の宿題に足した。**

**③ ⚠ `STILL_EPSILON` は1フレームの移動量で見るのでフレームレートに依存する。**

⚠ **ヘッドレスは fps が高く、`spd 60` のユニットの1フレーム移動量が `STILL_EPSILON`（0.5）を下回るため、⚠ 歩いている者を「止まった」と誤判定した。**
→ ⚠ **`_all_settled()` は「窓の始まりの位置からの総移動量」で見る形にした。⚠ `_enemies_settled()` は直していない**（段階4の数字が動く）。

**④ ⚠ `fire` の1行目の `gap` は効かない。**

⚠ **`_last_fire_sec` の初期値が `-999.0` なので、⚠ どんな `gap` を書いても最初のフレームで通る。**
→ ⚠ **待たせたいときは「待つだけの行」を2つ書く**（`lineup` がその形）。

### 11-2. ⚠ 段階4の巻き込み数（**新しい実測値。次の回はここを基準にする**）

⚠ **`-- scenario=area`。⚠ `src` で撃った本人を絞って数えた**（同じ `t` の通常攻撃を除く）。

| スキル | 段階4（旧） | ⚠ **この回（新）** |
|---|---|---|
| `skill_dbg_area_narrow` | 2 | ⚠ **3** |
| `skill_dbg_area_wide` | 4 | **4** |
| `skill_dbg_area_far` | 4 | **4** |
| `skill_dbg_area_heal`（`heal` の行） | 3 | **3** |

⚠ **`narrow` が 2 → 3 に増えたのは、敵の段が変わって `radius` の中に1体多く入ったため**（人間の決定4で許容）。

### 11-3. ⚠ 検証が本当に重なりを検出するか（**無音で通るのが一番怖いため**）

⚠ **`char_priest` の `attack_range` を一時的に 300（＝`char_archer` と同値）に戻して `lineup` を走らせ、必ず戻した。⚠ 本番コードは1行も触っていない。**

| 壊し方 | 出た値 |
|---|---|
| `char_priest` を 180 → 300 | ⚠ **`最小間隔 party=0.0（型跨ぎ 0.0）`** ＝ **重なりを検出した** |
| 戻す | ✅ **`最小間隔 party=119.9`・`61 entries, 0 errors, 1 warnings`** |

### 11-4. ⚠ あとから足したもの（**位置の行に「狙う相手」を出す**）

⚠ **人間から「近くの敵に反応せず、無視して後ろの敵に行くことがある」の報告を受けて足した**（2026-08-21）。
⚠ **`battle_last.jsonl` には `target_unit_id` が1件も出ないので、⚠ 狙う相手をログから切り分けられなかった。**
→ ⚠ **`_dump_positions()` の各行に `狙う=<unit_id>` を足した。⚠ 本番コードは触っていない。**

⚠ **切り分けの結果は §11-5。⚠ 直すのは別タスク。**

### 11-5. ⚠ 「近くの敵を無視して奥へ行く」の切り分け（**この回の変更が原因ではない**）

⚠ **`lineup` の静止時点で再現した**（射程の段を入れた状態）：

```
party_0（剣士 射程60） x=840.2  狙う=enemy_1_0
enemy_1_2（狼）        x=813.0   ← 剣士から 27.2。無視されている
enemy_1_3（狼）        x=857.8   ← 剣士から 17.6。無視されている
enemy_1_0（射程持ち）  x=900.0   ← 剣士から 59.8。こちらを殴っている
```

⚠ **射程を変更前に戻して同じシナリオを走らせても起きた。⚠ むしろ変更前のほうが差が大きい：**

| | 狙う相手までの距離 | ⚠ 無視している一番近い敵までの距離 |
|---|---|---|
| ⚠ **変更前**（射程 50 / 300） | 294.8 | ⚠ **94.7** |
| ⚠ **変更後**（この回の段） | 59.8 | ⚠ **17.6** |

⚠ **原因は `_acquire_target_if_needed()`（`battle_controller.gd:778-785`）が、⚠ 狙う相手が生きている限り二度と選び直さないこと。**
⚠ **狙う相手は戦闘開始の1回だけ決まる**（そのとき味方は `x=200/300/400`・敵は `x=900〜1200` なので、全員が一番左の敵を選ぶ）。
⚠ **そのあと狼が左へ歩いて追い越すが、味方は選び直さないので脇を通り過ぎる。**

⚠ **これは意図された決め**（`battle_controller.gd:864`「⚠ 対象を選び直さない」／`NEXT_STEPS`「狙う相手は死ぬまで変えない」）。⚠ **バグではなく副作用。**

⚠ **追い越しが起きるのは「射程の違う敵が混ざるステージ」だけ。⚠ 本番の `stage_001` ウェーブ4（スライム2＋狼2）は、スライムが `840+60=900`・狼が `840+180=1020` で止まるので追い越さない**（⚠ **計算であって実測ではない**）。

→ ⚠ **人間の決定（2026-08-21）：案B「歩いているあいだだけ、より近い敵が居たら乗り換える。攻撃を始めたら固定」。⚠ 別タスクにする。**
→ ⚠ **その回で段階4〜6の実測値（巻き込み数・`recast` のダメージ・召喚の位置）が全部動く。⚠ この EXEC の §11-2 が比較の基準になる。**

### 11-6. ⚠ ヘッドレスを9回走らせた

⚠ **`godot.log` は保持5本。⚠ この回より前に人間が遊んだぶんの出力パネルは、押し出されて残っていない。**
