# EXEC：装備画面に持ち物を入れる ／ 装飾の導線（回3-b）

> ⚠⚠ **2026-09-22。⚠ 器（画面・共通部品）が作り直しになり、⚠ `BS-1`〜`BS-3` を覆すので `EXEC` を書く側。**
> ⚠ **値の正は `docs/DECISIONS.md`。⚠ ここに値を書き写さない。**
> ⚠ モックは `c:\Users\admin\Downloads\equipment_mock.html`（⚠ 依頼書は `docs/03_log/PRE_PLAN_EQUIPMENT_UI_MOCK.md`）。

---

## 1. 人間の裁き（**そのまま。⚠ 覆さない**）

| | 人間の言葉・選択 |
|---|---|
| きっかけ | ⚠ **「そもそも装飾をつけれない」「どこからつけるかわからない」** |
| 持ち物 | ⚠ **「いまいんべんとりもべつにひょうじしてるが　しょうじきいっしょのほうがべんりだからそうしたいかも」** |
| 全体 | ⚠ **「UI がはんざつでわかりにくい」** |
| 導線 | ⚠ **「アイテムの説明に一本化したほうがいいかも　装飾と鍛えるのは」**（＝モックの **A案**） |
| 倉庫 | ⚠⚠ **別窓はやめる。⚠ 「ギルドに倉庫のカードを戻す」**（2026-09-22 の裁き） |
| 装飾の操作 | ⚠⚠ **「段階を上げる・壊す・重ねる」は説明の窓に足す** |
| 閉じた枠 | ⚠⚠ **B案＝次の1枠だけ鍵で出す** |
| 等級の色 | ⚠ **いまのまま**（⚠ 青系の等級4〜6 は直さない） |
| 装飾の鍛え | ⚠ **装飾は「鍛える」ではない**（⚠ 装飾は「段階を上げる」。⚠ 混ぜない） |

---

## 2. 覆すもの（**必ず跡を残す**）

| 覆す相手 | いつ | どう変わるか |
|---|---|---|
| **`BS-1`** | 09-15 | ~~倉庫は OS の別窓。⚠ ゲームの窓の右隣に付いていく~~ → ⚠⚠ **別窓はやめる。⚠ 画面の中に置く** |
| **`BS-2`** | 09-15 | ~~倉庫はこの窓だけ。⚠ ギルドの倉庫のカードは消す~~ → ⚠⚠ **ギルドにカードを戻す** |
| **`BS-3`** | 09-15 | ~~出すのは拠点とギルド系だけ（`ALLOWED_SCENES`）~~ → ⚠ **器ごと消える** |
| **2026-09-08 の決定** | 09-08 | ~~未開放の枠はそもそも表示しない~~ → ⚠⚠ **次の1枠だけ鍵で出す**（⚠ B案） |
| **部位の行5本** | — | ~~文字1行＋［選ぶ］［鍛える］［外す］× 5~~ → ⚠ **装備マス5個＋説明の窓に寄せる**（⚠ 灰色のボタン12個が消える） |

⚠⚠ **`BS-1` の狙い（「ゲームの窓を塞がずに持ち物を見る」）は残る。** ⚠ 変わったのは**置き場**だけ。

⚠ **`BS-10`（1ページ 100 マス・10×10・全5ページ）は変えない。** ⚠ モックの「マス 61/500」「1/5」は**実装どおり**。

### ⚠ 人間の裁きが無いもの（**着手前に聞く**）

- ⚠ **ビルドの行**（⚠ ビルド選択・焼く・適用）。⚠ モックは装備画面から消している（⚠ 育成画面にも同じ行がある）。
  ⚠⚠ **人間は「消す」と言っていない。⚠ 実装の前に聞くこと。**

---

## 3. ⚠⚠ 下地はもう在る（**作る前に見ること**）

⚠ **`grep` で取った事実**（2026-09-22）。⚠ **新しく作るのは「刺す」まわりだけ**。

| モックが要るもの | いまどこに在るか |
|---|---|
| 説明の窓（品の詳細） | ⚠ **`scenes/ui/components/item_detail.gd`**（⚠ `ItemDetail`）。⚠ **装飾の枠（`PartSlotRow`）も既に描く**（`item_detail.gd:252`） |
| 鍛える | ⚠ **倉庫の詳細にもう在る**（`warehouse_screen.gd:326 _on_forge_pressed()`） |
| 装飾の段階を上げる・壊す | ⚠ **在る**（`_on_part_upgrade_pressed()` / `_on_part_dismantle_pressed()` → `upgrade_part()` / `dismantle_part()`） |
| ルーンを重ねる | ⚠ **在る**（`_on_rune_merge_pressed()` → `merge_runes()`） |
| 捨てる | ⚠ **在る**（`_add_discard_button()`） |
| 持ち物・素材・図鑑の3タブ／ページャ／枠を買う | ⚠ **在る**（`warehouse_screen.tscn` の `Tabs`。⚠ `expand_inventory()`） |
| 着けていない装備にも刺せる | ⚠ **もうそうなっている**（⚠ `get_part_reject_reason()` に「装備中」の縛りが無い） |
| 上書きで刺さない | ⚠ **もうそうなっている**（⚠ `PART_REJECT_OCCUPIED`） |
| 次の等級の性能 | ⚠ **口が在る**（`get_instance_stats_at_grade()`）。⚠ **画面がまだ使っていない** |

⚠⚠ **＝「倉庫の別窓の中身」＝ `warehouse_screen.tscn` そのもの**（`inventory_window.gd:25`）。
⚠ **別窓をやめる＝この画面を装備画面の右の列に埋める**、という話。⚠ 中身を作り直すわけではない。

---

## 4. 作るもの（**4段。⚠ 1段ずつ通す**）

### 【1】⚠ 器：**操作つきの説明の窓**を切り出す

- ⚠ `warehouse_screen` の **詳細パネル（`ItemDetail` ＋ `ActionRow`）を部品にする**。
  ⚠ **2画面以上で使うので `scenes/ui/components/`**（AGENTS.md）
- ⚠ 中身は押した品の種類で変わる（⚠ 装備 ／ 装飾 ／ 素材）。⚠ **画面ごとに if を書かない**
- ⚠ 足すのは **［着ける］／［外す］** と **装飾の枠の操作**だけ
- ⚠⚠ **`ItemDetailPopup`（ホバー）とは別物。** ⚠ あちらはマウスを素通しするのでボタンを載せられない

### 【2】⚠⚠ 刺す導線（**この回の本題**）

- ⚠ **枠をドラッグの受け先にする**（⚠ 持ち物の装飾をつまんで枠へ落とす＝**2手**）
- ⚠ **枠を押すと、⚠ 刺せる装飾が持ち物で光る**（⚠ 見出しは自動で「装飾」へ。⚠ 押して刺すのも**2手**）
- ⚠ 吹き出しは**名前と（空き）だけ**（⚠ 一覧は持ち物に任せる＝⚠ 2026-09-22 の `BS-14` を細くする）
- ⚠ **閉じた枠は次の1つだけ鍵で出し、⚠ 「等級◯ で開きます」と言う**（⚠ B案）
- ⚠ **門1（3面クリア前）は1行だけ出す**（⚠ 「装飾は 3面をクリアすると使えるようになります」）
- ⚠ **門3（装飾を1つも持っていない）は空の一覧で終わらせない**

### 【3】⚠ 装備画面を組み替える

- ⚠ 左＝ステータス（⚠ **「いま」と「刺したら／着けたら」の2列**）／ 中＝装備マス5＋説明の窓 ／ 右＝持ち物
- ⚠ **部位の行5本を消す**（⚠ 操作は説明の窓へ）
- ⚠ **選んでいる印は真鍮の枠**（⚠ 行頭の `>` をやめる）
- ⚠ 真鍮のボタンは**1画面に1個**（⚠ 「鍛える」だけ）

### 【4】⚠ 別窓を畳む

- ⚠ `InventoryWindow`（器）を消す ／ ⚠ `ItemGrid.route_screen_drop()`（⚠ 別窓どうしのドラッグの自前の受け）も要らなくなる
- ⚠ **ギルドに倉庫のカードを戻す**（⚠ `GuildScreen.GUILD_SCENES`）
- ⚠⚠ **最後にやる。** ⚠ 先に消すと、⚠ 途中の段で持ち物をどこからも見られなくなる

---

## 5. 触るもの

| ファイル | 何を |
|---|---|
| ⚠ **新設** `scenes/ui/components/item_action_panel.gd`（⚠ 名前は仮） | ⚠ 操作つきの説明の窓。⚠ **`.tscn` を持たない**（⚠ `EmptyState` と同じ流儀） |
| `scenes/guild/equipment_screen.gd` ／ `.tscn` | ⚠ 3列に組み替え。⚠ 部位の行を消す |
| `scenes/guild/warehouse_screen.gd` ／ `.tscn` | ⚠ 詳細パネルを部品へ。⚠ **埋め込めるようにする**（⚠ ヘッダーの出し分け） |
| `scenes/ui/components/part_slot_icon.gd` | ⚠ **落とせるようにする**（`_can_drop_data` / `_drop_data`）。⚠ 閉じた枠の鍵 |
| `scenes/ui/components/item_grid.gd` ／ `item_slot.gd` | ⚠ **光らせる**口。⚠ 【4】で `route_screen_drop()` を消す |
| `scenes/inventory/inventory_window.gd` | ⚠ 【4】で消す |
| `scenes/guild/guild_screen.gd` | ⚠ 倉庫のカードを戻す |
| `localization/ja.csv` | ⚠ 門1・門3・鍵の文言（⚠ 3〜5行）。⚠ **`--import` まで設計役がやる** |
| `tests/debug_boot.gd` | ⚠ `layout` の検査 ／ ⚠ `shot` の絵（⚠ 18枚目を差し替え・⚠ 門1と門3も撮る） |

⚠ **`autoload/game_manager.gd` は触らない見込み**（⚠ 口は全部在る）。⚠ 触るなら理由を書くこと。

---

## 6. ⚠ 着手の前に書く「変わるはずの数字」

> ⚠ **装備画面の最小サイズが `593 x 518` から変わるはず**（⚠ 3列になるため）。
> ⚠ **初期状態の無効なボタンが 12個 から 0〜2個に減るはず**（⚠ 部位の行を消すため）。
> ⚠ **装飾を刺すまでの手数が 6手 から 2手になるはず。**

---

## 7. 完了条件

| どこで見るか | 何を |
|---|---|
| **ログ**（`scenario=layout`） | ⚠ 赤0 ／ ⚠ **装備画面に `PartSlots_◯` が出る** ／ ⚠ 無効なボタンの数 |
| **ログ**（`scenario=inventory` ほか） | ⚠ 平常値のまま（⚠ 倉庫の口を壊していない） |
| **ファイル** | ⚠ `git diff \| grep "^-func"` が**意図したものだけ** |
| **画面**（`scenario=shot`） | ⚠ 装備画面（⚠ 着けている ／ 持ち物を選んだ ／ 枠を選んだ）・⚠ 門1・⚠ 門3 |
| ⚠⚠ **人間**（`HUMAN_CHECK.md`） | ⚠ **つまんで落とせるか**・⚠ 光って見えるか・⚠ 2手で刺せるか（⚠ 手応えは絵で分からない） |

---

## 8. ⚠ 落とし穴（**先に書いておく**）

- ⚠⚠ **`warehouse_screen` は画面。⚠ 埋めるとヘッダー（戻る）が二重になる。** ⚠ 出し分けること
- ⚠⚠ **ドラッグの受けは `ItemGrid` の作り。⚠ 枠（`PartSlotIcon`）は `Panel`**。⚠ 受けを自分で書く側
- ⚠ **無名関数をシグナルにつながない**（⚠ `Lambda capture ... was freed`）
- ⚠ **再描画に `await` を持たせない**（⚠ `remove_child()` してから `queue_free()`）
- ⚠ **状態を変える前に全部の判定を終える**（⚠ `attach_part()` は既にその形）
- ⚠ **`.tscn` を大きく変える回。** ⚠ 消えた関数を毎回見ること

---

## 更新履歴

- **2026-09-22 … 新設。** ⚠ モックへの裁き4件（⚠ 倉庫はギルドへ戻す ／ 装飾の操作は説明の窓 ／ 閉じた枠は B案 ／ 等級の色はそのまま）。
  ⚠ **実装の下地を `grep` で取り直した**（⚠ 鍛える・段階を上げる・壊す・重ねるは**もう在る**）。
