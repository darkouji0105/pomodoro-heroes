# EXEC_PLAYTHROUGH — 一度通しで遊んでみる（⑫-c を飛ばして、詰まりを人間の体感で拾う）

⚠ **この回はコードを1行も書かない。** ⚠ **成果物は「実装」ではなく「宿題のリスト」。**
⚠ **画面は人間しか見られない。** ⚠ **設計役がやるのは、遊んだ直後の `godot.log` と `save_slot_0.json` を読むことだけ。**

⚠ **入力**：`docs/NEXT_STEPS.md`（⑫-c）／ `docs/02_exec/EXEC_BALANCE_TUNE.md` §9-2 ／ `docs/PROJECT_STATUS.md`「溜まっている宿題」。

---

## 0. 人間の決定（2026-08-25・着手前に確認済み）

| # | 決定 |
|---|---|
| **1** | ⚠ **⑫-c（残った穴の器）はやらない。** ⚠ **「いまはかんぺきなものをつくらなくてもいい」。⚠ 宿題に落として先へ行く** |
| **2** | ⚠ **`decor_material_4` の出口は、⚠ やるときは `max_part_tier` を **5** にする**（⚠ **作業場に4本目のレシピを足す案ではない**）。⚠ **この回ではやらない。⚠ 宿題47に書き残すだけ** |
| **3** | ⚠ **ルーンの入手は `F4` のままでよい**（宿題9）。⚠ **本番の入手経路を作らない** |
| **4** | ⚠ **戦闘結果に宝箱・持ち物が出ないのはまだ直さない**（宿題23）。⚠ **「宝箱が手に入っていれば良い」** |
| **5** | ⚠ **周回ステージ・スキップ周回は作らない**（宿題49） |
| **6** | ⚠ **次にやるのは「一度通しで遊んでみる」＝この回** |
| **7** | ⚠ **縛りは「スタミナだけ `F4`」**（⚠ **素直に遊ぶ／`F4` 自由 の2つは捨てた**） |
| **8** | ⚠ **前の回の画面4項目（`EXEC_BALANCE_TUNE` §5-C）は実施済み。⚠ ただし見えた数字は未報告**（⚠ **§9-0 に空欄で置く**） |

### 0-1. ⚠ 着手前に取れたもの（**設計役・2026-08-25**）

- ⚠ **前の回のセーブ確認（`EXEC_BALANCE_ECONOMY` §5-C ／ §5-B の B-5）は**取れた**。**
  ⚠ **`save_slot_0.json`（8/25 17:08）は装飾61件を持ち、⚠ `"count"` が float のものは **0件**（`count` は75件）。⚠ **宿題12は閉じてよい**
- ⚠ **同じ形の汚れが `story` に残っている**（⚠ **`"current_chapter": 1.0` ／ `"stars": 0.0` × 3ステージ**）。⚠ **`load_state()` の `int()` 一覧に `STORY` が無い**。⚠ **宿題57として足す。⚠ この回では直さない**
- ⚠ **いまのセーブは退避済み**（⚠ **`scratchpad/save_slot_0_before_playthrough.json`。⚠ 消す前の証拠**）

---

## 1. 何をするか

⚠ **`.gd` / `.tscn` / `.json` / `.csv` / `.tres` を1件も触らない。⚠ 書き換えるのは `.md` だけ。**

| | 誰が | 何を |
|---|---|---|
| **1** | ⚠ **設計役** | ⚠ **いまのセーブを退避する**（⚠ **済み。§0-1**） |
| **2** | ⚠ **人間** | ⚠ **タイトルの「セーブを消す」→ 新規で `stage_3` クリアまで遊ぶ**（§5-C） |
| **3** | ⚠ **人間** | ⚠ **気になった点を1行ずつメモする**（⚠ **原因は書かない。⚠ §2 の決め5**） |
| **4** | ⚠ **設計役** | ⚠ **遊び終わった直後に `godot.log` と `save_slot_0.json` を読む**（§5-A / §5-B） |
| **5** | ⚠ **設計役** | ⚠ **メモとログを突き合わせて `PROJECT_STATUS` の宿題に落とす**（§6） |

---

## 2. ⚠ 僕が自分で決めたもの（**人間が見ていない決め**）

> ⚠ **この章がこのEXECの本体。⚠ §0 の8件以外は全部ここ。⚠ 違うと思ったら差し戻してよい。**

### 決め1：⚠ この回はコードを1行も書かない

⚠ **遊んでいる最中に「ついでに直す」をやると、⚠ 出た詰まりがどの版のものか分からなくなる。**
→ ⚠ **直すのは次の回。⚠ この回の出口は「宿題のリスト」だけ。**

### 決め2：⚠ 終点は `stage_3` クリア

⚠ **`stage_3` のクリアで `research` / `shop` / `rune` / `workshop` の4画面が開く＝全画面が揃う点。**
⚠ **Lv100（集中39.9時間）まで行くのは通しではなく実測なので、⚠ 終点にしない。**
→ ⚠ **途中でやめてもよい。⚠ そこまでで出たメモは拾える。**

### 決め3：⚠ 遊ぶ前に、設計役はヘッドレスを1本も回さない（**`AGENTS.md`「§0 事前チェック」の例外**）

⚠ **`godot.log` は起動のたびにローテートされ、⚠ 保持は5本。⚠ 事前チェックで30本回すと、⚠ 遊んだ記録が押し出されて消える。**
⚠ **この回は `.gd` / `.json` を1件も触らないので、⚠ 回しても前の回と同じ結果しか出ない**（⚠ **前の回の30本は通っている＝`EXEC_BALANCE_TUNE` §9-0**）。
→ ⚠ **§5-§0 は「回さないこと」に置き換える。⚠ 例外なので、⚠ 次に器を触る回では必ず戻すこと。**

### 決め4：⚠ `F4` を押すのは **`stage_2` をクリアしたあとから**

⚠ **スタミナの入口はポモドーロのポーションだけで、⚠ ポモドーロが開くのは `stage_2` のクリア。**
⚠ **つまり最初は「初期スタミナ 20 ＝ 4周」だけで `stage_1` と `stage_2` を抜けることになる**（⚠ **負けたぶんは戻る＝`refund_stamina`。⚠ 減るのは勝った周だけ**）。
→ ⚠ **ここは通しでしか見えない詰まりなので、⚠ `F4` で埋めてしまうと観測できない。⚠ 最初の4周は素直にやること。**
→ ⚠ **`stage_2` を抜けたあとは、⚠ 決定7のとおりスタミナを `F4` で足してよい。**

### 決め5：⚠ メモの粒は「1行 ＝ 1つの気持ち悪さ」。⚠ 原因を書かない

⚠ **「〜だから〜すべき」と書くと、⚠ メモが実装指示に化けて、⚠ 次の回が「宿題の消化」ではなく「思いつきの実装」になる。**
→ ⚠ **書くのは「何をしようとして」「何が起きたか」だけ**（例：「装備画面で何を装備すればいいか分からなかった」）。
→ ⚠ **原因の切り分けは設計役がログと突き合わせてやる。**

### 決め6：⚠ 「宝箱が結果画面に出ない」は詰まりとして数えない

⚠ **決定4で「宝箱は手に入っていれば良い」と決まっている**（宿題23）。
→ ⚠ **同じことを何度もメモに書くと、⚠ 決まっている話が宿題に戻ってくる。⚠ 既に決まっている5件（§0 の 2〜5）はメモの対象外。**

### 決め7：⚠ 待ち時間のある機能は `F4` で飛ばす

⚠ **作業場は 30分 / 90分 / 3時間。⚠ 「30分待つ」を人間の確認項目に書いて止まった前科がある**（`NEXT_STEPS` §4）。
→ ⚠ **`F4` の「製作をすぐ完了させる」を使う。⚠ 待ち時間そのものが妥当かは、⚠ この回では測らない。**

### 決め8：⚠ ゴールドの詰まりは、この回では見えないものとして扱う

⚠ **`F4` の「ゴールド・ジェム・スタミナ」は1つのボタンで、⚠ スタミナだけ足せない**（`tests/debug_overlay.gd`）。
→ ⚠ **押した時点でゴールドも増える。⚠ 「ゴールドが足りなくて詰まる」は §9 で「測れなかった」と書く。**
→ ⚠ **`stage_2` を抜けるまではボタンを押さないので、⚠ 序盤のゴールド（初期100）の窮屈さだけは観測できる。**

---

## 3. ⚠ 変えないもの

- ⚠ **`.gd` / `.tscn` / `.json` / `.csv` / `.tres` を1件も触らない**（決め1）
- ⚠ **`E130` / `W21` は使わない**（⚠ **この回も赤黄を1本も足さない**）
- ⚠ **`base_level_cap`（20）＋ `level_cap_unlock` 8件×10 ＝ 100**（`E127`）
- ⚠ **素材ID・ノードID・レシピIDを改名しない。⚠ 新しい素材を作らない**
- ⚠ **`debug_boot` の `SCENARIOS`（31本）を消さない・足さない**
- ⚠ **`_sync_recipes_from_master()` の早期 `return` を戻さない** ／ ⚠ **`_roll_weighted_table()` にボーナスを足さない**

---

## 4. ⚠ 先に潰す落ち

| # | 落ち | 潰し方 |
|---|---|---|
| **1** | ⚠ **`godot.log` の保持は5本。⚠ 遊んだ記録が消える** | ⚠ **遊ぶ前も後も、⚠ 設計役はヘッドレスを回さない**（決め3）。⚠ **人間は遊び終わったら「終わった」と言うだけ。⚠ 貼らない** |
| **2** | ⚠ **「セーブを消す」で前の証拠が消える** | ⚠ **退避済み**（§0-1）。⚠ **宿題12の確認は消す前に取り終えている** |
| **3** | ⚠ **`F4` で全部出すと詰まりが見えない** | ⚠ **押すのは `stage_2` クリア後・スタミナ目的だけ**（決め4） |
| **4** | ⚠ **待ち時間で人間が止まる** | ⚠ **作業場は `F4` の「製作をすぐ完了させる」**（決め7） |
| **5** | ⚠ **開いていない画面を「無い」と書く** | ⚠ **`stage_1` → `guild` `equipment` `training` `warehouse` ／ `stage_2` → `pomodoro` `decoration` ／ `stage_3` → `research` `shop` `rune` `workshop`。⚠ 開く前に無いのは仕様**（決定4・2026-08-24） |
| **6** | ⚠ **決まっている話をメモに書いてしまう** | ⚠ **§0 の 2〜5（ルーンの入口・宝箱の表示・周回・器の穴）は対象外**（決め6） |
| **7** | ⚠ **メモが実装指示に化ける** | ⚠ **「何をしようとして」「何が起きたか」だけ書く**（決め5） |

---

## 5. 完了条件

### §0 事前チェック（**設計役**）

⚠ **この回に限り、⚠ ヘッドレスは「回さないこと」が条件**（決め3）。

| # | 条件 |
|---|---|
| **0-1** | ⚠ **`git status` で `.gd` / `.tscn` / `.json` / `.csv` / `.tres` の差分が **0件**であること** |
| **0-2** | ⚠ **セーブの退避が済んでいること**（`scratchpad/save_slot_0_before_playthrough.json`） |
| **0-3** | ⚠ **人間が遊び終わるまで、⚠ ヘッドレスを1本も回していないこと** |

### A. ログ（**設計役が読む**・⚠ **人間が遊び終わった直後**）

| # | 何を見るか |
|---|---|
| **A-1** | ⚠ **赤（`^ERROR`）が **0本**であること**（⚠ **遊んだログなので `E125` / `E129` の意図的な赤は出ない。⚠ 1本でも出たら本物**） |
| **A-2** | ⚠ **黄（`^WARNING`）に `skill_dbg_dot_odd` 以外が無いこと**（⚠ **本番キャラだけで遊ぶので、⚠ これも出ないのが期待値**） |
| **A-3** | ⚠ **`_sync_shop_from_master('daily') -> 13 slots` ／ `level cap validated: 20 + 80 (8 nodes) = 100, 0 errors` ／ `_sync_recipes_from_master() -> 3 recipes` が起動ごとに出ていること** |
| **A-4** | ⚠ **`screen_unlocked` の順が `stage_1` → 4画面 ／ `stage_2` → 2画面 ／ `stage_3` → 4画面 になっていること** |
| **A-5** | ⚠ **戦闘が「勝ち」で終わった回数と、⚠ `refund_stamina` が呼ばれた回数**（⚠ **負けた回数＝難易度の代わりの数字。⚠ 宿題50の穴埋めにはならないが、⚠ 「1回も負けなかった」かは分かる**） |

### B. ファイル（**設計役が読む**）

| # | 何を見るか |
|---|---|
| **B-1** | ⚠ **`save_slot_0.json` の `story.stages` が3つとも `cleared: true`** |
| **B-2** | ⚠ **`character_growth` の `level` と `stats` に `.0` が無いこと**（⚠ **今回は本当にレベルを上げた状態で読める＝前の回はデバッグセーブだった**） |
| **B-3** | ⚠ **`materials` の16件のうち、⚠ 遊び終わった時点で **0 のままのもの**を数える**（⚠ **入口が本当に機能しているかの実測。⚠ `economy` の「入口が0件」とは別の数字**） |
| **B-4** | ⚠ **`pending_chests` に宝箱が実際に入っていること**（⚠ **決定4「手に入っていれば良い」の確認。⚠ 結果画面に出ないことは見ない**） |
| **B-5** | ⚠ **`story.current_chapter` が `1.0` のままであること**（⚠ **宿題57。⚠ 直していないので変わらないのが正しい**） |

### C. 画面（**人間だけ**）

⚠ **順に進める。⚠ 各行の「見るもの」は、⚠ 詰まったかどうかを判定するためのもので、⚠ 正解の数字を当てるものではない。**

| # | やること | ⚠ 見るもの |
|---|---|---|
| **C-1** | ⚠ **タイトルで「セーブを消す」→「はじめから」** | ⚠ **ボタンが「はじめから」に変わる** |
| **C-2** | ⚠ **`F4` を押さずに `stage_1` に挑む** | ⚠ **スタミナ 20 → 15。⚠ 勝てるか。⚠ 何をすればいいか画面から分かるか** |
| **C-3** | ⚠ **`stage_1` クリア後、⚠ 開いた4画面（ギルド・装備・育成・倉庫）を1周見る** | ⚠ **⚠ この時点で「やることがある」画面がいくつあるか**（⚠ **素材も装備も無い状態で開く**） |
| **C-4** | ⚠ **`F4` を押さずに `stage_2` に挑む** | ⚠ **⚠ 残りスタミナで足りるか。⚠ 足りなければ、⚠ そこが最初の詰まり**（決め4） |
| **C-5** | ⚠ **`stage_2` クリア → ポモドーロを開き、⚠ 1回だけ集中を回す**（⚠ **短くしてよい**） | ⚠ **ポーションが手に入り、⚠ スタミナが増えるところまで繋がるか** |
| **C-6** | ⚠ **ここから `F4` の「ゴールド・ジェム・スタミナ」を使ってよい**（決め4・決め8） | ⚠ **—** |
| **C-7** | ⚠ **育成でレベルを上げる。⚠ パッシブが解放される Lv20 まで行く** | ⚠ **必要素材が足りるか。⚠ 上がったことが画面で分かるか** |
| **C-8** | ⚠ **装備を1つ着け、⚠ 鍛冶で等級を上げ、⚠ 装飾を1つ刺す** | ⚠ **⚠ 刺す枠が開くのは等級3から。⚠ そこに辿り着けるか** |
| **C-9** | ⚠ **`stage_3` に挑んでクリアする** | ⚠ **勝てるか。⚠ 何回負けたか** |
| **C-10** | ⚠ **開いた4画面（研究・ショップ・ルーン・作業場）を1周見る** | ⚠ **⚠ それぞれ「1回操作できる」ところまで行けるか**（⚠ **作業場は `F4` の「製作をすぐ完了させる」を使う＝決め7**） |
| **C-11** | ⚠ **拠点で「セーブする」を押して終わる** | ⚠ **—**（⚠ **設計役が §5-B を読むために要る**） |

⚠ **メモは通しの間ずっと。⚠ 1行 ＝ 1つの気持ち悪さ。⚠ 原因は書かない**（決め5）。

---

## 6. `PROJECT_STATUS.md` へ足す宿題

| # | 宿題 |
|---|---|
| **57** | ⚠ **`load_state()` の `int()` 一覧に `STORY` が無い。⚠ セーブに `"current_chapter": 1.0` と `"stars": 0.0` が残る**（⚠ **`stars` は書くとき `game_manager.gd:5476` が `int()` を通すのでクリアし直せば戻る。⚠ `current_chapter` は誰も読まない＝実害はセーブの形だけ**）。⚠ **`CLAUDE.md` #3 と同じ形** |
| **58** | ⚠ **`stage_2` をクリアするまでスタミナの入口が無い**（⚠ **ポモドーロが開くのが `stage_2` のクリアのため**）。⚠ **初期スタミナ 20 ＝ 4周で `stage_1` と `stage_2` を抜ける必要がある**（⚠ **負けた周は戻るので、⚠ 減るのは勝った周だけ**）。⚠ **通しで詰まるかを §9 に書く** |

⚠ **`decor_material_4` の出口については、⚠ 宿題47に「やるときは `max_part_tier` を 5 にする（人間の決定・2026-08-25）」と、⚠ 「段階5の追加は **36件ではなく9件**」を書き足す。**

---

## 7. ⚠ ドキュメントのズレ（**報告のみ・勝手に直さない**）

| # | ズレ |
|---|---|
| ⚠ **40** | ⚠ **`NEXT_STEPS` §1-1 と宿題47 の「`max_part_tier` を5にすると `part_*_5` **36件**の追加になる」が誤り。⚠ 非ルーンの装飾は **9通り × 4段階 = 36件**（宝石3・護符2・紋章4）なので、⚠ 段階5で増えるのは **9件**（`ja.csv` も9行）。⚠ しかも `part_config.tres` は `script = ...` の1行だけ＝**`max_part_tier` も配列2本も設計役が直せる**（⚠ `part_config.gd:35` のコメント自身が手順を書いている）** |
| ⚠ **41** | ⚠ **`GAME_DESIGN` 9-3 の表が「中間素材の製作 ✅ デモ範囲」のままだが、⚠ 人間の決定7（2026-08-25）は「作業場は装飾のランダム製作だけ・中間素材は入れない」。⚠ 決定が優先という理解で進めている** |
| ⚠ **42** | ⚠ **`EXEC_BALANCE_TUNE` §6 の宿題番号（51 / 52 / 53）が `PROJECT_STATUS` の実番号（52 / 53 / 54）と1つずれている。⚠ 正は `PROJECT_STATUS` 側** |

⚠ **未報告のズレは 34 / 37 / 38 / 39 ＋ 上の3件＝**7件**。⚠ 次に見つけたものは 43 番。**

---

## 9. 実施結果

### 9-0. ⚠ 前の回の画面4項目（**人間・実施済み。⚠ 見えた数字は未報告**）

| # | 条件 | 見えた数字 |
|---|---|---|
| **C-1** | 育成画面で Lv1 の必要数が `training_material_1` **3個** | ⚠ **未報告** |
| **C-2** | ⚠ **`F4`「素材を全種類」→ 20回上げて Lv20 の必要数が **4個**（⚠ 旧なら22個）** | ⚠ **未報告**（⚠ **ここが 22 だと式の差し替えが効いていない**） |
| **C-3** | ショップ（日替わり）に `training_material_1` の枠が **4つ**・総数 **13** | ⚠ **未報告** |
| **C-4** | `stage_3` を1回クリアして経験値素材が **x6** | ⚠ **未報告** |

### 9-1. §0 事前チェック

⚠ **未実施**（⚠ **人間が遊び終わってから設計役が §5-A / §5-B を取る**）。

### 9-2. 通しで出たメモ（**人間**）

⚠ **未実施。**

### 9-3. ⚠ 分かったこと（**次の回への申し送り**）

⚠ **未実施。**
