# EXEC — **無音の穴を4件塞ぐ**（死亡中のCD ／ `atk_multiplier` ／ DoT の購読 ／ 知らない欄）

前提は `docs/NEXT_STEPS.md` §5（宿題）。直前の回は `docs/02_exec/EXEC_SKILL_AURA.md`。
検証の道具は `res://tests/debug_boot.tscn`。

⚠ **この回で塞ぐのは「エラーが1つも出ないまま壊れている」穴だけ。** ⚠ **見た目は1ミリも変わらない。**
⚠ **バランスの実測より先にやる**（この4件のうち3件がダメージと発動可否の数値に効くため。⚠ **先に測ると測り直しになる**）。

---

## 0. 人間が決めたこと（**本文と矛盾する場合はこちらが優先**・2026-08-22）

| # | 決めたこと | 内容 |
|---|---|---|
| **1** | **束** | ⚠ **無音の穴を潰す回にする**（見た目・ドキュメント・大きい器の穴は別の回） |
| **2** | **`atk_multiplier`** | ⚠ **使えるようにする。**「攻撃力◯倍」の欄を `buff` に1つ足す |
| **3** | **`target.range`** | ⚠ **今回は触らない。**（⚠ **器も実行時の読みもあり、バグではない。本番データに書くかはバランスの話**）。⚠ **宿題の記述だけ直す** |

---

## 0-1. ⚠ 設計役が自分で決めたもの（**人間が見ていない決め・要確認**）

| # | 決めたこと | なぜそうしたか |
|---|---|---|
| **1** | ⚠ **死亡中はCDを進めない。⚠ ガードは `tick_cooldowns()` の中に置く**（呼ぶ側で弾かない） | ⚠ **呼ぶ側（`battle_controller._process()`）で弾くと、⚠ 次に「全員を回すループ」を足す人がまた死者を通す。⚠ 関数が自分で守る形にすれば、呼び出し元が増えても壊れない**（`_applies_to()` / `find_unit()` と同じ考え方） |
| **2** | ⚠ **復活したときにCDを戻さない**（死んでいた間ぶんは進まないだけ） | ⚠ **戻す＝別の仕様。⚠ 「死亡中は時間が止まる」だけにする。⚠ 戻す形にすると、復活直後に全スキルが撃てて、⚠ 復活の価値がバランスの話に化ける** |
| **3** | ⚠ **`atk_multiplier` の欄名は `atk_mult_pct`（int・%）** | ⚠ **`multiplier` は `buff` に書けない**（既存 E の「buff は multiplier を書けない」）。⚠ **同じ綴りを別の意味で使わない。** ⚠ **% の整数にするのは `heal_taken_pct` / `reduction_pct` と揃えるため**（この器は「積み上げは和」で統一されている） |
| **4** | ⚠ **合計してから 1.0 に足す**（`atk_multiplier = 1.0 + 合計/100`） | ⚠ **掛け合わせない。⚠ `stat_mod()` / `heal_taken_pct()` / 軽減・貫通と同じ「積み上げは和」。⚠ ここだけ積にすると、2つ付けたときの答えが器の中で2通りになる** |
| **5** | ⚠ **下限は 0.0（マイナスにしない）** | ⚠ **`-200%` を書くと符号が反転して「殴ると回復する」になる。⚠ 回復とダメージで表示の色も介入点も別なので、⚠ 符号を跨がせない**（`skill_resolver.gd` が名指しで警告している形） |
| **6** | ⚠ **`atk_multiplier` は `_rebuild_unit_mods()` で組み直す**（`stat_mod` と同じ経路） | ⚠ **「ゼロから組み直す」形に乗せれば、⚠ 剥がれないバフ・二重に乗るバフが構造的に起きない。⚠ 条件・オーラ・寿命切れの対応がタダで付く** |
| **7** | ⚠ **DoT の結果も `SkillRuntime._dispatch_events()` を通す。⚠ 通し方は「`StatusRegistry` が `SkillRuntime` を知る」ではなく、⚠ `battle_controller` が仲介する** | ⚠ **器（`RefCounted`）どうしを相互参照させない**（PLAN 7-3・段階3で `SkillResolver` → `SkillRuntime` の相互参照を踏んでいる）。⚠ **`battle_controller` は既に両方のシグナルを受けているので、⚠ 受けた `results` を `SkillRuntime` へ渡すだけで済む** |
| **8** | ⚠ **反応から生まれた行動は、さらなる反応を生まない**（PLAN 10-2）。⚠ **DoT の購読で撃った結果は、もう一度購読へ回さない** | ⚠ **毒 → 反射 → 毒 … の無限ループを構造で止める。⚠ 台帳の横断ルールをそのまま守る** |
| **9** | ⚠ **効果の直下の「知らない欄」は**黄**にする（赤にしない）** | ⚠ **赤にすると、⚠ 既存の JSON に1つでも見落としがあった瞬間にゲームが起動しなくなる。⚠ 何件出るか実装前に分からない。⚠ 出た件数を見てから、次の回で赤に上げるかを人間が決める** |
| **10** | ⚠ **「知らない欄」の一覧は `SkillSchema` に1本だけ持つ** | ⚠ **2本目を作ると、欄を足したときに片方だけ直す。⚠ この器で3回踏んでいる形** |
| **11** | ⚠ **E は E116 から・W は W16 から** | ⚠ **E115 / W15 まで使用済み。⚠ W6 と W7 は欠番のまま** |

---

## 1. いま何がどうなっているか（**実コードで確認済み・2026-08-22**）

| # | 穴 | 実コード |
|---|---|---|
| **A** | **死亡中にCDが回る** | ⚠ **`unit.gd:276-278` の `tick_cooldowns()` に生死のガードが無い。⚠ `battle_controller.gd:453` が `_all_units()` で死者にも回している** |
| **B** | **`atk_multiplier` が常に 1.0** | ⚠ **`unit.gd:91` の宣言以外に**書き込む場所が1つも無い**。⚠ 読む側は `skill_resolver.gd:400`（ダメージ倍率に掛けている） |
| **C** | **DoT で購読が発火しない** | ⚠ **購読を配るのは `skill_runtime.gd:421` の `_dispatch_events(entry, results)`。⚠ DoT の結果は `status_registry.gd:669` の `effects_applied` から `battle_controller._on_skill_effects_applied()`（表示）へ直行していて、⚠ そこを通らない** |
| **D** | **効果の直下に「知らない欄」の検出が無い** | ⚠ **塞いであるのは `intervene{}`（E107）と `zone{}`（E113）の中だけ。⚠ 効果の直下は typo が無音で無視される** |

⚠ **`target.range` は穴ではなかった**（§0 の決定3）。⚠ **`skill_resolver.gd:106-107` が実行時に読んでおり、`char_debug_life` の1件が実際に書いている。**

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ D（知らない欄）は既存データに火が付く可能性がある

⚠ **何件出るか実装前に分からない。⚠ 赤にすると1件でもあった瞬間に起動しなくなる。**
→ ⚠ **黄で出す**（§0-1 の9）。⚠ **出た件数を §11 に必ず書く。**

### 2-2. ⚠ C（DoT の購読）で無限ループを作らない

⚠ **毒が反射を呼び、反射がまた毒を…は器の外からは見えない。⚠ フレームが落ちるだけでエラーは出ない。**
→ ⚠ **PLAN 10-2 のとおり、⚠ 購読から生まれた結果は購読へ回さない。**

### 2-3. ⚠ A（死亡中のCD）は既存の数字を動かす

⚠ **`recast` や `summon` のシナリオで、⚠ 死んだユニットのCDが止まるぶん撃てる回数が変わりうる。**
→ ⚠ **段階4〜6と介入点・オーラの実測値を全部取り直して、⚠ 動いたものは §11 に書く。**

### 2-4. ⚠ B（`atk_multiplier`）は「掛ける」ので桁が飛ぶ

⚠ **`stat: atk` の加算と違い、⚠ 倍率は元の値に比例する。⚠ 検証用の値を大きくすると damage が万を超えて読みにくい。**
→ ⚠ **検証用は `+100%`（2倍）にする。⚠ 200 → 400 で桁が変わらず読める。**

### 2-5. ⚠ 編集したら `grep` で当たったことを確認する

⚠ **置き換えたあとに `grep` で残りを数えて、1件ずつ「これは残ってよいものか」を見ること**（オーラで1箇所やり損ねた）。

---

## 3. 実装（ファイル別）

### 3-A. 死亡中にCDを進めない

**`scripts/systems/unit.gd`** … `tick_cooldowns()` の先頭に `if not is_alive(): return`。

⚠ **呼ぶ側（`battle_controller`）は触らない**（§0-1 の1）。⚠ **復活時に戻さない**（§0-1 の2）。

### 3-B. `atk_multiplier` を使えるようにする

**`scripts/systems/skill_schema.gd`**：`BUFF_ATK_MULT_PCT = "atk_mult_pct"` を足す。

| # | 赤にする条件 |
|---|---|
| **E116** | `atk_mult_pct` が整数でない、または `0`（何も起きない欄は書かせない・E39 と同じ） |
| **E117** | `atk_mult_pct` が `buff` 以外に書いてある |

**`scripts/systems/status_registry.gd`**：
- `_make_entry()` に `"atk_mult_pct": 0`（⚠ **持たない件にも必ず持たせる**）
- `_fill_buff()` で読む。⚠ **`has_stat` と同じ扱いにする**（`stat` を持たない buff も「何かする buff」として通す）
- `atk_mult_pct(unit_id) -> int` … ⚠ **`_intervene_sum()` と同じ形。`_applies_to()` を通す**（オーラでも効く）
- ⚠ **`_rebuild_unit_mods()` の中で `unit.atk_multiplier` を組み直す**（§0-1 の6）。⚠ **`1.0 + 合計/100`。⚠ 下限 0.0**

⚠ **`unit.gd` に関数を足さない。** ⚠ **`atk_multiplier` を書くのは `_rebuild_unit_mods()` の1箇所だけ**（2箇所目を作ると剥がれ方がズレる）。

### 3-C. DoT の結果を購読へ回す

**`scripts/systems/skill_runtime.gd`**：`notify_results(results: Array) -> void` を1本足す。
⚠ **中身は `_dispatch_events()` を呼ぶだけ。⚠ 待ち行列の entry を持たない結果なので、⚠ 「誰が撃ったか」は `results` の `source_unit_id` から取る。**

**`scenes/adventure/battle_controller.gd`**：`_on_skill_effects_applied()` の中で、⚠ **`StatusRegistry` から来たものだけ** `_skill_runtime.notify_results(results)` へ回す。

⚠ **シグナルの接続元で分ける**（`_status.effects_applied` と `_skill_runtime.effects_applied` は別々に繋がっている）。⚠ **同じハンドラのまま「どちらから来たか」を見分けようとしないこと。** ⚠ **`SkillRuntime` から来たものを回すと、⚠ 購読が購読を呼んで PLAN 10-2 を破る。**

→ ⚠ **`_status.effects_applied` の接続先を専用の関数（`_on_status_effects_applied()`）に分け、⚠ その中で表示（既存の1本）と購読の両方へ回す。**

### 3-D. 効果の直下の「知らない欄」を黄で出す

**`scripts/systems/skill_schema.gd`**：`EFFECT_FIELDS_KNOWN`（1本だけ）を作り、⚠ **効果の直下のキーを突き合わせる。**

| # | 出すもの |
|---|---|
| **W16** | ⚠ **効果の直下に知らない欄がある**（⚠ **赤にしない。§0-1 の9**） |

⚠ **一覧に入れる欄**（実データから拾うこと。⚠ 憶測で書かない）：`type` / `host` / `status_id` / `stack` / `max_stack` / `duration_sec` / `until` / `interval_sec` / `multiplier` / `attack_type` / `scale_from` / `stat` / `value` / `target` / `chance` / `trigger` / `delay_sec` / `react` / `condition` / `intervene` / `zone` / `heals` / `unit_id` / `count` / `offset_x` / `atk_mult_pct` ほか。
⚠ **`grep` で実データの全キーを列挙してから書くこと。** ⚠ **憶測で書くと、正しい欄が黄で出る。**

### 3-E. 検証用データ ／ `tests/debug_boot.gd`

| シナリオ | 見るもの |
|---|---|
| `atk_mult` | ⚠ **`+100%` のバフで damage が 200 → 400 になること** |
| `dot_react` | ⚠ **毒のダメージで購読（反射）が発火すること。⚠ 連鎖しないこと** |
| （既存） `summon_wipe` | ⚠ **死亡中にCDが止まること**（⚠ **味方を全滅させるシナリオが既にある**） |

⚠ **`characters.json` の候補一覧にも足すこと。** ⚠ **`ja.csv` も。**

### 3-F. 触らないファイル

⚠ **`battle_formula.gd`（式そのもの）／ `skill_activation.gd` ／ `battle_session.gd` ／ `unit_view.gd`**
⚠ **本番スキルの JSON 全件 ／ `stages.json` ／ 射程の段の値 ／ `target.range`（決定3）**

---

## 4. 変えないもの

- ⚠ **復活したときにCDを戻さない**（§0-1 の2）
- ⚠ **「積み上げは和」**（§0-1 の4）
- ⚠ **PLAN 10-2**（反応から生まれた行動は反応を生まない）
- ⚠ **見た目**（この回で画面は1ミリも変わらない）

---

## 5. 完了条件 — **§0 事前チェック**（⚠ **設計役・ヘッドレス**）

1. ⚠ **全シナリオ（既存16 ＋ 新規2）で `ERROR:` / `SCRIPT ERROR:` / `Parse Error` が1行も出ないこと**
2. ⚠ **`skills validated` が `0 errors` であること。⚠ 黄の件数を §11 に書くこと**（⚠ **W16 が何件出たかがこの回の主要な観測結果**）

## 6. 完了条件 — **ログ / ファイル**（⚠ **設計役が読む**）

### 6-A. 死亡中のCD

3. ⚠ **`summon_wipe` で味方が全滅したあと、⚠ 死者の `cast` が1本も出ないこと**（今までどおり）
4. ⚠ **死亡中にCDが進んでいないことを、⚠ 復活のあるシナリオ（`intervene_legacy`）で見る**：⚠ **復活した敵が、死んでいた秒数ぶん早くスキルを撃たないこと**

### 6-B. `atk_mult_pct`

5. ⚠ **`atk_mult` で damage が `200 → 400`（+100%）になること**
6. ⚠ **同じ相手・同じ撃ち手で前後を比べていること**（⚠ **敵が死んで別の敵に飛んでいないこと**）
7. ⚠ **バフが切れたら 200 に戻ること**
8. ⚠ **オーラ（`host: point`）に書いても効くこと**（⚠ **`_applies_to()` を通しているか**）

### 6-C. DoT の購読

9. ⚠ **毒のダメージで反射の `damage` が出ていること**（⚠ **今までは出ていなかった**）
10. ⚠ **その反射がさらに反射を呼んでいないこと**（PLAN 10-2）
11. ⚠ **`pool`（範囲の毒沼）でも同じであること**

### 6-D. 知らない欄

12. ⚠ **W16 が何件出たかを §11 に書くこと。⚠ 出た欄の名前も全部書くこと**
13. ⚠ **正しい欄が黄で出ていないこと**（⚠ **出ていたら一覧の書き漏らし。⚠ 一覧を直す**）

### 6-E. 既存が壊れていないこと

14. ⚠ **`area` 3/4/4/3 ／ `recast` 2/8 ／ `summon` の `x` が −60/−120 ／ `mitigate` 200/300/200/120 ／ `pierce` 194/200 ／ `lineup` 99.4 ／ `pool` の6件が対**
15. ⚠ **動いたものがあれば §11 に実測値を書く**（⚠ **A は数字を動かしうる・§2-3**）
16. ⚠ **`save_slot_0.json` が変わらないこと**

### 6-F. ⚠ 足した検証が本当に赤を出すか

17. ⚠ **E116 / E117 / W16 を実測する**（⚠ **W16 は正しい欄を1つ typo にして確かめる**）

## 7. 完了条件 — **画面**

⚠ **無い。** ⚠ **この回で画面は1ミリも変わらない。**
⚠ **変わって見えたらそれは事故なので、⚠ 人間が気づいたら報告してほしい**（確認項目にはしない）。

---

## 8. 将来コードを変えたときに見る項目

- ⚠ **W16 を赤に上げるか**（⚠ **出た件数を見てから人間が決める**）
- ⚠ **`atk_mult_pct` の下限0が効くか**（⚠ **データ側で `-200` を書くと E116 で弾かれるので実機では踏めない**）
- ⚠ **購読が `host: battle` / `point` に広がったときに、⚠ DoT の経路が二重に配らないか**

---

## 9. Ziva に渡せる部分

⚠ **無い。** ⚠ **4件とも `.gd` の変更が先に要る。**

---

## 10. 終わったあとに足す宿題

- ⚠ **解消：死亡中にCDが回る ／ `atk_multiplier` が常に 1.0 ／ DoT で購読が発火しない**
- ⚠ **一部解消：知らない欄の検出**（⚠ **効果の直下は黄まで。⚠ 赤に上げるかは次の判断**）
- ⚠ **記述の訂正：`target.range` は「未設定」ではなく「本番データに書いていない」**（⚠ **器も実行時の読みもある**）
- ⚠ **NEW：復活してもCDは戻らない**（§0-1 の2）

---

## 11. ⚠ 実施結果（**2026-08-22・設計役がヘッドレスで実行して判定**）

⚠ **§5・§6 は全部通った。§7（画面）は無い。**

| 項目 | 結果 |
|---|---|
| 5-1 全シナリオで赤が1行も出ない | ✅ **17シナリオで `赤=0`** |
| 5-2 検証の件数 ／ ⚠ **W16 が何件出たか** | ✅ **`79 entries, 0 errors, 1 warnings`**。⚠ **W16 は0件**（黄1本は `skill_dbg_dot_odd` の端数＝従来どおり） |
| 6-A-3 死者が撃たない | ✅ **`summon_wipe` で全滅後の `cast` が0本**（従来どおり） |
| 6-A-4 復活した敵が早く撃たない | ✅ **`intervene_legacy` が従来と同じ流れ**（復活 → 免疫 → 被回復低下が同じ順で出る） |
| 6-B-5 倍率が効く | ✅ **`200 → 400`（+100%）** |
| 6-B-6 同じ相手・同じ撃ち手 | ✅ **1発目と2発目は `enemy_1_2`。⚠ 3発目は敵が死んで `enemy_1_3` に移ったが、⚠ 確定ダメージなので値は比較できる** |
| 6-B-7 切れたら戻る | ✅ **`status_end why=expire` のあと `200`** |
| 6-B-8 オーラでも効く | ⚠ **未実測**（⚠ **`_applies_to()` を通す実装にはしたが、`host: point` に `atk_mult_pct` を書いた検証データを作っていない**） → §8 |
| 6-C-9 毒で購読が発火する | ✅ **毒6発 → `react` 6件。⚠ この回の前は0件だった** |
| 6-C-10 連鎖しない | ✅ **6発に対して6件。増えていない**（PLAN 10-2 の `from_reaction` が効いている） |
| 6-C-11 `pool` でも同じ | ⚠ **未実測**（⚠ **範囲の毒沼に購読持ちを置く検証データを作っていない**） → §8 |
| 6-D-12 W16 の件数 | ✅ **0件。⚠ 既存データに typo は1つも無かった** |
| 6-D-13 正しい欄が黄で出ていない | ✅ **0件なので、⚠ 一覧の書き漏らしも無い** |
| 6-E-14 既存が壊れていない | ✅ **`area` 3/4/4/3 ／ `recast` 2/8 ／ `summon` の `x` −60/−120 ／ `mitigate` 200/300/200/120 ／ `pierce` 194/200 ／ `pool` の zone 12件** |
| 6-E-15 動いたもの | ✅ **1つも動かなかった**（⚠ **§2-3 で「A は数字を動かしうる」と書いたが動かなかった**） |
| 6-E-16 セーブ | ✅ **`save_slot_0.json` は存在しないまま** |
| 6-F-17 壊すと出る | ✅ **§11-3** |

### 11-1. ⚠ 実装中に踏んだこと

**① ⚠ シナリオの設計を間違えた（「反射」という言葉が2つの器を指している）。**

⚠ **初稿は `skill_dbg_mit_reflect`（介入点の反射）で「毒で購読が発火するか」を試そうとした。⚠ あれは購読ではない。**
⚠ **`intervene.reflect_pct` は `_apply_damage()` の中で処理され、しかも DoT では意図的に返さない設計。⚠ 宿題の経路を1ミリも通らない。**
⚠ **結果は `react 合計=0`。⚠ 「直っていない」ように見えるが、⚠ 実際は測る場所が違っていた。**
→ ⚠ **`type: react`（`skill_dbg_react_thorns`）に差し替えた。⚠ 購読は「毒を受ける本人」に付いていないと発火しないので、⚠ 同じキャラに購読と自分がけの毒の両方を持たせた。**

**② ⚠ E63（何もしない buff）が新しい欄を知らず、正しいデータを赤にした。**

⚠ **`atk_mult_pct` だけを持つ buff が「stat / value も intervene{} も無い」で弾かれた。**
→ ⚠ **判定に足した。⚠ 新しい補正の欄を足すたびにここも直す必要がある**（`EFFECT_FIELDS_KNOWN` と2箇所）。

**③ ⚠ ②の直し方が雑で、今度は別の赤が出た。**

⚠ **`has_stat` に `atk_mult_pct` を混ぜたら、⚠ `stat` を持たない buff に E37〜E39（`stat` が10軸に無い／`value` が0以外の整数でない）が出た。**
→ ⚠ **`has_stat`（stat / value の検証を走らせるか）と `has_atk_mult`（何かする buff か）を別の変数にした。**

**④ ⚠ `EFFECT_FIELDS_KNOWN` は実データから列挙した。**

⚠ **`grep` で全 JSON の効果の直下のキーを数え上げ（24種）、⚠ コードが読む欄（`chance` / `charge_scales`）を足した。**
⚠ **憶測で書いていたら、正しい欄が黄で出ていた**（W16 が0件で済んだのはこのため）。

### 11-2. ⚠ `target.range` は穴ではなかった（**宿題の記述が違っていた**）

⚠ **器も実行時の読みもある**（`skill_resolver.gd:106-107` が射程で母集団を絞っている）。⚠ **`char_debug_life` の1件は実際に `range: 300.0` を書いている。**
⚠ **書いていないのは本番スキルだけ。⚠ つまり「バグ」ではなく「データにまだ書いていない」。**
→ ⚠ **人間の決定3で今回は触らない。⚠ 宿題の記述を直す。**

### 11-3. ⚠ 足した検証が本当に出ることを確かめた

| 壊し方 | 出たもの |
|---|---|
| `scale_from` を `scale_frm` に | ✅ **W16 `効果[0] に知らない欄がある: 'scale_frm'（書いても何も起きない）`**（⚠ **黄が 1 → 14 に増えた**） |
| `atk_mult_pct: 0` | ✅ **E116 `atk_mult_pct が0以外の整数でない`** ＋ 器側の `push_error` |
| `damage` に `atk_mult_pct` | ✅ **E117 `atk_mult_pct は buff にしか書けない（type: 'damage'）`** |

⚠ **戻したあと `79 entries, 0 errors, 1 warnings` に戻ることも毎回確認済み。**

### 11-4. ⚠ ヘッドレスを28回走らせた
