# 実装計画：新設計をどの順番で作るか

**このファイルは「順番」と「依存関係」だけを持つ。** 何を作るかは`GAME_DESIGN.md`。実装の手順は各`EXEC`。

**`PLAN_CHARACTER_GROWTH_LOOP.md`と`HANDOVER_STATS_AND_EQUIPMENT_V2.md`はこのファイルで置き換わった。** 両方とも冒頭に「破棄」と書くこと。

**決定内容をこのファイルにコピーしないこと。** `GAME_DESIGN.md`の該当章を指す。同じ内容が2箇所にあると、修正時にどちらが正か分からなくなる。

---

## 0. 前提

**設計を一通り詰め直した結果、既存の実装と食い違う箇所が多い。** 差分は`GAME_DESIGN.md` 12章。

**特に大きいもの：**

| | 実装 | 新設計 |
|---|---|---|
| ステータスの軸 | 4本 | **10本** |
| 等級の上限 | 3 | **10** |
| `parts` | 長さ2固定・種類なし | **可変長・種類つき** |
| レベル | ステータスが上がる | **割り振りポイント＋パッシブ** |
| 装備の入手 | 作業場・ショップ・宝箱 | **ステージの抽選ドロップが主力** |
| 作業場 | 素材変換＋装備製作 | **中間素材＋装飾のくじ**（既存レシピはほぼ全滅） |

---

## 1. セーブ構造を触る回をどう分けるか

**これがこの計画の核。**

### セーブに触る変更の一覧

| 変更 | セーブのどこ |
|---|---|
| 10軸 | `character_growth.<id>.stats`にキーが6つ増える |
| 割り振りポイント | `character_growth`に新フィールド |
| パッシブの解放状態 | 同上 |
| スキル選択 | `character_growth.skills`（**現在`{}`のまま**） |
| 等級10 | `equipment_instances.<id>.grade`の値域が広がる（**構造は変わらない**） |
| `parts`の可変長・種類つき | `equipment_instances.<id>.parts`の**要素の型が変わる** |
| 装飾のインベントリ | 新しい入れ物 |
| プリセット2階層 | 新しい入れ物 |
| 新しい素材・トークン | `materials`にキーが増える（**構造は変わらない**） |

### 分け方の判断

**「キーが増えるだけ」と「型が変わる」を区別する。**

- **キーが増えるだけのもの**（10軸・素材・割り振りポイント・パッシブ・プリセット）は、既存セーブで0埋め・空配列になればよい。**移行処理が要らないので、いつやってもよい**
- **型が変わるもの**（`parts`の要素が`null`から`{装飾ID, 等級, 出目}`のオブジェクトに）だけが本当の破壊的変更

**既存セーブは捨ててよい**（`GAME_DESIGN.md` 14章）。デモ前なので移行処理は書かない。`_normalize_equipment_from_save()`が不正な値を`null`に戻す既存の仕組みで足りる。

- [x] **確定：分ける。** セーブ構造を触る回を1回にまとめない（2026-08-14）

**移行処理が要らないので、まとめる理由が無い。** 「キーが増えるだけ」のものは既存セーブで0埋め・空配列になればよく、「型が変わる」`parts`も既存セーブを捨てる判断が出ている。**まとめる動機は移行処理の重複を避けることだったが、そもそも移行処理を書かない。**

> **装備の第2弾は「ステータス9軸と同じ会話でやる予定が、装備だけで終わった」前例がある。** 大きくまとめるほうがリスクが高い。

**ただし各回で必ず確認すること：**

- [ ] 新しいキーが無い既存セーブで、`push_error`ではなく**0埋め・空配列**になる
- [ ] ロード時に`_recalc_stats()`を通る経路がある

---

## 2. 依存関係

**順番を決める前に、何が何を待つのかを確定させる。**

```
10軸 ──┬─→ 装備の equip_stats を10軸に
        ├─→ 敵データに mag / mdef
        ├─→ 装飾の効果（宝石・紋章が軸を参照する）
        └─→ 割り振りポイント（振れる軸が要る）

レベルの役割転換 ──┬─→ スキル選択（Lv5/10/15/20で候補が開く）
                    └─→ パッシブ

スキル選択 ──→ ルーン（武器＝スキル1、アクセ＝スキル2に紐づく）

等級10 ──→ スロットの種類 ──→ 装飾を刺す ──→ 鍛冶屋トークン
                              └─→ ルーン枠

分解方式 ──→ 素材の供給 ──→ バランス実測
ステージの抽選ドロップ ──┘
```

### ここから出る制約

- **装飾は等級3にならないと刺せない。** 等級10への拡張とスロットの種類が先
- **ルーンはスキル選択が動いていないと紐づけられない。** `select_skill()`は現在空実装で呼び出し元0件
- **バランス実測は、分解とドロップが動いてから。** 素材の供給経路そのものが変わった
- **敵の`mag`/`mdef`は10軸と同時。** 片方だけだと`mdef`が死に軸になる
- **作業場は既存レシピがほぼ全滅する。** 作り直しに近く、他と独立しているので後回しでよい

---

## 3. 順番

- [x] **確定（2026-08-14）。** 2章の依存関係と突き合わせ、矛盾が無いことを確認した

**変えるときは2章の依存関係を先に見ること。**

**状態列は、タスクが1つ終わるたびに1行だけ直す。** 完了を記録する場所はここ1箇所（`PROJECT_STATUS.md`の実装済み表はEXEC単位、こちらは段階単位）。

| # | 段階 | 依存 | 規模 | 状態 |
|---|---|---|---|---|
| 1 | **ポモドーロのアラーム音** | なし | 小 | ✅ 完了 |
| 2 | **ステータス10軸**（式・敵の`mag`/`mdef`・上限） | なし | 大 | ✅ 完了（器・式の2回に分けた） |
| 3 | **レベルの役割転換**（割り振り・パッシブ・スキル解放） | 2 | 大 | ✅ **完了**（2026-08-25。`EXEC_CHARACTER_PASSIVES`。⚠ **パッシブは「選ぶ」のをやめ、⚠ 解放されたものが全部効く形にした＝`GAME_DESIGN` 5-2 / 5-4 に実装を寄せた**。⚠ **本番3キャラ × 5件＝15件・`passives.json` を3本新設**。⚠ **レベル上限を 30 → 100 にした**。⚠ **画面も人間が実機で確認済み**。⚠ **育成画面の詳細を2カラムに組み替えた＝縦のはみ出し**） |
| 4 | **等級10とスロットの種類** | 2 | 中 | ✅ **完了**（2026-08-22。`EXEC_MATERIAL_TIERS` ＋ `EXEC_DECORATION` §13） |
| 5 | **分解方式とステージの抽選ドロップ** | 4 | 中 | ✅ **完了**（2026-08-23。分解は 4 と同時／ドロップは `EXEC_STAGE_DROPS`・宝箱方式） |
| 6 | **装飾（宝石 → 護符 → 紋章）** | 4 | 大 | ✅ **完了**（2026-08-22。`EXEC_DECORATION`。⚠ **護符の固有効果は未実装＝軸で見分けている暫定**） |
| 7 | **パーティ選択画面とプリセット** | 3 | 中 | ✅ **完了**（2026-08-23。`EXEC_PARTY_PRESETS`。⚠ **2階層・参照方式**。⚠ **プリセットに名前を付けられない**） |
| 8 | **ルーン** | 3, 4, **7** | 大 | ✅ **完了**（2026-08-24。`EXEC_RUNES`。⚠ **画面16項目も人間が実機で確認済み**。⚠ **5種 × 5段階＝25件・`runes.json` 新設＝マスター7本目**。⚠ **かけらは未実装＝段階5で重ねられない**。⚠ **本番の入手経路が無い＝`F4` だけ**） |
| 9 | **機能の段階解放** | 全部（**器は早めに**。下記） | 小 | ✅ **完了**（2026-08-24。`EXEC_SCREEN_UNLOCK`。⚠ **画面12項目も人間が実機で確認済み。⚠ 装備も育成も閉じた状態で `stage_1` に勝てた＝`GAME_DESIGN` 9-6 を満たしている**。⚠ **画面IDが8つ増え、⚠ `stages.json` の `unlocks` が引き金**。⚠ **9-5 の10段を4段に畳んだ＝本番ステージが3本しか無いため**。⚠ **`initial_state_config.tres` は人間が編集済み**） |
| 10 | **研究ボードの作り替え** | 3 | 中 | ⚠ **済**（2026-08-25。⚠ **2ボード18ノード・枝は戦闘/宝箱・上限は8件×+10＝ちょうど100**。`EXEC_GUILD_RESEARCH_V2.md`） |
| 11 | **作業場の作り直し** | 5 | 中 | ⚠ **済**（前半＝廃止：2026-08-23・`EXEC_WORKSHOP_RETIRE` ／ 後半＝復活：2026-08-25・`EXEC_WORKSHOP_REVIVE`。⚠ **装飾のランダム製作3レシピ・`stage_3` で解放・研究に作業場枝2件**。⚠ **中間素材（研究用素材）は入れていない＝5系統目の素材が要るため人間が見送った**） |
| 12 | **バランス実測** | 5, 6, 8 | — | ⚠ **入れた**（前半＝測る：2026-08-25・`EXEC_BALANCE_ECONOMY`／後半＝入れる：2026-08-25・`EXEC_BALANCE_TUNE`。⚠ **Lv100×3キャラが 集中 160.9 → 39.9 時間**（`stage_2`）。⚠ **式を線形から二次へ差し替え＝`GAME_DESIGN` 5-2「序盤を安く・後半を跳ねさせる」**。⚠ **経験値素材は `training_material_1` に一本化＝`stage_3` の `∞` が消えた**。⚠ **`.tres` / `ja.csv` / `.tscn` を1件も触っていない＝人間の Inspector 作業なし**。⚠ **`construction_material_4` の入口・`decor_material_4` の出口・鍛冶/装飾/宝箱の「勘の数値」は残件**。⚠ **①戦闘の秒数と装飾の出目は後回し＝宿題50**。⚠ **⑫-c（残った穴の器）は 2026-08-25 に人間が見送り＝宿題47 / 52 / 53 のまま。⚠ 次は「一度通しで遊ぶ」＝`EXEC_PLAYTHROUGH`**） |
| 13 | **SDキャラとアニメーション** | なし | **専用の会話** | ⚠ **段階14 のあと**（⚠ **人間の決定・2026-08-25**） |
| ⚠ **14** | ⚠ **シナリオの作り替え（マップ探索型フロア）** | ⚠ **5, 6, 9** | ⚠ **大＝6段階に割った** | ⚠ **未着手**（2026-08-25 に方針と割り方を確定。⚠ **台帳は `docs/01_plan/PLAN_SCENARIO_MAP.md`。⚠ やるべきこと全32件は §9-1、⚠ 割り方は §9-2**。⚠ **`GAME_DESIGN` 3章・4章を置き換える**。⚠ **フロア5本・ノード4種・レリック（フロア内限定・パッシブの器を借りる）・視界（たいまつ）・移動に紐づく宝箱・自動周回。⚠ `chest_id` はフロア×レアリティ・抽選のハズレ枠は廃止**。⚠ **`unlocks` はポモドーロをフロア1へ前倒し＝宿題58 が閉じる**。⚠ **宿題49（周回・スキップ）は「やらない」から「やる」へ戻った**。⚠ **マップの形は層構造（合流あり）・5層＝`DISCUSSION_scenario_map.md` の「合流なし」を覆した**。⚠ **人間の判断が要る未決は残っていない。⚠ 次の回は段階の割り方と `EXEC` の1本目**。⚠ **難ダンジョンはこの段階に含めない。⚠ 平面グリッドはそちらの候補として退避**） |
| ⚠ **14-a** | ⚠ **フロアの器**（`stages.json` 5本・`unlocks` 付け替え・状態キー1本・マップ生成・移動・`scenario=floor`） | ⚠ **—** | ⚠ **大** | ⚠ **済**（2026-08-25・`EXEC_SCENARIO_FLOOR.md`。⚠ **`stage_1..3` → `floor_1..5`・層構造5層・ノード11〜16件**。⚠ **全ルート総当たりで「ボスに着かないルート」0本＝合流ありが効いている**。⚠ **30シナリオの平常値は不変**。⚠ **`.tres` / `ja.csv` / `.tscn` を1件も触っていない＝人間の作業ゼロ**。⚠ **`ja.csv` に `ui_floor_1..5` が無く画面にキー名が出る＝14-c で足す**） |
| ⚠ **14-b** | ⚠ **宝箱の作り直し**（`floor_N_<rarity>` 20件・ハズレ枠廃止・移動に紐づく抽選・深度補正） | ⚠ **14-a** | ⚠ **中** | ⚠ **済**（2026-08-25・`EXEC_SCENARIO_CHEST.md`。⚠ **`chests.json` 24件・ハズレ枠0件**。⚠ **1周あたり 0.3個 → 2.01個。⚠ 層1の legendary 0.0% → 層5 で 9.9%**。⚠ **出現率0%でも各周1個＝最低1回の保証が効いている**。⚠ **`weapon_steel_sword` に入口ができて宿題26 が閉じた**。⚠ **数値は `adventure_config.gd` の `@export` なので Inspector 作業なし**。⚠ **人間の作業は `ja.csv` 4行の再インポートだけ**） |
| ⚠ **14-c** | ⚠ **マップ画面**（マップ・マス・休憩・HP持ち越し・`adventure_select` と `battle_controller` の改修） | ⚠ **14-a** | ⚠ **大** | ⚠ **ログの章は済**（2026-08-25・`EXEC_SCENARIO_MAP_SCREEN.md`。⚠ **`floor_map` を新設・最小 796x388 で収まる**。⚠ **スタミナと報酬とクリア記録はボスだけ・道中は動かさない**。⚠ **HPをフロア内で持ち越し、休憩で満タン**。⚠ **1ノード＝1ウェーブ**。⚠ **30シナリオの平常値は不変**）。⚠ **画面12項目（G-1〜G-12）も確認済み**（2026-08-25・⚠ **30項目を1回の通しで見た回**） |
| ⚠ **14-d** | ⚠ **レリック（戦闘系）**（`relics.json`・`passive_ids` への合流・選択画面・持ち物一覧） | ⚠ **14-c** | ⚠ **中** | ⚠ **ログの章は済**（2026-08-25・`EXEC_SCENARIO_RELIC.md`。⚠ **12件・3人用6/1人用6**。⚠ **パッシブの器をそのまま借りたので `get_effective_stats()` も効果の解釈も触っていない**。⚠ **ただし `skill_schema.gd` の検証に「レリックはキャラに紐づかない」例外を入れた＝報告事項**。⚠ **`skills validated` が 94 → 106 entries, 0 errors**）。⚠ **画面7項目（R-1〜R-7）も確認済み**（2026-08-25） |
| ⚠ **14-e** | ⚠ **フロア内ショップとたいまつ**（無料ガチャ・消耗品・視界） | ⚠ **14-c** | ⚠ **中** | ⚠ **ログの章は済**（2026-08-25・`EXEC_SCENARIO_SHOP.md`。⚠ **ここで初めてフォグが入った＝奥のマスが `？` になる**。⚠ **たいまつ 4段階で 1/2/3/5 層先・300/800/2000 G**。⚠ **ボスの層だけは grade 0 でも見える**。⚠ **無料ガチャは `grant_chest()` の1本を通す**。⚠ **`Balance` の Inspector 作業は**発生しなかった**＝`adventure_config.gd` の `@export` で足りた**）。⚠ **画面11項目（S-1〜S-11）も確認済み**（2026-08-25） |
| ⚠ **14-f** | ⚠ **周回の自動処理** | ⚠ **14-b, 14-c** | ⚠ **中** | ⚠ **ログの章は済**（2026-08-25・`EXEC_SCENARIO_AUTORUN.md`。⚠ **宿題49 が閉じた**。⚠ **「結果だけ作る」のではなく内部で本当に歩かせた＝宝箱の抽選と保証を2箇所に書かないため**。⚠ **20周で 1.75個/周・歩数 5.0＝初回と同じ経路を通っている**）。⚠ **画面7項目も確認済み**） |
| ⚠ **14-g** | ⚠ **宝箱を見つけたことを画面に出す** | ⚠ **14-b, 14-c** | ⚠ **小** | ⚠ **済**（2026-08-25・`EXEC_SCENARIO_CHEST.md` §9。⚠ **通しで遊んで出た唯一の指摘「移動時の宝、演出が無いだけなのか手に入れたのか分からない」への対応**。⚠ **`floor_chest_found(chest_id, rarity)` を1本足し、⚠ 戦闘マスへ飛ぶ前に0.9秒の演出を挟む。⚠ レアリティを色で出す＝台帳 §4-5 が半分進んだ**） |
| ⚠ **14-h** | ⚠ **段階14 の数値調整**（層数・ノード出現比・たいまつと回復の値段・レアリティ表） | ⚠ **14-a〜14-g** | ⚠ **小** | ⚠ **済**（2026-08-28。⚠ **触ったのは `stages.json` の `layers` と `adventure_config.gd` の `@export` だけ。⚠ `.tres` / `.tscn` / `ja.csv` / 画面はゼロ**。⚠ **層数 5 → 6・ショップを2層に分散（4層30 ／ 5層25）＝ショップに行ける周が 約50% → 約72%**。⚠ **たいまつ 300/800/2000 → 50/150/400 G・フロア内回復 200 → 60 G＝1周のゴールド（50〜130 G）を基準にした**。⚠ **`floor_chest_chance_pct` 35 → 30 で移動が1回増えたぶんを相殺＝1周あたり 1.79 → 1.87個**。⚠ **`scenario=economy` の出力は前後で完全に同一**。⚠ **道中の戦闘に報酬が無い件は宿題63 として残した＝重みの底上げだけ**） |

### なぜアラームが1番か

**演出ではなく機能の欠落。** 音が鳴らないと作業終了に気づけず、振り返り入力の120秒を無音で失う。25分働いたのにノーカウントになるのは「やって後悔した」の最も直接的な形。**他と独立していて小さいので、先に片付ける。**

### なぜ10軸が2番か

**触れる軸が増えないと、装飾も割り振りも設計できない。** `_stat_keys()`に足せば育成・研究・装備の合成が全部追従する構造になっているので、**ここを通せば後続が楽になる。**

### なぜ装飾が6番か

等級10とスロットの種類（4）が入らないと刺す場所が無い。**現状は等級上限3・枠が開くのは等級5なので、枠は永久に0個。**

### 9番（機能の段階解放）だけは、器を早めに入れる

**最後にまとめて入れると、開発中ずっと全機能が開いた状態で進むことになる。** 解放順のバグ（開くべきでない画面に入れる、開くべき画面が開かない）に、いちばん最後まで気づけない。

**`unlocked_screens`と`scenario_chapter`の紐づけだけ早めに入れ、機能が増えるたびに1行ずつ登録する。** 9番に残すのは「章の割り当てを決めて全部埋める」作業だけにする。

> **章の割り当ては未決**（`GAME_DESIGN.md` 14章）。**順番は確定している**（同 9-5）ので、器は先に作れる。

---

## 4. 各段階で「どこを見るか」

**完了条件は担当者ではなく「ログ／ファイル／画面」で分ける**（`AGENTS.md`）。

**このタスク群は画面に出ない項目が多い。** F3のデバッグパネル（`battle_debug_panel.gd`の`_format_unit()`）に10軸を出す追加が要る。

| 段階 | 主にどこで見るか |
|---|---|
| 10軸 | **ログ**（`get_effective_stats()`の戻り値）＋ **F3パネル** |
| レベルの役割転換 | **画面**（育成画面の割り振りUI）＋ ログ |
| 等級10とスロット | **ログ**（`get_open_part_slot_count()`の戻り値）＋ 画面 |
| 分解とドロップ | **ログ**（付与された素材）＋ 画面 |
| 装飾 | **画面**（刺す・外す・出目の表示） |
| プリセット | **画面**（切り替えて反映されるか） |
| ルーン | **画面**（戦闘中の挙動）＋ 人間の目 |

---

## 5. 着手前に確認すること

**PLANの「対応済み」を信じない。** ドキュメントと実コードのズレが7回起きている。

- [ ] `attack_interval_sec`と`cooldown_sec`を、戦闘がマスターから直読みしていないか（**装備の第1弾で`get_effective_stats()`が呼ばれていなかったのと同じ形の見落としの可能性が高い**）
- [ ] `battle_controller.gd`が`spd`と`def`を今どう使っているか
- [ ] `_normalize_equipment_from_save()`が`parts`の型変更に耐えるか
- [ ] `select_skill()`の呼び出し元が本当に0件か
- [ ] `MasterDataLoader`に`get_all_characters()`が無い件（`training_screen.gd`の`CHARACTER_IDS`が決め打ち）

---

## 6. 罠

### `GEMS`が通貨として実装済み

**宝石の内部キーに`gem`を使えない。** `state_keys.gd`の20行目。`part_` / `stone_`などの接頭辞にする。**リリース後に改名できない。**

### `MasterDataLoader`が返す数値は`float`

等級の係数もロール値も**必ず`int()`で包む。** ％系は`int`で持つ（`crit_rate: 25`＝25%）。

### `.tres`は既定値を書き出さない

**実際に効いている数値は`.gd`の`@export`初期値にある。** `adventure_config.tres`などは`script = ExtResource(...)`の1行しか無い。

**この罠で既に1件死んでいる：** `gold_per_focus_minute` / `stamina_per_focus_minute` / `materials_per_focus_minute`が3つとも0。

### 状態にマスターの複製を持たない

個体が持つのは等級と刺さっているもののIDだけ。**枠の数を状態に持たず、`grade`から毎回計算する。**

### 200行を超える既存ファイルは設計役が全文を書く

`game_manager.gd`と`battle_controller.gd`の両方に手を入れる段階がある。**差し替えを渡したら`grep`で当たったことを確認する。**

### 装備画面が重くなる

装飾を刺す機能を足すと、着脱・鍛冶・装飾・出目表示・トークン使用で5つを抱える。**破綻しそうなら個体を選んだあとのサブ画面に切り出す**（`GAME_DESIGN.md` 9-4）。

---

## 7. デモに入らないもの

**作らない判断をしたものを、間違って作らないための一覧。**

- 拠点の資源自動生産（放置要素）
- 家具の作成、**掘削**（家具のグラフィック待ち）
- 作業場での装備製作、素材の変換（**ショップに一本化**）
- 宝箱専用のダンジョン
- イベント交換所、DLCショップ、戦闘プレビュー画面、ボス画面
- タイマー装飾のマルチプレイ共有
- 物語のテキスト（**画面の器だけ作る**）
