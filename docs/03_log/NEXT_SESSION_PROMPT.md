# 次のセッションに渡すプロンプト（2026-09-20 に書いた）

⚠ このファイルの下の「---」から先を、そのまま次のセッションの最初のメッセージに貼る。

---

main の 6137f20 から始めて。作業ツリーはクリーン。origin/main も同じ（push 済み）。

## ⚠⚠ この回の題目：**いまの仕様駆動の方式についての相談**（⚠ 人間の指定）

⚠⚠ **この回は実装をしない。** ⚠ 進め方そのものを話す回。
⚠ コードを書き始めないこと。⚠ 先に現状を読んで、⚠ 論点と選択肢を出して、⚠ 人間に決めてもらう。

⚠ 相談のために先に読むもの（⚠ 読むだけ。⚠ 書き換えない）
  `CLAUDE.md`（⚠ 「何度も事故った点」10件がいまの方式の産物）
  `AGENTS.md`（⚠ 二段構え・完了条件の3分割・誰が取るか）
  `docs/NEXT_STEPS.md` §0-UI-P → §0-UI-O（⚠ 直近2日の回し方が全部ここに出ている）
  `docs/PROJECT_STATUS.md`「現在地」

### ⚠ 相談で扱ってほしいこと（⚠ 設計役の見立て。⚠ 人間が足しても減らしてもよい）

- ⚠⚠ **台帳が増えすぎている。** ⚠ `NEXT_STEPS.md` は §0-UI-A 〜 §0-UI-P まで伸び、
  ⚠ 同じ決定が「決定」「宿題」「報告」の3か所に散って**食い違いが出た**（⚠ 09-20 に実例2件）
- ⚠⚠ **決定が同じ日に何度も覆る。** ⚠ 09-20 だけで決定38 が2回変わった（⚠ 2〜4層 → 1〜3ランダム）。
  ⚠ 覆すこと自体は悪くないが、⚠ **覆した跡が本文に残らない**と次の回が古い記述を正だと思う
- ⚠⚠ **絵が見られないことの埋め合わせが場当たり。** ⚠ 09-20 は「3区画が同じ形」を人間が1枚の絵で見つけた。
  ⚠ 設計役は赤0で通していた（⚠ 検査が「割り切れるか」を見ていなかったから）。
  ⚠ **数字の合図をどう先に決めるか**が方式の話
- ⚠ **「実装してある」と「気づける」が別**（⚠ ショップの行は出ていたのに「地味すぎてわからない」）。
  ⚠ 完了条件に「気づけるか」を入れるか
- ⚠ **`PRE_PLAN` → `EXEC` → `IMPL_LOG` の三点セットが、いまのやり方では回っていない**
  （⚠ 実装役を使わない体制になったのに型だけ残っている）

⚠ 結論は人間が出す。⚠ 設計役は「いまこうなっている」と「選択肢と代償」までを出す。

---

## ⚠ 直近2日で入ったもの（⚠ 実機で見た／見ていないを分けてある）

### ✅ 人間が実機で見たもの（2026-09-20）
- ⚠ 拾いものの窓が開く ／ ⚠ 通路のできごとの窓 ／ ⚠ 3人のHPバー ／ ⚠ 真鍮の札・手描きの通路・層の目盛り
- ⚠ 区画の切れ目が消えたこと ／ ⚠ 分岐が区画ごとに違う形になったこと

### ⚠ まだ実機で見ていないもの
- ⚠⚠ **ボスの後に自動で出るショップ**（`6137f20`・⚠ 階ごとに1回）
- ⚠ **一本しかない道に罠が付かなくなったこと**（`6137f20`）
- ⚠ **シナリオ（フロア探索）まるごと**（⚠ 人間「難ダンジョンの後でいい」＝**急がない**）

## ⚠ 09-20 の人間の決定（⚠ 覆すなら合意を取り直す）

- ⚠ `DungeonEdgeLines` は動かさない ／ ⚠ マスの文言（たたかう／戦闘）は揃えない
- ⚠ 拾いものの窓は `RunLootWindow`（⚠ 翻訳キー `ui_dungeon_chest_*` は**使っているので残す**）
- ⚠ 区画の切れ目の表示は出さない（⚠ 区画そのものは残る）
- ⚠ サイドの道は**区画ごとに 1〜3 を振り、出口側に寄せる**
- ⚠ ボスの後はショップを**自動で見せる**（⚠ 行も残す）
- ⚠ **一本しかない道に罠を置かない**（⚠ 得は置いてよい）
- ⚠ 宝箱の `chest_id` `"dungeon_chest"` はそのまま
- ⚠ **ダンジョン用の UI フォルダを作るかは保留**（⚠ 人間の提案。⚠ フォルダ新設は承認が要る）

## ⚠⚠ 覆さないと決めたこと（⚠ 触るなら人間の合意を取る）

- ⚠ 見た目の値を持つのは `tools/theme_builder.gd` と `theme/main_theme.tres` だけ。⚠ `theme_override_` を書かない
- ⚠ ボタンは5階層。⚠ 戻るは全画面で左上・文字は「戻る」。⚠ **例外は窓**（⚠ 戦闘の結果窓・拾いもの＝下に「閉じる」）
- ⚠ 窓の縁と題の帯は全部のモーダル（`WindowPanel` / `WindowTitlePanel` / `WindowTitleLabel`）
- ⚠ 数値はスクリプトに直書きしない（⚠ Balance の Config か master の JSON）
- ⚠ 装備の個体は `add_to_inventory()` だけが作る ／ ⚠ 状態を変える前に全部の判定を終える
- ⚠ 器は別のまま：`FLOOR_RUN` と `DUNGEON_RUN` を1つにまとめない（台帳 §7）。⚠ 共有するのは「口」と「部品」だけ
- ⚠ 拒否仕様：戦闘プレビュー画面 ／ ボス画面（AGENTS.md）
- ⚠ バランスの数値は**調整のフェーズでまとめて決める**。⚠ 回復量は「とりあえずそのまま」

## ⚠ 平常値（2026-09-20・実測）

`--import` 赤0 ／ `theme` 欠け0 ／ `layout` 0 ／ `area` 0 ／ `charge` 0 ／ `result` 0 ／ `result_defeat` 0 ／
`status_tone` 0 ／ `status_ui` 0 ／ `enemy_sp` 0 ／ `boss_sp` 0 ／ `dungeon` 0 ／ `dungeon_battle` 0 ／ `floor` 0 ／
`drops` 0 ／ `base_chest` 0 ／ `inventory` 0 ／ `economy` 0 ／ `gain` 0 ／ `glyphs` 0（⚠ 表 **49件**・NG 0・線画 **36枚**）

⚠ 黄は全シナリオで `skill_dbg_dot_odd` の1件（⚠ 前からある）。⚠ `floor` は+2件・`inventory` は+2件・`drops` は+1件（⚠ どれもわざと弾く検査）。
⚠⚠ 赤が出るのが正しいのは `workshop` 2（E129）と `unlock` 1（E125）。

⚠ `dungeon` で見る数字（2026-09-20 に足した）：
⚠ **区画の数で割り切れない層 = 2〜5 件**（⚠ 0 が続いたら3区画が同じ形に戻った疑い）／
⚠ **一本しかない道に付いた罠 = 0 件**（⚠ 得は 10〜16 件）／ ⚠ 通れないノード 0 ／
⚠ 降りたあと「ショップを自動で出したか」= false ／ ⚠ マップの「ショップへ」の行 = 子2個・見える=true・280 x 38。

⚠ 最小サイズ：⚠ フロアのマップ **766 x 646** ／ ⚠ 難ダンジョンのマップ **1086〜1094 x 217** ／
⚠ 拾いもの（`run_loot_window`）**620 x 342** ／ ⚠ レリック選択 **712 x 204** ／ ⚠ 商人 **888 x 320** ／ ⚠ 戦闘の HUD 581 x 145。

## ⚠ 実行環境

```powershell
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes --import
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=dungeon
```

- ⚠ `Start-Process` ＋ `-RedirectStandardOutput` / `-RedirectStandardError` で実行し、⚠ `[System.IO.File]::ReadAllLines(path, UTF8)` で読む
- ⚠⚠ 新しい `class_name` を足したら必ず `--import` を先に。⚠ Godot は同時に起動できない（⚠ 数秒あけて順に）
- ⚠ 絵は取れない。⚠ 表示・色・活性・レイアウトは人間しか見られない。⚠ 位置と数は `get_global_rect()` などで数字にできる
- ⚠ `.tres` を手で書き換えない。⚠ Theme は `tools/theme_builder.gd` を直して `scenario=theme` を回す
- ⚠ コミットは日本語。⚠ 1つ入れるごとにコミット。⚠ **push は聞かずにしてよい**（⚠ main 以外へ押さない／強制しない／赤が残っているものは押さない）
- ⚠ 返信の最後に「人間がすべきこと」を書く（⚠ 無いときは「無し」と書く）

## ⚠ 直っていない弱いところ

- ⚠ **拾いものの窓を検査の中で開いて閉じられない**（⚠ 3通り試して「Lambda capture ... was freed」が6本。⚠ 原因未特定）
- ⚠ シナリオからレリック選択を開く経路がヘッドレスで通っていない
- ⚠ 吹き出し（`SlotActionPopover`）は外を押しても閉じない
- ⚠ `ResourceGainEffect.set_muted()` は `play()` に効かない
- ⚠ 通路の窓を閉じてから進む流れは**ヘッドレスで通せない**（⚠ 閉じるのは人だけ）
