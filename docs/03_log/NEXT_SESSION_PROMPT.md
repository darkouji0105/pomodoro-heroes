# 次のセッションに渡すプロンプト（2026-09-26 版）

> ⚠ **この下の枠をそのままコピーして、次のセッションの1通目に貼る。**
> ⚠ 前の版（2026-09-23・「持ち物のモックを裁く回」）は**この版に置き換えた**。
>   ⚠ 人間が持ち帰ったのは持ち物だけのモックではなく、**全画面の作り直し一式**だったため。

---

```
main の最新（origin/main と同じ）から始めて。作業ツリーは .claude/settings.json と
docs/pomodoro-heroes-ui-docs/（人間が置いた・未コミット）以外クリーン。

## ⚠⚠ この回の題目：**UI の作り直し一式を読んで、計画を立てて合意を取る**

⚠ 人間が別の AI と作った **UI の手本一式** を `docs/pomodoro-heroes-ui-docs/docs/ui/` に置いた。
⚠ 人間の言葉：「⚠ これを見ていろいろなおしてほしい」。
⚠ 中身は **44画面の静的 HTML ＋ 同じ数のスクリーンショット ＋ 決まり（UI_GUIDE.md）＋ 数値（ui_tokens.json）**。
⚠ 世界観は「⚠ ギルドマスターの机に書類を広げる」（羊皮紙・革・真鍮・判）。**いまの画面とは見た目も画面の数も大きく違う**。

⚠⚠ **規模が大きいので、この回は「読む → ぶつかる所を洗い出す → 回に分ける → 人間の合意」まで。**
⚠⚠ **合意が取れるまでコードを1行も触らないこと**（CLAUDE.md「大きい変更は先に方針を出して合意を取る」）。
⚠ 合意が取れて時間が残れば、**1回目（おそらくテーマ）だけ**着手してよい。

---

## ⚠ 1. 読む順

1. `CLAUDE.md` ／ `AGENTS.md`（⚠ 特に「Themeの扱い」「theme_override_ を書かない」「フォルダ構造」「翻訳キー」）
2. ⚠⚠ **`docs/pomodoro-heroes-ui-docs/docs/ui/README.md` → `UI_GUIDE.md` → `ui_tokens.json`**
   （⚠ `docs/` 配下は指示されたもの以外読まない決まりだが、⚠ **このフォルダは人間の指示で読む**）
3. `screenshots/*.png` を**全部見る**（⚠ 44枚。⚠ 画像として読める）。⚠ 細かい文言と数値は `screens/*.html`
4. `docs/DECISIONS.md`（⚠ **全章**。⚠ 特に `UI-n` `NAV-n` `BT-3` `BS-n` `EQ-n` `CN-n` と難ダンジョンの決定26・28・48）
5. `docs/03_log/DUNGEON_SPEC_2026-09-24.md`（⚠ いまのダンジョンの動き）／ `docs/03_log/PRE_PLAN_WEAPON_RELIC.md`（⚠ 武器・鍛冶・遺物）
6. `docs/03_log/PRE_PLAN_INVENTORY_REWORK_MOCK.md`（⚠ 持ち物のモック依頼書。⚠ **この一式でたぶん不要になった**＝人間に確かめる）

## ⚠ 2. 洗い出す（⚠ 推測で書かない。⚠ `grep` とファイルで確かめる）

### 2-1. 画面ごとの表（**44画面すべて**）

| 列 | 中身 |
|---|---|
| 画面（ファイル名） | 例：Character / Belongings / DungeonMap |
| いまの Godot の画面 | ⚠ 在るならシーンのパス。⚠ 無ければ「新規」 |
| 種類 | ⚠ **見た目だけ変える** ／ ⚠ **画面の組み替え** ／ ⚠ **新しい仕組みが要る**（⚠ ゲームのルールやデータが増える） |
| 要る素材 | ⚠ フォント・質感の画像・絵（⚠ **モックの絵は全部仮**） |
| ぶつかる決定 | ⚠ 決定表の番号 |

### 2-2. ⚠⚠ すでに見えている「ぶつかる所」（**2026-09-26 に設計役が拾った。⚠ 全部ではない**）

| | 手本の言い分 | いまの決定・実装 |
|---|---|---|
| **ダンジョンの形** | ⚠⚠ DungeonGate「**500層まで**」「**10層ごとのつまみ**」「30層の出口から31層から潜る」／ DungeonFork「**もう10層**」／「ノルマ札」 | ⚠⚠ 決定26「**1ラン＝3階×25層**」／ 決定28「区画＝5層・3択」／ 決定48「ボスの後にわかれ道」。⚠ **ダンジョンのルールそのものが違う**＝見た目の話ではない |
| **テーマの置き場** | README「`resources/ui/theme.tres` として作って」 | ⚠ `UI-1`：**値を持つのは `tools/theme_builder.gd` と `theme/main_theme.tres` だけ**。⚠ **新しいフォルダは人間の承認が要る** |
| **ボタンの名前** | brass / leather / ghost / red / back | ⚠ `UI-4`：**名前は用途で付ける**（`PrimaryButton` / `GhostButton` / `DangerButton` / `BackButton`・⚠ 既定が Secondary） |
| **色** | 真鍮 `#B98A2C` ／ バフ `#2F79BD` ／ デバフ `#A8402F` など | ⚠ いまは真鍮 `a8791f`（`UI-5`）／ バフ `58a7ee`・デバフ `c4534a`（`BT-3`）。⚠ **決定を覆すことになる**＝人間に確かめる |
| **フォント** | 見出し Shippori Mincho B1 ／ サイン Yuji Boku・Klee One・Zen Kurenaido | ⚠ いま入っているのは **NotoSansJP だけ**。⚠ **フォントのファイルは素材**（⚠ OFL だが入れるのは人間の判断） |
| **画面の入口** | 下の**施設の帯**（本部・詰所・鍛冶場・持ち物・記録）／ 本部は**広間**で家具が入口 | ⚠ いまは拠点の下段ボタン ＋ **ギルドのカード 3×2**（倉庫は 09-23 に5枚目に入れたばかり） |
| **デスクトップの小窓**（Companion） | ポモドーロのあいだだけ出る小さな窓 | ⚠ **OS の別窓**になるはず。⚠ 09-23 に倉庫の別窓を消したばかり（人間「⚠ もう倉庫の別窓はいらない」）。⚠ **別物として作るか**を確かめる |
| **新しい画面** | 依頼掲示板・詰所・署名・記録・設定・届いた宝箱・昇級・小窓 など | ⚠ 設定は `BS-13`（別ファイル）だけ決まっている。⚠ 掲示板・署名・ノルマは**仕組みごと無い** |
| **遺物** | 記録（Records）の中に遺物の置き場が見当たらない（⚠ 要確認） | ⚠ `EQ-8`：遺物は「持ち物の画面の図鑑」に置く |

⚠ **ほかにも必ずある。⚠ 全画面を見て足すこと。**

## ⚠ 3. 人間に聞くこと（⚠ **番号で答えられる短い形**にする）

⚠ 人間は「1ア」「B２」のように答える。⚠ 「もっとくわしく」と言われたら、⚠ 1問ずつ「何が起きるか → 選択肢 → おすすめ」で書き直す。

- ⚠ **フォルダの置き場**：`docs/pomodoro-heroes-ui-docs/docs/ui/` と入れ子になっている。⚠ `docs/ui/` へ移すか（⚠ README の中のパスは `docs/ui/`）
- ⚠ **git に入れるか**：⚠ **19MB**（⚠ ほとんどが png）。⚠ 入れる ／ 入れない（`.gitignore`）／ png だけ外す
- ⚠⚠ **ダンジョンの形**：手本の「500層・10層ごと」に**ルールを変える**のか、⚠ **見た目だけ取り入れて今の 3階×25層のまま**にするのか
- ⚠ **色**：手本の色に**全部寄せる**（⚠ `UI-5` `BT-3` などを覆す）か、⚠ 一部だけか
- ⚠ **フォント**：入れるか（⚠ 誰がファイルを用意するか）
- ⚠ **持ち物のモック依頼書**（`PRE_PLAN_INVENTORY_REWORK_MOCK.md`）：⚠ この一式で**不要になった**か
- ⚠ **順番**：どの画面から作り直すか

## ⚠ 4. 回に分ける（⚠ 1回＝1通し）

⚠ `NEXT_STEPS.md` の「これからの回」の表に入れる。⚠ たたき台（⚠ 人間が並べ替える）：

1. ⚠ **テーマ**：`ui_tokens.json` の値を **`tools/theme_builder.gd` に**写す（⚠ 新しいフォルダを作らない）。⚠ ボタン5階層は**今の variation の名前のまま**中身を差し替える
2. ⚠ **共通の部品**：紙（9スライス）・紙のタブ・台帳の行・判・説明の窓・確かめの窓・HPバー（⚠ `scenes/ui/components/`）
3. ⚠ **見た目だけ変える画面**を1枚ずつ（⚠ 1回1画面。⚠ スクリーンショットと HTML を両方見る）
4. ⚠ **画面の組み替え**（⚠ 施設の帯・持ち物・鍛冶場・育成のタブ）
5. ⚠ **新しい仕組みが要る画面**（⚠ 掲示板・署名・記録・設定・小窓）＝⚠ **仕組みを決める回を先に置く**
6. ⚠ ダンジョンの形を変えるなら、⚠ **別の回で決定から**（⚠ 決定26・28・48 を覆す）

⚠ 画面ごとの見比べは `scenario=shot`（⚠ いまは19枚）で撮った絵と `screenshots/*.png` を並べる。

## ⚠ 5. 決まったら

- ⚠ `docs/DECISIONS.md` に1行ずつ（⚠ 覆した相手は取り消し線で残す。⚠ 章は `UI-n` ほか）
- ⚠ 回の表を `docs/NEXT_STEPS.md` に
- ⚠ 色・手応え・遷移は `docs/HUMAN_CHECK.md` へ（⚠ 絵で見られるものは足さない）

## ⚠ 6. 残っているもの（**この回で片づけなくてよい**）

- ⚠ `HUMAN_CHECK.md` の **1件**（⚠ ギルド → 倉庫 → 戻る）
- ⚠ 回3-c：一時通貨「遺物片」→「コイン」（決定45）
- ⚠ `AGENTS.md` の `scenes/inventory/`（⚠ もう無い）／ `CLAUDE.md` の「絵は18枚」（⚠ 19枚）／ 決定22 が古い（⚠ 宝箱はいま持ち帰る）。⚠ **直してよいか人間に聞いている**

## ⚠ 止まる条件（`CLAUDE.md` のまま）

- ⚠ 大きい変更は方針を出して合意を取ってから ／ ⚠ 1つの症状に2手まで
- ⚠ 返信の最後に「⚠ 人間がすべきこと」を書く（⚠ 無ければ「無し」）

## ⚠ 実行環境

# 絵を撮る（⚠ --headless を付けない。⚠ 窓が十数秒出る。⚠ 覆うと1枚も撮れない）
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=shot shot_dir=<パス>
```
