# 次のセッションに渡すプロンプト（2026-09-23 版・2本目）

> ⚠ **この下の枠をそのままコピーして、次のセッションの1通目に貼る。**
> ⚠ 貼るときに、**モックの HTML（と画像）を一緒に渡す**こと。
> ⚠ 前の版（2026-09-23・1本目）は「コンセプトを決める回」だった。⚠ **それは済んだ**（⚠ `DECISIONS.md` の `CN-n` / `EQ-n`）。

---

```
main の最新（origin/main と同じ・push 済み）から始めて。作業ツリーは .claude/settings.json 以外クリーン。

## ⚠⚠ この回の題目：**持ち物・装備・鍛えるのモックを裁く。⚠ 実装はしない**

⚠ 人間が別の AI にモックを描いてもらった。⚠ この1通目に HTML（と画像）が付いている。
⚠ 依頼書は `docs/03_log/PRE_PLAN_INVENTORY_REWORK_MOCK.md`（⚠ §4 の11枚・§5 の4問）。
⚠⚠ **裁いて決定表に入れるまで、装備・倉庫まわりのコードを1行も触らないこと。**

---

## ⚠ 1. まず読む

1. `CLAUDE.md` ／ `AGENTS.md`
2. `docs/NEXT_STEPS.md` の **§0-UI-W**（倉庫の別窓をやめた）と **§0-UI-V**（回3-b を止めた経緯）
3. `docs/DECISIONS.md` の **`CN-1`〜`CN-3`**（コンセプト）／ **`BS-9` `BS-10` `BS-15` `BS-20`**（持ち物）／ **`EQ-1`〜`EQ-14`**（武器・鍛冶・遺物）／ 決定45（一時通貨をコインに）
4. `docs/03_log/PRE_PLAN_INVENTORY_REWORK_MOCK.md`（⚠ 依頼書）と `docs/03_log/PRE_PLAN_WEAPON_RELIC.md`（⚠ コンセプトの写し・§0-3 が人間の裁き）

## ⚠ 2. モックを置く

- ⚠ 受け取った HTML を `docs/03_log/inventory_rework_mock.html` に置く（⚠ 2枚なら `_1` `_2`）。⚠ 前例は `modal_ui_mock.html`
- ⚠ **置く前に全部読む**（⚠ 人の書いたものをそのまま置かない）

## ⚠ 3. 裁く（**設計役の仕事は「実コードの事実と突き合わせる」こと**）

### 3-1. ⚠ 依頼書の決まりを破っていないか（⚠ 1件ずつ挙げる）

- ⚠ 色は依頼書 §0 の値だけか ／ ⚠ ボタンは5階層のままか（⚠ 真鍮は1画面に1個）
- ⚠⚠ **「はい」が左・「いいえ」が右**（⚠ 前回のモックは両案とも逆だった）
- ⚠ 戻るは左上・文字は「戻る」（⚠ 窓だけ下に「閉じる」）／ ⚠ 窓の幅は 300/400/560/720
- ⚠ 絵文字を使っていないか（⚠ NotoSansJP に無い）
- ⚠ **マス目・ページ送り・枠を買うを描いていないか**（⚠ `BS-10` でやめた）／ ⚠ 別窓に戻していないか
- ⚠ 取り返しのつかない操作（⚠ 装飾を外す・分解する）が赤か
- ⚠ 1280 x 720 に収まっているか（⚠ 人間が止めた理由は「⚠ がめんがきれてる」）

### 3-2. ⚠ 実コードとぶつからないか（⚠ **推測で書かない。`grep` してから**）

⚠ 2026-09-23 に取った事実（⚠ 変わっていないか確かめ直すこと）：

| | 事実 | どこ |
|---|---|---|
| 装備の生まれ方 | ⚠ **必ず等級1で生まれる**（⚠ `EQ-13`「等級つきで落ちる」は器が変わる） | `_create_equipment_instance()` |
| 装備を作る | ⚠ **作れない**。⚠ 作業場は装飾の抽選3件だけ（⚠ `draw` の形は使い回せる） | `recipes.json` |
| 分解 | ⚠ **在る**。⚠ 払ったぶんの 0.5倍（切り捨て） | `dismantle_equipment()` |
| 鍛える | ⚠ **失敗しない**。⚠ 等級は最大10 | `forge_equipment()` / `EquipmentConfig` |
| 誰が着けられるか | ⚠ **制限なし** | `get_equip_reject_reason()` |
| 容量 | ⚠ **まだ在る**（⚠ 500マス・入らないなら止める判定が残る） | `get_inventory_free_slots()` ほか |
| 倉庫の入口 | ⚠ **ギルドの5枚目のカード**（⚠ 仮） | `GuildScreen.CARD_ORDER` |
| 全キャラ強化 | ⚠ 研究に `stat_boost_all` 8ノード | `research.json` |

⚠ モックが「無い値・無い仕組み」を前提にしていたら、⚠ **作るのか・描き直すのか**を人間に聞く。

### 3-3. ⚠ 人間に選んでもらう

- ⚠ 依頼書 §5-1〜§5-4 の問いごとに、**モックの A案／B案を並べ、⚠ おすすめを1つ付けて**出す
- ⚠ **選択肢は短く・番号で答えられる形**にする（⚠ 人間は「1ア」「B２」のように答える）
- ⚠ 人間が「もっとくわしく」と言ったら、⚠ 1問ずつ「何が起きるか → 選択肢 → おすすめ」で書き直す

## ⚠ 4. 決まったら

1. ⚠⚠ **`docs/DECISIONS.md` に1行ずつ入れる**（⚠ `BS-n` / `EQ-n`。⚠ 覆した相手は取り消し線で残す）
2. ⚠ **`docs/02_exec/EXEC_EQUIPMENT_UI.md` を書き直す**（⚠ いまの4段は 09-22 の前提＝3列の装備画面。⚠ もう古い）
3. ⚠ **実装の回を分けて `NEXT_STEPS.md` の表に入れる**（⚠ 1回＝1通し）。⚠ 例：
   ① 拠点の容量をなくす（`BS-10` `BS-20`）→ ② 持ち物の画面 → ③ 装備画面 → ④ 作業場で装備を作る（`EQ-11`）→
   ⑤ 鍛冶のレベル・失敗・トークン（`EQ-5`〜`EQ-7`）→ ⑥ 遺物・初回の記録（`EQ-8` `EQ-9`）→ ⑦ 武器のジャンル・型（`EQ-1`〜`EQ-3`）
4. ⚠ 画面で分からないもの（⚠ 色・活性・手応え・遷移）は `docs/HUMAN_CHECK.md` へ

## ⚠ 5. 残っているもの（**この回で片づけなくてよい**）

- ⚠ `HUMAN_CHECK.md` の **1件**（⚠ ギルド → 倉庫 → 戻る）
- ⚠ **回3-c**：一時通貨の「遺物片」→「コイン」（決定45・`ja.csv` の文字だけ）
- ⚠ `AGENTS.md` に消えた `scenes/inventory/` が残っている ／ ⚠ `CLAUDE.md` の「絵は18枚」→ 19枚。⚠ **直してよいか人間に聞いている**
- ⚠ 拠点の右上の素材の帯が右端にくっついている（⚠ 前からの可能性が高い・§0-UI-W-3）

## ⚠ 止まる条件（`CLAUDE.md` のまま）

- ⚠ 1つの症状に2手まで ／ ⚠ 大きい変更は方針を出して合意を取ってから
- ⚠ 返信の最後に「⚠ 人間がすべきこと」を書く（⚠ 無ければ「無し」）

## ⚠ 実行環境

# 絵を撮る（⚠ --headless を付けない。⚠ 窓が十数秒出る。⚠ 覆うと1枚も撮れない。⚠ いまは19枚）
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=shot shot_dir=<パス>
```
