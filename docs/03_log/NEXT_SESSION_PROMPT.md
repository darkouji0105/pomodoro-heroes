# 次のセッションに渡すプロンプト（2026-09-23 版）

> ⚠ **この下の枠をそのままコピーして、次のセッションの1通目に貼る。**
> ⚠ 前の版（2026-09-22）は「回の表から題目を選ぶ」形だった。⚠ **今回は題目が決まっている**（⚠ コンセプト）。

---

```
main の `e5e127a` から始めて。作業ツリーはクリーン。origin/main も同じ（push 済み）。

## ⚠⚠ この回の題目：**コンセプトを決める。⚠ 実装はしない**

⚠⚠ **人間が実機を見て止めた**（2026-09-22）。⚠ 言葉のまま：
> ⚠ **「がめんがきれてる　いったん考え直したほうがいいと思う　コンセプトから
> ⚠ インベントリ性とこの画面があってないんだと思う」**
> ⚠ **「というかインベントリシステムじゃなくてもいいかもね」**

⚠⚠ **だからこの回は、⚠ 先に「どっちのゲームか」を決める。**
⚠ **決まるまで装備・倉庫まわりのコードを1行も触らないこと。**

---

## ⚠⚠ 決めること（**1つめが本丸**）

### ⚠⚠ 論点1：コンセプト文書が2つあり、言っていることが違う

⚠ **推測ではない。⚠ 2026-09-22 に `docs/00_concept/` を読んで引用した。**

| | 言っていること |
|---|---|
| ⚠ **ピッチA**（`docs/00_concept/CONCEPT.md`・42行） | ⚠ 「ポモドーロをこなすと**報酬**がもらえる、拠点育成×簡易戦闘」／ ⚠ 「**ポモドーロをやったらご褒美という約束を裏切らない**」／ ⚠ 「**1プレイ＝1ポモドーロサイクル（15〜50分）**」／ ⚠ 「勝敗より継続的な育成」 |
| ⚠ **ピッチB**（`docs/00_concept/game_design_doc.md`・128行） | ⚠ 「キャラ育成 × ダンジョン探索**ローグライク**」／ ⚠ 「タルコフから**リスクと所有欲**を抽出（⚠ ただし**重量テトリス的な煩雑さは採用しない**）」／ ⚠ 「**死亡でアイテム全ロスト**」／ ⚠ 「1ラン＝3階×25層」 |

⚠ **25分の休憩に触る画面**なのか、⚠ **1ラン30分超の探索ゲーム**なのかで、
⚠ 持ち物 UI に許される重さが**正反対**になる。⚠ **ここが決まらないと UI の案を選べない。**

### ⚠ 論点2：拠点の「500マスの倉庫」は要るのか

⚠⚠ **ピッチB のインベントリ節は、全部「ランの中の鞄」の話**（⚠ 探索アイテムと戦利品が
⚠ **同じ枠で競合** ／ 初期枠は少なめ ／ ショップで拡張）。
⚠⚠ **拠点の500マスはコンセプト文書のどこにも根拠が無い。**

⚠ 出どころは `docs/01_plan/PLAN_INVENTORY.md`（2026-09-02）：
⚠ 決定1「**マインクラフト風のマス目にする**」／ ⚠ 決定2「**拠点の倉庫にも容量を持たせる**」
（⚠⚠ **`PLAN_HARD_DUNGEON.md` の「拠点の `INVENTORY` は無限のまま。さわらない」を覆したと明記されている**）／
⚠ 決定8「**500マス**」。

### ⚠ 論点3：決まったあと、何を作り直すか

⚠ 2026-09-22 に**装備画面を3列にした**（⚠ 左＝ステータス ／ 中＝装備マスと説明の窓 ／ 右＝持ち物）。
⚠⚠ **これは残してある。⚠ 消していない。** ⚠ 論点1・2 の結論しだいで**作り直しになる**。

---

## ⚠ この回の進め方（**実装しない回**）

1. ⚠ **まず読む**：⚠ `CLAUDE.md` ／ `AGENTS.md` ／ `docs/NEXT_STEPS.md` の **§0-UI-V**（⚠ 止めた経緯と数字）
2. ⚠ **人間が別の AI とコンセプトの相談をしている。⚠ その結論を待って裁く**
   （⚠ 人間が持ち帰った案を、⚠ **実コードの事実と突き合わせる**のが設計役の仕事）
3. ⚠⚠ **決まったら `docs/DECISIONS.md` に1行ずつ入れる**（⚠ 覆した相手も書く）
4. ⚠ そのあとで `docs/02_exec/EXEC_EQUIPMENT_UI.md` を**書き直す**（⚠ いまの4段は論点2 の前提で書いてある）

⚠ **人間が別の案を出したらそちらが優先**（⚠ 思いつきは最優先・`NEXT_STEPS.md` の決まり）。

---

## ⚠⚠ 裁くときに使う実測（**2026-09-22 に `grep` と JSON から取った**）

| | 数 |
|---|---|
| ⚠ 品の種類（全部） | **91**（⚠ 装飾61 ／ 素材16 ／ **装備11** ／ ダンジョン専用2 ／ 消耗品1） |
| ⚠ 装備の種類 | **11**（⚠ 武器3・頭2・上半身2・下半身2・アクセサリー2） |
| ⚠ 装飾の種類 | 61（⚠ 段階①〜④が各14種、⑤が5種） |
| ⚠⚠ 拠点の倉庫 | **500マス**（⚠ 10列 × 10行 × 5ページ。⚠ **最初から500**） |
| ⚠⚠ ランの鞄 | **8枠**（⚠ 素材は1枠に10個まで重なる） |
| ⚠ 装備の部位 | 5 ／ ⚠ 装飾の枠は最大8（⚠ **等級3で1つ目**：3,4,5,5,6,7,8,9） |
| ⚠ 装備の等級 | 1〜10（⚠ 素材で「鍛える」） |

⚠⚠ **`slot_position`（⚠ マスに置いた位置を覚える欄）は在るのに呼び出し元が0件。**
⚠ ＝**並べ替えは一度も作られていない**。⚠ 倉庫を畳むなら欄ごと消せる。⚠ 残すなら作る必要がある。

⚠ **`expand_inventory()`（枠を買う）は難ダンジョンのショップの売り物にもなっている。**
⚠ 倉庫を畳むなら**そこも一緒に決め直す**。

---

## ⚠ いまの状態（**2026-09-22 に入ったもの。⚠ 全部 push 済み**）

| コミット | 中身 |
|---|---|
| `82ec663` | ⚠⚠ **窓を撮れるようにした**（⚠ 拾いもの・戦闘の結果・レリック選択・わかれ道・商人・共通モーダル） |
| `ba0e427` | ⚠ 装飾の枠をマスにして吹き出しで操作（⚠ 決定 `BS-14`） |
| `c1386e0` `7bd98d3` | ⚠ モックの依頼書 ／ ⚠ モックを裁いた（⚠ 決定 `BS-15`〜`BS-19`）＋ `EXEC_EQUIPMENT_UI.md` |
| `3661672` | ⚠⚠ **装備画面を3列に**（⚠ 持ち物を画面の中へ。⚠ **これが論点**） |
| `e5e127a` | ⚠ 「装飾はまだ使えない」が積もる不具合を直した |

⚠ **`docs/HUMAN_CHECK.md` は 5 件**（⚠ 全部この3列の画面のもの）。
⚠⚠ **作り直すなら見ずに捨てる。⚠ 先に人間へ「見るか捨てるか」を聞くこと。**

---

## ⚠ 台帳（**4つ。⚠ 役割が違う**）

| ファイル | 役割 |
|---|---|
| ⚠⚠ `docs/DECISIONS.md` | ⚠⚠ **いま効いている値。⚠ 1決定＝1行。⚠ 値の正はここだけ** |
| `docs/NEXT_STEPS.md` | ⚠ いまの議題（⚠ 一番上が「これからの回」／ ⚠ **§0-UI-V が今回の前段**） |
| `docs/HUMAN_CHECK.md` | ⚠ 人間が実機で見るもの（⚠ **いま5件**） |
| `docs/WORKFLOW.md` | ⚠ 1タスクの通し方（⚠ 2者・10段階） |

⚠ `PLAN_◯◯.md` は**とっかかり**（⚠ 値は古いことがある）。⚠ 前の回は `docs/03_log/NEXT_STEPS_2026-09.md`。

---

## ⚠ 実行環境

```powershell
# ヘッドレス（ログを見る）
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=dungeon
# 絵を撮る（⚠ --headless を付けない。⚠ 窓が十数秒出る。⚠ 覆うと1枚も撮れない）
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=shot shot_dir=<パス>
```

- ⚠ `Start-Process` ＋ `-RedirectStandardOutput` / `-RedirectStandardError`。⚠ 読むのは `[System.IO.File]::ReadAllLines(path, UTF8)`
- ⚠ **赤は `^ERROR` で数える**（⚠ ただの `ERROR` だと `push_error` の行も数えて倍に見える）
- ⚠ **平常値**：⚠ `--import` 赤0・黄0 ／ ⚠ `layout` `glyphs` `economy` `theme` ほか **赤0・黄1** ／
  ⚠ `inventory` 赤0・**黄3** ／ ⚠ `drops` 黄2 ／ ⚠ `unlock` 赤1（⚠ わざと）／ ⚠ `workshop` 赤2（⚠ わざと）／
  ⚠ `shot` **18枚・赤0**。⚠ **黄1件は全シナリオ共通で `skill_dbg_dot_odd`**
- ⚠ 新しい `class_name` と `ja.csv` の再インポートは **`--import` で済む**（⚠ 人間を待たない）
- ⚠ Godot は同時に起動できない。⚠ `.tres` は手で書き換えない（⚠ Theme は `tools/theme_builder.gd` ＋ `scenario=theme`）
- ⚠ **人間が遊んだあとは先に `user://logs/godot.log` を読む**（⚠ 自分で回すと5本で押し出されて消える）
- ⚠ コミットは日本語。⚠ **push は聞かずにしてよい**（⚠ main 以外へ押さない／強制しない／**赤が残っているものは押さない**）
- ⚠ 返信の最後に「人間がすべきこと」を書く（⚠ 無いときは「無し」と書く）

---

## ⚠ 人間がすべきこと

⚠⚠ **コンセプトの相談の結論を持ってくる**（⚠ ピッチA か B か、⚠ 倉庫を畳むか）。
⚠ ⚠ 相談に渡す文書は **`CONCEPT.md` ／ `game_design_doc.md` ／ `PLAN_INVENTORY.md` の §1〜§2 ／
⚠ `DECISIONS.md` の §4**（⚠ 合計およそ240行）。
```

---

## 更新履歴

- **2026-09-23 … 全面的に書き直した。** ⚠ 題目を「⚠ コンセプトを決める（⚠ 実装しない）」に固定した。
  ⚠ 人間が 2026-09-22 に実機を見て止めたため（⚠ `NEXT_STEPS.md` §0-UI-V）。
- **2026-09-22 … 進め方が変わったので書き直した**（⚠ 台帳4つ・絵が撮れる・2者・見る回・着手前の1行）。
