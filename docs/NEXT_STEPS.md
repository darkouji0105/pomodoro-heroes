# 次にやること：ドキュメントを新しい設計に合わせて書き直す

**このファイルは「次の1タスク」だけを書く。** 終わったら次のタスクの内容に書き換える。全体の状況は`PROJECT_STATUS.md`、ルールは`AGENTS.md`と`CLAUDE.md`、**ゲームの中身は`GAME_DESIGN.md`**。

**このファイルだけ読めば着手できるように書いてある。** 過去のタスクを知らない前提で読んでよい。

**コードは1行も書かない。**

---

## 0. 何が起きたか

**設計を一通り詰め直した。結果は`GAME_DESIGN.md`にまとまっている。**

**既存ドキュメントの多くが、新しい設計と食い違っている。** このまま次の実装に入ると、古い記述を読んで作り、あとで作り直すことになる。

### 前のバージョンのこのファイルは破棄する

**前は「バランスを実測する」と書いてあった。** その前提は崩れた。

| 前提 | いま |
|---|---|
| 装備は作業場・ショップ・宝箱から入る | **ステージの抽選ドロップが主力。作業場での装備製作は廃止** |
| 作業場は素材変換と装備製作 | **変換も装備製作も廃止。中間素材と装飾のくじだけ** |
| 鍛冶素材は3段階 | **4段階。しかも建築素材・装飾素材も4段階で揃える** |
| 素材は戦闘とショップから | **分解が主要な供給源になった** |
| ステータスは9軸 | **10軸（`mdef`を追加）** |
| 等級の上限は3 | **10** |

**測る対象そのものが作り直しになる。** このファイル自身が2章で「素材の種類・ステータスの軸・等級の構造が全部変わるため、いま測っても作り直しになる」と書いていた状況に、また戻った。

**バランス実測は、構造が実装されてから行う。**

---

## 1. なぜこれが次なのか

**PLANと実コードのズレで7回事故している。** 今回はそれに加えて、**PLANとPLANのズレ**が発生している。

`PLAN_STATS_AND_FORMULAS.md`は9軸で書かれている。`GROWTH_LOOP_CURRENT.md`は等級上限3・レベルでステータスが上がる前提で書かれている。`DEMO_CHECKLIST.md`は装備の第2弾までのスコープで書かれている。

**どれも「正しく見える」ので、次の会話で読まれると気づかずに古い設計で作る。**

**正しい地図を作ってから動く。**

---

## 2. やること

**8ファイルを`GAME_DESIGN.md`に合わせる。** 上から順に。

### 2-1. `CONCEPT.md`

- [ ] **対象プラットフォームを「PCのみ（Steam前提）」で確定**（現在「未確定」と書かれている）

**それ以外は変えない。** 設計原則3つとコアループは有効。

### 2-2. `GROWTH_LOOP_CURRENT.md`

- [ ] **冒頭に「このファイルは2026-08-13時点の実コードの記録であり、設計は`GAME_DESIGN.md`に置き換わった」と明記する**

**中身は消さない。** 実コードの記録としての価値は残っている。**「決めたこと」としては読まないこと**が伝わればよい。

### 2-3. `PLAN_STATS_AND_FORMULAS.md`

**変更が一番多い。**

- [ ] 1章の軸の表を**10軸**にする（`mdef`を追加。`def`と同じ除算式）
- [ ] `atkspd`の上限を「**秒数の下限**（`min_attack_interval_sec`）」に変更する。％の上限にしない理由を残す
- [ ] `haste`の上限を**100%**にする（現在「不要」と書かれている）
- [ ] **チャージスキルとルーンCDとの衝突**を注意書きとして足す
- [ ] 3章「入れないと決めたもの」に**属性防御を落とした判断が`def`/`mdef`には当てはまらない理由**を追記する（プリセットが2階層になったため）
- [ ] 4章の`growth_per_level`の扱いに、**割り振りポイントにも同じ制約（％系を入れない）が適用される**ことを追記
- [ ] 7章の未決から「％系の上限値」を落とす

### 2-4. `PLAN_CHARACTER_GROWTH_LOOP.md`

- [ ] **冒頭に「破棄」と明記する。** `PLAN_IMPLEMENTATION.md`で置き換わった

**このファイルは「決めたこと」の台帳だったが、その役割は`GAME_DESIGN.md`が持つようになった。** 中身の9割が新設計と食い違っており、部分的に直すより置き換えたほうが早い。

**⚠ ただしこのファイルにしか無い情報が2つある。移す先を確認すること。**

- 等級付き素材の「プレイヤーから見れば1種類、データとしては`item_id`が3つ」という扱い（→ `DATA_SCHEMA.md`へ）
- 「本体の係数は必ず効かせる。枠は上乗せであって、鍛冶の見返りの本体にしない」という判断（→ `GAME_DESIGN.md` 6-2へ）

### 2-5. `DATA_SCHEMA.md`

**`GAME_DESIGN.md` 14章「データ構造に追加が必要なもの」がそのまま作業リスト。**

- [ ] `character_growth`に割り振りポイント（獲得済み・配分）
- [ ] `character_growth`にパッシブの解放状態
- [ ] `character_growth.skills`（現在`{}`のまま。`select_skill()`も空実装）
- [ ] キャラプリセット・編成プリセット（**参照方式**。編成はキャラプリセットのIDを持つ）
- [ ] `equipment_instances.parts`を**可変長**に
- [ ] `parts`の要素を`{装飾ID, 等級, 出目}`のオブジェクトに
- [ ] 装飾のインベントリ（**種類＋等級のスタック品**。刺すまで個体を持たない）
- [ ] ノルマトークン・鍛冶屋トークン・レア素材・ダンジョン用素材
- [ ] ルーンの重ね段階（1〜5）とルーンのかけら
- [ ] 周回ステージの解放状態、スキップ周回の記録
- [ ] 移動系ルーンの移動量設定（キャラプリセットに含む）
- [ ] 4-5作業場から**変換レシピと装備レシピの記述を落とす**

### 2-6. `DEMO_CHECKLIST.md`

**`GAME_DESIGN.md` 13章のスコープに合わせて全面的に入れ替える。**

- [ ] 装飾3種・ルーン・割り振りポイント・パッシブ・プリセットの項目を**追加**
- [ ] 作業場の「装備が作れる」「素材変換」を**削除**、「装飾のランダム製作」を追加
- [ ] 研究を「カテゴリ別ボードの周回」に書き換え
- [ ] ショップを**4タブ**に
- [ ] ステージに「周回ステージ」「スキップ周回」を追加
- [ ] **機能の段階解放**の項目を追加
- [ ] トレーニングモードは**デモに入れる**（デバッグ用途を兼ねる）
- [ ] 物語を読む画面は**器だけ**
- [ ] 設定画面は**実装しながら決める**と明記

### 2-7. `PROJECT_STATUS.md`

**決定済み表と未確定表への反映。**

- [ ] **`GAME_DESIGN.md`を第1層の正として冒頭に明記する**
- [ ] 「反転した決定」の表に、`GAME_DESIGN.md` 12章の内容を反映
- [ ] 決定済み表に今回の決定を追加（**数が多い。`GAME_DESIGN.md`を指すポインタで済ませてよい**）
- [ ] 未確定表から、解決したものを落とす
  - 鍛冶屋トークンの入手方法／宝箱の中身の配分／週・月の区切り／作業場が何を作るか／等級4〜10／ルーンの入手経路／スキル候補6個／％系の上限値／素材3段階と等級10段階の噛み合わせ／パッシブの中身の枠組み
- [ ] 未確定表に、新しく出たものを追加
  - 章ごとの解放割り当て／敵の`mag`・`mdef`の設計／`crit_rate`超過分の変換レート／`min_attack_interval_sec`の値／等級帯と素材段階の刻み

### 2-8. `HANDOVER_STATS_AND_EQUIPMENT_V2.md`

- [ ] **冒頭に「破棄」と明記する。** このファイルは9軸・等級の未確定を前提に書かれており、`GAME_DESIGN.md`で置き換わった

> このファイル自身が「前のファイルは捨てるか、冒頭に『破棄』と書くこと」というルールを持っている。同じ扱いをする。

---

### 2-9. `PLAN_IMPLEMENTATION.md`（新設・作成済み）

**実装の順番と依存関係だけを持つ新しいPLAN。** `PLAN_CHARACTER_GROWTH_LOOP.md`と`HANDOVER_STATS_AND_EQUIPMENT_V2.md`を置き換える。

- [ ] 1章「セーブ構造を触る回をどう分けるか」の判断を確定させる
- [ ] 3章の順番を、2章の依存関係を見て組み直す

---

## 3. 終わったあとにやること：実装ステップを確定させる

**叩き台は`PLAN_IMPLEMENTATION.md`にある。** ドキュメントを直したら、そこの1章と3章を埋める。

### 分かっている制約

**セーブ構造を触る変更が多い。** バラバラにやると移行処理が何度も要る。

セーブに触るもの：10軸／`parts`の可変長と種類／割り振りポイント／パッシブ／スキル選択／プリセット2階層／等級10／新しい素材とトークン。

**`_recalc_stats()`が毎回レベルから計算し直す設計なので、式を変えれば既存セーブは追従する。** ただし今回は**キーそのものが増える**ので、0埋めの確認が要る。

**既存セーブは捨ててよい**（`GAME_DESIGN.md` 14章）。デモ前なので移行処理は書かない。

### 決めること

- [ ] **セーブ構造を触る回を1回にまとめるか、分けるか**
- [ ] まとめる場合、1タスクとして大きすぎないか（**装備の第2弾は「ステータス9軸と同じ会話でやる予定が、装備だけで終わった」前例がある**）
- [ ] 各ステップの完了条件を「ログ／ファイル／画面」のどこで見るか

### 順番の叩き台

**`PLAN_IMPLEMENTATION.md` 3章に13段階の表がある。** 依存関係は同2章。

**この順番はまだ確定していない。**

---

## 4. このタスクでやらないこと

- **コードを1行も書かない**
- 数値を決めない（バランスは構造が入ってから）
- パッシブ15個・装飾の効果一覧・ルーンの効果一覧を作らない（**判断ではなく作業。実装時に埋める**）
- シナリオのテキストを書かない

---

## 5. 罠

### `GAME_DESIGN.md`をコピーして各ファイルに貼る

**同じ内容が2箇所にあると、修正時にどちらが正か分からなくなる。**（`PROJECT_STATUS.md`の「EXECにコードを載せない」と同じ考え方）

**各PLANには「`GAME_DESIGN.md`の該当章を見よ」というポインタを置き、そのファイル固有の情報だけを残す。**

### 古い記述を消さずに追記だけする

**「反転した決定」は、古いほうを消すか、明確に打ち消すこと。** 両方残っていると、次に読む人が新しいほうを見つけられない。

### 実装役に投げる

**このタスクは判断そのもの。** 設計役が書く。

### 「対応済み」と書いて確認しない

**7回起きている。** 直したら、そのファイルを開いて該当箇所を目で見る。

---

## 6. 完了条件

### ファイル

1. 2章の9ファイルすべてに手が入っている
2. `HANDOVER_STATS_AND_EQUIPMENT_V2.md`と`PLAN_CHARACTER_GROWTH_LOOP.md`の冒頭に「破棄」がある
3. `GROWTH_LOOP_CURRENT.md`の冒頭に「旧版」の注記がある
4. `CONCEPT.md`のプラットフォームが確定している
5. **`GAME_DESIGN.md`の内容が各ファイルにコピーされていない**（ポインタになっている）

### 記録

1. `PROJECT_STATUS.md`の未確定表から、解決した論点が落ちている
2. 同表に、新しく出た論点が入っている
3. **実装ステップの順番と、その依存関係の理由が書かれている**
4. **セーブ構造を触る回を何回に分けるかが決まっている**

### 次のタスク

1. **このファイルが、次の1タスクの内容に書き換わっている**
