# 次にやること：**実行中のスキル層と `trigger`（段階2）**

**このファイルは「次の1タスク」だけを書く。** 終わったら次のタスクの内容に書き換える。全体の状況は`PROJECT_STATUS.md`、ルールは`AGENTS.md`と`CLAUDE.md`、**ゲームの中身は`GAME_DESIGN.md`**。

**このファイルだけ読めば着手できるように書いてある。** 過去のタスクを知らない前提で読んでよい。

`PLAN_IMPLEMENTATION.md` 3章の**3番の残り**（レベルの役割転換のうち、割り振り・スキル解放・**スキルの中身**が終わり、**パッシブが残っている**）。**パッシブは段階3の後ろなので、まだ来ない。**

---

## 0. 前のタスクは終わった

**スキルの中身12個が完了した（2026-08-16・実機確認まで通過）。** 指示書は **`docs/02_exec/EXEC_SKILL_CONTENT.md`**、決定台帳は **`docs/01_plan/PLAN_SKILL_CONTENT.md`**。

⚠ **これは段階2の前に人間の判断で挟んだ回。** 段階1で足した受け口が実機で1度も通っていないまま段を積まないため。

入ったもの（詳細はEXEC。**ここには複製しない**）：

- **`skills.json` が6件 → 18件。** 3人 × 6候補（Lv1×2 ＋ Lv5/10/15/20 で1つずつ）
- `characters.json` の `skills[]` が各キャラ6件。`ja.csv` に12行
- ⚠ **`.gd` は1行も触っていない**

**その1つ前は「スキルの器の付け替え（段階1）」**（`EXEC_SKILL_TEMPLATE_PHASE1.md`・コミット `4e4bd11`）。`skills.json` が `activation` / `target` / `effects[]` になり、`SkillSchema` / `SkillActivation` が新設され、`SkillResolver` が入口2つに作り直された。

### 段階1で入った受け口の現状（**段階2で作り直さないこと**）

| 受け口 | 今の利用者 |
|---|---|
| `sort: farthest` / `lowest_hp` / `highest_hp` | ✅ **埋まった**（貫きの矢／追い討ち・集中治療／兜割り） |
| `attack_type: "true"`（確定ダメージ） | ✅ **埋まった**（3件） |
| `scale_from` の HP派生・`distance`・`of: target` | ✅ **埋まった** |
| `target.range` | ⚠ **ゼロのまま**（18件とも省略）。座標定数を決める回 |
| ダメージの介入点（`_step_crit_override` / `_step_reduction`） | ⚠ **ゼロのまま**。**段階3** |

⚠ **`resolve()` は既に `trigger` を見ていて、`cast` 以外は `push_warning` して【その効果だけ】飛ばす。段階2でここが本番になる。**

---

## 1. このタスク：**段階2（実行中のスキル層 ＋ `trigger`）**

**`PLAN_SKILL_TEMPLATE.md` 19章の段階2。** これができると**多段と遅延が書けるようになる。**

| やること | 中身 | PLAN |
|---|---|---|
| **「実行中のスキル」層を新設する** | `battle_controller`（入力と表示）と `SkillResolver`（1回ぶんの解決）の**あいだ**。タイムライン・待ち行列・演出シーンとの往復を持つ | 7-1 |
| **`trigger` を実装する** | `cast` / `delay:N` / `charge_start`。⚠ **`event:◯◯` は受け口だけ**（アニメが無いため） | 6-2 |
| **発火の経路を1本にする** | ⚠ **ここが一番重要。**`cast` も `delay` も `event` も、**全部おなじ「発火した」処理**へ入る | 6-5 |
| **待ち行列にタイムアウト** | ⚠ **無音で消えるのがこのプロジェクトの定番事故** | 6-6 |
| **中断** | 使用者が死んだ・戦闘が終わったら残りを捨てる | 6-6 |
| **待ち行列を外から取り消せる形にする** | ⚠ **後から変えられない7つの1つ**（20章の6番） | 6-8 |
| 待ち行列の要素に**種別タグ** | 投射物／近接／魔法。条件のマッチングに要る | 6-8 |

### ⚠ この4つを外すと作り直しになる

1. ⚠ **段階2を「タイマー専用」で作ってはならない**（6-5）。そう作ると新層が「時間で進むもの」になり、アニメを繋ぐときに**新層ごと作り直し**になる。アニメが入るとき足すのは「**ビューからの合図を待ち行列に流し込む分岐1本だけ**」で済む形にする
2. ⚠ **待ち行列を「スキルごとの private」にしてはならない**（6-8）。中断は**自分で自分を捨てるだけ**なので private でも成立してしまい、**気づかずに閉じた作りにしやすい**
3. ⚠ **「実行中のスキル層」と「状態の器」は別物**（7-2）。**混ぜると、術者が死んだ瞬間に敵に付けたDoTが消える。** 状態の器は**段階3**。この回では作らない
4. ⚠ **`SkillResolver` の契約を変えない**（7-3）。**時間を持たず、次のフレームを知らず、ノードを触らない。** **新層はビューを知ってよい。resolver は今後も知らない**

### ⚠ 3分類を混ぜない（6-6）

| 状況 | 扱い |
|---|---|
| 合図が来た・対象0体 | **正常。** 静かに空振り |
| 狙点に着弾したが誰も居ない | **正常。** 着弾は起きているので合図は来る |
| **合図そのものが来ない** | **異常。** `push_warning` |

---

## 2. 人間が決めたこと（**2026-08-16。EXEC の §1 に決定として書くこと**）

| # | 論点 | 決定 |
|---|---|---|
| 1 | **タイムアウトしたとき、発火させるか捨てるか**（PLAN 21章の未決だったもの） | ⚠ **発火させる ＋ `push_warning`。** 遅れてでもダメージは出す。無音で消える形を作らない |
| 2 | **新層のファイル名とクラス名** | **`scripts/systems/skill_runtime.gd` / `class_name SkillRuntime`**（新しいフォルダは作らない） |
| 3 | **`charge_start` を段階2で実装するか** | ⚠ **実装する。** `_on_charge_button_down()` から新層に流す |
| 4 | **`delay` の検証をどうやるか** | ⚠ **検証用の一時データを足さない。** **`skill_rapid_volley`（速射）を「2体同時」から「2連射」に書き換える**（`effects[]` を2つ並べ、片方に `trigger: "delay:N"`）。**実スキルで検証する** |

### まだ決まっていないこと（**着手前に人間に聞く**）

- ⚠ **ウェーブ間・戦闘終了時に待ち行列をどうするか**（中断の一種）。PLAN 14-4 の「ウェーブ間のCD・HP・状態」も未決のまま
- **`skill_rapid_volley` を2連射にしたとき、`target` をどうするか**（`count: 2` の2体同時をやめて `count: 1` の2連射にするのか、2体×2連射にするのか）。⚠ **やめると `count: 2` の利用者がゼロに戻る**

### 体制

**直近6タスクと同じ。実装役（MiniMax）は使わず、設計役が全部書く。PRE_PLAN も IMPL_LOG も作らない。**
⚠ **`battle_controller.gd` は915行。途中の書き換えは設計役が全文を書く。**

---

## 3. 調査済みの事実（**`grep`し直さなくてよい**・2026-08-16確認）

| | 事実 |
|---|---|
| `skill_resolver.gd` | **459行**。`select_targets()` / `resolve()` / `fold_charge_ratio()` の3本が公開 |
| `resolve()` の戻り値 | `{ "unit_id", "amount", "is_heal", "is_crit" }` の配列。**`battle_controller.gd` 607〜611行がそのまま読む** |
| `resolve()` は既に `trigger` を見ている | **`cast` 以外は `push_warning` してその効果だけ飛ばす**（182〜191行。段階2でここが本番になる） |
| `resolve()` は `host` も見ている | `none` 以外は `push_warning` して飛ばす（段階3） |
| `skill_activation.gd` | **52行**。`blocked_reason()` 1本 ＋ 理由7定数。⚠ **発動可否の条件を他所に書かないこと** |
| `skill_schema.gd` | **382行**。⚠ **`trigger` の形の検証は既にある**（362〜369行。`cast` / `charge_start` / `event:◯◯` / `delay:<数値>`。`cast` 以外は黄） |
| ロード時検証 | `MasterDataLoader._validate_all_skills()`（391行）。**育成画面か戦闘画面に入って初めて走る**（起動直後ではない） |
| そのログ | `[MasterDataLoader] skills validated: 18 entries, 0 errors, 0 warnings` ⚠ **これが唯一の `print`** |
| `battle_controller.gd` | **915行**。⚠ **途中の書き換えは設計役が全文を書く** |
| `_process()` | **292〜330行**。`_tick_charge` → `_update_skill_buttons` → CD → 対象再選択 → 攻撃/移動 → 勝敗判定。⚠ **新層のtickをどこに挟むかはここを見て決める** |
| `_fire_skill()` | **591〜617行**。`blocked_reason()` → `fold_charge_ratio()` → `resolve()` → `_pop_damage()` → `start_cooldown()`。⚠ **新層が入るのはここ** |
| チャージの持ち方 | `_charging: Dictionary`（60行）と `_tick_charge()`（680行）。**押しっぱなしの時間を1つ持つだけ** |
| チャージの入口 | `_on_charge_button_down()`（624行）／`_on_charge_button_up()`（638行）。⚠ **`charge_start` を流すのは前者** |
| 戦闘中に座標が動く場所 | ⚠ **`_step_unit()` の390〜391行の1箇所だけ**（通常攻撃の間合い詰め）。**スキルは1つも座標を動かさない** |
| `skills.json` | **18件**。`trigger` を書いた効果は**1件も無い**（＝全部 `cast`） |
| `effects` は既に配列 | **多段は効果を並べて書く。`repeat` のような欄は作らない**（PLAN 6-2） |
| `unit_view.gd` | **105行**。`pop_damage` は会心だけ色とフォントが違う |
| **アニメーションは無い** | ⚠ **`event:◯◯` の合図を出す側が存在しない。** 受け口だけ作る（PLAN 6-4） |
| `BattleUnit` の派生値 | `static create()` で生成時に確定。**戦闘中に変わる想定が無い**（段階3で効く） |

### 行数

| ファイル | 行数 |
|---|---|
| `game_manager.gd` | **2832** |
| `battle_controller.gd` | **915** |
| `skill_resolver.gd` | 459 |
| `skill_schema.gd` | 382 |
| `unit.gd`（`BattleUnit`） | 185 |
| `battle_formula.gd` | 67 |
| `skill_activation.gd` | 52 |

---

## 4. このあと来るもの（**このタスクではやらない**）

| 段 | 実装するもの | 書けるようになるもの |
|---|---|---|
| **3** | **状態の器** ＋ `buff` / `dot` ＋ **購読** ＋ **条件** ＋ 回復/状態付与/死亡の介入点 | デバフ・DoT・罠・反射・追撃・コンボ・**パッシブの条件発動**・復活 |
| **4** | `mode: area` | 範囲・誘導しない投射物 |
| **5** | `phases[]` / `recast` | 再発動・構え型 |
| **6** | `spawn` | 召喚・分裂（⚠ **座標の規則が要る**） |

⚠ **パッシブは段階3の後ろ。** 条件発動が要求する層は、`buff` / `dot` が要求する層と**同一のもの**。**2回に分けて作る意味が無い。**

⚠ **段階3は大きすぎるので割る**（PLAN 19章）。**状態の器を先に固めてから、購読と条件を乗せる。**

### ⚠ 段階3の器で、後から変えられないもの

**PLAN 20章に7つある。段階3に入る前に必ず読むこと。** 特に：

- **状態は1ユニットに複数個持てる**（「上書き」前提で作ると作り直し）
- **状態の寿命は秒数だけではない**（`until: "charge_end"` を取れる形にする）

---

## 5. 罠

### ドキュメントの「実装済み」を信じない

**ズレが8回起きている。** `grep`で関数の中身を見てから判断する。読んだ結果ドキュメントが間違っていたら、**勝手に直さず報告する。**

### 編集したら`grep`で当たったことを確認する

「戦闘だけ反映されない」で1タスク溶かした事故がある。**このタスクも戦闘の中心を触る。**

### 無音で消えるものを作らない

**このタスクの本題がこれ。** 合図が来ない・待ち行列が捨てられる・タイムアウトする、のどれもが**エラーを出さずにダメージが消える**形になりうる。**`push_warning`を必ず置く。**

### `MasterDataLoader`が返す数値は`float`

`int()`で包み忘れるとセーブに`.0`が乗る。⚠ **`delay:0.3` は文字列から取り出すので、`is_valid_float()` → `float()` の順で読むこと**（`SkillSchema._is_trigger_shape()` が同じ判定をしている）。

### 状態を変える前に全部の判定を終える

`blocked_reason()`が空を返してから初めて発動に進む。**途中で状態を触ってから弾かない。**

### 再描画に`await`を持たせない

`remove_child()`してから`queue_free()`する。

### インデントはタブ

`.gd`はタブ。**`.json`も既存ファイルはタブ。** `ja.csv`はUTF-8（BOMなし）。編集後はGodotで再インポートが要る（人間の作業）。

### Godotを起動できない

**動かして確かめられるのは人間だけ。「動きました」と書かない。** 完了条件は「ログ」「ファイル」「画面」の3つに分け、**同じことを2箇所に書かない。**

⚠ **完了条件に書くログは、実際に`print`があるものだけにする。**

---

## 6. 検証の道具

**デバッグオーバーレイが全画面の右上に常駐している**（`res://tests/debug_overlay.gd`）。`0`キーで表示切替（ゴールド・素材・研究解放など）。

戦闘画面は**`F3`でデバッグパネル**（右上・速度1〜8倍・`K`敵1体撃破・`L`全滅・`J`味方全員に物理の一撃・`M`同じく魔法・`V`強制勝利・`B`強制敗北・**`S`CDリセット**）。**ユニット1体につき2行で10軸と実測ダメージが出る。**

**このタスクの検証は、`S`（CDリセット）で18スキルのうち装備した2つを撃って数字が変わらないことを土台に、`skill_rapid_volley` の2連射が「1発目のあとに遅れて2発目が出る」のを目で見るのが中心になる。** ⚠ **速度8倍だと遅延が見えにくい。等速で見ること。**

⚠ **スキルは18候補あるが、1キャラ2枠しか持ち込めない。** 検証するスキルはギルドのスキル選択画面で先に付け替えること。
⚠ **Lv15 / Lv20 のスキルは、研究でレベル上限を上げないと選べない**（`base_level_cap` 10）。**`skill_rapid_volley` は Lv15。**
⚠ **`F4`は`_unhandled_input`に届かない。`F2`・`0`・英字キーは届く。**
⚠ **ロード時のログはタイトルの「つづきから」でしか出ない。**

---

## 7. このタスクでやらないこと

- **状態の器・`buff`/`dot`・購読・条件・パッシブ**（段階3）
- **`mode: area`**（段階4）／**`phases[]` / `recast`**（段階5）／**`spawn`**（段階6）
- **アニメーション本体**（PLAN 6-4。⚠ **イベント名の規約だけ 6-3 で決まっている：攻撃判定は`hit1`から連番・詠唱の終わりは`cast_end`**）
- **12スキルのバランス調整**（`PLAN_SKILL_CONTENT.md` 5-2。倍率と`weight`は全部仮）
- ⚠ **`scale_from` に積を書けるようにすること**（下記8の1番。**器の話なので `PLAN_SKILL_TEMPLATE.md` 側で決めてから**）
- **`target.range` を入れること**（座標定数の回）
- **戦闘の2次元化**（PLAN 16章）
- **ルーン・プリセット・等級10**（`PLAN_IMPLEMENTATION.md` 3章）
- **通常攻撃を`effects[]`に乗せること**（PLAN 21章の担当外。接続点が3つ揃ってから判断する）

---

## 8. 引き継いだ宿題

`PROJECT_STATUS.md`にもあるが、**この回に関係しそうなものだけ**。

1. ⚠ **`scale_from` は「和」しか書けない**（`PLAN_SKILL_TEMPLATE.md` 5-5-1）。`atk × (1 + hp_lost_ratio)` が書けず、**割合の変数は必ず「レベルで伸びない定数項」**になる。**器の穴。PLAN側で決める**
2. ⚠ **`PLAN_SKILL_TEMPLATE.md` 5-5-2 の「想定レンジ 50〜500」が実データと合わない**（Lv1 は `atk 14〜18` / `mag 16` / `def 3〜6`）
3. ⚠ **`PLAN_SKILL_TEMPLATE.md` 21章の未確定4件は、実は段階1のEXECで決着している**（`sort`5値を全部実装／リソースは作らない／遮蔽は入れない／`count`の上限は設けない）。**PLAN側は未確定のまま**（`scale_from`の食い違いは2026-08-16に修正済み）
4. **`target.range` が18件とも未設定**（座標定数とセットで後決め）
5. **ダメージの介入点（`_step_crit_override` / `_step_reduction`）は利用者ゼロのまま**（段階3）
6. **`skill_reckless_strike`（捨て身の一撃）で自死できる。** 止めるなら `SkillActivation.blocked_reason()` に1行
7. **`DATA_SCHEMA.md` 3-1 のダメージ計算式が古い**（減算と書いてあるが実際は除算）／**4-3 の`stats`が4軸のまま**
8. **`atk_multiplier`が常に1.0**（バフの受け口として既にある）
9. **`battle_controller.gd` 13〜17行の座標定数がハードコード**（2次元化のときに一緒に見る）
10. **検証用のものはリリース前に消す**（デバッグオーバーレイ・戦闘/ポモドーロのデバッグパネル）

---

## 9. 終わったあと

**このファイルを、次のタスク（段階3の前半＝状態の器）の内容に書き換える。**
