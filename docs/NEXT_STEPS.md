# 次にやること：**⚠⚠ E（掃除）→ UI を全部仕上げる**

> ⚠⚠ **2026-09-06。⚠ 人間が順番を決めた。⚠ 言葉のまま：**
> ⚠ **「⚠ Eをした後に　⚠ UI関連を全部しあげてからのほうがいいかも　⚠ ダンジョンは、それを治してる間にイメージを固める」**
> ⚠ **「⚠ UIは、テスト用のページを作ってそこで全部確認できるようにしたい」**
> ⚠ **「⚠ まだたりないＵＩなどをぜんぶかたずけたい　⚠ あとアセットを使わなくていいものは次全部やりたい」**

## 0. ⚠⚠ この回の並び（**人間の決定・2026-09-06**）

| 順 | 何を | なぜ |
|---|---|---|
| ⚠ **1** | ⚠⚠ **E（掃除）** | ⚠ **`--import` の赤1本・測れない画面3枚・説明文の穴を先に消す** |
| ⚠ **2** | ⚠⚠ **UI テストのページ**（⚠ **新規**） | ⚠ **人間「⚠ そこで全部確認できるようにしたい」。⚠⚠ 以降の UI を1画面から見て回れるようにする＝⚠ 通しプレイをしなくても確かめられる** |
| ⚠ **3** | ⚠⚠ **足りない UI を全部** | ⚠ **難ダンジョンの9件だけでなく、⚠ 拠点側で欠けているものも含む**（下の表） |
| ⚠ **4** | ⚠ **ダンジョンの中身**（⚠ B・C・D） | ⚠⚠ **人間は「⚠ 治してる間にイメージを固める」＝⚠ 設計役から催促しない** |

⚠⚠ **振り分けの基準：⚠ 「⚠ アセット（絵・音）が要るか」。⚠ 人間の指示「⚠ アセットを使わなくていいものは次全部やりたい」。**

### 0-1. ⚠ E（掃除）の中身

⚠⚠ **4件を入れた（2026-09-06）。⚠ `--import` の赤が 1 → 0 本になった。**

| | 中身 | 状態 |
|---|---|---|
| ⚠⚠ **宿題70 / 74** | ⚠ **`tests/test_common_infra.gd` の `CHEST_TYPE` の赤** | ✅ **閉じた。⚠ `CHEST_INSTANCE_ID` ＋ `CHEST_ID` に直した**（⚠ **`open_chest()` が見るのは `instance_id` のほう**） |
| ⚠ **宿題75** | ⚠ **素材16件の説明文** | ✅ **`ja.csv` に16行**（⚠ **下の注意**） |
| ⚠ **宿題76** | ⚠ **`ui_dungeon_corridor_chest` の未使用行** | ✅ **消した**（⚠ `_title` のほうは使われているので残した） |
| ⚠ **宝箱0個の文言** | ⚠ **「⚠ ポーションを0個、宝箱を1個」と出ていた** | ✅ **0 のほうを読み上げない。⚠ `ui_base_potion_received` を足した**（⚠ **文をコードでつながず、⚠ 文ごとキーを分けた**） |
| ⚠ **宿題73** | ⚠ **`layout` で測れない3枚** | ⏳ **UI テストのページと同時に解く**（下） |
| ⚠ **空の `plan/`** | ⚠ **`.gitkeep` だけ。⚠ `AGENTS.md` の構成に無い** | ⏳ **⚠ 消すか構成に足すか＝人間の決定が要る** |
| ⚠ **`AGENTS.md` 冒頭** | ⚠ **「Ziva: ChatGPT/Claude/Gemini対応」が実態と違う** | ⏳ **⚠ 実態を人間しか知らない** |

⚠⚠ **素材の説明文で1つ手加減した**（⚠ **報告**）：⚠ **`training_material_2..4` と `decor_material_4` は、⚠ いま出口が1つも無い**
（⚠ 宿題46 / 47）。⚠ **「レベル上げに使う」と書くと嘘になるので、⚠ この4件だけ系統と段だけを書いた。**
⚠ **出口ができたら文を足すこと。**

⚠⚠ **ズレ53（報告のみ・直していない）**：⚠ **宿題「倉庫の宝箱タブ0件の表示を `ui_warehouse_no_chest` に直す」は、⚠ 既に直っている**
（⚠ `warehouse_screen.gd:424` が `ui_warehouse_no_chest` を呼んでいる）。⚠ **`PROJECT_STATUS.md` の宿題が古い。**
| ⚠⚠ **宿題73** | ⚠ **`layout` で測れない3枚**（⚠ `dungeon_chest` / `dungeon_relic_select` / `dungeon_shop`）。⚠ **`_ready()` が条件を満たさないとマップへ戻すため測定が止まる**（⚠ 宿題72） | ⚠ **中。⚠⚠ UI テストのページと同じ問題＝⚠ 一緒に解くのが安い** |
| ⚠ **宿題75** | ⚠ **素材16件に `ui_desc_*` が無い**（⚠ `ja.csv` に16行） | ⚠ **小** |
| ⚠ **宿題76** | ⚠ **`ui_dungeon_corridor_chest` が未使用の行になった** | ⚠ **極小** |
| ⚠ **小さいもの** | ⚠ **倉庫の宝箱タブ0件の文言 ／ 宝箱0個の受け取り報告 ／ 空の `plan/` フォルダ ／ `AGENTS.md` 冒頭の Ziva の記述** | ⚠ **小** |

### 0-2. ⚠⚠ 足りない UI（**難ダンジョン以外。⚠ 宿題から拾った**）

| | 中身 | アセット |
|---|---|---|
| ⚠⚠ **設定画面が無い** | ⚠ **音量・ミュートの UI が1つも無い**（⚠ `SoundConfig` の3欄が起動時に適用されるだけ）。⚠ **セーブ構造ごと決める回が要る** | ⚠ **要らない** |
| ⚠ **研究の確認モーダル** | ⚠ **解放に確認が無い**（⚠ `Modal.confirm` の待ち方を確かめてから） | ⚠ **要らない** |
| ⚠ **倉庫の宝箱タブ** | ⚠ **0件のとき `ui_warehouse_empty` が出る**（⚠ 正しくは `ui_warehouse_no_chest`） | ⚠ **要らない** |
| ⚠ **ポモドーロの演出** | ⚠ **セット完了の知らせ ／ 作業中のタイトルを大きく ／ 休憩明けの自動開始** | ⚠ **要らない**（⚠ 音は入っている） |
| ⚠ **BGM** | ⚠ **`play_bgm()` が無い。⚠ バスと音量欄だけ在る** | ⚠⚠ **要る（音源）＝⚠ 次に回さない** |

### 0-3. ⚠⚠ UI テストのページ（**この回で形を決める**）

⚠ **既に在るもの**：⚠ **`tests/test_ui_common.tscn`**（⚠ **`PrimaryButton` / `ResourceDisplay` / `DialogBase` だけ。⚠ ゲームから開けない**）。
⚠ **`tests/base_screen_debug.tscn` / `modal_test.tscn` も同じく単体で起動する形。**

⚠⚠ **決まったこと（人間の決定・2026-09-06）**：

| ⚠ 問い | ⚠ 裁き |
|---|---|
| ⚠ **①何を載せるか** | ⚠⚠ **両方**（⚠ **上段＝全画面を開くランチャー ／ ⚠ 下段＝部品のカタログ**） |
| ⚠ **②どこから入るか** | ⚠⚠ **拠点にデバッグ入口を作る**（⚠ `OS.is_debug_build()` のガード付き） |

⚠⚠ **リリース前に消すものが1つ増える**（⚠ **ポモドーロの「残り1秒にする」ボタンと同じ扱い＝⚠ 宿題に書く**）。

⚠⚠ **宿題73 と同じ壁がある**：⚠ **`_ready()` で条件を満たさないと戻す画面が3枚あり、⚠ 単に開くだけでは出せない。**
⚠ **＝⚠ 「⚠ 戻さない条件」を先に作る必要がある**（⚠ **レリック＝relic のマスに立つ ／ ショップ＝ボスを倒した先 ／ 宝箱＝拾い待ちがある**）。
⚠ **これを作ると `scenario=layout` でも測れるようになる＝⚠ 宿題73 が同時に閉じる。**

---

## 1. ⚠ 難ダンジョンの UI（**上の「3」の中身。⚠ 台帳 §5-9**）

⚠ **全文は `docs/01_plan/PLAN_HARD_DUNGEON.md` §5-9。⚠ 下は目次。**

| # | ⚠ 中身 | ⚠ 重さ | ⚠ 引っかかり |
|---|---|---|---|
| ⚠ **37** | ⚠ **マップを真ん中に**（⚠ 今は右寄り） | ⚠ 小〜中 | ⚠ **原因が未特定。⚠ 列の割り当てではない（`dungeon_map.gd:423-445` を読んだ）** |
| ⚠ **38** | ⚠ **分岐の中で分岐**（⚠ 5ノード以内） | ⚠ **つまみ1つ** | ⚠⚠ **決定30 を覆す（前日）。⚠ 長い斜め線が戻る恐れ** |
| ⚠ **39** | ⚠ **モーダルをウィンドウ形式に** | ⚠ 中 | ⚠ **`Modal` は全画面共通＝他画面に及ぶ** |
| ⚠ **40** | ⚠ **自動で鞄に入れる＋確認できる** | ⚠⚠ **大** | ⚠⚠ **決定27・29 を覆す（前日・段階20-e / 20-f）** |
| ⚠ **41** | ⚠ **鞄からいつでも捨てられる** | ⚠ **小** | ⚠ **口は在る。⚠ 導線だけ** |
| ⚠ **42** | ⚠ **スタック数**（⚠ 最上級素材5 ／ ポーション3） | ⚠⚠ **大** | ⚠⚠ **鞄の「1個＝1枠」を変える＝根っこ** |
| ⚠ **43** | ⚠ **ポーションの回復量の調整** | ⚠ **未決** | ⚠⚠ **文が2通りに読める。⚠ 人間に確かめる** |
| ⚠ **44** | ⚠ **宝箱の中身を最大3個に** | ⚠ **小** | ⚠ **データだけ** |
| ⚠ **45** | ⚠ **ダンジョン内のゴールドを見れるように** | ⚠ **要確認** | ⚠⚠ **既に出ている。⚠ 表示名が「遺物片」** |
| ⚠ **46** | ⚠ **付けているレリックの確認** | ⚠ 小〜中 | ⚠ **出す場所が未決** |
| ⚠ **47** | ⚠ **敵・ボスのテーブルを個別に調整** | ⚠ 中 | ⚠ **「何を個別に」が未決** |

⚠ **「⚠ 今はまだ」と人間が書いたもの**：⚠ **ボスの種類・強敵で選択の余地を作る**（⚠ 台帳 §5-9-1。⚠ **戦闘に触るので一番後ろ**）。

### 1-1. ⚠⚠ 決まったこと（**2026-09-06・同じ日の2回目。⚠ 台帳 §5-9-4**）

| # | ⚠ 決まったこと |
|---|---|
| ⚠ **45** | ⚠ **「了解」＝⚠ 既に出ていると分かった。⚠⚠ 改名の指示は来ていない＝触らない** |
| ⚠ **38** | ⚠ **5ノードの直線の中に分岐を入れる。⚠ 中は 3〜4 ノード** |
| ⚠ **40** | ⚠⚠ **画面から離れるときに自動で鞄へ入れる。⚠ 結果は見せる**（⚠ 画面そのものは残る） |
| ⚠ **42** | ⚠⚠ **ポーション以外（＝素材）からスタックする。⚠ ポーションは1個＝1枠のまま** |
| ⚠ **43** | ⚠ **42 が「ポーションは後回し」なので自動的に後ろへ** |

### 1-1-1. ✅ **41 を入れた**（**2026-09-06。⚠ 人間の指示「⚠ 41はじっそうして」**）

⚠ **人間の言葉：⚠ 「⚠ マップからアイテムを選んだら捨てられるように」。**
⚠⚠ **他の10件は台帳に留めたまま。⚠ 41 だけ作った。**

| | 中身 |
|---|---|
| ⚠ **触った** | ⚠ **`dungeon_map.gd` の `_rebuild_bag_actions()` と `_on_discard_bag_pressed()`。⚠ `.tscn` は触っていない**（⚠ 鞄のマス目・詳細・ボタンの行は 18-d から在った） |
| ⚠ **口** | ⚠⚠ **`discard_dungeon_bag_item()`。⚠ 既に在ったものを呼ぶだけ＝1本のまま** |
| ⚠ **出る条件** | ⚠ **鞄のマスを押して、⚠ 品が入っているとき。⚠ 空きマスでは出ない** |
| ⚠⚠ **品の種類を問わない** | ⚠ **戦利品にも出る**（⚠ **前は「誰に使うか」だけで、⚠ 使えない品には1つもボタンが出なかった**） |
| ⚠ **翻訳** | ⚠ **`ja.csv` に2行**（⚠ **捨てるボタンの文言は拾いもの画面と同じ `ui_dungeon_pickup_discard_bag` を使い回した＝⚠ 同じ意味に2つ目のキーを作らない**） |
| ⚠ **確認を出していない** | ⚠ **倉庫（`ui_warehouse_discard_confirm`）と違い、⚠ ランの鞄は出れば全部消えるもの（決定7）＝⚠ 取り返しのつかなさの度合いが違う** |

⚠ **取れた根拠**：⚠ **`--import` の赤は1本（宿題70・据え置き）／ `scenario=dungeon` 0/1 ／ `scenario=layout` 0/1。**
⚠ **`dungeon_map.tscn` 最小 524 x 224（据え置き）。**
⚠⚠ **押した結果は取れていない**（⚠ **ヘッダは絵が取れない＝人間が見る**）。

#### ⚠ 人間が実機で見る項目（**3件**）

1. ⚠⚠ **マップの鞄のマスを押すと「⚠ 鞄から捨てる」が出るか**（⚠ **戦利品＝素材のマスでも出ること。⚠ 前はポーションでしか何も出なかった**）
2. ⚠ **押すと1個減り、⚠ 上の「鞄 7/8」が動くか。⚠ 下に「⚠ 鞄から捨てた」と出るか**
3. ⚠ **空きマスを押しても何も出ないこと**（⚠ **押しても何も起きないボタンを作っていない**）

⚠ **47 の返事も来た：⚠ 「⚠ 個別に調整はすべてをばらばらで」**（⚠ **層ごと・区画ごと・種類ごとの全部。⚠ 台帳 §5-9-4**）。
⚠⚠ **47 は重い。⚠ 着手の前に「⚠ いま敵がどう決まっているか」を `grep` で取ってから設計を出す。**

### 1-2. ⚠ 設計役の見立て（**覆してよい**）

- ⚠ **小さいものから：⚠ 45（文言）→ 44（つまみ）→ 41（導線）→ 38（つまみ1つ）**
- ⚠⚠ **42 と 40 は鞄の根っこを触るので、⚠ 2つで1タスクにするほうが安い**（⚠ **別々だと鞄の数え方を2回直す**）
- ⚠ **43 は 42 が決まってから**

---

## 2. ⚠ 入ったもの（**⚠ 通しプレイ2回目で確かめ終わった。⚠ 6項目とも○＝台帳 §5-8**）

| コミット | 中身 |
|---|---|
| `fe27dad` | ⚠⚠ **不1：⚠ 戦闘の戦利品は「勝ったとき」に配る**（⚠ **踏んだ時点で配っていた＝戦闘が起きず報酬だけ入っていた**）。⚠ `clear_dungeon_battle()` を新設 |
| `31b0731` | ⚠ **決定31（⚠ 通路の宝箱はその場限り）／ ⚠ 決定32（⚠ たいまつの等級を表示）／ ⚠ 決定33（⚠ 素材は4段だけ・5つの表の19箇所）** |
| `e4ab039` | ⚠⚠ **決定36：⚠ 拾いもの・宝箱をマップの上に重ねて出す**（⚠ `class_name DungeonChest` ／ `open_as_overlay()` ／ `closed`） |
| `b4b63b5` | ⚠ **1回目の結果と UI の裁き（決定31〜35）を台帳に書き取った** |

⚠⚠ **決定36 で穴が1つ閉じた**：⚠ **通路に宝箱があるマスに着くと、⚠ そのマスの中身（宝箱・レリック・休憩）に永久に入れなかった**
（⚠ **不1 と同じ穴が戦闘以外にも開いていた**）。⚠ **`_pending_node_entry` で入り直す。**

### 2-1. ⚠ 平常値（**この回の直しのあと**）

⚠ **`dungeon` 0/1 ／ `dungeon_battle` 0/1 ／ `layout` 0/1 ／ `inventory` 0/4 ／ `area` 0/1。**
⚠ **`dungeon_map.tscn` 最小 524 x 224（据え置き）。⚠ `--import` の赤は1本（宿題70）。**

### 2-2. ⚠ そのあと（**順番は人間が決める**）

| | 中身 | 状態 |
|---|---|---|
| ⚠⚠ **17-g** | ⚠ **数値の実測7件**（⚠ 未決4 / 1-b / 1-c / 2-b / 3-b / 5 / 6）⚠ ＋ 入口のコスト（未決7） | ⚠ **人間の指示「数字なんて後でいい」で後ろへ回した。⚠ 通しが安定してから** |
| ⚠ **16** | ⚠ **フロアのゲームバランス**（`floor_1..5`） | ⏸ **保留**（⚠ 決定2「難しいほう → 簡単なシナリオ」） |
| ⚠ **17-f** | ⚠ **弱者狙いAIと予兆** | ⚠ **一番後ろ**（⚠ 人間の指示「戦闘関連はさいご」） |

⚠ **外の台帳から採ると決めた未実装の項目は台帳 §5-4-1**（⚠ **鍵とチェスト ／ 強敵ノード ／ 開始ボーナス3択 ／
出撃ゴールド ／ リュックの段階式 ／ 戦闘開始時アイテム ／ バイオーム**）。⚠ **強敵と戦闘開始時アイテムは戦闘に触るので最後。**

### 2-3. ⚠ 番号の続き

⚠ **`E140` から ／ `W35` から** ／ ⚠ **未報告のズレは 53 から ／ 宿題は 77 から**。
⚠ **宿題75＝素材16件に説明文（`ui_desc_*`）が無い。**
⚠ **宿題76＝`ja.csv` の `ui_dungeon_corridor_chest` が未使用の行になった**（⚠ 決定31 でボタンを消したため。⚠ 消していない）。

### 2-4. ⚠ 予想できている落ち

- ⚠⚠ **ルートを「列挙」しないこと**（⚠ **25層では終わらない。⚠ `_count_dungeon_routes()` を使う**）
- ⚠ **通路を `str(entry)` で読まない**（⚠ `[{to, effect}]`。⚠ 読む口は `get_dungeon_edges()` の1本）
- ⚠⚠ **拾い待ちを片付けずに歩かない**（⚠ **戦利品も拾い待ちへ行くので、⚠ 溜めると次の検証に混ざる**）
- ⚠⚠ **戦利品を配る口を2本目にしない**（⚠ **`_grant_dungeon_node_gains()` の1本だけ。⚠ 呼ぶのは `move_in_dungeon` / `clear_dungeon_battle` / `clear_dungeon_boss` / `open_dungeon_chest` / `open_dungeon_corridor_chest`**）
- ⚠ **層の重み4本と `dungeon.json` の `layers` の長さを揃える**（⚠ ずれても `clampi` で黙って動く＝`W31`）
- ⚠⚠ **絵文字を足したら必ず `scenario=glyphs` を回す**（⚠ **フォントは 1,274 字しか無い**）
- ⚠ **`MasterDataLoader` が返す数値は必ず `int()` で包む**（CLAUDE.md 3番）

---

## 3. ✅ **段階20-e を入れた（拾いものを選んで鞄に入れる）**（2026-09-05・同じ回）

⚠⚠ **人間の言葉**：⚠ **「何を拾ったか、表示するように」「⚠ モーダルの中にアイテムとして見せて」
「⚠ インベントリの中に何を入れるか選べるように」。**
⚠ **裁き：⚠ 宝箱の画面を使い回す ／ ⚠ 宝箱も「選ぶ」形に揃える ／ ⚠ 鞄の中身を捨てて入れ替えられる。**

⚠⚠ **これはコンセプト文書の「何を残し何を捨てるかはプレイヤーが選ぶもの」の実装。**
⚠ **`add_to_dungeon_bag()` のコメントが 17-a から「画面は 17-d」と予告していた穴が、ここで閉じた。**

| | 中身 |
|---|---|
| ⚠ **器** | ⚠ **`DUNGEON_RUN_PENDING_LOOT`（拾い待ち・`{item_id: 個数}`）。⚠⚠ 鞄の枠を1つも使わない** |
| ⚠ **積むもの** | ⚠ **宝箱（マス・通路）／ 通路の資源** |
| ⚠ **積まないもの** | ⚠ **戦闘・ボスの戦利品**（⚠ **黙って鞄へ。⚠ 毎回選ばせると煩い。⚠ 覆してよい**） |
| ⚠ **口** | ⚠ **`take_dungeon_pending_loot()` ／ `take_all_dungeon_pending_loot()` ／ `discard_dungeon_pending_loot()` ／ `clear_dungeon_pending_loot()` ／ `discard_dungeon_bag_item()`** |
| ⚠ **画面** | ⚠ **`dungeon_chest.tscn` を使い回す**（⚠ **拾い待ちのマス目 ＋ いまの鞄のマス目 ＋ 詳細**） |
| ⚠⚠ **満杯のとき** | ⚠ **拾い待ちには積まれる**（⚠ 枠が無い）。⚠ **鞄へ入れようとして初めて弾かれる＝「捨てて入れ替える」余地** |
| ⚠⚠ **出るとき** | ⚠ **残りは消える**（⚠ **引き返さないので拾い直せない**） |
| ⚠ **翻訳** | ⚠ **`ja.csv` に8行** |

⚠ **取れた根拠（`scenario=dungeon`）**：

```
  開ける -> 拾い待ちへ { construction_material_2: 2, forging_material_2: 2, training_material_2: 2 }
		   ／ 鞄 2 -> 2（⚠ 増えないのが正解）／ 通貨 24 -> 44
	拾い待ち = {...} ／ マス目 6 個
  ⚠ 1個だけ鞄へ -> true ／ 鞄 2 -> 3 ／ 拾い待ちが1つ減る
  ⚠ 残りを全部入れる -> {...} ／ 鞄 8/8 ／ 拾い待ち = {}
  --- 鞄が満杯のとき ---
  ⚠ 満杯（8/8）で開ける -> 拾い待ちへ {...}（空でないのが正解）
  ⚠ 満杯のまま鞄へ入れる -> false
  ⚠ 鞄から1個捨てる -> true ／ そのあと入れる -> true（⚠ 入れ替えられる）
  ⚠ 残りを置いていく -> {...} ／ 拾い待ち = {}（空が正解）
```

⚠ **`scenario=dungeon` を3回回して 0/1 で安定。**

#### ⚠ この回で1つ踏んだ（**道具側**）

⚠ **層が25になって戦闘が増えたため、⚠ 宝箱に着く時点で鞄が満杯のことがある。**
⚠ **「1個だけ入れる」の検証が false になって赤を出した**（⚠ **本番コードは正しい**）。
→ ⚠ **検証の前に1個捨てて空きを作る形に直した。⚠ 満杯の枝は次の節が測っている。**

#### ⚠⚠ 人間が実機で見る項目（**5件**）

1. ⚠⚠ **宝箱を開けると、⚠ 中身がマス目で出て、⚠ 鞄には自動で入らないこと**
2. ⚠ **マスを押すと「鞄に入れる」「置いていく」が出るか。⚠ 「入るだけ全部入れる」も効くか**
3. ⚠⚠ **下に「いまの鞄」が並び、⚠ 押すと「鞄から捨てる」が出るか**（⚠ **満杯のとき捨てて入れ替えられるか**）
4. ⚠ **鞄が満杯だと「鞄に入れる」が押せず、⚠ 「鞄が満杯」と出るか**
5. ⚠⚠ **通路の資源でも同じ画面が出るか**（⚠ **モーダルではなく画面。⚠ 中身を見せるため**）

### 1-6. ✅ **20-d を入れた（通路のできごとを知らせる／レリックの説明文）**（2026-09-05・この回）

⚠⚠ **人間が実機を見ての指示**：
⚠ **「⚠ 通路のイベントは、何かわかるような演出がしたい　モーダルとかなんかで」**
⚠ **「⚠ あとレリックなどの説明文が欲しい」**

| | 中身 |
|---|---|
| ⚠ **何が起きたか** | ⚠ **`GameManager.get_last_dungeon_edge_event()` を新設**（⚠ `{effect, amount, items, left_behind}`） |
| ⚠⚠ **状態に入れない** | ⚠ **`_state` ではなくメンバ変数。⚠ セーブに残るものではなく「いま出す1回」だけの値**（⚠ **入れると `load_state()` の正規化と状態の表を触ることになる**） |
| ⚠ **画面** | ⚠ **`Modal.notify()`。⚠ `await` しない**（⚠ **待つと戦闘への遷移が止まる**） |
| ⚠ **宝箱** | ⚠ **モーダルを出さない**（⚠ **画面そのものが演出**） |
| ⚠ **レリックの説明** | ⚠ **`ja.csv` に `ui_desc_<relic_id>` を12件。⚠⚠ コードは1行も触っていない**（⚠ `ItemDetail._add_description()` が既に全種で呼ばれていた） |
| ⚠ **翻訳** | ⚠ **`ja.csv` に18行**（⚠ モーダル6件 ＋ レリック12件） |

#### ⚠⚠ 実測で1つ嘘を見つけた

⚠ **鞄が満杯で資源を拾えなかったとき、⚠ モーダルが「通路で拾いものをした（0）」と出ていた。**
→ ⚠ **`left_behind` を足し、⚠ 「鞄が満杯で置いてきた」の文言に分けた**（⚠ **宝箱の戻り値と同じ形**）。
→ ⚠ **罠（鞄）で鞄が空だったときは、⚠ 何も落としていないのでモーダルを出さない。**

```
  ⚠ 通路のできごと = { effect: trap_hp, amount: 31, items: {}, left_behind: {} }
  ⚠ 通路のできごと = { effect: trap_currency, amount: 20, items: {}, left_behind: {} }
  ⚠ 通路のできごと = { effect: resource, amount: 18, items: {}, left_behind: {} }
  ⚠ 通路のできごと = { effect: resource, amount: 0, items: {}, left_behind: { training_material_2: 2 } }
```

#### ⚠ レリックの説明（**12件とも入った**）

```
  relic_banner_of_war        編成3人の攻撃力が15%上がる。
  relic_berserker_mark       付けた1人のHPが半分以下のあいだ、攻撃力が40%上がる。
  relic_thorns               付けた1人が攻撃を受けたとき、攻撃力の25%を相手に返す。
  ⚠ 説明が無いレリック = 0 件（0 が正解）
```

⚠ **装備・装飾・ルーンは既に数字が出ている**（⚠ **説明が無かったのはレリックだけだった**）。
⚠ **素材16件には説明が無い＝宿題75。**

#### ⚠ 人間が実機で見る項目（**3件**）

1. ⚠⚠ **罠・資源の通路を通ると、⚠ モーダルが出て「何が起きたか」が読めるか**
2. ⚠ **鞄が満杯のときに資源の通路を通ると、⚠ 「鞄が満杯で置いてきた」と出るか**（⚠ **「拾った（0）」ではないこと**）
3. ⚠⚠ **レリックのマスでマスを押すと、⚠ 効果の文章が出るか**（⚠ **前は名前と「全体／1人だけ」だけだった**）

### 1-7. ✅ **20-c を入れた（通路の字を線の中点へ／現在地を真ん中に）**（2026-09-05・この回）

⚠⚠ **人間が実機を見ての指示**：
⚠ **「⚠ 通路にアイコンは、通路の真ん中に表示して」**
⚠ **「⚠ あと地図に戻ると、そこを真ん中にするようにして、⚠ 今のままだと毎回下にスクロールしないといけない」**

| | 中身 |
|---|---|
| ⚠ **通路の字** | ⚠ **線の中点に Label を置く**（⚠ `DungeonEdgeLines`）。⚠ **`_edge_prefix()`（マスの前に付ける）を消した＝描き方を1本に** |
| ⚠ **なぜ Label か** | ⚠ **`draw_string` ではない。⚠ カラー絵文字が Label で出ることは実測済みだが、⚠ `draw_string` では確かめていない** |
| ⚠⚠ **`❔` をやめた** | ⚠ **25層・等級1 では1層先しか見えず、⚠ 104本のうち101本に `❔` が付いて画面が埋まった**（⚠ 実測で気づいた）。⚠ **「見えていない」は線の色（暗い灰）が既に言っている** |
| ⚠ **スクロール** | ⚠ **いま立っているマスを真ん中に寄せる。⚠ 描き直したときだけ1回**（⚠ 毎回だと手でスクロールできなくなる） |

⚠ **取れた根拠（`scenario=layout`）**：

```
  dungeon_map.tscn   最小 524 x 224（基準 1280 x 720）
	⚠ 通路の線 = 104 本 ／ 真ん中の字 = 2 個（⚠ 前は 101 個だった）
	⚠ スクロール位置 = 1204 / 中身の高さ 2244（⚠ 前は 0 だった）
```

#### ⚠⚠ この回で2つ踏んだ（**どちらも実測で見つけた**）

1. ⚠ **寄せる要求を `_redraw_edges()` の「前」に立てていた** → ⚠ **レイアウト前の呼び出しで消費され、⚠ スクロールが 0 のまま終わっていた。**⚠ **要求は後に立てる。**
2. ⚠⚠ **`button.position` は行（`HBoxContainer`）の中の座標で、⚠ y がほぼ 0 だった** → ⚠ **線を引くのと同じく `get_global_rect()` から `MapArea` の中の位置に直した。**

⚠ **`ScrollContainer` の中身が伸びる前に `scroll_vertical` を入れても 0 に丸められる。**
⚠ **`get_v_scroll_bar().max_value` が伸びるまで要求を消費しないようにした。**

#### ⚠ 人間が実機で見る項目（**3件**）

1. ⚠⚠ **通路の絵文字が線の真ん中に出ているか**（⚠ **マスの前ではなく**）
2. ⚠ **`❔` が消えて画面がすっきりしたか**（⚠ **見えていない通路は暗い灰の線だけ**）
3. ⚠⚠ **戦闘や宝箱から地図に戻ったとき、⚠ いま立っているマスが真ん中に来ているか**（⚠ **下へスクロールしなくてよいか**）

### 1-8. ✅ **20-a を入れた（1階25層・ラン3階）**（2026-09-05・同じ回）

| | 中身 |
|---|---|
| ⚠ **層数** | ⚠ **1階 8層 → 25層**（⚠ `dungeon.json` の `layers` を25行に。⚠ 5層ずつの区切りで書いた） |
| ⚠ **重み** | ⚠ **層の重み4本を 8 → 25 要素に**（⚠ **地帯（バイオーム）を入れるときはこの並びをまとめ直す**） |
| ⚠ **ランの上限** | ⚠ **`DungeonConfig.max_floors = 3`（決定26）。⚠ 未決1-c が閉じた** |
| ⚠ **口** | ⚠ **`can_descend_dungeon_floor()` / `get_dungeon_max_floors()`**（⚠ **画面で階の数を数えない**） |
| ⚠ **画面** | ⚠ **フロア表示が「2/3」に。⚠ 最後の階では「もう1枚潜る」が出ない**（⚠ 撤退は出る） |
| ⚠⚠ **道具** | ⚠⚠ **全ルートの「列挙」をやめ「数える」形にした**（⚠ 下） |

#### ⚠⚠ 総当たりが終わらなくなる落ちを、層を増やす前に潰した

⚠ **列挙（DFS）は層数に対して指数で増える。⚠ 8層で181本 → 25層では数えきれない。**
→ ⚠ **`_count_dungeon_routes()` に置き換えた**：

- ⚠ **ルート数は動的計画法で数える**（⚠ 深い層から「そこからボスへ何通りか」を足し上げる。⚠ ノード数に比例）
- ⚠ **「通れないノード」は幅優先で入口から届くかを見る**
- ⚠ **「行き止まり」は「出口が無くボスでもないノード」を数える**

⚠ **実測：⚠ 全ルート 47,385 本を数え切った**（⚠ 列挙では出せない数）。

#### ⚠ 実測（前 → 後）

```
				 前（19-f）   後（20-a）
  層              8            25
  ノード           20 件        63 件
  通路             31 本        104 本
  全ルート          28 本        47,385 本
  歩数             8            25
  1マスから出る    最大2本      最大2本（据え置き）
  行き止まり        0 件         0 件（据え置き）
  通れないノード     0 件         0 件（据え置き）
  通路の線          31 本        104 本（＝通路の数と一致）
```

⚠ **平常値：⚠ 17シナリオとも据え置き**（⚠ `dungeon` 0/1 ／ `layout` 0/1 ／ `glyphs` 0/1 ／ `materials` 0/1 ／
`parts` 0/2 ／ `drops` 0/2 ／ `research` 0/1 ／ `workshop` 2/1 ／ `economy` 0/1 ／ `floor` 0/3 ／
`unlock` 1/1 ／ `passives` 0/1 ／ `presets` 0/1 ／ `runes` 0/1 ／ `area` 0/1 ／ `dungeon_battle` 0/1 ／ `inventory` 0/4）。
⚠ **`floor_1..5` は1文字も触っていない。⚠ `--import` の赤は1本（宿題70）。**

#### ⚠ 人間が実機で見る項目（**3件**）

1. ⚠⚠ **マップが25層になっていて、⚠ スクロールで下まで見られるか**（⚠ **長すぎないか＝外の台帳が「一番危ない」と書いている項目**）
2. ⚠ **上のフロア表示が「1/3」になっているか**
3. ⚠⚠ **3階目のボスを倒したあと「もう1枚潜る」が消え、⚠ 「持ち帰る」だけが残るか**

### 1-9. ⚠ 予想できている落ち

- ⚠⚠ **ルートを「列挙」しないこと**（⚠ **25層では終わらない。⚠ `_count_dungeon_routes()` を使う**）
- ⚠ **通路を `str(entry)` で読まない**（⚠ `[{to, effect}]`。⚠ 読む口は `get_dungeon_edges()` の1本）
- ⚠ **層の重み4本と `dungeon.json` の `layers` の長さを揃える**（⚠ ずれても `clampi` で黙って動く＝`W31`）
- ⚠ **`E140` から ／ `W34` から** ／ ⚠ **未報告のズレは 53 から ／ 宿題は 76 から**（⚠ **宿題75＝素材16件に説明文（`ui_desc_*`）が無い**）

---

## 4. ✅ **段階19-a を入れた（見た目を絵文字＋文字＋色に）**（2026-09-04・この回）

### 2-1. ⚠⚠ フォントは人間が入れた。⚠ 繋ぐのは設計役がやった

| | |
|---|---|
| ⚠ **人間** | ⚠ **`assets/fonts/segoe-ui-emoji.ttf` を置いた** |
| ⚠ **設計役** | ⚠ **`NotoSansJP-VariableFont_wght.ttf.import` の `fallbacks` に繋いだ**（⚠ **Inspector 作業ゼロ。⚠ `--import` で Godot が UID 付きに正規化した**） |

⚠⚠ **実測（`scenario=glyphs`）**：

```
  名前 = 'Noto Sans JP Thin' / fallback = 1 本
	fallback: 'Segoe UI Emoji'
  表の件数 = 30 / NG = 0 件
```

⚠⚠ **入れてもらった絵文字フォントは 1,274 字しか無い**（⚠ **COLR/CPAL のカラー絵文字**）。
⚠ **無かったもの（豆腐になる）：⚠ `⚔ 🗡 🛡 🧪 🏹 🪖 🪵 🧱 🛏 ⛑ 📿 🏅 🎖 🤺 🦄`。**
⚠ **在ったものだけで組んだ。⚠ 思いついた絵文字が在るとは限らない＝足したら必ず `scenario=glyphs` を回すこと。**

### 2-2. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠⚠ **1本化** | ⚠ **`scripts/utils/glyphs.gd`（`class_name Glyphs`）。⚠ 絵文字を書く場所はここだけ。⚠ 画面のコードに直接書かない**（⚠ **フォントを替えるときここだけ直せば全画面が入れ替わる**） |
| ⚠ **戦闘のユニット** | ⚠ **`BattleUnit.master_id` を新設**（⚠ **味方＝`character_id` ／ 敵＝`enemy_type_id` ／ 召喚＝`summon_unit_id`。⚠ `unit_id`（"party_0"）とは別物**）。⚠ **`unit_view.tscn` に `GlyphLabel` を足して体の上に出す** |
| ⚠ **アイテム** | ⚠ **`ItemIcon` の左上に「種類」の絵文字**（⚠ **`IconConfig.glyph_font_size`。⚠ 0 で消せる**） |
| ⚠⚠ **漢字は残した** | ⚠ **中央の漢字2文字（`ui_icon_*` 78行）を置き換えていない**（⚠ **絵文字は種類ごと11種なので、⚠ 置き換えると91件が11種の見た目に潰れ、⚠ どの品か分からなくなる**）。⚠ **`ja.csv` は1行も書き換えていない** |
| ⚠ **ダンジョンのマス** | ⚠ **絵文字＋文字**（⚠ **絵文字だけにしない＝フォントが無い環境で全部同じ豆腐になる。⚠ 文字が保険**） |
| ⚠ **道具** | ⚠ **`scenario=glyphs` を新設**（⚠ **`Glyphs` の全定数を `has_char()` で見る。⚠ `E138`＝フォントに無い字がある ／ `W32`＝fallback が0本**） |

### 2-3. ⚠ 役割の分かれ方（**4つとも別のことを言っている**）

| どこ | 何を言うか |
|---|---|
| ⚠ **絵文字（左上）** | ⚠ **どの種類か**（⚠ 武器 / 頭 / 上半身 / 脚 / アクセ / 宝石 / 護符 / 紋章 / ルーン / 素材 / 消耗品） |
| ⚠ **漢字2文字（中央）** | ⚠ **どの品か**（⚠ 91件をひとつずつ） |
| ⚠ **数字（右下）** | ⚠ **何段か・何等級か** |
| ⚠ **背景の色** | ⚠ **等級（10色）** |

### 2-4. ⚠⚠ 人間が実機で見る項目（**4件。⚠ 設計役には絵が取れない**）

1. ⚠⚠ **絵文字が豆腐（□）になっていないか**（⚠ **色が付いて出るか。⚠ ここが一番見たいところ**）
2. ⚠ **戦闘で味方3人が 💂 🎯 😇、⚠ 敵が 🐸 🐺、⚠ ボスが 👑 で出るか**（⚠ **体の色は今までどおり青／赤／紫**）
3. ⚠ **倉庫のマスで、⚠ 左上に種類の絵文字・⚠ 中央に漢字2文字・⚠ 右下に数字が並ぶか**（⚠ **40px に3つ入って潰れていないか**）
4. ⚠ **ダンジョンのマスが「🔥 戦闘」「🎁 宝箱」「⛺ 休憩」「🔮 レリック」「🏰 ボス」「❓ ？」で出るか**

⚠ **潰れて読めないときは `IconConfig.glyph_font_size` を Inspector で下げる（⚠ 0 で絵文字だけ消せる）。**

### 2-5. ⚠ 平常値

⚠ **17シナリオとも据え置き**（⚠ **`glyphs` 0/1 を新設**）。
⚠ **`materials` 0/1 ／ `parts` 0/2 ／ `drops` 0/2 ／ `research` 0/1 ／ `workshop` 2/1 ／ `economy` 0/1 ／
`floor` 0/3 ／ `unlock` 1/1 ／ `layout` 0/1 ／ `passives` 0/1 ／ `presets` 0/1 ／ `runes` 0/1 ／ `area` 0/1 ／
`dungeon` 0/1 ／ `dungeon_battle` 0/1 ／ `inventory` 0/4。**
⚠ **`--import` の赤は1本（宿題70）。**

---

## 5. ✅ **段階19-b / 19-d を入れた（宝箱のマス・層の作り替え）**（2026-09-04）

### 3-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **19-b** | ⚠ **5つ目のノード種 `chest`。⚠⚠ 「その場で開く」（案A・人間の決定22・台帳 §4-10）** |
| ⚠ **口** | ⚠ **`open_dungeon_chest(node_id) -> {granted, left_behind}` ／ `was_dungeon_chest_opened(node_id)`** |
| ⚠ **画面** | ⚠ **`scenes/adventure/dungeon_chest.tscn` / `.gd` を新設**（⚠ **決定21「宝箱も別画面」。⚠ 部品は `ItemGrid` / `ItemDetail` を共有**） |
| ⚠⚠ **積まない** | ⚠ **拠点の `PENDING_CHESTS` に1件も積まない**（⚠ **実測 0 → 0。⚠ 積むと「死んでも宝箱は残る」＝決定7 が崩れる**） |
| ⚠ **配る口** | ⚠ **`_grant_dungeon_node_gains()` の1本のまま。⚠ 戻り値を `void` → `Dictionary` にした** |
| ⚠ **19-d** | ⚠ **層 6 → 8 ／ 各層 `[1,2,3,2,3,2]` → `[1,3,4,4,3,4,3,2]` ／ 分岐の密度を `DungeonConfig.branch_spread` のつまみに**（⚠ **既定 1 ＝前と同じ挙動**） |
| ⚠ **足した** | ⚠ **`E137`（開けても何も出ない）／ `W30`（宝箱のマスが1つも出ない）／ `W31`（層数と重みの長さがずれた）** |
| ⚠ **翻訳** | ⚠ **`ja.csv` に6行** |

### 3-2. ⚠ 取れた根拠（**`scenario=dungeon`**）

```
  'd_3_0' に立った / 鞄 1/8 ／ 通貨 12 ／ 開けたか=false（⚠ 踏んだだけでは開かない）
  開ける -> 入った { construction_material_2: 2, dungeon_potion_heal: 1, training_material_2: 2 }
		   ／ 鞄 1 -> 6 ／ 通貨 12 -> 32
  開けたか=true ／ ⚠ もう一度開ける -> 入った {  }（1マス1回）
  ⚠ 拠点の未開封の宝箱 0 -> 0（増えない＝全ロストの対象から外れない）
  --- 鞄が満杯のとき ---
  ⚠ 鞄が満杯（8/8）で開ける -> 入った {  } ／ 置いてきた { dungeon_potion_heal: 1, forging_material_2: 2, training_material_2: 2 }
  ⚠ 1個も入らなかったが開けたことは残る=true（⚠ 引き直せない）
```

### 3-3. ⚠ 動いていないことの確認

⚠ **`floor_1..5` は1文字も触っていない**（⚠ **`floor` の全ルート 15/25/25/30/40 本 ／ `economy` の `floor_1` 50.0 G が不動**）。
⚠ **`dungeon_map.tscn` 最小 524 x 552 → 524 x 648**（⚠ **層が2つ増えたぶん縦に伸びた。⚠ 720 に収まる**）。

### 3-4. ⚠⚠ 人間が実機で見る項目（**4件**）

⚠ **入り方：冒険選択 → 難ダンジョン**

1. ⚠ **マップが8層になっていて、⚠ 画面からはみ出していないか**（⚠ **前は6層**）
2. ⚠ **「🎁 宝箱」のマスが出るか。⚠ 踏むと別画面に移るか**
3. ⚠ **「開ける」を押すと中身がマス目で出て、⚠ 鞄に入るか。⚠ 押したあと「開ける」が消えること**
4. ⚠⚠ **鞄が満杯のときに開けると、⚠ 赤い字で「鞄が満杯で置いてきた個数」が出るか**（⚠ **中身は入らないのが正解**）

### 3-5. ⚠ この回で報告したもの

- ⚠⚠ **ズレ51**：⚠ **人間が「conceptsのなんかに書いてた」と言った通路の記述が、⚠ `docs/00_concept/` の8ファイルに1件も無い**（⚠ **勝手に直していない＝報告のみ。⚠ 狙いは決定23 で埋まった**）
- ⚠⚠ **宿題73**：⚠ **`dungeon_chest.tscn` を `LAYOUT_SCENES` に入れていない**（⚠ **`_ready()` の戻すガードで測定が止まる＝宿題72 と同じ穴**）。⚠ **`dungeon_relic_select` / `dungeon_shop` と合わせて3枚が測れていない**
- ⚠ **宿題74**：⚠ **`tests/test_common_infra.gd` の赤（`CHEST_TYPE`）は宿題70 のまま開いている**

---

## 0-A. ✅ **段階17-e-3 を入れた（レリックとショップを別画面へ）**（2026-09-04・この回）

### 0-A-1. ⚠⚠ 人間の決定21

⚠ **「遺物や宝箱やショップは別画面でするべきだと思う」「コンポーネントを使う」。**
⚠ **17-e / 17-e-2 では `dungeon_map` の中に並べていた。⚠ それを切り出した。**

| | 中身 |
|---|---|
| ⚠ **新規** | ⚠ **`scenes/adventure/dungeon_relic_select.tscn` / `.gd`**（⚠ **候補3件をマス目で出し、⚠ 押すと詳細。⚠ 全体用は「取る」、⚠ 1人用は3人ぶんのボタン**） |
| ⚠ **新規** | ⚠ **`scenes/adventure/dungeon_shop.tscn` / `.gd`**（⚠ **鞄に入る品はマス目、⚠ 鞄の枠とたいまつは行。⚠ 一時通貨と鞄の残りを上に出す**） |
| ⚠ **借りていない** | ⚠ **`floor_relic_select` / `floor_shop`**（⚠ **器も通貨も別。⚠ 台帳 §7**） |
| ⚠ **共有した** | ⚠ **部品だけ（`ItemGrid` / `ItemSlot` / `ItemDetail`）。⚠ `ItemDetail` にレリックの枝を足した**（⚠ **`SLOT_KIND_RELIC`。⚠ 名前と「全体／1人だけ」まで。⚠ 効果の文章は出さない＝宿題62 と同じ判断**） |
| ⚠ **`dungeon_map`** | ⚠ **レリックのマスを踏む → 遷移 ／ ボスの先 → 「商人と話す」ボタン。⚠ それだけになった** |
| ⚠ **翻訳** | ⚠ **`ja.csv` に3行** |

### 0-A-2. ⚠ 平常値

⚠ **`layout` 0/1 ／ `dungeon` 0/1 ／ `dungeon_battle` 0/1 ／ `inventory` 0/4 ／ `floor` 0/3 ／ `unlock` 1/1 ／ `workshop` 2/1 ／ `parts` 0/2 ／ `presets` 0/1 ／ `area` 0/1 ／ `economy` 0/1。**
⚠ **`dungeon_map.tscn` 最小 524 x 552（⚠ 節を2つ外したので少し縮んだ）。⚠ 倉庫は 1036 x 416 のまま。**

### 0-A-3. ⚠ 人間が実機で見る項目（**5件**）

1. ⚠ **レリックのマスを踏むと、⚠ 別画面に移って候補3件がマスで出るか**
2. ⚠ **マスを押すと下に名前と「全体」「1人だけ」が出るか。⚠ 取るとマップへ戻るか**
3. ⚠ **ボスを倒すと「商人と話す」が出て、⚠ 押すと別画面に移るか**
4. ⚠ **ショップでポーションのマスを押すと詳細と「買う(30)」が出るか。⚠ 鞄が満杯だと押せないこと**
5. ⚠ **「マップへ戻る」でマップに帰るか**（⚠ **買ったものが鞄・枠・たいまつに反映されているか**）

### 0-A-4. ⚠⚠ 宿題72（**この回で踏んだ。⚠ 道具の制約**）

⚠ **`scenario=layout` に新しい2枚を入れたら、⚠ 測定が終わらなくなった**（⚠ **8分で止めた**）。
⚠ **原因：⚠ どちらも `_ready()` で条件を満たさないと `SceneManager.change_scene()` でマップへ戻す。**
⚠ **測定ループは「開いて `add_child` して測る」だけなので、⚠ 戻されると `current_scene` が入れ替わって止まらない。**
→ ⚠ **2枚は `LAYOUT_SCENES` から外した。⚠ 最小幅は測れていない。**
→ ⚠ **測るなら「戻さない条件」を先に作る**（⚠ **レリック＝relic のマスに立つ ／ ショップ＝ボスを倒した先**）。

---

## 0-B. ✅ **段階17-e-2 を入れた（ランのレリック）**（2026-09-04）

### 0-A-1. ⚠⚠ 人間の決定20

⚠ **「レリックは簡単なほうのシナリオのほうと共有でいいよ」。**
⚠ **`relics.json` の12件をそのまま使う。⚠ ダンジョン専用の表は作らない。⚠ 効果の器（パッシブ）も共有。**

| | 共有するか |
|---|---|
| ⚠ **表（`relics.json` 12件）** | ✅ **共有** |
| ⚠ **効果の器（パッシブ）** | ✅ **共有** |
| ⚠ **置き場** | ❌ **別**（⚠ `DUNGEON_RUN_RELICS`。⚠ `FLOOR_RUN_RELICS` を借りない＝台帳 §7） |
| ⚠ **戦闘へ渡す口** | ❌ **別**（⚠ `get_dungeon_relic_passives()`。⚠ フロアの行と1本にまとめない） |
| ⚠ **選ぶ画面** | ❌ **別**（⚠ `dungeon_map` の中に出す。⚠ `floor_relic_select` は `FLOOR_RUN` を読む） |

### 0-A-2. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠⚠ **候補は「ノードごとに固定」。⚠ 状態の欄を増やさず、⚠ 種で引く** | ⚠ **引き直すと描き直すたびに候補が入れ替わる。⚠ 状態に持たせると欄が1つ増える。⚠ （ダンジョン・フロア・ノードID）から決まる種なら両方避けられる** |
| **2** | ⚠ **専用の画面を作らず `dungeon_map` の中に出した** | ⚠ **17-d の流儀（⚠ 鞄もショップも同じ画面）。⚠ 画面を増やすと戻り先が増える** |
| **3** | ⚠ **脱落しているキャラには付けられない** | ⚠ **そのランのあいだ戦闘に出ないので、⚠ 付けても効かない** |
| **4** | ⚠ **1マスにつき1つ**（⚠ `cleared` で覚える） | ⚠ **フロア側と同じ。⚠ 踏み直しても取れない** |

### 0-A-3. ⚠ 取れた根拠（**`scenario=dungeon`**）

```
  'd_3_0' の候補 = [relic_warding_stone, relic_iron_wall, relic_champion_blade]（3 件）
  ⚠ もう一度引く -> 同じ3件（⚠ 描き直しても入れ替わらない）
  ⚠ そのマスに居ないのに取る -> false
  レリックを取る relic_warding_stone（全体用） -> true / 所持 0 -> 1
  ⚠ 同じマスでもう1つ取る -> false（1マス1つ）
	char_priest / char_archer / char_swordsman に効くレリック = [relic_warding_stone]
```

⚠ **平常値**：⚠ **11シナリオとも据え置き**（⚠ `workshop` 2/1 ／ `unlock` 1/1 ／ `floor` 0/3 ／ `inventory` 0/4 ／ ほか 0/1）。
⚠ **`dungeon_map.tscn` 最小 524 x 556（基準 1280 x 720）。**

### 0-A-4. ⚠ 人間が実機で見る項目（**4件**）

1. ⚠ **レリックのマスを踏むと、⚠ 候補が3件出るか**（⚠ **アイコン＋名前＋「全体」「1人だけ」の別**）
2. ⚠ **「1人だけ」のレリックを選ぶと、⚠ 「誰に付けるか」の3人が出るか**
3. ⚠ **取ると候補が消え、⚠ 同じマスでもう一度取れないこと**
4. ⚠⚠ **取ったレリックが戦闘で効いているか**（⚠ **`relic_warding_stone` などパッシブの効果。⚠ 戦闘に入って確かめる**）

---

## 0-C. ✅ **段階17-e を入れた（ランのショップ・たいまつ・鞄の枠）**（2026-09-04。⚠ **画面は人間が確認済み**）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **ショップ** | ⚠ **`dungeon.json` に `shop` の表（品4件）**。⚠ **ボスを倒した先でだけ開く（決定15）。⚠ 途中では `get_dungeon_shop_entries()` が空を返す** |
| ⚠ **品揃え** | ⚠ **回復ポーション 30 ／ 蘇生ポーション 60 ／ 鞄の枠 +2 が 40 ／ たいまつ 35**（⚠ **全部仮置き**） |
| ⚠ **たいまつ** | ⚠ **`torch_reveal_layers = [1,2,3,5]`（Config・つまみ）。⚠ `is_dungeon_node_revealed()` が判定の1本。⚠ 見えないマスは「？」** |
| ⚠ **鞄の枠** | ⚠ **`DUNGEON_RUN_BAG_SLOTS` を増やす。⚠ 倉庫の `expand_inventory()` を借りていない（通貨も器も別）** |
| ⚠ **足した** | ⚠ **`E136`（知らない kind・値段0・items.json に無いID・枠0） ／ `W29`（ショップが無い・ポーションが1件も並ばない＝セーフティネットが消える）** |
| ⚠ **翻訳** | ⚠ **`ja.csv` に10行** |

### 0-A-2. ⚠⚠ 17-a の実装を1つ覆した（**たいまつ**）

⚠ **17-a は「たいまつはフロア単位で 0 に戻す」と書いて、そう実装していた。**
⚠⚠ **買える場所がボスの先のショップだけ（決定15）なので、⚠ 戻すと「買った瞬間に無駄になる品」だった**
（⚠ **そのフロアはもう全部踏み終わっている**）。
→ ⚠ **鞄・通貨・戦闘時 MAX HP と同じく持ち越す**（台帳 §4-7-1）。

### 0-A-3. ⚠ 取れた根拠（**`scenario=dungeon`**）

```
  ⚠ ボスの手前で get_dungeon_shop_entries() = 0 件（0 が正解＝店は途中に無い）
  たいまつ 等級0 / 1 層先まで見える（上限 等級3） ／ 見えているノード = 4 件
  --- ボスを倒した先のショップ ---
  品揃え = 4 件 ／ 一時通貨 = 152
	0: item  dungeon_potion_heal    30 ／ 断る理由 'bag'（⚠ 鞄が満杯だから）
	1: item  dungeon_potion_revive  60 ／ 断る理由 'bag'
	2: bag_slot                     40 ／ 断る理由 ''
	3: torch                        35 ／ 断る理由 ''
  鞄の枠を買う -> 枠 8 -> 10 ／ 通貨 152 -> 112
  たいまつを買う -> 見える層 1 -> 2
  次のフロアへ降りる -> たいまつ 等級1 -> 1（持ち越した） / 2 層先
	⚠ 入口で中身が見えているノード = 7 件（⚠ たいまつ 等級0 なら 4 件だった）
  ⚠ 通貨0で買う -> false / 理由 'currency'
```

⚠ **平常値**：⚠ **16シナリオとも据え置き**（⚠ `workshop` 2/1 ／ `unlock` 1/1 ／ `floor` 0/3 ／ `inventory` 0/4 ／ ほか）。
⚠ **`dungeon_map.tscn` 最小 524 x 552（基準 1280 x 720）。**

### 0-A-4. ⚠ 人間が実機で見る項目（**5件**）

⚠ **入り方：冒険選択 → 難ダンジョン → ボスまで進んで倒す**

1. ⚠ **ボスを倒す前は店が出ていないこと**
2. ⚠ **ボスを倒すと「ボスの間の商人」が4品出るか。⚠ 値段と「買う」が並ぶか**
3. ⚠ **鞄が満杯だとポーションの「買う」が押せないこと**（⚠ **払ってから入らないのを止めている**）
4. ⚠ **鞄の枠を買うとマス目が 8 → 10 に増えるか**
5. ⚠⚠ **たいまつを買って「もう1枚潜る」と、⚠ 次のフロアで遠くのマスの中身（戦闘/休憩/レリック）が見えるか**（⚠ **買う前は「？」**）

### 0-A-5. ⚠ 残したもの

⚠ **レリック（`relic` ノード）はまだ何も起きない＝17-e-2。⚠ 台帳の 17-e から切り出した。**

---

## 0-D. ✅ **段階18-e を入れた（枠の拡張と捨てる口）**（2026-09-04。⚠ **画面は人間が確認済み**）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **拡張** | ⚠ **`INVENTORY_EXTRA_SLOTS`（買った枠・状態1本）＋ `expand_inventory()`**。⚠ **ゴールドで1回1ページ（100マス）。⚠ 2000 G から 2000 G ずつ上がる。⚠ 上限10ページ** |
| ⚠ **捨てる** | ⚠ **`discard_inventory_slot(index)`**（⚠ **マスの番号で呼ぶ。⚠ 1個ずつ。⚠ 戻りは無い。⚠ 確認モーダル**） |
| ⚠ **画面** | ⚠ **倉庫のヘッダに「枠を買う(2000)」／ 選んだマスの操作に「捨てる」** |
| ⚠ **足した** | ⚠ **`W28`（拡張がタダの設定）。⚠ `E135` に「買っても増えない」「1ページも持てない」を追加** |
| ⚠ **翻訳** | ⚠ **`ja.csv` に4行** |

### 0-A-2. ⚠ 設計役が決めたこと（**人間の返事を待たずに進めた。⚠ 覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠ **対価はゴールド**（⚠ 研究ではない） | ⚠ **研究は「戦闘の強化」。⚠ 倉庫の枠を混ぜると枝の意味が濁る。⚠ ゴールドは使い道が1つ増えるだけ** |
| **2** | ⚠ **1回1ページ（100マス）** | ⚠ **「6ページ目が開く」で増えたことが見える。⚠ 20マスずつだと分からない** |
| **3** | ⚠ **上限10ページ（1000マス）** | ⚠ **無限に買えると「有限である」意味が消える** |
| **4** | ⚠ **捨てるのは1個ずつ。⚠ 装備も捨てられる** | ⚠ **1マス＝1個だから。⚠ 装備は「素材にする」のほうが得だが、⚠ 逃げ道は塞がない** |

### 0-A-3. ⚠ 取れた根拠（**`scenario=inventory`**）

```
  いま 500 マス / 5 ページ ／ 次の拡張 2000 G ／ 断る理由 'gold'
  ⚠ 所持金0で買う -> false / 理由 'gold' / マス 500（増えない）
  2000 G で買う -> true / マス 500 -> 600 ／ ページ 5 -> 6 ／ 所持金 0
  2回目の値段 = 4000 G（1回目より高い）
  マス目の長さ = 600（上限と同じ＝並びが枠に追従している）
  上限まで買う -> 10 ページ / 理由 'max' / 値段 0
  0 番（stamina_potion）を捨てる -> true / 使用 316 -> 315
  ⚠ 空のマスを捨てる -> false ／ マスの外（-1） -> false
```

⚠ **平常値**：⚠ **15シナリオとも据え置き**（⚠ `workshop` 2/1 ／ `unlock` 1/1 ／ `floor` 0/3 ／ `parts` 0/2 ／ `drops` 0/2 ／ ほか 0/1。⚠ `inventory` 0/4）。⚠ **倉庫の最小 1036 x 416 も変わらず。**

### 0-A-4. ⚠ 人間が実機で見る項目（**3件。⚠ 18-e ぶんだけ**）

1. ⚠ **倉庫のヘッダに「枠を買う(2000)」が出て、⚠ 所持金が足りないと押せないこと**
2. ⚠ **買うとページが 5 → 6 に増え、⚠ 所持金が減ること**
3. ⚠ **マスを選んで「捨てる」→ 確認が出て、⚠ 「はい」で1個減ること**（⚠ **「いいえ」で減らないこと**）

### 0-A-5. ⚠ この回で踏んだこと（**道具側**）

⚠ **満杯を作る手順が「初期のマス数」だけを下げていて、⚠ 買った拡張ぶん（+500マス）を数えていなかった。**
⚠ **その結果「満杯なのに6本の口が全部通る」状態になり、⚠ ポモドーロの検証が赤を出した**（⚠ **検証が効いていた**）。
⚠ **道具側で拡張ぶんも 0 に戻すよう直した。⚠ 本番コードは触っていない。**

---

## 0-E. ✅ **段階18-d を入れた（ダンジョンの鞄をマス目に）**（2026-09-03。⚠ **画面は人間が確認済み**）

| | 中身 |
|---|---|
| ⚠ **口** | ⚠ **`GameManager.get_dungeon_bag_slot_layout()`**（⚠ **長さ＝鞄の枠。⚠ 空きマスは空の Dictionary**） |
| ⚠⚠ **借りていない** | ⚠ **倉庫の `get_inventory_slot_layout()` を呼んでいない**（⚠ **器が別＝`PLAN_HARD_DUNGEON.md` §7。⚠ 借りると鞄に拠点の持ち物が並ぶ**）。⚠ **共有したのは部品3つだけ（`ItemSlot` / `ItemGrid` / `ItemDetail`）** |
| ⚠ **画面** | ⚠ **`dungeon_map.gd` の「回薬 x1」の行を8マスのマス目に。⚠ 押す→詳細＋「誰に使うか」（⚠ 倉庫と同じ流儀）** |
| ⚠ **足した** | ⚠ **`SLOT_ENTRY_COUNT`**（⚠ **鞄の個数。⚠ ラン専用の品は拠点に1個も無いので、⚠ 拠点の所持数を引くと 0 と出る**） |

⚠ **取れた根拠**：

```
  鞄のマス目 = 8 マス（枠 8 と同じ） / 中身 8（使用 8 と同じ）
	先頭のマス = construction_material_1 ／ 鞄の個数 = 2（⚠ 拠点の所持数ではない）
  dungeon_map.tscn 最小 524 x 548（基準 1280 x 720）… 収まる
```

⚠ **平常値**：⚠ **`layout` 0/1 ／ `dungeon` 0/1 ／ `inventory` 0/4。⚠ `--import` の赤は1本（宿題70）。**

⚠ **人間が実機で見る項目（4件）**：

1. ⚠ **ダンジョンの鞄が8マスで出て、⚠ 空きマスが見えるか**（⚠ **あと何個拾えるかが分かること**）
2. ⚠ **マスを押すと詳細（回復ポーションの説明）が出るか**
3. ⚠ **「誰に使うか」の3人ボタンが出て、⚠ 押すと効いてマスが減るか**
4. ⚠ **戦利品のマス（素材）を押しても、⚠ 使うボタンが出ないこと**

---

## 0-F. ✅ **段階18-c / 18-c-2 / 18-f を入れた（マス目・詳細・ドラッグ）**（2026-09-03。⚠ **画面は人間が全部確認済み**）

### 0-A-0-b. ⚠⚠ 18-f：ドラッグ＆ドロップで動かせる（**人間の指示**）

⚠ **人間の言葉：「ドラッグアンドドロップで動かせるようにしたい」。**

| | 中身 |
|---|---|
| ⚠ **並びの器** | ⚠ **`GameStateKeys.INVENTORY_ORDER`（トップレベル・1本）。⚠ 長さ＝マス数。⚠ `""` が空きマス** |
| ⚠ **動かす口** | ⚠ **`GameManager.move_inventory_slot(from, to)` の1本。⚠ 交換（swap）。⚠ 挿入にしない** |
| ⚠ **部品** | ⚠ **`ItemSlot` に `_get_drag_data` / `_can_drop_data` / `_drop_data`**（⚠ **プロジェクト初のドラッグ。⚠ Godot の仕組みをそのまま使った**） |
| ⚠ **荷物** | ⚠ **「何番目のマスか」だけ。⚠ 中身は入れない**（⚠ **落ちるまでに壊された品が荷物の中で生き続けるのを防ぐ**） |
| ⚠ **作り直し** | ⚠ **拾ったもの＝前から空きマスへ ／ 無くなったもの＝マスを空ける ／ ⚠ 穴は詰めない** |

### 0-A-0-c. ⚠⚠ ズレ50：`slot_position` は使えなかった

⚠ **`INVENTORY` の `slot_position` は「item_id ごとに1つ」。⚠ ポーション5個は5マスなのに位置は1つしか持てない。**
⚠ **＝「1マス＝1個」（人間の決定3）の並びを表現できない。⚠ 欄と `update_inventory_slot_position()` を消した**（⚠ **呼び出し元は元から0件**）。
⚠ **18-a の時点では「欄がもう在る」と報告していた。⚠ 使えるかまでは見ていなかった。**

⚠ **取れた根拠**：

```
  マス目の長さ = 500（上限と同じ） / 中身のあるマス = 315
  0 -> 325 へ動かす -> true / そのマス = 'weapon_iron_sword' / 0 番は（空）
  もう一度読む -> 0 番は（空）のまま（＝並びを覚えている）
  1個拾う -> 使用 315 -> 316 / 0 番に入った（空きの先頭）
  ⚠ マスの外へ動かす（-1 / 500） -> false / false
  装備する（1 番の eq_2） -> true / そのマス =（空）（決定7 と噛み合っている）
```



### 0-A-0. ⚠⚠ 18-c-2：押したら詳細が出る（**人間の要望・同じ回で入れた**）

⚠ **人間の言葉：「clickしたら装備の詳細などが見れるようにしたい」。**
⚠ **`ItemDetail`（共有部品・`scenes/ui/components/item_detail.gd`）を新設した。⚠ 宿題62 と同じ穴。**

| 何を選んだか | 出る行 |
|---|---|
| ⚠ **装備の個体** | ⚠ **名前と等級 ／ ステータス（0 の軸は出さない）／ 刺さっている装飾の枠（空きも）／ 装備中のキャラ ／ 素材にしたときの戻り ／ 次の等級のコスト** |
| ⚠ **装飾** | ⚠ **名前と個数 ／ 効く軸と幅（⚠ 出目は刺すときに振れるので確定値ではない）／ 壊した戻り ／ 段階を上げるコスト** |
| ⚠ **ルーン** | ⚠ **名前と個数 ／ 重ねるコスト**（⚠ **壊しても素材にならないので戻りの行は出さない**） |
| ⚠ **全部** | ⚠ **説明文**（⚠ **ja.csv に `ui_desc_<item_id>` が在るときだけ。⚠ 無ければ行ごと出ない**） |

⚠⚠ **数字は1つも組み立て直していない**（⚠ **`get_instance_stats()` / `get_part_stat_value()` / `get_dismantle_refund_total()` / `get_forge_cost()` に聞くだけ。⚠ 画面と本体で数字が食い違う穴を作らない**）。

⚠ **取れた根拠**（⚠ **絵は取れないが「出る行」は取れる＝これが唯一の確かめ方**）：

```
  選んでいないとき: ["マスを選ぶと、ここに詳細が出ます"]
  part_charm_def_1 … 物理防御の護符① x5 ／ 物理防御 +3〜4 ／ 壊す 3 ／ 段階を上げる 飾り石 10
  part_rune_buff_1 … 力のルーン① x5 ／ 重ねる 2
  weapon_iron_sword（等級4・装飾を1つ刺した）
	  鉄の剣  等級4
	  攻撃 +17
		[1] 攻撃の宝石①  攻撃 +3
		[2] （空き）
	  素材にする 19
	  鍛える 鍛冶の塊 20
  stamina_potion … スタミナポーション x3 ／ スタミナを回復する。冒険に行く回数が増える
```

⚠ **`ja.csv` に `ui_desc_*` を3件足した**（スタミナポーション・ランのポーション2件）。⚠ **残りは要るときに足す。**


### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **倉庫の持ち物タブ** | ⚠ **`ItemGrid` に差し替えた**（⚠ **20列 × 5行 ＝ 100 マス／ページ・5ページ**）。⚠ **上に「マス 316/500」とページ送り（◀ 1/5 ▶）** |
| ⚠⚠ **`ScrollContainer` をやめた** | ⚠ **タブの中身を `VBoxContainer` にした。⚠ これで `scenario=layout` が中身を測れる**（⚠ **108 x 80 → 1036 x 416**） |
| ⚠ **操作** | ⚠ **マスを押す → 下に「できること」が出る**（⚠ **素材にする／壊す／段階を上げる／重ねる。⚠ 中身は前と同じ**）。⚠ **押した瞬間に走らせない**（⚠ マス目は押し間違えやすい） |
| ⚠ **装備画面** | ⚠ **キャラの装備マス5枠を1行で出した**（⚠ **人間の決定7 が目に見える形になった。⚠ `get_equipment_slot_entries()` の呼び出し元がゼロでなくなった**） |
| ⚠ **翻訳** | ⚠ **`ja.csv` に4行**（`ui_warehouse_capacity` / `page_prev` / `page_next` / `select_hint`） |
| ⚠ **消した** | ⚠ **`_create_inventory_entry()` / `_create_instance_entry()`**（⚠ **行を積む古い描き方。⚠ 残すと2本目の描き方になる**） |

### 0-A-2. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠⚠ **`ItemGrid` の合図に「何番目のマスか」を足した**（`slot_pressed(entry, index)`） | ⚠⚠ **空のマスは中身が全部同じ（空の Dictionary）。⚠ 中身だけで照合すると、⚠ どの空き枠を押しても最初の1つが選ばれる。⚠ 装備の5枠で実際に踏んだ** |
| **2** | ⚠ **装備画面は「マス目を足す」だけ。⚠ 既存の行は消していない** | ⚠ **装飾・ルーン・鍛冶の操作をあちらが持っている。⚠ 操作を2箇所に増やさない。⚠ マスは「見える形」と「部位を選ぶ」だけ** |
| **3** | ⚠ **持ち物タブから `ScrollContainer` を外した** | ⚠ **中が測れないという既知の穴（宿題68）。⚠ 20列 × 5行に固定したので、⚠ もう縦にも横にも伸びない** |
| **4** | ⚠ **図鑑・宝箱タブは触っていない** | ⚠ **マス目にするのは持ち物だけ（台帳 §5-1）** |

### 0-A-3. ⚠ 取れた根拠（**`scenario=layout`。⚠ 画面の絵は取れない**）

```
  warehouse_screen.tscn   最小 1036 x 416（基準 1280 x 720）… 収まる
	⚠ 前は 108 x 80（＝ScrollContainer の中が測れていなかった）
  equipment_screen.tscn   最小  889 x 527 … 変わらず（5枠のマスは Scroll の中）
```

⚠ **平常値**：⚠ **`layout` 0/1 ／ `inventory` 0/4 ／ `parts` 0/2 ／ `presets` 0/1 ／ `runes` 0/1 ／ `workshop` 2/1 ／ `drops` 0/2 ／ `materials` 0/1**。

### 0-A-4. ✅ 宿題68 は倉庫については閉じた

⚠ **「アイコンを足した回の横幅が測れない」（2026-08-31）は、⚠ 倉庫の持ち物タブについては測れるようになった。**
⚠ **ショップ・装備の一覧・レリック選択は `ScrollContainer` のままなので、⚠ そちらは開いたまま。**

> ⚠⚠ **2026-09-05 に1枚増えた**：⚠ **ダンジョンのマップ（`dungeon_map.tscn`）。⚠ 段階19-e で
> `ScrollContainer` を入れたため（⚠ 人間の指示「画面をスクロールできるように」）。
> ⚠ 層数が可変なので、⚠ 倉庫のように固定グリッドにして解くことはできない。**
> ⚠ **代わりに「引いた線の本数」を取れるようにした**（⚠ `DungeonEdgeLines.get_line_count()`）。

### 0-A-5. ⚠⚠ 人間が実機で見る項目（**8件。⚠ 設計役には取れない**）

⚠ **入り方：拠点 → ギルド → 倉庫（持ち物タブ）／ 装備画面**

1. ⚠ **持ち物タブがマス目になっていて、⚠ 20列 × 5行 が画面からはみ出していないか**
2. ⚠ **上に「マス ◯/500」と「◀ 1 / 5 ▶」が出ているか。⚠ ◀ が1ページ目で押せないこと**
3. ⚠ **▶ を押すと次のページに変わり、⚠ 中身が入れ替わるか**（⚠ **物が少ないと2ページ目は空マスだけ**）
4. ⚠ **空のマスが薄く出ているか**（⚠ **「まだ入る」ことが見えること**）
5. ⚠ **マスを押すと下に名前と「できること」が出るか。⚠ 押しただけでは何も起きないこと**
6. ⚠ **「素材にする」「壊す」「段階を上げる」「重ねる」が前と同じように効くか**
7. ⚠⚠ **装備画面の上に5マス（頭・上半身・脚・武器・アクセサリー）が並び、⚠ 着けているものがそこに出るか**
8. ⚠⚠ **装備すると倉庫のマスから消えること。⚠ 外すと戻ってくること**（⚠ **決定7 が画面で成り立っているか**）
9. ⚠⚠ **マスを押すと、下に詳細が出るか**（⚠ **装備なら等級・ステータス・刺さっている装飾の枠。⚠ 装飾なら効く軸と幅**）
10. ⚠ **スタミナポーションを押すと説明文が1行出るか**（⚠ **`ui_desc_*` を書いていない品は説明が出ないのが正解**）

⚠ **ドラッグの4項目は §1-1。**

---

## 0-G. ✅ **段階18-b を入れた（容量の器と入口6本）**（2026-09-03）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **新規** | ⚠ **`InventoryConfig`（`initial_slots` 120・仮置き）＋ `.tres` ＋ `balance.tscn` に1行**（⚠ **人間の Inspector 作業ゼロ**） |
| ⚠ **口** | ⚠ **`get_inventory_slot_max()` / `get_inventory_free_slots()` / `can_accept_inventory()`**（⚠ **拡張ぶんを足すのも 18-e でここ1本**） |
| ⚠⚠ **変更** | ⚠ **`add_to_inventory()` の戻り値を `void` → `int`（実際に入った数）**。⚠ **満杯なら 0 と `W25`。⚠ ここは最後の砦であって判定の場所ではない** |
| ⚠ **入口5本** | ⚠ **宝箱＝開けさせない ／ ショップ＝買わせない ／ 作業場＝キューに残す ／ ポモドーロ＝集中の端数へ戻す ／ ダンジョンの撤退＝入るぶんだけ＋`left_behind`** |
| ⚠ **足した** | ⚠ **`E135`（割り当て漏れ・`initial_slots` が 0）／ `W25`（入り切らなかった）** |
| ⚠ **道具** | ⚠ **`scenario=inventory` に「満杯にしてから6本を叩く」節**（⚠ **`parts` は上限を外してから測る形に直した**） |

### 0-A-2. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠⚠ **ポモドーロで入らなかったぶんは「集中の端数」へ戻す** | ⚠ **集中した時間はやり直せない。⚠ 倉庫を空ければ次のポモドーロで受け取れる。⚠ 台帳の初版は「消耗品はマスの外だから安全」と書いていたが、⚠ 決定5 でその前提が消えた** |
| **2** | ⚠ **中身が素材だけの宝箱は満杯でも開ける** | ⚠ **素材はマスを使わない（決定5）。⚠ 締めると理由なく開けなくなる** |
| **3** | ⚠ **作業場の抽選は `rolls` の数だけ多めにマスを要求する** | ⚠ **引く前に中身が分からない。⚠ 足りないより安全側へ倒す** |
| **4** | ⚠ **`Balance.inventory` が null のときは上限 9999** | ⚠ **0 を返すと、割り当て漏れの日から「何も拾えないゲーム」になり、⚠ 原因が容量だと気づけない** |

### 0-A-2b. ⚠⚠ 人間の決定7（**同じ回で実装した**）

⚠ **「キャラに装備すると、⚠ インベントリのマスから外れてキャラの装備マスへ移る」。**

- ⚠ **`get_inventory_slot_entries()` は装備中の個体を返さなくなった**（⚠ **同じ個体が2マスを占めない**）
- ⚠ **`get_equipment_slot_entries(character_id)` を新設**（⚠ **5枠。⚠ 空の枠も返す**）
- ⚠⚠ **外すときだけ容量の判定が要る＝口が7本目**（⚠ **満杯なら外させない。⚠ 外してから置き場が無いと個体が宙に浮く**）
- ⚠ **着けるときは判定が要らない**（⚠ **着ける個体がマスから出て、⚠ 前のものが戻る＝差し引き 0 以下**）

```
  装備する -> true / インベントリ 11 -> 10 マス（1 減った）
  キャラの装備マス = 5 枠 / 埋まっている = 1 枠（head=armor_iron_helm・他は空）
  外す -> true / インベントリ 10 -> 11 マス（元に戻った）
  ⚠ 満杯（10/10）で外す -> false / まだ装備している = true
```


### 0-A-2c. ⚠⚠ 人間の決定8（**同じ回で実装した**）

⚠ **「取りあえず500作っとこう」「100ますを5ページ　20×5×5」。**
⚠⚠ **未決7（重ねないと装飾でマスが埋まる）と未決3（初期のマス数）が同時に閉じた。**

```
[GameManager] inventory config validated: 500 マス ＝ 100 マス（20列 × 5行）× 5 ページ, 0 errors
  0 ページ目 … 中身 100 件 ／ 1 ページ目 … 100 件 ／ 2 ページ目 … 100 件
  3 ページ目 … 中身 16 件 ／ 4 ページ目 … 0 件
  全ページの合計 316 件 ＝ 使用中 316 マス（一致）
  ⚠ 範囲外のページ（-1 と 5） -> 0 件 / 0 件
```

⚠ **`W26`＝`initial_slots` が1ページのマス数で割り切れないとき**（⚠ **最後のページが半端になる。⚠ 壊れはしない**）。

### 0-A-3. ⚠ 取れた根拠（**`scenario=inventory` の出力。⚠ 全部そこから写した**）

```
  満杯にした： 315/315 ／ 空き 0
  ① add_to_inventory              -> 0 個入った（0 が正解）
  ② open_chest（持ち物が入る宝箱） -> false ／ 未開封 1 -> 1（減らない）
  ③ purchase_shop_item            -> false ／ 所持金 1000099（払っていない）
  ④ collect_craft                 -> false ／ キュー 1 件（残る）
  ⑤ grant_stamina_potions(50 分)  -> 0 個 ／ 集中の端数 0 -> 50 分（⚠ 時間が戻った）
  ⑥ retreat_from_dungeon          -> 持ち帰り 素材3件 ／ ⚠ 置いてきた { weapon_iron_sword: 2 }
```

### 0-A-4. ⚠⚠ ズレ49（**台帳の推測が2件とも外れていた**）

| | 台帳の初版 | ⚠ 実コード |
|---|---|---|
| **装飾の段階上げ** | ⚠ **「材料は消えるのに結果が入らない＝一番危ない経路」** | ⚠⚠ **溢れない。⚠ `upgrade_part()` も `merge_runes()` も「複数消して1個足す」＝マスは純減する。⚠ 手を入れていない** |
| **ポモドーロ** | ⚠ **「消耗品はマスの外だから自動的に解決する」** | ⚠ **決定5 で消耗品はマスに入る。⚠ 前提が消えていた。⚠ 別の解き方（端数へ戻す）に差し替えた** |

⚠ **どちらも「ドキュメントの記述を信じない・`grep` で中身を見る」（CLAUDE.md 1番）で見つけた。**

### 0-A-5. ⚠ 平常値（**動いていないことの確認**）

⚠ **`materials` 0/1 ／ `parts` 0/2 ／ `drops` 0/2 ／ `research` 0/1 ／ `workshop` 2/1 ／ `economy` 0/1 ／ `floor` 0/3 ／ `unlock` 1/1 ／ `layout` 0/1 ／ `passives` 0/1 ／ `presets` 0/1 ／ `runes` 0/1 ／ `area` 0/1 ／ `dungeon` 0/1 ／ `dungeon_battle` 0/1**。
⚠ **`inventory` は 0/4**（⚠ **黄は `W25` 3本＝満杯の証拠と、⚠ `skill_dbg_dot_odd` の1本**）。
⚠ **一度 `parts` が 1/62 に動いた**（⚠ **装飾を1種300個ずつ配る下ごしらえが容量で弾かれた**）。⚠ **道具側で上限を外して 0/2 に戻した。**

---

## 0-H. ✅ **段階18-a を入れた（マス1個の部品とマス目の器）**（2026-09-03）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **台帳** | ⚠ **`docs/01_plan/PLAN_INVENTORY.md` を新設**（⚠ **人間の決定6件。⚠ 段階を6つに割った**） |
| ⚠ **口** | ⚠ **`GameManager.get_inventory_slot_entries()` / `get_inventory_slots_used()`**（⚠ **「何がマスを1つ占めるか」を決める唯一の口。⚠ 画面側で `item_type` を見て数え直さない**） |
| ⚠ **新規** | ⚠ **`scenes/ui/components/item_slot.tscn` / `.gd`（`class_name ItemSlot`）**（⚠ **中身の絵は `ItemIcon` に任せる。⚠ 空のマスも同じ部品で描く**） |
| ⚠ **新規** | ⚠ **`scenes/ui/components/item_grid.gd`（`class_name ItemGrid`）**（⚠ **`.tscn` を持たない。⚠ 置く側が `columns` を決める。⚠ 中身が枠を超えたら赤を出す＝黙って切り捨てない**） |
| ⚠ **道具** | ⚠ **`scenario=inventory` を1行**（⚠ **`SCENARIOS` に1行・`_report_inventory()`。⚠ シーンは増やしていない**） |

### 0-A-2. ⚠ 人間の決定（**2026-09-03**）

1. ⚠ **拠点の倉庫にも容量を持たせる（拡張可）** … ⚠⚠ **`PLAN_HARD_DUNGEON.md` §4-1「無限のまま」を覆した**
2. ⚠ **同じ品は重ねない（1マス＝1個）**
3. ⚠ **汎用素材はマスに入れない。⚠ 消耗品は入れる**
4. ⚠ **段階18 を難ダンジョンの 17-e より先にやる**（⚠ **「インベントリはダンジョンでも使うし」**）

### 0-A-3. ⚠ 取れた根拠（**`scenario=inventory` の出力。⚠ 全部そこから写した**）

```
  最初の状態： 0 マス
  素材を 999 個足す -> 0 マス（増えないのが正解） / 所持 1004 個
  消耗品を 3 個足す -> 3 マス（+3 が正解＝重ねない）
  装飾を 5 個足す   -> 8 マス（+5 が正解）
  装備を 2 本足す   -> 10 マス（+2 が正解＝個体）
  内訳 = { "item": 8, "instance": 2 } ／ 合計 10 マス
  ⚠ 装飾 61 種を 5 個ずつ持つと -> 315 マス
  ⚠ 重ねれば 67 マスで済む（種類ぶん）
  ItemGrid: 中身 315 件 / 枠 320 -> マス 320 個 / 空き 5 個
  先頭のマス is_empty=false ／ 末尾のマス is_empty=true
```

### 0-A-4. ⚠⚠ この回で一番大事な数字（**未決7・18-b の入力**）

⚠ **装飾を「61種 × 5個」持つだけで 315 マス。⚠ 重ねれば 67 マス。⚠ 差は約 4.7 倍。**

- ⚠ **決定3（重ねない）＋決定5（装飾もマスに入る）の組み合わせがそのまま出ている**
- ⚠ **初期のマス数をどこに置いても、⚠ 作業場のくじを回すと埋まる**（⚠ **くじは1回1個**）
- ⚠ **裁き方は3つ**：⚠ **①消耗品と装飾だけ重ねる例外を入れる ②マス数を大きく取る（数百） ③売る/捨てるを厚くする**
- ⚠ **18-b の前に決めなくてよい**（⚠ **容量の器は数え方が変わっても壊れない**）。⚠ **ただし 18-c で人間が見る前には決まっていること**

### 0-A-5. ⚠ 平常値

⚠ **`inventory` 赤0 黄1 ／ `--import` の赤は1本（宿題70）。⚠ ほかのシナリオは触っていない。**

---

## 0-I. ✅ **段階17-d を入れた（ダンジョンの画面）**（2026-09-02。⚠ **画面12項目は 2026-09-03 に人間が確認済み**）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **新規** | ⚠ **`scenes/adventure/dungeon_map.tscn` / `.gd`**（⚠ **人間の決定：「新しいものを作る」。⚠ `floor_map` からコードを1行も共有していない＝台帳 §7**） |
| ⚠ **画面の中身** | ⚠ **層構造（下が入口・上がボス）／ 3人の「戦闘時 MAX HP ／ 素の MAX HP」と脱落の赤 ／ フロア・一時通貨・鞄 ／ ポーションを誰に使うか ／ 続行・撤退・その場で降りる・拠点へ** |
| ⚠ **入口** | ⚠ **冒険選択のフッターに「難ダンジョン」ボタン1個**（⚠ **一覧の行にしなかった理由は下の決め1**）。⚠ **ランの途中なら「続きから」** |
| ⚠ **戦闘との往復** | ⚠ **`TransferKeys.DUNGEON_NODE_ID` を渡して戦闘へ**（17-b で入れた口）。⚠ **戦闘から戻る先を差し替えた**（⚠ **ランが続いていればマップへ、⚠ 死亡していれば冒険選択へ**） |
| ⚠ **翻訳** | ⚠ **`ja.csv` に29行（`ui_dungeon_*`）。⚠ `--import` も設計役が回した** |
| ⚠ **道具** | ⚠ **`scenario=layout` の `LAYOUT_SCENES` に1行**（⚠ **測るためにランへ入れてポーションを鞄に入れてから開く**）。⚠ **ラン専用の品を拠点の倉庫に入れないよう、⚠ `_report_layout` の持ち物投入から除外した** |

### 0-A-2. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠⚠ **入口をステージ一覧の行ではなく、⚠ フッターのボタンにした** | ⚠⚠ **一覧に「見出し＋1行」を足したら、⚠ 冒険選択が縦に溢れた（実測 708 → 784。⚠ 基準 720）。⚠ 検証用ステージ5本と合わせるとフッターが画面の外へ出る。⚠ ボタンなら 708 のまま**（⚠ **横は 260 → 368**） |
| **2** | ⚠ **画面のコードを `floor_map.gd` と共有しなかった** | ⚠ **人間の決定（「新しいものを作る」）。⚠ 読むキーが別の定数で、⚠ 仕様も別（スタミナ無し・引き返さない・撤退はボスの後だけ・全ロストがある）** |
| **3** | ⚠ **ポーションの行は「品 × 3人ぶんのボタン」** | ⚠ **誰に使うかを1行で選べる。⚠ 効く相手の判定は書かない（`use_dungeon_item()` が弾く＝判定を2箇所にしない）** |
| **4** | ⚠ **脱落したキャラの行を消さず、⚠ 赤で「脱落」と出す** | ⚠ **消すと「誰が欠けたか」が分からないまま3人目で死亡する** |
| **5** | ⚠ **`dungeon_run_changed` が `""` で飛んできたら描き直さない** | ⚠ **撤退・全ロストの直後。⚠ 描き直すと空のマップになる（遷移は押した側がする）** |
| **6** | ⚠ **`relic` ノードは踏めるが何も起きない**（⚠ **メッセージだけ**） | ⚠ **レリックは 17-e。⚠ 踏めなくすると行き止まりになり、⚠ そのルートを選んだ人がボスに着けない** |

### 0-A-3. ⚠ 取れた根拠（**`scenario=layout`。⚠ 画面の絵は取れないので数字だけ**）

```
  dungeon_map.tscn        最小 524 x 558（基準 1280 x 720）… 収まる
  adventure_select.tscn   最小 368 x 708（基準 1280 x 720）… 収まる
	⚠ 一覧に行を足した場合は 260 x 784 で ⚠ 縦にはみ出した（+64）。⚠ だからボタンにした
```

⚠ **`dungeon_map` は「ランに入った状態＋鞄にポーション2件」で開いて測っている**（⚠ **空だとポーションの行が1行も出ず、⚠ 一番横に長い行を測れない**）。

### 0-A-4. ⚠ 平常値（**動いていないことの確認**）

⚠ **`dungeon` 0/1 ／ `dungeon_battle` 0/1 ／ `floor` 0/3 ／ `economy` 0/1 ／ `unlock` 1/1 ／ `passives` 0/1 ／ `layout` 0/1**。
⚠ **`scenario=floor` の全ルート数 15 / 25 / 25 / 30 / 40 本は前と同じ。⚠ `--import` の赤は1本（宿題70）。**
⚠ **`ja.csv` の再インポートは「済んでいる」**（`scenario=passives` の合図）。

### 0-C-5. ✅ 人間が実機で見た項目（**2026-09-03・12項目とも確認済み**）

⚠ **入り方：冒険選択 → フッターの「難ダンジョン」ボタン。**

1. ⚠ **冒険選択のフッターに「難ダンジョン」が出ていて、⚠ 押すとマップに入れるか**（⚠ **ボタンが4個並ぶ。⚠ 横に溢れていないか**）
2. ⚠ **マップの層が下から上に並び、⚠ いま居るマスに `▶`、⚠ 通ったマスに `✓` が付くか**
3. ⚠ **押せるのは1つ上の層だけか**（⚠ **横のマス・下のマスは押せないこと＝引き返さない**）
4. ⚠ **3人の HP が「戦闘時MAX/素」で出ているか。⚠ 戦闘のあとに数字が減るか**（⚠ **これが目減り**）
5. ⚠ **脱落したキャラが赤く「脱落」と出るか。⚠ その人が次の戦闘に出ていないこと**
6. ⚠ **戦闘マスを押す → 戦闘 → 「戻る」でマップへ帰ってくるか**（⚠ **負けたときは冒険選択へ帰る**）
7. ⚠ **ポーションの行に「回薬 x1」とアイコンが出て、⚠ 3人ぶんのボタンが並ぶか。⚠ 押すと HP が増えて鞄が減るか**
8. ⚠ **効かない相手に押したときに「その相手には効かない」と出て、⚠ 鞄が減らないこと**
9. ⚠ **ボスに勝つまで「もう1枚潜る」「持ち帰る」が出ていないこと。⚠ 勝ったあとに出ること**
10. ⚠ **「持ち帰る」で拠点の倉庫が増え、⚠ ポーションは消えていること**（⚠ **ラン専用は持ち帰れない**）
11. ⚠ **「その場で降りる」で鞄が全部消えること**
12. ⚠ **休憩マスを踏むと「休憩した」と出て、⚠ HP が増えること**

### 0-A-6. ⚠ この回で見えたこと

- ⚠ **冒険選択はもう縦の余裕がほとんど無い**（⚠ **708 / 720**）。⚠ **次に1行足すと溢れる。⚠ 検証用ステージ5本を消すか、器を見直すことになる**
- ⚠ **`relic` ノードは踏み損**（17-e で埋まる）。⚠ **`rest` は 17-c で効くようになっている**

### 0-A-7. ⚠ 宿題

- ⚠ **宿題71：冒険選択の縦が 708 / 720 で余裕が無い**（⚠ **上記**）
- ⚠ **宿題69：`AGENTS.md` の状態構造に `DUNGEON_RUN` の行が無い**（⚠ **設計役は `AGENTS.md` を勝手に書き換えない**）
- ⚠ **宿題70：`tests/test_common_infra.gd` が `--import` で赤1本**（⚠ **宿題7 の実体**）

---

## 0-J. ✅ **段階17-c を入れた（ラン専用アイテムと休憩）**（2026-09-02）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **品** | ⚠ **`items.json` に2件**（`dungeon_potion_heal` / `dungeon_potion_revive`。⚠ **`item_type: "dungeon"`＝17-a で先に入っていた型。⚠ これで 0 件 → 2 件**）。⚠ **`dungeon_effect` の欄を新設**（⚠ **ID の綴りで見分けない**） |
| ⚠ **つまみ** | ⚠ **`DungeonConfig` に5本**（`potion_heal_pct` 30 ／ `revive_max_hp_pct` 50 ／ `revive_hp_pct` 50 ／ `rest_heal_pct` 40 ／ `rest_revive_count` 1）。⚠ **全部仮置き。⚠ `.tres` は既定値のままなので Inspector 作業ゼロ** |
| ⚠ **使う口** | ⚠ **`use_dungeon_item(item_id, character_id)` の1本**。⚠ **鞄から1個減って効く。⚠ 無駄撃ちは弾く**（⚠ **脱落者に回復・生きている者に蘇生・鞄に無い品**）。⚠ **判定を全部終えてから状態を触る**（CLAUDE.md 6番） |
| ⚠ **休憩** | ⚠ **`rest` ノードを踏むと `apply_dungeon_rest()`**（⚠ **回復が先・そのあと脱落者を `rest_revive_count` 人まで**）。⚠ **「休む」ボタンは作っていない＝踏んだら効く** |
| ⚠ **供給** | ⚠ **`dungeon.json` の戦利品表に混ぜた**（⚠ **battle に回復20・boss に回復20/蘇生8**）。⚠ **ランのショップは 17-e** |
| ⚠ **足した** | ⚠ **`E134` / `W24`**（⚠ **割合が1〜100の外・知らない `dungeon_effect`・回復ポーションが0件・蘇生の手段がゼロ ／ W24＝ラン専用の品が戦利品表に1件も無い**） |
| ⚠ **翻訳** | ⚠ **`ja.csv` に4行**（`ui_res_*` 2件・`ui_icon_*` 2件＝**回薬 / 蘇薬**）。⚠ **`--import` は設計役が回した** |
| ⚠ **道具** | ⚠ **`scenario=dungeon` に節13-C**（⚠ **シナリオもシーンも増やしていない**） |

### 0-A-2. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠⚠ **回復ポーションと休憩は「上限を上げたうえで HP を上限まで戻す」** | ⚠ **「上限だけ上げて HP は据え置き」にすると、⚠ 蘇生の直後にできる HP＜上限の差を埋める手段が無く、⚠ 差が二度と埋まらないまま残る**（⚠ **決定18「上限で満タンから始まる」が崩れる**） |
| **2** | ⚠ **HP＜上限になるのは蘇生の直後だけ** | ⚠ **決定13（復帰は素の 50%／HP はそのまた 50%）を守るとそうなる。⚠ 復帰直後は素の 25% で戦うことになる＝§4-9-2 の意図どおり** |
| **3** | ⚠ **休憩をポーションより強くした**（40% ＞ 30%） | ⚠ **休憩は「その層の戦闘・レリック・戦利品を諦める」のがコスト（§4-9-1）。⚠ ポーションと同じ効き目だと選ぶ理由が薄い** |
| **4** | ⚠ **無駄撃ちを関数側で弾いた** | ⚠ **鞄の枠が資源なので、⚠ 「満タンの者に使って1枠消える」を画面の作り込みに任せない** |
| **5** | ⚠ **効果の綴りは `items.json`（表）、⚠ 効き目の数値は `DungeonConfig`（つまみ）** | ⚠ **AGENTS.md の判定表。⚠ 品を1件増やしたときに書き足すのは表のほうだけ** |

### 0-A-3. ⚠ 取れた根拠（**`scenario=dungeon` の節13-C。⚠ 全部そこから写した**）

```
  戦闘のあと：ランのMAX HP = { priest 17, archer 0, swordsman 120 } ／ 出られる編成 = [priest, swordsman]
  回復: char_priest 戦闘時MAX HP 17 -> 38（素 70 の 30%） ／ HP = 38（上限と同じ）
  ⚠ 脱落者に回復 -> false ／ 生きている者に蘇生 -> false（どちらも鞄が減らない）
  蘇生: char_archer 戦闘時MAX HP 0 -> 40 ／ HP 20（素 80 の 50% と 25%）
  鞄 {heal:2, revive:1} -> {heal:1} ／ 脱落 = 0 人
  ⚠ 鞄に無い品を使う -> false
  休憩ノード: 回復 40% ／ 蘇生 1 人まで
	priest 38 -> 66 ／ archer 40 -> 72（⚠ swordsman は満タンなので対象外）
  abandon -> 失った鞄の中身: { dungeon_potion_heal: 1 }（⚠ ラン専用は持ち帰らない）
  素の MAX HP 70/80/120 -> 70/80/120 ／ 動いたキャラ = 0 人
  dungeon config validated: ... ラン専用の品 { "heal": 1, "revive": 1 }, 0 errors
```

### 0-A-4. ⚠ 平常値（**動いていないことの確認**）

⚠ **`dungeon` 0/1 ／ `dungeon_battle` 0/1 ／ `floor` 0/3 ／ `economy` 0/1**（⚠ **黄は `skill_dbg_dot_odd` の1本**）。
⚠ **`--import` の赤は1本のまま**（⚠ **`tests/test_common_infra.gd`＝宿題70。⚠ 触っていない**）。
⚠ **`scenario=floor` の全ルート数 15 / 25 / 25 / 30 / 40 本、⚠ `scenario=economy` の 50 / 70 / 90 / 110 / 130 G。⚠ どちらも前と同じ。**

### 0-A-5. ⚠ この回で見えたこと（**17-e と 17-g の材料**）

- ⚠ **鞄8枠にポーションが混ざるようになった。⚠ 17-a の実測で「フロア2 で満杯」だったので、⚠ ポーションを持つと戦利品がさらに入らない**（⚠ **一貫原則の「保険と持ち帰り価値の二択」が初めて効く形。⚠ 枠拡張＝17-e の値段が効く前提**）
- ⚠ **回復30% ＜ 休憩40% は仮置き。⚠ 目減りの実測（1戦で 45% 減った回がある＝17-b）に対して、⚠ どちらも1回では戻し切れない**
- ⚠ **蘇生の入口が2つある**（⚠ **ポーション（boss 表の重み8）と休憩（1人）**）。⚠ **どちらも薄いので、⚠ 2人目の脱落はほぼ戻せない。⚠ 17-g で測る**

---

## 0-K. ✅ **段階17-b を入れた（戦闘との接続）**（2026-09-02）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **新規** | ⚠ **`TransferKeys.DUNGEON_NODE_ID`**。⚠ **`FLOOR_NODE_ID` と別の口。⚠ 両方を同時に入れない** |
| ⚠ **戦闘** | ⚠ **`battle_controller.gd` にダンジョン専用の枝**（`_dungeon_node_id` / `_dungeon_waves` / `_build_dungeon_waves()` / `_is_dungeon_boss()` / `_save_dungeon_hp()` / `_finish_dungeon_battle()`）。⚠ **`in_floor_run` の枝に `or` を1つも足していない** |
| ⚠ **敵** | ⚠ **`GameManager.get_dungeon_node_wave()` の1本から引く**（⚠ **17-a で入れて呼び出し元ゼロだったもの。⚠ 戦闘画面は `battle_pool` を読まない**） |
| ⚠ **HP** | ⚠ **`get_effective_stats()` の `hp` 軸だけをランの MAX HP に差し替えて `BattleUnit.create()` へ渡す**。⚠ **`unit.max_hp` がランの MAX HP になるので、⚠ `BUFF_ON_DEATH` の `unit.max_hp × ratio` も自然にそちらを指す** |
| ⚠ **目減り** | ⚠ **`GameManager.apply_dungeon_battle_result(hp_by_character)` を新設＝書き戻しの口1本**（⚠ **`DUNGEON_RUN_HP` と `DUNGEON_RUN_MAX_HP` の両方に戦闘終了時の HP を入れる。⚠ 係数も下限も無い＝未決4**）。⚠ **`W23`＝書き戻す HP が空** |
| ⚠ **脱落** | ⚠ **`is_dungeon_character_downed()` / `get_dungeon_active_members()` / `get_dungeon_character_max_hp()` / `get_dungeon_character_hp()`**。⚠ **脱落したキャラは `_init_party_units()` が戦闘に出さない**（⚠ **添字は編成の位置のまま＝書き戻し先がずれない**） |
| ⚠ **死亡** | ⚠ **編成が全員脱落した時点で `abandon_dungeon_run()`（鞄を失う）。⚠ 判定は `apply_dungeon_battle_result()` の中の1箇所** |
| ⚠ **ボス** | ⚠ **`is_dungeon_boss_node()` を新設。⚠ 勝ったときだけ `clear_dungeon_boss()`。⚠ 呼ぶ口は `_finish_dungeon_battle()` の1本**（⚠ **負けたら呼ばない＝報酬は入らない**） |
| ⚠ **道具** | ⚠ **`scenario=dungeon_battle` を1行**（⚠ **`SCENARIOS` に1行・`Driver` に2連戦モード。⚠ シーンは増やしていない**）。⚠ **`scenario=dungeon` には「目減り・脱落・死亡」の節（13-B）を足した** |

### 0-A-2. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠ **戦闘終了時の HP を、⚠ ランの MAX HP と HP の**両方**に入れた** | ⚠ **台帳 §4-4 の「戦闘終了時の HP がそのまま次の MAX HP になる」を字義どおりに採った。⚠ 結果 HP＝MAX HP が常に成り立つ。⚠ **人間が決定18 でこれを仕様として確定した**（§4-4-3）：⚠ **「戦闘時 MAX HP＝それ以上回復できない上限。⚠ 戦闘は上限で満タンから始まる。⚠ ポーションで戻すのはその上限」。⚠ コードは1行も変えていない** |
| **2** | ⚠ **ダンジョンでは `stages.json` を1行も引かない**（`_stage_data` は空のまま） | ⚠ **引くと `stage id not found: dungeon_hard` の赤が出る。⚠ `stage_id` はログの見出しにしか使っていない** |
| **3** | ⚠ **負けたときは「もう一度」を出さない** | ⚠ **負けた時点でランが終わっている（全滅＝死亡）。⚠ 押しても敵を組めない** |
| **4** | ⚠ **戻る先は当面 `adventure_select`** | ⚠ **ダンジョンのマップ画面がまだ無い（17-d）。⚠ ここでシーンを増やさない** |
| **5** | ⚠ **`scenario=dungeon_battle` を2連戦にした**（道中 → ボス） | ⚠ **1本だと「ボスに勝ったときだけ `clear_dungeon_boss()`」が見られない。⚠ シーンは増やさず `Driver` に遷移を1回足した** |
| **6** | ⚠ **「全員脱落＝死亡」は `scenario=dungeon` 側で見る**（⚠ **戦闘では起こしにくいため**） | ⚠ **戦闘で3人とも落とすには敵の強さを触ることになる。⚠ 書き戻しの口を直接叩けば同じ枝を通る** |

### 0-A-3. ⚠ 取れた根拠（**`scenario=dungeon_battle` の出力。⚠ 全部そこから写した**）

```
  素のMAX HP = { swordsman 120, archer 80, priest 70 } ／ ランのMAX HP = 同じ値（入った時点）
  1本目：道中の battle ノード 'd_2_0' で戦う（1手目）
  apply_dungeon_battle_result: swordsman 120 -> 66 / archer 80 -> 80 / priest 70 -> 0
  ⚠ 脱落 = 1 人 [char_priest] ／ 出られる編成 = [swordsman, archer]
  phase='map' ／ 鞄 = 1/8 ／ 一時通貨 = 12 ／ can_retreat = false
  --- 2本目：ボスのノード 'd_boss' で戦う（5手歩いた）---
  ⚠ char_priest は脱落しているので戦闘に出ない（ランの MAX HP が 0）
  apply_dungeon_battle_result: swordsman 66 -> 66 / archer 80 -> 63（⚠ priest の行は出ない）
  clear_dungeon_boss() -> フロア1 突破。撤退できる状態になった
  phase='boss_cleared' ／ 鞄 1 -> 7 ／ 一時通貨 12 -> 93 ／ can_retreat = true
  素の MAX HP 120/80/70 -> 120/80/70 ／ 動いたキャラ = 0 人
```

⚠ **`scenario=dungeon` の 13-B（戦闘を回さずに書き戻しの口だけ叩く）**：

```
  半分まで削る -> 死亡=false / ランのMAX HP = {35, 40, 60} / HP = 同じ
  1人だけ 0 にする -> 死亡=false / 脱落=true / 出られる編成 = 2 人
  全員 0 にする -> 死亡=true / is_in_dungeon()=false / 失った鞄={construction_material_1: 2}
```

### 0-A-4. ⚠ 平常値（**動いていないことの確認**）

⚠ **`dungeon` 0/1 ／ `dungeon_battle` 0/1 ／ `floor` 0/3 ／ `economy` 0/1 ／ `area` 0/1 ／ `unlock` 1/1**（⚠ **黄は全部 `skill_dbg_dot_odd` の1本。⚠ ダンジョン由来ではない**）。
⚠ **`scenario=floor` の全ルート数 15 / 25 / 25 / 30 / 40 本、⚠ `scenario=economy` の `floor_1` 50.0 G。⚠ どちらも前と同じ＝シナリオ側に手は当たっていない。**

### 0-A-5. ⚠ この回で見えたこと（**17-c と 17-g の材料**）

- ⚠⚠ **HP と MAX HP が常に同じ値になる**（上の決め1）。⚠ **人間が決定18 で仕様として確定した**（台帳 §4-4-3）。⚠ **残るのは「HP回復ポーション」の位置づけだけ＝17-c の頭で決め直す**（⚠ **上限を上げたあとの差を埋める役にするか、⚠ 廃して2件にするか**）
- ⚠ **道中の戦闘1回で剣士が 120 -> 66（45%）減った。⚠ 僧侶は 70 -> 0 で1回目の戦闘で脱落した**（⚠ **敵は `battle_pool` の1本＝スライム3体。⚠ 味方は Lv1**）。⚠ **蘇生（17-c）が無いと、⚠ ラン1本の中で編成が目に見えて痩せる**
- ⚠ **ボス戦は 7.5 秒で決着させている**（⚠ **道具が敵を全滅させる合図。⚠ 本当に勝てるかは測っていない**）

### 0-A-6. ⚠ 未報告のズレ・宿題（**17-a から持ち越し。⚠ この回も直していない**）

- ⚠ **宿題69：`AGENTS.md` の「GameManagerの状態構造」に `DUNGEON_RUN` の行が無い。⚠ 設計役は `AGENTS.md` を勝手に書き換えないので足していない。⚠ 人間の許可があれば1行足す**（⚠ **`FLOOR_RUN` の行の下**）
- ⚠ **宿題70：`tests/test_common_infra.gd` が `--import` で赤1本を出し続けている**（⚠ **`GameStateKeys.CHEST_TYPE` が無い。⚠ 宿題7 の実体**）

---

## 0-L. ✅ **段階17-a を入れた（難ダンジョンのランの器）**（2026-09-02）

### 0-A-1. ⚠ 段階の割り方（**人間が確認済み。7段階**）

| 段階 | 中身 | 画面 |
|---|---|---|
| ⚠ **17-a** | ⚠ **ランの器**（状態1本・マップ・移動・鞄・一時通貨・`item_type`・ノード種に紐づく戦利品・ボス後の続行/撤退・`scenario=dungeon`） | ⚠ **無し** ✅ **済** |
| ⚠ **17-b** | ⚠ **戦闘との接続**（ランの MAX HP・目減り・脱落・全滅＝死亡） | ⚠ **無し** |
| ⚠ **17-c** | ⚠ **ラン専用アイテム**（ポーション2件・休憩） | ⚠ **無し** |
| ⚠ **17-d** | ⚠ **ダンジョンの画面**（マップ・鞄・続行/撤退） | ⚠⚠ **人間の確認が要る初回** |
| ⚠ **17-e** | ⚠ **ランのショップとたいまつ**（一時通貨の使い道・枠拡張・レリック） | ⚠ **有り** |
| ⚠ **17-f** | ⚠ **弱者狙いAIと予兆** | ⚠ **有り** |
| ⚠ **17-g** | ⚠ **入口（未決7）と数値の実測**（未決4 / 1-b / 1-c / 2-b / 3-b / 5 / 6） | ⚠ **有り** |

⚠ **17-a を1回の通しにした根拠は 14-a の教訓**：⚠ **「欄を最初から全部作る。⚠ あとから足すと `load_state()` と `AGENTS.md` を何度も触る」。**

### 0-A-2. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **新規** | ⚠ **`resources/balance/master/dungeon.json`（マスター9本目）**。⚠ **層6・敵・ボス・⚠ ノード種ごとの戦利品表・一時通貨の額** |
| ⚠ **新規** | ⚠ **`resources/balance/configs/dungeon_config.gd`（`class_name DungeonConfig`）＋ `.tres`**。⚠ **層の出現比3本・ノード種ごとの戦利品確率4本・鞄の初期枠・フロアごとの通貨の伸び** |
| ⚠ **配線** | ⚠ **`balance.gd` に `@export var dungeon: DungeonConfig`。⚠ `balance.tscn` に1行**（⚠ **人間の Inspector 作業ゼロ**） |
| ⚠ **状態** | ⚠ **`GameStateKeys.DUNGEON_RUN` を1本。⚠ 欄は14個を最初から全部**（`dungeon_id` / `floor_index` / `phase` / `nodes` / `position` / `visited` / `max_hp` / `hp` / `bag` / `bag_slots` / `currency` / `torch_grade` / `relics` / `loot_count`）。⚠ **`FLOOR_RUN` は1文字も触っていない** |
| ⚠ **状態** | ⚠ **`GameStateKeys.ITEM_TYPE_DUNGEON`（ラン専用の型・決定17）**。⚠ **`items.json` 側は 0 件（ポーションは 17-c）。⚠ 型と持ち帰りの判定だけが先に入っている** |
| ⚠ **器** | ⚠ **`GameManager` にダンジョンの節を新設（約600行）**。⚠ **`_build_dungeon_map()` / `_connect_dungeon_layers()` / `_roll_dungeon_node_kind()` / `get_dungeon_moves()` は全部専用。⚠ シナリオ側を1本も借りていない** |
| ⚠ **器** | ⚠ **`MasterDataLoader` に `_cache_dungeons` / `get_dungeon()` / `get_all_dungeon_ids()`**。⚠ **`_cache_stages` へマージしていない** |
| ⚠ **足した** | ⚠ **`E133` / `W22`＝`_validate_dungeon_config()`**（⚠ **割り当て漏れ・重みの長さ違い・全部0の層・鞄0枠・`dungeon.json` の欠け・戦利品のIDが `items.json` に無い ／ W22＝休憩で戦利品が出る設定**） |
| ⚠ **道具** | ⚠ **`scenario=dungeon` を1行**（⚠ **`SCENARIOS` に1行・`_report_dungeon()`。⚠ シーンは増やしていない**） |

### 0-A-3. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠ **`dungeon.json` を新設した**（⚠ **`stages.json` に足さなかった。⚠ 人間が確認済み**） | ⚠ **`is_floor_stage()` が「`layers` を持つ＝フロア」で見分けている。⚠ ダンジョンも `layers` を持つので、同居させると見分けの口を書き換えることになり、⚠ `floor_1..5` の判定に手が当たる** |
| **2** | ⚠ **ノード種の定数（`DUNGEON_NODE_KIND_*`）を、⚠ 値が同じでもシナリオ側と分けた** | ⚠ **器が別（台帳 §7）。⚠ 共用すると片方の保険を直したときにもう片方が黙って変わる** |
| **3** | ⚠ **戦利品は「ノード種に紐づく確率」を `DungeonConfig` に、⚠ 「何が出るか」を `dungeon.json` に置いた** | ⚠ **AGENTS.md の判定表（つまみ＝Config ／ 表＝JSON）。⚠ ダンジョンを1本増やしたときに書き足すのは表のほうだけ** |
| **4** | ⚠ **`phase`（`map` / `boss_cleared`）を状態に持たせた** | ⚠ **「撤退できるのはボスの後だけ」（決定15）を、⚠ 画面ではなく状態で保証するため。⚠ `get_dungeon_moves()` も `phase` を見るので、⚠ ボスの先では進める先が空になる** |
| **5** | ⚠ **ボスの報酬は「踏んだとき」ではなく `clear_dungeon_boss()` で配る** | ⚠ **踏んだ時点で配ると、⚠ 負けても報酬が残る** |
| **6** | ⚠ **撤退のとき、⚠ ラン専用の型のものは持ち帰らずに捨てる** | ⚠ **決定17 の裏返し。⚠ 「拠点で買えない・使えない」ものが拠点の倉庫に並ぶのを、⚠ 型の判定1つで止めている** |
| **7** | ⚠ **鞄は「個数」で数える**（⚠ **種類ではない**） | ⚠ **コンセプト文書「ポーション・鍵・戦利品はすべて一律1枠」** |
| **8** | ⚠ **`get_dungeon_node_wave()` を 17-a で入れた**（⚠ **呼び出し元はまだゼロ**） | ⚠ **`battle_pool` の引き方を2箇所に散らさないため。⚠ 17-b が呼ぶ** |

### 0-A-4. ⚠ 取れた根拠（**`scenario=dungeon` の出力。⚠ 全部そこから写した**）

```
[GameManager] dungeon config validated: 6 層 / ダンジョン 1 本 / 戦利品の行 9, 0 errors
  dungeon_hard   層=6 各層=[1, 2, 3, 2, 3, 2] 生成ノード=13（+ボス1 = 14）
  ノード 14 件 / battle=10 boss=1 relic=1 rest=2
  全ルート = 30 本 / ⚠ ボスに着かなかったルート = 0 本 / 通れないノード = 0 件
  歩数の内訳 = { 7: 30 }（⚠ どのルートも同じ長さ＝層構造）
  ランのMAX HP = { "char_priest": 70, "char_archer": 80, "char_swordsman": 120 }（素と同じ値で始まる）
  鞄 = 0/8 ／ 一時通貨 = 0 ／ フロア = 1 ／ phase = 'map'
  ⚠ 入口 'd_1_0' へ戻ろうとする -> false / 位置 d_boss -> d_boss（引き返さない）
  ⚠ ボスを倒す前の can_retreat_from_dungeon() = false（層の途中に降り口は無い）
  clear_dungeon_boss() -> true / phase='boss_cleared' ／ 鞄 3 -> 6 ／ 通貨 36 -> 81
  ⚠ もう一度 clear_dungeon_boss() -> false（二重に倒せない）
  descend_dungeon_floor() -> フロア2 ／ 鞄・通貨・ランのMAX HP は同じ値のまま持ち越した
  空き 2 に 7 個入れようとする -> 2 個だけ入った ／ 満杯の鞄にもう1個 -> 0 個
  retreat -> 拠点 construction_material_1 5 -> 7 ほか5件（add_material 経由）
  abandon -> 拠点 construction_material_1 7 -> 7（増えない） / 失った鞄 3 個
  素の MAX HP 70/80/120 -> 70/80/120 ／ 動いたキャラ = 0 人
  ラン専用の item_type='dungeon' の品 = 0 件（17-c で足す）
```

⚠ **全シナリオの赤黄は平常値のまま**（⚠ **`floor` 0/3 ／ `workshop` 2/1 ／ `unlock` 1/1 ／ `parts` 0/2 ／ `drops` 0/2 ／ ほかは 0/1**）。
⚠ **`dungeon` の平常値は 赤0 黄1**（⚠ **黄は `skill_dbg_dot_odd` の1本。⚠ ダンジョン由来ではない**）。
⚠ **`scenario=floor` の全ルート数（15 / 25 / 25 / 30 / 40 本）と `scenario=economy` の `floor_1` 50.0 G は前と同じ。⚠ シナリオ側に手は当たっていない。**

### 0-A-5. ⚠ この回で見えたこと（**17-g の材料。⚠ 数値は仮置きなので直していない**）

- ⚠ **鞄8枠はフロア2の道中でもう満杯になった。⚠ フロア2 の戦利品6件が1つも入らなかった**（⚠ **`add_to_dungeon_bag(...) -> 0（鞄が満杯 8/8）` が7行**）。⚠ **未決5 の実測が1件取れた形。⚠ 「枠を買うか強化を買うか」（未決2-b）が効く前提の数字**
- ⚠ **1フロアの一時通貨は 81（戦闘4回＋ボス）。⚠ フロア2 は +25% で入る。⚠ 品揃え（17-e）が無いので使い道はまだゼロ**
- ⚠ **層6でも道中の戦闘は 4 回**（⚠ **層1は必ず戦闘・休憩2件・レリック1件を踏んだ経路**）。⚠ **`relic` を踏んでも 17-a では何も起きない**（⚠ **レリックは 17-e**）

### 0-A-6. ⚠ 未報告のズレ・宿題

- ⚠ **宿題69：`AGENTS.md` の「GameManagerの状態構造」に `DUNGEON_RUN` の行が無い。⚠ 設計役は `AGENTS.md` を勝手に書き換えないので足していない。⚠ 人間の許可があれば1行足す**（⚠ **`FLOOR_RUN` の行の下**）
- ⚠ **宿題70：`tests/test_common_infra.gd` が `--import` で赤1本を出し続けている**（⚠ **`GameStateKeys.CHEST_TYPE` が無い。⚠ 2026-08-28 に見つけたもの＝宿題7 の実体。⚠ この回も直していない**）

---

## ⚠ 旧1. **難ダンジョンの段階の割り方を決め、`EXEC` の1本目を書く**（✅ **2026-09-02 に済み＝この回。⚠ 上の §0-A**）

---

## ⚠ 旧々1. **`docs/FLOOR_GAMEPLAY_CURRENT.md` の §6 を裁く**（⏸ **保留になった**）

⚠ **人間の希望：⚠ 「そろそろゲームバランスを決めていくところ」。**
⚠ **数値を触る前に「いま何を決める遊びなのか」を1枚にした**（⚠ **`docs/FLOOR_GAMEPLAY_CURRENT.md`。⚠ 新規。⚠ 数字は全部 `scenario=floor` / `scenario=economy` の出力から取った。⚠ ドキュメントから1つも写していない**）。

⚠ **設計役はコードを1行も書かない回。⚠ 決まってから実装の回を切る。**

### 1-1. ⚠ 裁く論点は6件（**全文は `FLOOR_GAMEPLAY_CURRENT.md` §6**）

| # | 論点 | 設計役の推奨 |
|---|---|---|
| **1** | ⚠ **1周に「意味のある選択」を何回置きたいか**（⚠ **いま4回。⚠ ただし宝箱が「移動」に紐づくので、⚠ どのルートを通っても報酬の総量が動かない**） | ⚠ **B＝宝箱をノード種に紐づける**（⚠ **戦闘は多く落とす／休憩は落とさない**） |
| **2** | ⚠ **戦闘1回 ＝ 休憩1回 ＝ レリック1個 の交換レート**（⚠ **いま根拠が無い＝宿題50 / 56**） | ⚠ **基準は「1周のゴールド」。⚠ 先に HP の残りを出す道具が要る** |
| **3** | ⚠ **フロア5本を数値だけで分けるか、ルールを変えるか** | ⚠ **数値だけのままでよい**（⚠ **差別化は素材の系統で足りている**） |
| **4** | ⚠ **レリックが0個の周をどうするか**（⚠ **`floor_5` の生成で実際に起きた**） | ⚠ **A＝層5に `relic` を1つ必ず置く**（⚠ **層4の `shop` と同じ手。⚠ 新しい仕組みを増やさない**） |
| **5** | ⚠ **死んでいる素材7件** | ⚠ **触らない。⚠ `construction_material_3` の 155 周のほうが先** |
| **6** | ⚠⚠ **スタミナを1も払わずに宝箱を集められる**（⚠ **宿題67・この回で見つけた**） | ⚠ **A＝`start_floor()` で先に払う。⚠ ただし 2026-08-25 の決定「負けてもノーリスク」を覆すので勝手にやらない** |

### 1-2. ⚠ 論点6 を先に読むこと（**ほかの5件の前提が崩れている**）

⚠ **`start_floor()` はスタミナを払わない。⚠ 消費するのは「ボスに勝ったとき」だけ**（`battle_controller.gd:1671`）。
⚠ **負けても降りても減らない**（`:1851`）。⚠ **一方で拾った宝箱は `pending_chests`（トップレベル）に入るので `abandon_floor()` しても残る。**
→ ⚠ **「入る→6回歩く→降りる」でスタミナ0のまま宝箱が増える。**
→ ⚠ **`scenario=economy` の集中時間（42.3〜15.6 時間）は「1周5スタミナ」を前提にしているので、⚠ 前提ごと崩れている。**

### 1-3. ⚠ 予想できている落ち

- ⚠ **数値を1つ動かすたびに `scenario=economy` と `scenario=floor` を回し、⚠ `FLOOR_GAMEPLAY_CURRENT.md` の §3 / §4 の表を差し替えること**
- ⚠ **決まったことは `PLAN_SCENARIO_MAP.md` に書く。⚠ `FLOOR_GAMEPLAY_CURRENT.md` を2本目の台帳にしない**
- ⚠ **論点1 の B を採ると `_roll_chest_draw()` の呼び元が変わる。⚠ `economy` の全数字が動く。⚠ 触る前に「前」を取ること**

---

## 0-A. ✅ **仮アセットのアイコンを画面に出した**（2026-08-31・この回）

### 0-A-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **新規** | ⚠ **`resources/balance/configs/icon_config.gd`（`class_name IconConfig`）＋ `.tres`**。⚠ **等級の10色・段階を等級へ写す表2本・大きさ4件** |
| ⚠ **新規** | ⚠ **`scenes/ui/components/item_icon.tscn` / `.gd`（`class_name ItemIcon`）**。⚠ **`ItemIcon.create(item_id, grade)` の1行で呼べる** |
| ⚠ **配線** | ⚠ **`balance.gd` に `@export var icon: IconConfig`。⚠ `balance.tscn` に1行**（⚠ **人間の Inspector 作業ゼロ**） |
| ⚠ **差し替え** | ⚠ **`floor_map.gd` の `CHEST_COLORS`（4色の `const`）を廃止。⚠ `CHEST_RARITY_TIERS`（綴り→段数）だけ残し、⚠ 色は `Balance.icon` から引く** |
| ⚠ **足した** | ⚠ **`ja.csv` に `ui_icon_*` 78行**（素材16・装飾61・消耗品1）。⚠ **`items.json` 89件が全部埋まった**（レリック12は別途済み） |
| ⚠ **足した** | ⚠ **`items.json` の素材16件に `material_tier`（1〜4）**。⚠ **IDの末尾から切り出さないため**（⚠ **引く口は `GameManager.get_material_tier()` の1本**） |
| ⚠ **足した** | ⚠ **`E132`＝`_validate_icon_config()`**（⚠ **割り当て漏れ・色数と等級数の不一致・写す表が等級の外・`max_part_tier` / `max_rune_tier` との長さ違い**） |
| ⚠ **配線** | ⚠ **倉庫（装備の個体・持ち物・図鑑）／装備画面（一覧・刺せる装飾）／ショップ（13枠）／レリック選択** |
| ⚠ **道具** | ⚠ **`scenario=layout` に `_report_item_icons()` を足した**（⚠ **字・右下の数字・色を値で出す。⚠ 画面の絵は取れないため**）。⚠ **`LAYOUT_SCENES` に倉庫とショップを1行ずつ** |

### 0-A-2. ⚠ 人間の決定（**2026-08-31**）

1. ⚠ **共有部品にする**（`scenes/ui/components/`）
2. ⚠ **色は等級で分ける。⚠ 装備は10色**（⚠ **前の回の「4段に畳む」案を人間が覆した**）
3. ⚠ **全部に割り当てる**（装飾61件も）
4. ⚠ **2文字の漢字を中央・段数の数字を右下**（⚠ **`★` を10個並べない**）

### 0-A-3. ⚠ 設計役が決めたこと（**覆してよい**）

| # | 決め | 理由 |
|---|---|---|
| **1** | ⚠ **10色ランプの 1 / 4 / 7 / 10 番目を、⚠ 宝箱の既存4色（灰・青・紫・金）と同じ値にした** | ⚠ **「5本目の色を作らない」を、⚠ 4色を捨てずに10色へ含める形で満たした。⚠ 宝箱の見た目は1ピクセルも変わらない** |
| **2** | ⚠ **間の6色は 白緑 / 緑 / 濃青 / 青紫 / 桃 / 橙**（⚠ **線形補間ではなく手で選んだ**） | ⚠ **補間だと 2 と 3 が 4 と見分けられない** |
| **3** | ⚠ **`IconConfig` を新設した**（⚠ **既存 Config に混ぜなかった**） | ⚠ **`AGENTS.md`「1つのConfigに別領域の数値を混ぜない」** |
| **4** | ⚠ **`tier_grades = [1,4,7,10]` を `EquipmentConfig.forge_material_tier_min_grades` と別に持った** | ⚠ **境目は同じだが意味が違う**（⚠ **あちらは「どの鍛冶素材が要るか」**）。⚠ **読み合うと意味が混ざる** |
| **5** | ⚠ **ルーンだけ `rune_tier_grades = [1,3,6,8,10]`**（5段） | ⚠ **`PartConfig.max_rune_tier` が 5。⚠ 4段の表を使い回すと段階5が段階4と同じ色になる** |
| **6** | ⚠ **素材にも段数を出すため `items.json` に `material_tier` を足した** | ⚠ **足さないと「建材」が4件とも同じ灰色で並ぶ。⚠ IDの末尾から切り出すのは禁止（`ITEM_MASTER_PART_KIND` のコメント）** |
| **7** | ⚠ **字の割り当て**：⚠ **素材＝建材 / 練材 / 鍛材 / 飾材 ／ 装飾＝種別1字（玉 符 章 印）＋軸1字（命 攻 魔 防 護 速 早 会 痛）／ ルーン＝印＋（強 弱 移 癒 盾）／ 消耗品＝活力** | ⚠ **前の `NEXT_STEPS` の設計役案どおり。⚠ ルーンは「別系統」と書かれていたが、⚠ 実際は `part_rune_<効果>_<1..5>` で規則的だった** |
| **8** | ⚠ **レリック・消耗品には数字を出さない** | ⚠ **段数を持たないため。⚠ 色は既定（灰）** |

⚠ **25字すべて `NotoSansJP` に在ることを実測済み**（⚠ **豆腐0**）。

### 0-A-4. ⚠ 取れた根拠（**`scenario=layout` の出力**）

```
[GameManager] icon config validated: 10 色 / 段階 4 / ルーン段階 5, 0 errors
  weapon_wooden_sword        字='木剣' 右下='1'  色=(0.80, 0.80, 0.82)
  armor_iron_mail            字='鉄鎧' 右下='4'  色=(0.40, 0.70, 1.00)
  acc_ring_power             字='力輪' 右下='7'  色=(0.75, 0.45, 0.95)
  weapon_steel_sword         字='鋼剣' 右下='10' 色=(1.00, 0.80, 0.25)
  part_gem_atk_1             字='玉攻' 右下='1'  色=(0.80, 0.80, 0.82)
  part_gem_atk_4             字='玉攻' 右下='4'  色=(1.00, 0.80, 0.25)
  part_rune_shield_5         字='印盾' 右下='5'  色=(1.00, 0.80, 0.25)
  construction_material_1    字='建材' 右下='1'  色=(0.80, 0.80, 0.82)
  stamina_potion             字='活力' 右下=''   色=(0.80, 0.80, 0.82)
  relic_thorns               字='棘鎧' 右下=''   色=(0.80, 0.80, 0.82)
```

⚠ **全シナリオの赤黄は平常値のまま**（⚠ **`layout` 0/1 ／ `passives` 0/1 ／ `materials` 0/1 ／ `parts` 0/2 ／ `drops` 0/2 ／ `floor` 0/3 ／ `unlock` 1/1 ／ `workshop` 2/1 ／ `economy` 0/1 ／ `research` 0/1 ／ `presets` 0/1 ／ `runes` 0/1 ／ `area` 0/1**）。

### 0-A-5. ⚠ 測れていないもの（**宿題68・人間が見る**）

⚠ **アイコンは行の先頭に 40px を足す。⚠ 横幅への影響を `scenario=layout` では測れない。**
⚠ **倉庫・ショップ・装備の一覧はどれも `ScrollContainer` の中だから**（⚠ **既知の制約＝§3-1**）。
⚠ **持ち物を73種類入れてから測っても、⚠ 倉庫は `108 x 80` を返す**（⚠ **中が測れていない証拠。⚠ 「小さい数字を信じない」**）。

⚠ **人間が実機で見る項目**：

1. ⚠ **倉庫の持ち物タブ … アイコンが行の上に出て、⚠ 字が2文字とも読めるか**
2. ⚠ **倉庫の装備の個体 … 等級10 が金色・等級1 が灰色になっているか**
3. ⚠ **ショップの13枠 … 行が横にはみ出していないか**（⚠ **アイコン40px ＋ 商品名 ＋ 価格 ＋ 在庫 ＋ ボタン**）
4. ⚠ **装備画面の「刺せる装飾」 … 同上**
5. ⚠ **レリック選択 … 3件のアイコンに数字が出ていないこと**（⚠ **段数を持たないので空が正解**）
6. ⚠ **右下の数字が枠から出ていないか**（⚠ **位置は `icon_size_px` からコードで計算している**）

---

## 0-B. ⚠ ズレ48（**`NEXT_STEPS` の集中時間の表が道具の出力と合わない**）

⚠ **前の `NEXT_STEPS` §0-B（2026-08-28）は `floor_1` を 6.16 個/周・32.4 時間、⚠ `floor_5` を 14.23 個/周・14.1 時間 と書いていた。**
⚠ **2026-08-31 に `scenario=economy` を回すと 4.72 個/周・42.3 時間 と 12.79 個/周・15.6 時間。**

- ⚠ **この回は報酬に効く `.gd` / `.json` を1件も触っていない**（⚠ **`items.json` に足したのは `material_tier` だけ**）
- ⚠ **`c08f681`（宿題63）以降に動いた可能性が高いが、⚠ 原因は特定していない**
- ⚠ **正は道具の出力。⚠ `FLOOR_GAMEPLAY_CURRENT.md` §4 の表を使うこと**

---

## 0-C. ⚠⚠ `ja.csv` の再インポートは設計役ができる（**2026-08-31・この回の一番大きい発見**）

⚠ **`AGENTS.md`「CSVを編集したら FileSystem パネルで再インポートする」と、⚠ `CLAUDE.md`「`ja.csv` の再インポートは人間に依頼する」は もう正しくない。**

```powershell
& '...\godot.windows.opt.tools.64.exe' --headless --path d:\pomodoro-heroes --import
```

⚠ **これを回すと `localization/ja.ja.translation` が作り直される。⚠ 実測**（2026-08-31）：

| | |
|---|---|
| ⚠ **`ja.csv` を78行足した時刻** | 19:57 |
| ⚠ **`--import` を回した時刻** | 20:01 |
| ⚠ **`ja.ja.translation` の更新時刻** | ⚠ **20:01（＝作り直された）** |
| ⚠ **`scenario=passives` の合図** | ⚠ **「済んでいる」**（⚠ **`tr("ui_icon_part_gem_hp_1")` が漢字を返した**） |

→ ⚠ **`ja.csv` を触った回に人間を呼ぶ必要が無くなった。⚠ 往復が1つ消える。**
→ ⚠ **`class_name` の `--import`（2026-08-28の発見）と同じ形。⚠ 「エディタが要る」と書いてあるものを疑う価値がある。**
→ ⚠ **`AGENTS.md` / `CLAUDE.md` の該当行を直すのは人間**（⚠ **設計役は勝手に書き換えない**）。

⚠ **合図のキーは `ui_icon_part_gem_hp_1` に差し替え済み**（⚠ **ズレ37 の再発防止。⚠ 次の回も必ずその回のキーへ**）。

---

# 旧：**宿題62＝ホバーで説明が出る共有部品**

> ⚠ **2026-08-28。⚠ 段階14-h / 14-i と 宿題63 まで済んだ。⚠ 人間の作業は1回も発生していない**（⚠ **Inspector も `ja.csv` の再インポートも無し**）。
> ⚠ **平常値は変わっていない**：⚠ **`materials` 0/1 ／ `parts` 0/2 ／ `drops` 0/2 ／ `research` 0/1 ／ `workshop` 2/1 ／ `economy` 0/1 ／ `floor` 0/3 ／ `unlock` 1/1 ／ `layout` 0/1 ／ `passives` 0/1**

## ⚠ このタスク：**宿題62（ホバーで説明が出る共有部品）**

⚠ **レリックの選択画面で「何が起きるか分からないまま選ぶ」問題。** ⚠ **同じ穴が研究・装飾・スキル・ショップにもある。**
⚠ **レリック専用ではなく使い回せる部品として作る**（⚠ **`EXEC_SCENARIO_RELIC.md` §4-2 の要望**）。

⚠ **これは画面なので、⚠ 久しぶりに人間の確認が要る回になる。** ⚠ **置き場は `scenes/ui/components/`**（⚠ **2画面以上で使うため＝`AGENTS.md`**）。

⚠ **先に決めること**：⚠ **説明文をどこから引くか**（⚠ **`ja.csv` に `ui_<id>_desc` を足すのか、⚠ 効果の定義から組み立てるのか**）。⚠ **後者なら `skill_schema.gd` を読むことになるので、⚠ 規模が変わる。**

⚠ **順番は覆してよい。⚠ 候補は §6。**

---

## ⚠ 前のタスク（**段階14 の数値調整**）… ✅ **済み**（2026-08-28）

### ⚠ 何を変えたか（**5件**）

| # | 前 | 後 | なぜ |
|---|---|---|---|
| ⚠ **1** | ⚠ **層数 5** | ⚠ **層数 6** | ⚠ **1周の長さと選択の回数を増やす** |
| ⚠ **1-b** | ⚠ **`shop` は4層に重み25**（⚠ **行ける周が 約50%**） | ⚠ **4層を「ノード1個・`shop` 100%」の**合流点**に**（⚠ **5層のショップ枠は消して `relic` 25 / `rest` 20 へ**） | ⚠ **行ける周が **100%**。⚠ **index を固定する形は採れない**（⚠ **`floor_2` / `floor_3` / `floor_5` は「在るのに行けない」が残る＝`PLAN_SCENARIO_MAP.md` §10-2-C の表**）。⚠ **全ルート数は減らず（floor_5 は 39 → 40本）、⚠ 移動も6回のまま** |
| ⚠ **2** | ⚠ **たいまつ 0/300/800/2000 G** | ⚠ **0/50/150/400 G** | ⚠ **フロアを降りると消えるものが、⚠ 拠点ショップの恒久の品の最安（300 G）と同額だった。⚠ 1周のゴールドは 50〜130 G** |
| ⚠ **3** | ⚠ **フロア内回復 200 G** | ⚠ **60 G** | ⚠ **同上。⚠ さらに休憩ノードが無料で満タンなので、それより明確に安くないと押されない** |
| ⚠ **4** | ⚠ **`floor_chest_chance_pct` 35** | ⚠ **30** | ⚠ **移動が5回→6回に増えたぶんの相殺。⚠ 実測 1.79 → 1.87 個/周** |
| ⚠ **5** | ⚠ **レアリティの重み 5要素** | ⚠ **6要素**（⚠ **層6ぶんを明示**） | ⚠ **末尾流用だと層5と層6が同じ表になる。⚠ 各層の合計 100 は保った** |

⚠ **触らなかったもの**：⚠ **1周5スタミナ ／ 固定報酬 50〜130 G ／ 素材の配り方 ／ レアリティの中身 ／ 無料ガチャのプール ／ レリック12件の効果量**（⚠ **人間の決定：Lv100×3 の集中時間は**実質 約16時間**のままでよい**）。

⚠ **全文は `PLAN_SCENARIO_MAP.md` §10-2-A。⚠ ここに写さない。**

---

## 0-B. ⚠ ズレ44 を直した（**2026-08-28・`scenario=economy` が宝箱を数えるようになった**）

⚠ **`debug_boot.gd` だけ。⚠ 本番コードも `.json` も触っていない。⚠ 赤0黄1＝平常値。**

**Lv100×3 の集中時間（`training_material_1`）**

| | 前（固定報酬だけ） | ⚠ **後（宝箱込み）** |
|---|---|---|
| `floor_1` | 2 個/周・99.7 時間 | ⚠ **6.16 個/周・32.4 時間** |
| `floor_2` | 3 個/周・66.5 時間 | ⚠ **7.74 個/周・25.8 時間** |
| `floor_3` | 5 個/周・39.9 時間 | ⚠ **10.52 個/周・19.0 時間** |
| `floor_4` | 6 個/周・33.3 時間 | ⚠ **12.88 個/周・15.5 時間** |
| `floor_5` | 6 個/周・**33.3 時間** | ⚠ **14.23 個/周・14.1 時間** |

**研究の総コスト**

| | 前 | ⚠ **後** |
|---|---|---|
| `construction_material_1` | 最良 `floor_3` 5 個/周 → 54 周 | ⚠ **最良 `floor_2` 8.74 個/周 → 31 周** |
| `construction_material_2` | 最良 `floor_5` 5 個/周 → 47 周 | ⚠ **最良 `floor_5` 5.97 個/周 → 40 周** |
| `construction_material_3` | 最良 `floor_4` 1 個/周 → 230 周 | ⚠ **最良 `floor_5` 1.49 個/周 → 155 周** |
| `construction_material_4` | ⚠ **落ちない** | ⚠ **落ちない**（変わらず。⚠ **宝箱に1件も入っていない＝宿題48**） |

⚠ **1周の宝箱は 1.92 個/周 ＋ 無料ガチャ1回。⚠ 5フロアとも同じ数字が出るのが正解**（⚠ **層数6・出現率30% が共通のため。⚠ 層数か `floor_chest_chance_pct` をフロアごとに変えたらここが割れる**）。
⚠ **式の値 1.92 と、⚠ `scenario=floor` の実測 1.82（100周）が突き合わせ先**（⚠ **`economy` の見出し行にそう書いた**）。

⚠ **`construction_material_4` だけは何も変わらなかった。** ⚠ **宝箱に1件も入っていないため。⚠ 入口がショップ 5000G の1本だけという状態は動いていない。**

---

## 0-C. ⚠ `FloorConfig` を新設した（**2026-08-28・段階14-i-1**）

⚠ **人間の決定：⚠ 「こういうバランスをまとめて `.tres` で設定できるようにしたい」。⚠ Config の新設 ＋ フォルダにまとめる。⚠ フォルダ移動は次の回（14-i-2）。**

### 0-C-1. ⚠ 何をしたか

| | 中身 |
|---|---|
| ⚠ **新規** | ⚠ **`resources/balance/floor_config.gd`（`class_name FloorConfig`）＋ `floor_config.tres`** |
| ⚠ **配線** | ⚠ **`balance.gd` に `@export var floor: FloorConfig`。⚠ `balance.tscn` に `floor = ExtResource(...)`**（⚠ **人間の許可を得て設計役が手で書いた。⚠ Inspector 作業ゼロ**） |
| ⚠ **移した** | ⚠ **宝箱の率・レアリティ4本・たいまつ2本・フロア内ショップ2本を `AdventureConfig` から**（⚠ **`Balance.adventure.floor_X` → `Balance.floor.X`。⚠ 参照 24箇所**） |
| ⚠ **移した** | ⚠ **層のノード出現比を `stages.json` から**（⚠ **`layer_weight_battle/relic/rest/shop` の4本。⚠ `stages.json` の `layers` は `node_count` だけになった**） |
| ⚠ **消した** | ⚠ **`floor_rest_full_heal`**（⚠ **誰も読まない欄＝ズレ46**） |
| ⚠ **足した** | ⚠ **`E131` / `W21`＝`_validate_floor_config()`**（⚠ **割り当て漏れ・4本の長さ違い・重みが全部0の層・必ず通るショップの層が無い**） |

### 0-C-2. ⚠ ここで分かった一番大事なこと（**次からの前提が変わる**）

⚠ **新しい `class_name` は、⚠ ヘッドレスの `--import` でキャッシュに載る。⚠ 人間がエディタを開く必要は無い。**

```powershell
& '...\godot.windows.opt.tools.64.exe' --headless --path d:\pomodoro-heroes --import
```

⚠ **`AGENTS.md`「新しい `class_name` はエディタを1回通すまで認識されない」は**半分だけ正しかった**。⚠ 「エディタ」ではなく「インポート」が要るだけで、⚠ それはヘッドレスでできる。**
⚠ **実際の症状**：⚠ **`--import` を回す前は `SCRIPT ERROR: Parse Error: Could not find type "FloorConfig"` ＋ `Failed to load script "res://autoload/balance.gd"` で赤3本。⚠ `--import` のあと赤0。**
⚠ **`.godot/global_script_class_cache.cfg` を `grep` すれば載ったか分かる**（`AGENTS.md` の確かめ方と同じ）。

### 0-C-3. ⚠ `.tres` の書き方（**次の回で使う**）

⚠ **`.tres` は `uid` 無しで書いてよい。** ⚠ **`shop_config.tres` / `research_config.tres` / `workshop_config.tres` の3本が元から `uid` 無しで動いている。**
⚠ **ただしこれは**フォルダ移動のときに壊れる側**でもある（下記）。**

### 0-C-4. ✅ フォルダ整理（**14-i-2。⚠ 2026-08-28 に同じ日に済ませた**）

⚠ **`resources/balance/` の直下は空になった。⚠ `configs/`（44件）と `master/`（19件）の2つだけ。**
⚠ **`AGENTS.md` は人間の許可を得て設計役が書き換えた**（⚠ **フォルダ構造・Config のルール・`--import`・`uid_cache.bin`・拒否仕様の注記の5件**）。

**⚠ 踏んだ落ち（次に同じことをするとき用）**

1. ⚠ **`path=` を全部書き換えても赤15本が消えなかった**（⚠ **`Cannot open file 'res://resources/balance/pomodoro_config.tres'`**）。⚠ **原因は `.godot/uid_cache.bin` が古いパスを覚えていたこと。⚠ 消して再インポートしたら赤1本になった**
2. ⚠ **PowerShell の `Get-ChildItem <dir> -Include *.tres` は `-Recurse` か末尾 `\*` が無いと0件を返す。** ⚠ **1回目の書き換えが `configs/` の中に当たっておらず、⚠ 「書き換えたファイル: 2」で気づいた**（⚠ **CLAUDE.md 2番「編集したら grep で当たったことを確認する」が効いた**）

**⚠ 残った赤1本（⚠ 今回の移動とは無関係）**

⚠ **`--import` が `Failed to load script "res://tests/test_common_infra.gd" with error "Parse error"` を出す。**
⚠ **`git diff HEAD` で確認したところ、⚠ このファイルは今回1文字も触っていない＝**元から壊れていた**。**
⚠ **`--import` は「プロジェクトの全スクリプトを読む」唯一の口なので、⚠ ここで初めて見えた。⚠ 通常のシナリオは到達したものしか読まないため出ない。**
⚠ **宿題7「`tests/` の既存9件の棚卸し」の実体の1つ。⚠ 直していない。**

### 0-C-5. ⚠ 旧：次の回（**14-i-2**）で先に潰すこと（⚠ **済んだが手順として残す**）

⚠ **`configs/` に `.gd` と `.tres` をペアでまとめる**（⚠ **人間の決定。⚠ `master/` はそのまま**）。

- ⚠ **`balance.tscn` は13本すべてを `path=` で参照している。⚠ うち3本（`shop` / `research` / `workshop`）は `uid` を持たない**
  - ⚠ **移動すると `Balance.shop` / `Balance.research` / `Balance.workshop` が **null** になる。⚠ 赤は出ず、触った画面で初めて落ちる**
- ⚠ **各 `.tres` は中で `path="res://resources/balance/xxx_config.gd"` と自分のスクリプトを指している。⚠ `.gd` を動かすと `.tres` 側も全部書き換えが要る**
- ⚠ **`protection_*.tres` 3本は `ChestScheduleEntry` を参照している**（⚠ **`chest_schedule_entry.gd` も一緒に動かすことになる**）
- ⚠ **動かしたら `--import` を回してから全シナリオを回す。⚠ `Balance.X` が null になっていないかは、⚠ 拠点・ショップ・研究・作業場・装備・倉庫を通る `scenario` で見る**
- ⚠ **`AGENTS.md` のフォルダ構造に `configs/` を1行足すのは人間**（⚠ **設計役は `AGENTS.md` を書き換えない**）

---

## 0-D. ✅ 宿題63 を閉じた（**2026-08-28・段階14-i**）

⚠ **道中の戦闘ノードに報酬が1つも無く、⚠ 避けるのが常に最適だった問題。**
⚠ **人間の決定：B-1（総量据え置き・配分だけ変える）。**

| | 前 | 後 |
|---|---|---|
| ⚠ **ボスの `rewards.gold`** | ⚠ **50 / 70 / 90 / 110 / 130** | ⚠ **25 / 35 / 45 / 55 / 65** |
| ⚠ **`node_rewards.gold`**（新設） | ⚠ **無し** | ⚠ **10 / 14 / 18 / 22 / 26** |
| ⚠ **1周の期待ゴールド** | ⚠ **50 / 70 / 90 / 110 / 130** | ⚠ **同じ**（⚠ **幅が `floor_1` で 25〜65 G**） |

⚠ **判定の口は `GameManager.get_floor_node_rewards()` の1本**（⚠ **戦闘以外は空を返す。⚠ 呼ぶ側で `kind` を見ない**）。
⚠ **全文は `PLAN_SCENARIO_MAP.md` §10-2-B。**

### ⚠ ここで道具が嘘をついた（**ズレ47・⚠ 数字を疑って助かった3回目**）

⚠ **道中の期待戦闘回数を式で **3.50** と出したのに、⚠ 周回20回の実測が **2.85**（⚠ **約3σ・偶然では出ない差**）。**
⚠ **`start_floor()` を読んだら、⚠ 入口ノードは「戦わずに `position` に置いて `visited` に入れる」だけだった。⚠ 層1 は一度も戦闘にならない。**
→ ⚠ **式を層2から数えるよう直して **2.50**。⚠ 実測 2.25（20周・1.2σ）と噛み合った。**
→ ⚠ **道中のゴールドも 7/10/13/16/19 から **10/14/18/22/26** に引き直した。⚠ これで1周の期待ゴールドがちょうど元の 50/70/90/110/130 に戻る。**
⚠ **もし式を信じたまま出していたら、⚠ 1周のゴールドを 15% 減らしたまま「据え置き」と報告していた。**

---

## 1. ⚠ 旧：宿題63＝道中の戦闘ノードに報酬を付ける（✅ **済み**）

### 1-0. ⚠ なぜ次がこれなのか

⚠ **段階14-h で数値をいじっても閉じなかった唯一の穴。** ⚠ **`battle_controller.gd:1652` の `if (not in_floor_run) or is_boss:` が道中の報酬を空にしている**（⚠ **14-c の意図どおり。⚠ 動かすと1マス目で画面が全部開き、1周で25スタミナ払うことになる**）。
⚠ **一方で宝箱は「移動」に紐づくのでどのルートでも同じ数。** ⚠ **結果、⚠ 戦闘ノードは純粋なコストで、⚠ 避けるのが常に最適になっている。**
⚠ **段階14-h は `battle` の重みを底上げして「避けきれない」形にしただけ**（⚠ **最善ルートの道中戦闘 平均 2.4回 → 2.9回・試算**）。⚠ **報酬そのものは付いていない。**

### 1-1. ⚠ 先に決めること（**人間に聞く**）

1. ⚠ **道中の報酬は何か**（⚠ **ゴールドだけか ／ 素材も出すか ／ 宝箱の抽選を1回足すか**）
2. ⚠ **どこに書くか**（⚠ **`stages.json` の各フロアに `node_rewards` を1本 ／ `battle_pool` の要素ごと**）
3. ⚠ **画面解放とクリア記録は今までどおりボスだけ**（⚠ **`unlocks` と `mark_stage_cleared()` は絶対に道中へ広げない＝上のコメントの事故がそのまま起きる**）
4. ⚠ **スタミナも今までどおりボスだけ**（同上）

### 1-2. ⚠ 予想できている落ち（**先に潰すこと**）

- ⚠ **`scenario=economy` の「1周で入るもの」が全部動く。⚠ 触る前に1回回して「前」を取ること**
- ⚠ **`_economy_sources()` と `_economy_sinks()` は対。⚠ 入口を1つ足したら両方を見る**
- ⚠ **`economy` は `stages.json` の固定報酬しか数えていない。⚠ 宝箱ぶんを数えていない＝ズレ44**（下記）。⚠ **「Lv100 までの集中時間」を根拠にするなら、⚠ 先に道具を直すか、⚠ 手計算を併記すること**
- ⚠ **`apply_battle_rewards()` を2本目にしない。⚠ `grant_chest()` / `add_to_inventory()` も同じ**
- ⚠ **`is_floor_stage()` が `layers` を持つかで見分けている。⚠ `stage_dbg_*` 5本を巻き込まない**
- ⚠ **報酬を足すと道中の戦闘が「おいしい」側に振れる。⚠ 振れすぎると今度は**全部戦闘を踏むのが最適**になる。⚠ 休憩・レリック・ショップと釣り合う量にすること**

---

## 1-B. ⚠ その前のタスク（**画面30項目を1回の通しで見る**）… ✅ **済み**

**このファイルは「次の1タスク」だけを書く。** 終わったら次のタスクの内容に書き換える。全体の状況は `PROJECT_STATUS.md`、ルールは `AGENTS.md` と `CLAUDE.md`、**ゲームの中身は `GAME_DESIGN.md`**、**順番の台帳は `docs/PLAN_IMPLEMENTATION.md` 3章**。

**このファイルだけ読めば着手できるように書いてある。** 過去のタスクを知らない前提で読んでよい。

⚠ **台帳は `docs/01_plan/PLAN_SCENARIO_MAP.md`。⚠ リソースの入口と出口は `docs/01_plan/PLAN_RESOURCE_FLOW.md`。**
⚠ **段階14 の6段階（14-a〜14-f）は**ログの章だけ**通っている。⚠ 画面の章が**30項目**溜まっている。**
⚠ **1回の通しで全部見る。⚠ 段階ごとに人間を呼ぶより往復が減る。**

| 段階 | 画面の項目 | どこ |
|---|---|---|
| 14-c | ⚠ **12件**（G-1〜G-12） | `EXEC_SCENARIO_MAP_SCREEN.md` §5 |
| 14-e | ⚠ **11件**（S-1〜S-11） | `EXEC_SCENARIO_SHOP.md` §5 |
| 14-f | ⚠ **7件**（A-1〜A-7） | `EXEC_SCENARIO_AUTORUN.md` §4 |
| 14-d | ✅ **7件は済**（R-1〜R-7・2026-08-25） | `EXEC_SCENARIO_RELIC.md` §4 |

⚠ **`EXEC_PLAYTHROUGH.md`（保留していた通しの回）は `stage_1..3` を前提に書いてある。⚠ そのままでは使えない。⚠ 作り直すか、⚠ 上の30項目を先に片付けてから回すかを決めること。**

---

## 0-A. ⚠ 直前にやったこと（**2026-08-25・段階14 の実装。⚠ 6段階を通しで入れた**）

| 段階 | コミット | 中身 |
|---|---|---|
| **14-a** | `428fb9f` | ⚠ **`stage_1..3` → `floor_1..5`。⚠ 層構造5層・合流あり。⚠ 状態に `floor_run` を1本（9欄）。⚠ `scenario=floor`** |
| **14-b** | `3856d8e` | ⚠ **宝箱を24件に作り替え。⚠ ハズレ枠を廃止。⚠ 移動に紐づく抽選＋深度補正＋最低1回の保証。⚠ 0.3個/周 → 2.01個/周** |
| **14-c** | `d6e7d8f` | ⚠ **`floor_map` を新設。⚠ スタミナ・報酬・クリア記録はボスだけ。⚠ HPをフロア内で持ち越し。⚠ 1ノード＝1ウェーブ** |
| **14-d** | `2de6f3f` | ⚠ **レリック12件。⚠ パッシブの器をそのまま借りた。⚠ `skill_schema.gd` に例外を入れた（報告事項）** |
| **14-e** | `3afa0d9` | ⚠ **フォグとたいまつ（4段階）。⚠ フロア内ショップと無料ガチャ** |
| **14-f** | `a836bc3` | ⚠ **周回の自動処理。⚠ 宿題49 が閉じた** |

⚠ **`.tres` を1件も触っていない。⚠ 人間の Inspector 作業はゼロだった**（⚠ **台帳の見込みは外れ＝`PLAN_SCENARIO_MAP.md` §9**）。
⚠ **人間の作業は `ja.csv` の再インポートだけ。⚠ 14-e と 14-f のぶん **22行** がまだ未インポート**（⚠ **合図は `scenario=passives` の `ui_floor_shop_leave`**）。
⚠ **30シナリオの平常値は一度も動いていない**（`unlock` 赤1 ／ `workshop` 赤2 ／ `parts` と `drops` は黄2 ／ ほかは赤0黄1）。
⚠ **`scenario=floor` の黄は **3本** が平常値**（⚠ **`skill_dbg_dot_odd` 1 ＋ `take_relic` の弾き2**）。

---

## 0. ⚠ その前のタスク（**2026-08-25・シナリオの作り替えを「決める」回**）

**台帳は `docs/01_plan/PLAN_SCENARIO_MAP.md`（新規）と `docs/01_plan/PLAN_RESOURCE_FLOW.md`（新規）。⚠ コードは1行も書いていない。**

### 0-0. ⚠ 何が決まったか（**人間の決定8件。⚠ 全文は `PLAN_SCENARIO_MAP.md` §1**）

1. ⚠ **`AGENTS.md` の拒否仕様「ダンジョン」は「難ダンジョン（通行証・全ロスト）」を指すものとして残す**（⚠ **シナリオのマップ探索は別物。⚠ `AGENTS.md` への注記1行はまだ書いていない**）
2. ⚠ **この回はシナリオ（マップ探索）だけ決める。⚠ 難ダンジョンは仕様を書かない**
3. ⚠ **たいまつ＝フロア内状態**（アイテムにしない）／⚠ **通行証・救済チケット＝`ITEM_TYPE_KEY_ITEM`**（⚠ **定数だけあって使用0件のものを初めて使う**）
4. ⚠ **宝箱も作り直す。⚠ 抽選のハズレ枠（`weight: 70` の空欄）を廃止する**
5. ⚠ **`chest_id` は「フロア × レアリティ」にする**（⚠ **5×4＝20件**）
6. ⚠ **フロアは5本**（⚠ **いまの `stage_1..3` を作り替える**）
7. ⚠ **`unlocks` はポモドーロをフロア1へ前倒しする**（⚠ **宿題58 が閉じる**）
8. ⚠ **段階13（SDキャラとアニメーション）より先に置く**
9. ⚠ **マップの形は「層構造（合流あり）」**（⚠ **5層・1層2〜3ノード**）。⚠ **`DISCUSSION_scenario_map.md` の「合流なし」を覆した**。⚠ **平面グリッドは採らず、⚠ 難ダンジョンの候補として `PLAN_SCENARIO_MAP.md` §12-1 に退避**
10〜13. ⚠ **レリック**（⚠ **バフ選択ノードの中身に名前を付けたもの。⚠ ノード種は増えない**）。⚠ **フロア内限定 ／ 戦闘系が先・フロア系はあと ／ 3人用と1人用の両方 ／ `relics.json` を新規（マスター8本目）**。⚠ **全文は `PLAN_SCENARIO_MAP.md` §5-2**
   - ⚠ **器はパッシブをそのまま借りる**（⚠ **`unit.passive_ids` はただの文字列配列＝`battle_controller.gd:299`。⚠ `_restore_passives()` が全ユニットに付け直す＝`:766`**）。⚠ **`get_effective_stats()` も `skill_schema.gd` も触らない**
   - ⚠ **`condition` と `react` が既に使えるので、⚠ 条件つきレリックも新しい語彙なしで書ける**（⚠ **設計役が「規模が跳ねる」と言ったのは誤り。⚠ 訂正済み**）

### 0-1. ⚠ この回で見つけたもの（**実コードを `grep` した結果**）

- ⚠ **宿題59：ジェムに本番の入口も出口も1つも無い**（⚠ **`shop.json` 13枠が全部 `gold`・`chests.json` に `gems` が0件**）
- ⚠ **宿題60：`apply_pomodoro_rewards({})` が空の Dictionary で呼ばれている**（`pomodoro.gd:259`）。⚠ **ポモドーロが配るのはポーションと加護の宝箱だけ**
- ⚠ **宿題61：ゴールドの入口はステージの固定報酬1本・出口はショップ1本だけ**
- ⚠ **メモの矛盾2件**（⚠ **決定9で解消済み**）：⚠ **「合流なしの2分岐」は「ボスは固定位置」とも「たいまつ」とも両立しない**

### 0-2. ⚠ 直近の人間の決定（**2026-08-25。⚠ 覆すときは影響範囲が広い**）

1. ⚠ **⑫-c（残った穴の器）はやらない。⚠ 「いまはかんぺきなものをつくらなくてもいい」**（⚠ **宿題47 / 52 / 53 のまま残す**）
2. ⚠ **`decor_material_4` の出口は、⚠ やるときは `max_part_tier` を **5** にする**（⚠ **作業場に4本目のレシピを足す案は採らない。⚠ ただし今はやらない**）
3. ⚠ **ルーンの入手は `F4` のままでよい**（宿題9）
4. ⚠ **戦闘結果に宝箱・持ち物が出ないのはまだ直さない**（宿題23。⚠ **「手に入っていれば良い」**）
5. ~~⚠ **周回ステージ・スキップ周回は作らない**（宿題49）~~ ⚠ **覆った**（2026-08-25・同じ日）。⚠ **`DISCUSSION_scenario_map.md` の「周回は瞬時にスキップし結果だけ受け取る」を採用したため。⚠ 宿題49 は「やる」に戻った**
6. ~~⚠ **次は「一度通しで遊んでみる」**~~ ⚠ **保留**。⚠ **ステージが作り替わるので、⚠ 作り替えたあとに回す**（⚠ **`EXEC_PLAYTHROUGH.md` は消さない**）
7. ⚠ **パッシブは選ばない。⚠ 解放されたものが全部効く** ／ ⚠ **レベル100まで上げられる**（`base_level_cap` 20 ＋ 研究8件 × 10）
8. ⚠ **作業場は「装飾のランダム製作」だけ。⚠ 中間素材は入れない** ／ ⚠ **`construction_material_4` の入口は作らない**
9. ⚠ **素材IDは `<系統>_material_<1..4>` で固定** ／ ⚠ **コンボは作らない** ／ ⚠ **`target.range` は触らない**

---

## 1. ⚠ このタスク：**画面30項目を1回の通しで見る**

⚠ **設計役はコードを1行も書かない。⚠ 人間が遊び、⚠ 設計役は `godot.log` を読んで突き合わせる。**

### 1-0. ⚠ なぜ次がこれなのか

- ⚠ **段階14 の6段階は**ログの章だけ**通っている。⚠ 画面は人間しか見られない**
- ⚠ **段階ごとに人間を呼ぶと4往復になる。⚠ 1回にまとめれば1往復**
- ⚠ **フロアは通しで遊べる形になった**（⚠ **入口 → 歩く → 戦う → レリック → ショップ → ボス → 周回**）

### 1-1. ⚠ 何をするか

| | 誰が | 何を |
|---|---|---|
| **1** | ⚠ **人間** | ⚠ **`ja.csv` を再インポート**（⚠ **22行が未インポート。⚠ やらないと店の画面にキー名が出る**） |
| **2** | ⚠ **人間** | ⚠ **`F4` で「画面を全部解放」＋「ゴールド・ジェム・スタミナ」を押す** |
| **3** | ⚠ **人間** | ⚠ **`floor_1` を通しでクリアし、⚠ そのあと周回を1回押す**（⚠ **30項目は自然にほぼ全部通る**） |
| **4** | ⚠ **人間** | ⚠ **気になった点を1行ずつメモする**（⚠ **原因は書かない**） |
| **5** | ⚠ **設計役** | ⚠ **遊び終わった直後に `godot.log` を読む**（⚠ **ヘッドレスを走らせる前に**） |
| **6** | ⚠ **設計役** | ⚠ **メモとログを突き合わせ、⚠ 各 EXEC の画面の章に結果を埋める** |

### 1-2. ⚠ 設計役が先に知っていること（**人間が困る前に言う**）

- ⚠ **たいまつは `floor_1` では買えない**（⚠ **1周のゴールドが 50〜130 で最安が 300 G**）。⚠ **`F4` でゴールドを入れてから見ること**
- ⚠ **ショップのノードが0個のフロアがありうる**（⚠ **層のノード種が抽選のため**）。⚠ **出なかったら入り直す**
- ⚠ **道中の戦闘が0回のルートもありうる**（同上）
- ⚠ **宝箱はマップに出ない。⚠ 倉庫に溜まる**（⚠ **レアリティの色・エフェクトは未実装＝`PLAN_SCENARIO_MAP.md` §4-5 が未達**）
- ⚠ **周回は「クリア済みのフロア」にしか出ない**

### 1-3. ⚠ 予想できている落ち（**先に潰すこと**）

- ⚠ **`godot.log` は保持5本。⚠ 遊び終わるまで設計役はヘッドレスを1本も回さない**
- ⚠ **Godot を開いたまま `godot.log` を読むとロックされる。⚠ `FileShare.ReadWrite` で開くこと**（⚠ **2026-08-25 に踏んだ**）
- ⚠ **セーブは自動では書かれない。⚠ 続きから遊ぶなら拠点で「セーブする」を押すこと**（⚠ **前回 `saves/` が空だった**）
- ⚠ **`.gd` / `.tscn` / `.json` / `.csv` / `.tres` を1件も触らない。⚠ 書き換えるのは `.md` だけ**

---

## 1-B. ⚠ 前のタスクの手順（**段階14-a。⚠ 参考として残す**）

### 1-0. ⚠ なぜ 14-a が1本目だったのか

- ⚠ **画面を1枚も作らない。⚠ 検証がログだけで閉じる＝人間の作業がゼロ**（⚠ **`ja.csv` も `.tres` も `.tscn` も触らない**）
- ⚠ **一番大きいのがデータと状態。⚠ ここを先に固めれば、⚠ 14-c は「動く中身」に画面を被せるだけになる**
- ⚠ **1本目に画面を持ってくると、⚠ 最初から人間待ちになって進まない**

### 1-1. ⚠ 何をするか

| | 誰が | 何を |
|---|---|---|
| **1** | ⚠ **設計役** | ⚠ **`EXEC_SCENARIO_FLOOR.md` を書く**（⚠ **完了条件は「ログ」「ファイル」「画面」の3つに分ける。⚠ 14-a は「画面」の章が空になるはず**） |
| **2** | ⚠ **設計役** | ⚠ **`stage_dbg_*` **5本** の扱いを決める**（⚠ **`PLAN_SCENARIO_MAP.md` §10-1 の残り1件。⚠ フロア化しないなら戦闘画面へ直行する経路を残す**） |
| **3** | ⚠ **設計役** | ⚠ **実装する**（⚠ **§9-1 の 1・2・3・9・10・11・12・13・14・15・30**） |
| **4** | ⚠ **設計役** | ⚠ **`scenario=floor` を1本足し、⚠ `scenario=unlock` と `economy` を回す** |
| **5** | ⚠ **設計役** | ⚠ **人間に渡す前にヘッドレスを1回回す**（⚠ **`§0 事前チェック` はこの回から戻る**） |
| **6** | ⚠ **人間** | ⚠ **`AGENTS.md` の拒否仕様に注記1行を入れる**（⚠ **「ダンジョン＝難ダンジョンを指す」。⚠ 設計役は `AGENTS.md` を勝手に書き換えない**） |

### 1-2. ⚠ 予想できている落ち（**先に潰すこと**）

- ⚠ **`unlocks` を触るので `scenario=unlock` を必ず回す。⚠ 赤の平常値は **1本**（`E125`）。⚠ 消すと画面が永久に開かない**
- ⚠ **`stages.json` の `rewards` を各ノードへ配り直すと `scenario=economy` の「1周で入るもの」が動く。⚠ 前後の数字を EXEC に並べる**
- ⚠ **新しい状態キーを足したら `GameStateKeys` に定数を足し、⚠ `AGENTS.md` の表にも1行足す**
- ⚠ **`load_state()` の `int()` 一覧に新しいキーを足す**（⚠ **忘れるとセーブに `.0` が乗る＝CLAUDE.md 3番。⚠ 宿題57 が同じ形で起きている**）
- ⚠ **`.tres` / `ja.csv` / `.tscn` を1件も触らない。⚠ 14-a は人間の作業ゼロで閉じる設計。⚠ 触りたくなったら段階の切り方が間違っている**
- ⚠ **`scenario=floor` は `SCENARIOS` に1行足す。⚠ シーンを増やさない。⚠ 報告の枝は `_ready()` の `elif` にも要る＝3箇所**
- ⚠ **足した検証が本当に効くか、⚠ 2箇所で壊して確かめる。⚠ 壊したら必ず戻し、⚠ 平常値に戻ったことを再実行で確認する**
- ⚠ **`stage_dbg_*` **5本** を巻き込まない**（⚠ **`_build_debug_stage_list()` は解放の連鎖に入れていない。⚠ 同じ扱いを保つ**）
- ⚠ **`AGENTS.md` の拒否仕様「ダンジョン」への注記1行がまだ書けていない**（⚠ **人間の決定1。⚠ `AGENTS.md` は設計役が勝手に書き換えない。⚠ 人間に渡すこと**）

---


## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ ドキュメントの「実装済み」を信じない

**ズレが42回起きている。** ⚠ **`grep` で関数の中身を見てから判断する。⚠ 違っていたら報告する（勝手に直さない）。**

⚠ **未報告のズレは 11件（下の 34・37・38・39・40・41・42・44・46・47 と、⚠ **48**＝集中時間の表が道具の出力と合わない＝§0-B）。⚠ 次に見つけたものは 49 番。**

| # | ズレ | 直すなら |
|---|---|---|
| ⚠ **34** | ⚠ **`skill_schema.gd:330` のコメント「`of` を読まない source」は `scale_from` にしか当てはまらない。** ⚠ **`condition` は同じ source でも `of` が必須** | ⚠ **未着手。⚠ コメント側か、⚠ `condition` 側で `of` を任意にするか** |
| ⚠ **37** | ⚠ **`ja.csv` の再インポートの合図が「前の回のキー」を見ていた**（⚠ **`scenario=passives` の1行**） | ⚠ **その回のキーに差し替えて回避している**（今は `ui_research_category_workshop`） |
| ⚠ **38** | ⚠ **`GAME_DESIGN` 9-1 の作業場カテゴリに「変換レート」が残っている。⚠ 同じファイルの 9-3 と 2章（`:104`）が「変換は廃止・ショップに一本化」と書いている** | ⚠ **未着手。⚠ 9-1 の表の3つ目を消す** |
| ⚠ **39** | ⚠ **「数値は `.tres`（設計役は直せない）」が半分しか当たっていない**。⚠ **2026-08-25 に範囲を実測で狭めた（§1-1）**。⚠ **同じズレで「`_4` は月替わりショップだけ」も違う。⚠ `shop.json` に `weekly` / `monthly` のキーが無く `daily` しかない** | ⚠ **未着手。⚠ `PROJECT_STATUS` の宿題51 に実体が書いてある** |
| ⚠ **40** | ⚠ **「`max_part_tier` を5にすると `part_*_5` **36件**の追加になる」が誤り**（⚠ **前の `NEXT_STEPS` §1-1 と宿題47**）。⚠ **非ルーンの装飾は **9通り × 4段階 = 36件**（宝石3・護符2・紋章4）なので、⚠ 段階5で増えるのは **9件**（`ja.csv` も9行）。⚠ **`part_config.tres` は `script = ...` の1行だけ＝上限も配列2本も設計役が直せる** | ⚠ **宿題47 に書き足した。⚠ 実装はしていない** |
| ⚠ **41** | ⚠ **`GAME_DESIGN` 9-3 の表が「中間素材の製作 ✅ デモ範囲」のままだが、⚠ 人間の決定（2026-08-25）は「作業場は装飾のランダム製作だけ・中間素材は入れない」** | ⚠ **未着手。⚠ 決定が優先という理解で進めている** |
| ⚠ **42** | ⚠ **`EXEC_BALANCE_TUNE` §6 の宿題番号（51 / 52 / 53）が `PROJECT_STATUS` の実番号（52 / 53 / 54）と1つずれている** | ⚠ **未着手。⚠ 正は `PROJECT_STATUS` 側** |
| ⚠ **44** | ⚠ **`scenario=economy` の「Lv100 までの周回数と集中時間」が `stages.json` の固定報酬しか数えていない。** ⚠ **段階14-b で宝箱が「移動に紐づく」に変わったので、⚠ 宝箱ぶんの `training_material_1` が丸ごと抜けている**（⚠ **economy の出力も全フロア「宝箱（無し）」**）。⚠ **手計算だと floor_5 は宝箱から +6.4個/周 ＝ 実質 12.4個/周 ＝ **約16時間**。⚠ 報告値 33.3h の**ほぼ半分**** | ✅ **直した**（2026-08-28。⚠ **`debug_boot.gd` だけ。⚠ 本番コードは触っていない**）。⚠ **式で出す**（⚠ **`_roll_chest_draw()` を回すと研究の宝箱枝が乗り、呼ぶ順番で値が変わるため＝決め5**）。⚠ **足したのは `_economy_draw_material_expect()` / `_economy_layer_material_expect()` / `_economy_floor_chest_count()` / `_economy_floor_chest_materials()` / `_economy_floor_has_forced_shop()` / `_economy_stage_material_all()` の6本**。⚠ **無料ガチャは「必ず通る層」がある場合だけ数える**（⚠ **抽選で出る `shop` は在っても行けるとは限らない＝§10-2-C**）。⚠ **前後は下の表** |
| ⚠ **45** | ⚠ **`EXEC_SCENARIO_RELIC.md` §4-2 が「`PROJECT_STATUS.md` の宿題62 に落とした」と書いているが、⚠ 実際には落ちていなかった** | ✅ **直した**（2026-08-28。⚠ **`PROJECT_STATUS.md` に宿題62 の行を足した**） |
| ⚠ **47** | ⚠ **入口ノード（層1）は一度も戦闘にならない。** ⚠ **`start_floor()` が「戦わずに `position` に置いて `visited` に入れる」だけなので、⚠ `FloorConfig` の `layer_weight_battle[0] = 100` は**飾り**。⚠ 層1 を `relic` にしても何も起きない** | ⚠ **未着手（2026-08-28に発見）。⚠ 発見の経緯：⚠ 道中の期待戦闘回数を式で 3.50 と出したのに実測が 2.85（20周・約3σ）。⚠ 道具を疑って `start_floor()` を読んだ**（⚠ **`NEXT_STEPS` §4「全部同じ数字を返したら道具を疑う」の3回目**）。⚠ **式は 2.50 に直した。⚠ 層1 の重みをどうするかは未決** |
| ⚠ **46** | ⚠ **`floor_rest_full_heal` は誰も読まない欄だった。** ⚠ **`rest_at_node()` は `Balance` を1行も見ずに常に満タンにしている**（⚠ **`EXEC_SCENARIO_MAP_SCREEN.md` §5 と §7 は「これがつまみ」と書いていた**） | ✅ **欄ごと消した**（2026-08-28・段階14-i。⚠ **`FloorConfig` に持ち込まなかった**）。⚠ **割合回復にするなら `rest_heal_pct` を足して `rest_at_node()` に読ませる＝宿題64。⚠ 実装せずに欄だけ足さないこと** |

### 2-2. ⚠ 触る器について、先に台帳を `grep` する

⚠ **`EXEC` の §「変えないもの」に何か書く前に、⚠ `GAME_DESIGN` / `PLAN_IMPLEMENTATION` / `PROJECT_STATUS` を `grep` して「置き換えろ」が無いことを確かめる。**
⚠ **実例（2026-08-25・数値を入れる回）：⚠ `GAME_DESIGN` 5-2 が「線形の式を差し替える」と名指しで指示していた。⚠ `NEXT_STEPS` §1-2 の推奨（「人間に係数を決めてもらう」）より仕様側が具体的で、⚠ しかも設計役が直せる場所だった。⚠ 台帳を見ずに推奨どおり進めていたら、⚠ 人間の Inspector 作業を1往復発生させたうえで線形のままにしていた**（⚠ **§2-2 が効いた3回目**）。

### 2-3. ⚠ 定数は名前ではなく値を見る

⚠ **`SCALE_ALIVE_ENEMY` の値は `"alive_enemy"` ではなく `"alive_count_enemy"`。⚠ 名前を JSON に書いてロード時に赤3本・戦闘中に毎フレームの赤3886本を出した**（2026-08-25）。

### 2-4. ⚠ `@export` を改名すると `.tres` の値が黙って消える

⚠ **実例：`ChestScheduleEntry.chest_type`**（⚠ **`protection_*.tres` の7件が黙って空になる**）。
⚠ **`.tres` に行が無い `@export` は改名しても消えるものが無い**（§1-1）が、⚠ **画面と JSON が綴りを参照している**ので同じく改名しないこと。

### 2-5. ⚠ 大きな範囲の文字列置換をしない

⚠ **2026-08-23に `game_manager.gd` で715行を丸ごと消した。** → ⚠ **`Edit` で1箇所ずつ当てる。**

### 2-6. ⚠ 「同じ形の判定」が散っていたら1本に寄せる

今ある1本ものは：
- ⚠ **`BattleSession.find_unit()`** ／ **`battle_controller._all_units()`** ／ **`StatusRegistry.entries_for()`**
- ⚠ **`GameManager.get_forge_material_tier()`** ／ **`add_to_inventory()`**（装備の個体を作る唯一の口）
- ⚠ **`GameManager.get_part_reject_reason()`** ／ **`get_equip_reject_reason()`** ／ **`grant_chest()`**
- ⚠ **`GameManager._roll_weighted_table()`**（⚠ **抽選の本体はここ1本だけ**）
- ⚠ **`GameManager.set_party_member()`** ／ **`get_party_candidates()`**
- ⚠ **`GameManager._plan_build()` / `_write_build()`** ／ **`format_apply_report()`**
- ⚠ **`GameManager.get_battle_skills()` / `get_battle_passives()` / `get_battle_runes()`**
- ⚠ **`GameManager.unlock_research_node()`** ／ **`get_research_board_of()`** ／ **`_research_effect_total()`**
- ⚠ **`GameManager.start_craft()` / `collect_craft()`** ／ **`get_part_upgrade_cost()` / `get_part_dismantle_refund()`**
- ⚠ **`MasterDataLoader.rune_skill_data()`** ／ **`_merge_character_files()`**
- ⚠ **`debug_boot._economy_sources()` / `_economy_sinks()`**（⚠ **対になっている。⚠ 片方だけ直さないこと**）

### 2-7. ⚠ E / W の次番号

⚠ **`E132` まで使用済み → `E133` から。** ⚠ **`W21` まで使用済み → `W22` から**（⚠ **`W3` `W6` `W7` は欠番**）。
⚠ **`E132` は `_validate_icon_config()`**（`game_manager.gd`。⚠ **`IconConfig` の割り当て・色数・段階を写す表2本。⚠ 2026-08-31**）。⚠ **黄は足していない。**
⚠ **`debug_boot` の `push_error` / `push_warning` に E / W の番号は付けない**（⚠ **本番の検証と番号がぶつかる。⚠ 2026-08-31 に一度 `E132` を採ってしまい、⚠ 番号を外した**）。
⚠ **`E131` / `W21` は `_validate_floor_config()`**（`game_manager.gd`。⚠ **`FloorConfig` の割り当てと層の重み4本。⚠ 2箇所壊して鳴ることを確認済み**）。

⚠ **2026-08-25 に一度ぶつけた**：⚠ **段階14-d で `E128` を採ったが、⚠ `E128` は `research.json` の検証（`master_data_loader.gd:344`）が使用済みだった。⚠ この §2-7 に「`E130` から」と書いてあったのに見落とした。⚠ `E130` へ直した。**
⚠ **番号を採る前に `grep -o "E1[0-9][0-9]"` で実コードを数え直すこと。⚠ この行を信じない。**

---

## 3. 調査済みの事実（**`grep`し直さなくてよい**・2026-08-25確認）

> ⚠ **ここは「実コードの現在の状態」であって仕様ではない。** ⚠ **仕様は `GAME_DESIGN.md`。⚠ 台帳が「置き換えろ」と言っている項目は、ここに書いてあっても変わる**（⚠ **§2-2 の実例**）。

| | 事実 |
|---|---|
| ⚠ ログの実体 | ⚠ **`C:\Users\admin\AppData\Roaming\Godot\app_userdata\pomodoro-heroes\logs\`**。`battle_last.jsonl`（戦闘のたびに上書き）／ `godot.log`（保持5本） |
| ⚠ **セーブの実体** | ⚠ **`...\app_userdata\pomodoro-heroes\saves\save_slot_0.json`**（⚠ **`user://saves/` の下。⚠ 直下ではない**） |
| ロード時の正常な出力 | `skills validated: 94 entries, 0 errors, 1 warnings` ／ `basic attacks validated: 19 entries, 0 errors, 0 warnings` ／ `items validated: 89 entries, 0 errors` ／ `runes validated: 25 entries, 0 errors` ／ `balance item refs validated: 0 errors` ／ `_sync_research_tree_from_master() -> 20 nodes` ／ `level cap validated: 20 + 80 (8 nodes) = 100, 0 errors` ／ `_sync_recipes_from_master() -> 3 recipes (unlocked=3, skipped=0)` ／ `_sync_shop_from_master('daily') -> 13 slots` |
| ⚠ **マスターは7本** | ⚠ **`items` / `stages` / `shop` / `research` / `recipes` / `chests` / `runes`**（⚠ **`characters` `enemies` は配下にフォルダを持つ**） |
| ⚠ **数値の置き場** | ⚠ **§1-1 の表**（ズレ39・2026-08-25に範囲を狭めた） |
| ⚠ **レベル** | ⚠ **上限 100**（⚠ **`base_level_cap` 20 ＋ 研究 `level_cap_unlock` 8件 × 10**）。⚠ **`E127` が起動のたびに突き合わせる**。⚠ **式は `base + growth * (level - 1) * (level - 1) / 245.0`（`base=3` / `growth=1.0`）＝ Lv100 まで **1,595 個**／3キャラで **4,785 個** |
| ⚠ **上限で測るとき** | ⚠ **`Balance.character.max_character_level` を使う。⚠ `get_effective_level_cap()` は研究を解放するまで 20 を返す** |
| ⚠ **スタミナ** | ⚠ **1周 5**（`adventure_config.gd`）。⚠ **入口はポモドーロのポーションだけ**（⚠ **集中25分で1個・1個 +50 ＝ 集中1分あたり0.4周**）。⚠ **時間で回復しない**。⚠ **`25分 / +50` は `.gd` の既定値＝設計役が直せる** |
| ⚠ **研究** | ⚠ **2ボード20ノード**（⚠ **ボード1＝12件・ボード2＝8件**）。⚠ **枝は `combat` / `chest` / `workshop`**。⚠ **効果は5種類**。⚠ **総コスト 790 個**（`construction_material_1..4`） |
| ⚠ **作業場** | ⚠ **装飾のくじ3レシピ**（`craft_part_1..3`）。⚠ **投入は `decor_material_1..3` ×12。⚠ 30分/90分/3時間**。⚠ **`stage_3` のクリアで開く**。⚠ **キューは1本（研究で+1）** |
| ⚠ **パッシブ** | ⚠ **本番3キャラ × 5件＝15件。⚠ Lv20/40/60/80/100 で解放。⚠ 解放されたものが全部効く（選ばない）** |
| ⚠ **`condition` の書き方** | ⚠ **`source` に書けるのは 10軸 ＋ `hp_current` `hp_lost` `hp_ratio` `hp_lost_ratio` `elapsed_sec` `alive_count_ally` `alive_count_enemy` `wave_index` `stack` `status_has`**（⚠ **`distance` は除く**）。⚠ **`of` は必ず書く**（⚠ **ズレ34**） |
| ⚠ **`react` の出来事** | ⚠ **`attacked` / `dealt_damage` / `took_damage` の3つだけ** |
| ⚠ **素材** | ⚠ **16件**。`construction_` / `training_` / `forging_` / `decor_` × `_1..4`。⚠ **入口が無いものが3件（`training_material_2..4`）。⚠ 出口が無いものが4件（それに `decor_material_4`）** |
| ⚠ **装備の等級** | ⚠ **1〜10**。⚠ 段階は `forge_material_tier_min_grades = [1,4,7,10]`。⚠ **コストは `[8,12,16,20,24,28,32,36,40]`** |
| ⚠ **刺す枠** | ⚠ **長さ8の固定配列**（`null` 込み・**位置が枠を表す**）。⚠ **開く等級は `part_slot_min_grades = [3,4,5,5,6,7,8,9]`** |
| ⚠ **装飾** | ⚠ **61件**（宝石12・護符8・紋章16 ／ ルーン25）。⚠ **くじに入るのは前の36件だけ**。⚠ **段階上げは `[10,20,40]`・壊すと `[3,5,10,20]`**。⚠ **段階の上限は `max_part_tier = 4`**（⚠ **5にしたときに増えるのは **9件**。⚠ 36件ではない＝ズレ40**） |
| ⚠ **装備の個体** | ⚠ **一意キーは `instance_id`（`eq_N`）。⚠ `equipment_instances` に入る。⚠ `inventory` を通らない** |
| ⚠ **編成** | ⚠ **状態が唯一の正**。⚠ **書く口は `set_party_member()` の1本** |
| ⚠ **本番ステージ** | ⚠ **`floor_1`〜`floor_5` の5本**（⚠ **2026-08-25 の段階14-a で `stage_1..3` から作り替えた**）。⚠ **各6層 ＋ ボス1＝1周7ノード**（2026-08-28・段階14-h で 5層 → 6層）。⚠ **報酬・スタミナ・クリア記録はボスだけ**。⚠ **ゴールドは 50 / 70 / 90 / 110 / 130 G**。⚠ **経験値素材は 2 / 3 / 5 / 6 / 6 個/周**（⚠ **これに宝箱ぶんが乗る＝economy は数えていない＝ズレ44**）。⚠ **宝箱は移動あたり 30% ＋1周最低1回の保証＝実測 1.87 個/周**。⚠ **周回の自動処理あり**（宿題49 は閉じた） |
| ⚠ **ショップ** | ⚠ **`daily` の13枠だけ**（⚠ **`shop.json` に `weekly` / `monthly` のキーが無い＝ズレ39**）。⚠ **`training_material_1` が x10 / x30 / x75 / x180 の4枠**。⚠ **`slot_id` 12 は出口の無い `decor_material_4`＝宿題53** |
| ⚠ **画面の解放** | ⚠ **`stage_1` → `guild` `equipment` `training` `warehouse` ／ `stage_2` → `pomodoro` `decoration` ／ `stage_3` → `research` `shop` `rune` `workshop`** |
| ⚠ **`F4` のデバッグパネル** | ⚠ **`tests/debug_overlay.gd`。⚠ 「ゴールド・ジェム・スタミナ」「素材を全種類」「消費アイテムを全種類」「装備を全種類 1個ずつ」「装飾を全種類」「研究を全部解放」「画面を全部解放」「製作をすぐ完了させる」「セーブする」** |
| 行数 | `game_manager` **約5200** ／ `battle_controller` **約1800** ／ `debug_boot` **約2800** ／ `master_data_loader` **約1350** |

```
battle_controller  … 入力と表示。ノードを触る唯一の層。⚠ ルーンの発火もここ（_fire_runes）
					 ⚠ パッシブの掛け直しもここ（_step_passives → _restore_passives）
	  ↓ cast()
SkillRuntime       … 待ち行列。trigger・購読の配布と発火・中断
	  ↓ 効果1件ずつ
SkillResolver      … 1つの効果を確定した対象に当てる。時間も段も知らない
	  ↓ host が none 以外
StatusRegistry     … 状態。寿命はスキルより長い

BattleLog          … 静的クラス。どの層からも呼べる（Autoload ではない）
```

### 3-1. ⚠ 検証の道具（**人間に頼る前にこれを回す**）

```powershell
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=area
```

- ⚠ **`Start-Process` ＋ `-RedirectStandardOutput` / `-RedirectStandardError` で実行する**（直接叩くと1行も返らない）
- ⚠ **読むのは `[System.IO.File]::ReadAllLines(path, UTF8)`**（`Get-Content` は化ける）
- ⚠ **警告とエラーは `-RedirectStandardError` のほう**（`push_warning` / `push_error` は stdout に出ない）
- ⚠ **赤黄を数えるときは行頭で絞る**（`^(ERROR|WARNING)`）
- ⚠ **今あるシナリオ（32本）**：⚠ **`floor`**（⚠ **段階14 の検証をまとめて持っている＝マップ生成・全ルート総当たり・宝箱の分布・レリック・たいまつ・周回。⚠ 黄3本が平常値**）／ `area` / `recast` / `recast_expire` / `summon` / `summon_wipe` / `lineup` / `mitigate` / `pierce` / `shield` / `reflect` / `reflect_self` / `intervene_legacy` / `aura` / `aura_follow` / `pool` / `atk_mult` / `dot_react` / `status_ui` / `status_ui_over` / `materials` / `parts` / `drops` / `presets` / `layout` / `runes` / `unlock` / `passives` / `research` / `workshop` / `economy` / `training`
- ⚠ **`training` はヘッドレスで終わらない**（⚠ **窓あり専用。⚠ 全シナリオを回すときは除く＝30本**）
- ⚠ **`materials` / `parts` / `drops` / `presets` / `layout` / `unlock` / `research` / `workshop` / `economy` は戦闘を回さない**（`kind: "report"`）
- ⚠ **`economy` は数値を直したら必ず回す**（⚠ **前後の数字を並べる唯一の道具**）
  - ⚠ **`_economy_sources()` と `_economy_sinks()` は対。⚠ 入口か出口を1つ足したら両方を見る**
  - ⚠ **末尾の宿題43の枝は状態を丸ごと入れ替える。⚠ 何かを足すならその前に置くこと**
  - ⚠ **`drops` は `chests.json` しか見ていない。⚠ `stages.json` の固定報酬を見るのは `economy` の「1周で入るもの」の行だけ**（宿題54）
  - ⚠ **`⚠ Lv1 の突き合わせ` は式の差し替えを検出できない**（⚠ **Lv1 はどの式でも3個。⚠ 宿題55**）
- ⚠ **`layout` は「はみ出していないか」を数字で見る唯一の道具**。⚠ **器を足した回・件数を増やした回は必ず回すこと**
  - ⚠ **測る器を足すときは `LAYOUT_PATHS` / `LAYOUT_ROWS` / `LAYOUT_SCENES` に1行足す**
  - ⚠ **排他で切り替わる器・段階解放で消える器は `LAYOUT_SCENE_SHOW` に1行足す**
  - ⚠ **測るのは一番外側の `Container`。⚠ ルートを測ると必ず 0 が返る**（ズレ36）
  - ⚠ **基準は `SCREEN_SIZE`（1280 x 720）。⚠ ヘッドレスの viewport（1280 x 1280）を使わないこと**
  - ⚠ **`ScrollContainer` の中は測れない**（⚠ **縦は人間しか見られない**）
- ⚠ **黄の平常値は 1本**（`skill_dbg_dot_odd`）。⚠ **`drops` と `parts` はもう1本ずつ多いのが正解**
- ⚠ **赤の平常値は 0本。⚠ ただし `unlock` は 1本（`E125`）・`workshop` は 2本（`E129`）出るのが正解**
- ⚠ **`ja.csv` を触った回は、⚠ 再インポートが済むまで研究画面の効果が「？」になる。⚠ 済んだかは `scenario=passives` の `ja.csv の再インポート:` の1行で分かる**
  - ⚠ **合図が見るキーは、⚠ その回に足したキーへ必ず差し替えること**（⚠ **ズレ37。⚠ 今は `ui_research_category_workshop`**）
- ⚠ **1本あたり10〜20秒。⚠ 30本で9分ほど。⚠ 10本ずつ3回に分けて回すこと**（⚠ **PowerShell ツールの既定タイムアウトは2分。⚠ `timeout` を伸ばすこと**）
- ⚠ **シナリオは `SCENARIOS` に1行足す。シーンを増やさない**
  - ⚠ **報告の枝は `_ready()` の `elif` にも1行要る**（⚠ **`REPORT_*` の定数と合わせて3箇所**）
- ⚠ **足した検証が本当に効くか、2箇所で壊して確かめる。⚠ 壊したら必ず戻し、平常値に戻ったことを再実行で確認する**
- ⚠ **画面のスクリプトは `debug_boot` から読み込まれない。⚠ `--check-only --script` で `Parse Error` を見る**（⚠ **ただし Autoload が読まれないため `Identifier not found: GameManager / Balance / SceneManager` は必ず出る。⚠ これは無視してよい**）

---

## 4. 罠（**直近で実際に踏んだものだけ**）

### ⚠ 完了条件に「数字」だけでなく「意味」を書くと取り違える（**2026-08-25**）

⚠ **数値を入れる回で、⚠ 完了条件に「出口が0件のものが 4件 → 1件になる」と書いた。⚠ 実際に一本化が消したのは `_2..4` の**入口**であって、⚠ 出口は4件のまま。**
→ ⚠ **数字は予想どおりでも、⚠ 自分の変更が表のどちらの列を動かすのかを取り違えていた。**
→ ⚠ **完了条件を書いたら、⚠ 「その変更が触るのは入口か出口か」を1回だけ声に出して確かめる。**

### ⚠ 全シナリオを回している最中にコードを触らない

⚠ **2026-08-23に `game_manager.gd` を編集し、赤560本の偽陽性を出した。**
→ ⚠ **回している間は `.gd` / `.tscn` / `.json` / `.csv` を触らない。⚠ `.md` はよい。**

### ⚠ 測る道具が「0」や「全部同じ数字」を返したら、まずその道具を疑う（**2026-08-25に2回目**）

⚠ **1回目**：⚠ `scenario=layout` が6シーンとも `最小幅 0` を返していたのに流した（ズレ36）。
⚠ **2回目**：⚠ **ギルドが `72 x 72` を返していた。⚠ 段階解放で5個とも `visible = false` のため。**
→ ⚠ **もっともらしい小さい数字を信じない。**

### ⚠ 既存の器を「そのまま呼ぶ」前に、中に何が乗っているか見る（**2026-08-25**）

⚠ **`_roll_chest_draw()` の中に `get_research_chest_draw_bonus()` が入っていた。**
→ ⚠ **抽選の本体を `_roll_weighted_table()` に切り出し、⚠ ボーナスは呼ぶ側に置いた。**

### ⚠ 上限を返す関数が、その時点の状態を見ていることがある（**2026-08-25**）

⚠ **`get_effective_level_cap()` は研究を解放するまで 20 を返す。**
→ ⚠ **上限で測りたいときは `Balance.character.max_character_level` を使う。**

### ⚠ 画面の完了条件に「待つ」を書かない（**2026-08-25**）

⚠ **作業場の回で「30分待つ」を書き、人間の確認がそこで止まった。**
→ ⚠ **待ち時間のある機能は、`F4` に飛ばすボタンを同じ回で用意する。**

### ⚠ 人間がまだやっていないことを「終わっている」と書かない（**2026-08-25**）

⚠ **`NEXT_STEPS` に「人間の宿題は全部終わっている」と書いたが、⚠ セーブの確認が未了だった**（`28f1254` で訂正）。

### ⚠ 検証で在庫を「減らすために操作を繰り返す」書き方をしない

⚠ **2026-08-24**：⚠ **`while count > 1: merge_runes()` が150回回り、出力が数万行になった。**
→ ⚠ **`economy` は `level_up_character()` を99回回さず、式を直接99回評価している。**

### ⚠ Bash ツールに PowerShell の書き方を渡さない

⚠ **ヒアストリング（`@'...'@`）を渡してコミットメッセージの先頭に `@` が入った。⚠ Bash では heredoc（`<< 'EOF'`）。**

### ⚠ `.tscn` を触らずコードでノードを足すときは、隣の兄弟の `size_flags` を見る

⚠ **5個前提の並びに6個目を足して、拠点の下段が丸ごと左右にはみ出した。**

### ⚠ 新しい `class_name` は、エディタを1回通すまで認識されない

⚠ **確かめ方**：`grep -c "<クラス名>" .godot/global_script_class_cache.cfg`

### ⚠ 正常系に警告を付けない・`print` を増やさない

**出したい記録は `BattleLog` へ。**（⚠ **`tests/` は例外。あちらは `print` が出口**）

### インデントはタブ

`.gd`はタブ。**`.json`も既存ファイルはタブ**（⚠ トップレベルだけ半角スペース2つのファイルが在る＝`stages.json` / `chests.json` / `runes.json` / `recipes.json`）。`ja.csv`はUTF-8（BOMなし・LF）。

---

## 5. 引き継いだ宿題

**⚠ 全件は `PROJECT_STATUS.md`「溜まっている宿題」を見ること。** ここは判断待ちと大きい穴だけ。

### ⚠ 人間の判断待ち（**このタスクで効くもの**）

1. ⚠ **`decor_material_4` に出口が無い**（宿題47）。⚠ **`max_part_tier` を上げるとアイテム36件の追加になる**
2. ⚠ **`training_material_2..4` が入口も出口も無い**（宿題52）
3. ⚠ **`shop.json` の `slot_id` 12 が出口の無い素材を 5000G で売っている**（宿題53）
4. ⚠ **`construction_material_4` の入口がショップだけ**（宿題48。⚠ **人間が「触らない」と決めた**）
5. ⚠ **`GAME_DESIGN` 4-1 の周回ステージ・スキップ周回が実装に無い**（宿題49）。⚠ **2026-08-25 に「やる」へ戻った**（⚠ **`PLAN_SCENARIO_MAP.md` §6**）
6. ⚠ **①戦闘の秒数と装飾の出目を測っていない**（宿題50）。⚠ **鍛冶・装飾・宝箱・ショップ価格の数値は「勘」のまま**（宿題56）
7. ⚠ **`W16`（知らない欄）を赤に上げるか** ／ ⚠ **godot MCP の設定を消すか** ／ ⚠ **`tests/` の既存9件の棚卸し** ／ ⚠ **Ziva の `.bak` が7件残っている**
8. ⚠ **`ja.ja.translation` が縮んだ理由が不明**（⚠ **再インポートで作り直される。⚠ どのコミットにも含めていない**）

### ⚠ 器の穴（**大きいものだけ**）

9. ⚠ **ルーンのかけらが無い** ／ ⚠ **ルーンの本番入手経路が無い**（`F4` だけ）
10. ⚠ **`GROWTH_PASSIVES`（状態）とキャラプリセットの `passives` が、誰も読まない欄として残っている**
11. ⚠ **`crafting_queue` の `output_item_id` / `recipe_type` が `draw` レシピでは `""` になる**
12. ⚠ **作業場のアップグレードが無い** ／ ⚠ **掘削（9-3-1）はデモ範囲外**
13. ⚠ **`react` の中で `buff` / `heal` を出す形が本番に0件** ／ ⚠ **`host: point` が本番に0件**
14. ⚠ **プリセットに名前を付けられない**（自動名）／ ⚠ **キャラプリセットを消せない** ／ ⚠ **`party_changed` シグナルが無い**
15. ⚠ **等級10の「部位固有のパッシブ」が未実装** ／ ⚠ **護符が宝石と仕組み上同じ**
16. ⚠ **移動のロック中であることが画面に出ない** ／ ⚠ **ルーンのCDが画面に出ない** ／ ⚠ **パッシブが戦闘画面のどこにも一覧で出ない**
17. ⚠ **召喚の同時数に上限が無い** ／ ⚠ **召喚はスキルもパッシブも持てない**
18. ⚠ **「死亡時発動」と「他人の蘇生」はまだ書けない** ／ ⚠ **多段の2発目に投射物が出ない**
19. ⚠ **反射は1段だけ** ／ ⚠ **DoT は反射しない** ／ ⚠ **シールドが複数付いたときの吸う順が未定**
20. ⚠ **オーラの範囲が画面に描かれない** ／ ⚠ **状態の色が3つしかない** ／ ⚠ **状態の残り時間がマスに出ない**
21. ⚠ **研究にゴールド払いが無い**（⚠ **`GAME_DESIGN` 9-1 は「ゴールドと各種資源」と言っている**）
22. ⚠ **研究の宝箱枝が「抽選回数」だけ** ／ ⚠ **ボードは2枚しか無い**

### ⚠ 表示の穴

23. ⚠ **戦闘結果の報酬画面に `rewards.inventory` が出ない**（⚠ **宝箱も出ない**）
24. ⚠ **素材欄・倉庫の持ち物タブが Dictionary のキー順**
25. ⚠ **`apply_battle_rewards()` が `gems` と `stamina` を読まない** ／ ⚠ **`open_chest()` が `stamina` を読まない**
26. ⚠ **`weapon_steel_sword` がどこからも出ない** ／ ⚠ **`ChestScheduleEntry.chest_type` だけ語が揃っていない**
27. ⚠ **プリセットを適用したとき、編成の3人の間で装備が移るぶんはメッセージに出ない**（意図的）

### 片付け

28. **検証用のものはリリース前に消す**（`stage_dbg_*` ／ `skill_dbg_*` ／ `st_dbg_*` ／ `char_debug_*` ／ `enemy_dbg_*` ／ `passive_dbg_*` ／ `summons.json` ／ `tests/debug_boot` ／ `tests/debug_overlay` ／ `ui_status_ch_*` ／ ⚠ **`GameManager.get_party_candidates()` の `OS.is_debug_build()` 分岐**）
29. ⚠ **`GameManager.PRESET_EQUIPMENT_ENABLED` の定数と分岐が残っている**
30. ⚠ **`ui_skill_select_passive_slot` と `ui_skill_select_passive_candidates_header` の2行が未使用**
31. ⚠ **共有部品 `BuildPresetRow` を作れていない**（⚠ **育成と装備に同じ行が2本ある**）
32. ⚠ **`pomodoro_config.gd` の `gold_per_focus_minute` / `stamina_per_focus_minute` / `materials_per_focus_minute` が誰も読まない欄**

---

## 6. 終わったあと

**このファイルを、次のタスクの内容に書き換える。**
⚠ **`debug_boot` の `SCENARIOS` は消さない**（次の回で使い回す）。
⚠ **`PLAN_IMPLEMENTATION.md` 3章の 14-a〜14-f の状態列を直す**（⚠ **段階単位の完了はあそこ1箇所で持つ**）。
⚠ **段階14 のあとに残っているもの**：
  1. ⚠ **フロア系レリック**（宝箱の率・たいまつ・休憩の回復量・ショップの割引＝決定11 で「あと」に回した）
  2. ⚠ **数値の調整13件**（`PLAN_SCENARIO_MAP.md` §10-2）。⚠ **特にレアリティの中身・たいまつの値段・ノードの出現比は勘のまま**
  3. ⚠ **レアリティの色・エフェクト**（§4-5 が未達）
  4. ⚠ **宿題62（ホバーで説明が出る共有部品）**
  5. ⚠ **段階13（SDキャラとアニメーション＝専用の会話）**
  6. ⚠ **難ダンジョン**（⚠ **`PLAN_SCENARIO_MAP.md` §12。⚠ やると決めたら拒否仕様を先に裁く**）
⚠ **素材を1件でも動かしたら `scenario=economy` を回し直し、⚠ 前後の数字を EXEC に並べる。**
⚠ **`§0 事前チェック`（人間に渡す前にヘッドレスを1回回す）は次の回から戻る**（⚠ **前の回は `.gd` / `.json` を1件も触らないための例外だった**）。
⚠ **決まったことは `PLAN_SCENARIO_MAP.md` に書く。⚠ このファイルに写さない。**
⚠ **`EXEC_PLAYTHROUGH.md`（通しで遊ぶ回）は保留のまま残す。⚠ ステージが作り替わったあとに回す。**
⚠ **段階13（SDキャラとアニメーション＝専用の会話）は段階14 のあと**（⚠ **人間の決定・2026-08-25**）。
