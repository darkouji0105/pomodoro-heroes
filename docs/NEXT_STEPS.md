# 次にやること：**人間に選んでもらう**（状態のUIが入った）

**このファイルは「次の1タスク」だけを書く。** 終わったら次のタスクの内容に書き換える。全体の状況は`PROJECT_STATUS.md`、ルールは`AGENTS.md`と`CLAUDE.md`、**ゲームの中身は`GAME_DESIGN.md`**、**決定台帳は`docs/01_plan/PLAN_SKILL_TEMPLATE.md`**。

**このファイルだけ読めば着手できるように書いてある。** 過去のタスクを知らない前提で読んでよい。

⚠ **今回も「次に何をやるか」から人間に確認すること**（§1）。⚠ **順番の判断は人間がする。**

---

## 0. 前のタスクは終わっている（**2026-08-22・状態のUI**）

**指示書は `docs/02_exec/EXEC_STATUS_UI.md`。** ⚠ **§5・§6（ログ／ファイル）は設計役がヘッドレスで実測済み。**

⚠ **§7（画面）が未確認なら、まずそれをやること。** ⚠ **`EXEC_STATUS_UI.md` §7 に12項目ある。⚠ 人間しか取れない。**

### 0-1. ⚠ 直近3回で入ったもの（**全件は `PROJECT_STATUS.md`**）

| 回 | 入ったもの |
|---|---|
| **オーラ** | ⚠ **`host: point` の状態が範囲内のユニットに直接効く。`zone{}`（`radius` / `team` / `follow`）** ／ ⚠ **周期回復（`dot` の `heals: true`）** ／ ⚠ **範囲の出入りを `ev: "zone"` で記録** |
| **無音の穴** | ⚠ **死亡中はCDを進めない** ／ ⚠ **`atk_mult_pct`（攻撃力の倍率）** ／ ⚠ **DoT の周期ダメージで購読（`react`）が発火する** ／ ⚠ **効果の直下の「知らない欄」を W16（黄）で出す** |
| **状態のUI** | ⚠ **状態が画面に出るようになった。⚠ 味方＝スキルボタンの左に縦・敵＝HPバーの上に横** ／ ⚠ **色付きのマス ＋ 漢字1文字**（⚠ **画像素材が1枚も無いため `ColorRect` ＋ `Label`**） ／ ⚠ **色は3つだけ**（`dot`＝赤 ／ `dot heals`＝緑 ／ `buff` と `react`＝青） ／ ⚠ **`StatusRegistry.entries_for()` を新設**（⚠ **状態を引く唯一の口**） |

### 0-2. ⚠ 直近の人間の決定（**覆すときは影響範囲が広い**）

1. ⚠ **コンボは作らない**（2026-08-22）
2. ⚠ **`target.range` は触らない**（⚠ **穴ではない。本番データに書くかはバランスの話**）
3. ⚠ **バランスの実測は「だいぶ後」**（2026-08-22。⚠ **`PROJECT_STATUS.md` 794章「測る対象そのものが作り直しになる」の前提がまだ生きている。⚠ 作業場の作り直しが未着手**）
4. ⚠ **状態の色は3つだけ。⚠ デバフも青に含める**（⚠ **器に「良い／悪い」の欄が無い。分けるなら欄が要る**）
5. ⚠ **味方の状態はスキルボタンの左に縦並び**（⚠ **理由：スキルボタンはいずれ絵だけになる**）
6. ⚠ **マスの数を決め打ちにしない。⚠ 件数に応じて大きさを変える**（16 / 12 / 8px の3段）

---

## 1. ⚠ このタスク：**次に何をやるかを決める**

### 1-1. ⚠ 候補（**設計役の推奨は「作業場の作り直し」**）

| 候補 | 中身 | 大きさ |
|---|---|---|
| ⚠ **作業場の作り直し**（推奨） | ⚠ **`GAME_DESIGN.md` 9-3 で決定済みなのに未着手。⚠ 素材変換と装備製作を廃止し、中間素材と装飾のランダム製作にする。⚠ これが未着手な限り、バランスは測っても作り直しになる**（決定3） | 大 |
| **パッシブ15個の中身** | ⚠ **3人 × 5個（Lv20/40/60/80/100）。⚠ 枠組みは決定済み**（`GAME_DESIGN.md` 5-4）。⚠ **`.gd` を触らず JSON と `ja.csv` だけで終わる見込み＝Ziva に渡せる。⚠ 器の残りの穴が実データであぶり出される副次効果もある** | 中 |
| **見た目の続き** | ⚠ **オーラの範囲が画面に描かれない**（宿題16）／ ⚠ **状態の残り時間がマスに出ない** ／ ⚠ **多段の2発目に投射物が出ない**（宿題13） | 中 |
| **購読を `host: battle` / `point` へ広げる** | ⚠ **罠（`point` × 購読）が書けない。⚠ `host: battle` は誰も読まない**（宿題6） | 大 |
| **音の第2弾** | ⚠ **`play_bgm()` が未実装。⚠ 戦闘SE・UI SE がゼロ。⚠ 音量設定UIは設定画面ごと無い** | 中 |

⚠ **推奨の理由**：⚠ **決定3で「バランスは後」と決めた根拠が作業場だから。⚠ ここを畳まないと、次に何を作っても同じ理由で先送りが続く。**

⚠ **違うと思ったら言うこと。順番の判断なので覆してよい。**

### 1-2. ⚠ 着手前に決めること（**候補によって変わる。⚠ 設計役が勝手に決めない**）

⚠ **候補を選んだら、⚠ 設計役が「決めること」を推奨付きで出してから聞くこと。**

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ ドキュメントの「実装済み」を信じない

**ズレが16回起きている。** ⚠ **`grep` で関数の中身を見てから判断する。⚠ 読んだ結果ドキュメントが違っていたら報告する（勝手に直さない）。**

⚠ **直近2回で実際に見つけたズレ**：

- ⚠ **15件目：`NEXT_STEPS` が本番ステージを `stage_001` 〜 `003` と書いていた**（⚠ **実データは `stage_1` / `stage_2` / `stage_3`**。⚠ **このファイルでは直してある**）
- ⚠ **16件目：「`ja.csv` はヘッドレス起動で自動的に再インポートされる」が違う**（⚠ **§4 参照。⚠ 未修正・人間の判断待ち**）

### 2-2. ⚠ 「同じ形の判定」が散っていたら1本に寄せる

⚠ **この器で3回踏んでいる。** → ⚠ **`BattleSession.find_unit()` ／ `battle_controller._all_units()` ／ `StatusRegistry._applies_to()` の3本に寄せてある。⚠ 新しい配列・新しい `host` を足したらここだけ直す。**

⚠ **`StatusRegistry` は今回1本増えて4本になった**：
- ⚠ **`_hosts(entry, unit_id)`** … 宿主一致・範囲内だけを見る。⚠ **新しい `host` を足すときに直すのはここ**
- ⚠ **`_applies_to(entry, unit_id)`** … `active`（条件）のゲート ＋ `_hosts()`。⚠ **「効くか」を判断する側は必ずこちら**
- ⚠ **`entries_for(unit_id)`** … 画面が状態を引く唯一の口。⚠ **`_hosts()` を直接呼んでよいのはここだけ**（条件が偽の状態も返す＝半透明で描くため）

### 2-3. ⚠ 画面の完了条件は「その配置で本当に起きるか」を確かめてから書く

⚠ **3回続けて外した**（後衛の召喚は狙われない／反射を持つキャラが前衛でない／味方どうしのオーラは相対距離が変わらない）。
→ ⚠ **`EXEC_STATUS_UI.md` §1-1 では先に本番データを全部引いてから書いた。⚠ この形を続けること。**

### 2-4. ⚠ E / W の次番号

⚠ **E117 まで使用済み → E118 から。** ⚠ **W17 まで使用済み → W18 から**（**W6 と W7 は欠番**）。

---

## 3. 調査済みの事実（**`grep`し直さなくてよい**・2026-08-22確認）

| | 事実 |
|---|---|
| ⚠ ログの実体 | `C:/Users/admin/AppData/Roaming/Godot/app_userdata/pomodoro-heroes/logs/battle_last.jsonl`。⚠ **戦闘のたびに上書き** |
| ⚠ 出力パネル | `.../logs/godot.log`。⚠ **保持5本。設計役が走らせると人間のログが1本消えるので、読む前に走らせない** |
| ロード時の正常な出力 | ⚠ **`skills validated: 79 entries, 0 errors, 1 warnings`**（黄1本は `skill_dbg_dot_odd` の端数＝**出るのが正解**）／ `basic attacks validated: 19 entries, 0 errors, 0 warnings` |
| 出る出来事 | `battle_start` / `wave` / `cast` / `damage` / `heal` / `dot` / `react` / `status_add` / `status_end` / `status_clear` / `condition` / `intervene` / `recast` / `spawn` / `zone` / `result` |
| ⚠ **`battle_last.jsonl` の欄名** | ⚠ **`status_add` は `{t, ev, status, kind, unit, src, life, dur}`。⚠ `status_id` ではなく `status`。⚠ `unit_id` ではなく `unit`** |
| ⚠ 座標の定数 | **`GROUND_Y = 240` ／ 味方 `PARTY_BASE_X=200` `STEP=100` ／ 敵 `ENEMY_BASE_X=900` `STEP=100`** |
| ⚠ **射程の段** | ⚠ **`60`（前衛）／ `180`（中衛）／ `300`（後衛）／ `420`（最後衛）。⚠ 同じウェーブに出る型は別の段に置く** |
| ⚠ ユニットを引く | ⚠ **`BattleSession.find_unit()` の1本** |
| ⚠ 走査 | ⚠ **`battle_controller._all_units()`（味方 → 敵 → 召喚）。並びを変えない** |
| ⚠ **状態が効くか** | ⚠ **`StatusRegistry._applies_to()`。⚠ 画面が引くのは `entries_for()`**（§2-2） |
| ⚠ 編成 | ⚠ **状態が唯一の正**（`battle_controller.gd`）。⚠ **`stages.json` の `party_id` では決まらない** |
| ⚠ スキル枠 | **`SKILL_SLOT_COUNT` は 2**（⚠ **1体で「効果を付ける」＋「殴る」を書くと枠が尽きる**） |
| ⚠ **本番ステージ** | ⚠ **`stage_1` / `stage_2` / `stage_3` の3本 × 各5ウェーブ。⚠ 5波目が全部ボス**（⚠ **`stage_001` ではない**） |
| ⚠ **本番の編成** | ⚠ **`party_default` ＝ 僧侶(180) / 弓(300) / 剣士(60) の3人固定** |
| ⚠ **本番で出る状態は3件だけ** | ⚠ **`status_power_slash_atk_up`（剣士・自分に `atk+6`・8秒）／ `status_wide_sweep_guard`（剣士・チャージ中だけ `def+20`）／ `status_holy_burn`（僧侶・敵全員に毒・6秒・`max_stack` 5）** |
| ⚠ **本番の敵は `skills` を持たない** | ⚠ **`enemy_slime` / `enemy_wolf` / `boss_slime_king` の3体とも。⚠ 敵が味方に状態を付ける経路が本番に無い** |
| ⚠ **本番に `react` と `host: point` が0件** | ⚠ **器はあるがプレイヤーには一度も見えない**（⚠ **検証用シナリオでしか出ない**） |
| ⚠ **画像素材が0件** | ⚠ **`assets/images/` は `.gitkeep` だけ。⚠ アイコンは描けない** |
| 検証用ステージ | `stage_dbg_enemy_skill` ／ `_condition` ／ `_intervene` ／ `_passive` ／ `_area` |
| 行数 | `game_manager` 3048 ／ `battle_controller` 1712 ／ `status_registry` 1516 ／ `skill_schema` 1490 ／ `skill_resolver` 858 ／ `debug_boot` 853 ／ `master_data_loader` 675 ／ `skill_runtime` 536 ／ `battle_log` 379 ／ `unit` 368 ／ `unit_view` 178 ／ `status_chips` 168 ／ `battle_session` 133 |

```
battle_controller  … 入力と表示。ノードを触る唯一の層
	  ↓ cast()
SkillRuntime       … 待ち行列。trigger・購読の配布と発火・中断
	  ↓ 効果1件ずつ
SkillResolver      … 1つの効果を確定した対象に当てる。時間も段も知らない
	  ↓ host が none 以外
StatusRegistry     … 状態。寿命はスキルより長い

BattleLog          … 静的クラス。どの層からも呼べる（Autoload ではない）
```

### 3-1. ⚠ 検証の道具（**人間に頼る前にこれを回す**）

```powershell
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=area
```

- ⚠ **`Start-Process` ＋ `-RedirectStandardOutput` / `-RedirectStandardError` で実行する**（直接叩くと1行も返らない）
- ⚠ **読むのは `[System.IO.File]::ReadAllLines(path, UTF8)`**（`Get-Content` は化ける）
- ⚠ **警告とエラーは `-RedirectStandardError` のほう**（⚠ **`push_warning` / `push_error` は stdout に出ない**）
- ⚠ **今あるシナリオ（20本）**：`area` / `recast` / `recast_expire` / `summon` / `summon_wipe` / `lineup` / `mitigate` / `pierce` / `shield` / `reflect` / `reflect_self` / `intervene_legacy` / `aura` / `aura_follow` / `pool` / `atk_mult` / `dot_react` / **`status_ui`** / **`status_ui_over`** / `training`（⚠ **`training` は窓あり専用**）
- ⚠ **1本あたり10〜20秒。⚠ 全部回すと4分以上かかるので、⚠ PowerShell の既定タイムアウト（2分）に当たる。分けて回すこと**
- ⚠ **シナリオは `SCENARIOS` に1行足す。シーンを増やさない**
- ⚠ **`fire` の行に使える欄**：`gap`（間隔の上書き）／`prepare`（`damage_party` / `kill_party`）／`skill: ""`（下ごしらえ・待つだけの行）
- ⚠ **1行目の `gap` は効かない**（`_last_fire_sec` の初期値が `-999`）。⚠ **待たせたいときは「待つだけの行」を2つ書く**
- ⚠ **同じスキルを続けて撃つときは `gap` を CD 以上にする**（⚠ **短いと撃てずに黙って飛ぶ**）
- ⚠ **位置は `_dump_positions()` が出す**（`合図` / `静止` / `決着` の3回。`狙う=` も出る）
- ⚠ **検証用スキルは `characters.json` の候補一覧にも足すこと**（`game_manager.gd:2129`）
- ⚠ **数値で区別したいなら `attack_type: "true"`**（`physical` は敵の `def` で潰れる）
- ⚠ **敵の `hp` は 400。1体に 400 を超えて当てると死んで、次の一撃が別の敵に飛ぶ**
- ⚠ **足した検証が本当に赤を出すか、データを一時的に壊して確かめる。無音で通る検証が一番怖い**
- ⚠ **人間に渡す前に、全シナリオを1回ずつ回す**

---

## 4. 罠（**直近で実際に踏んだものだけ**）

### ⚠ 新しい `class_name` は、エディタを1回通すまで認識されない（**今回1タスク分足止めされた**）

⚠ **新規 `.gd` に `class_name` を書いても、⚠ ヘッドレス実行では `.godot/global_script_class_cache.cfg` に登録されない。**
⚠ **参照している側が `Parse Error: Could not find type "..."` でロードできず、⚠ 戦闘が始まらないまま `GIVE_UP_SEC`（180秒）まで回る。**

→ ⚠ **`Node` 型 ＋ `has_method()` で逃げないこと**（`AGENTS.md`「エラーを理由にルールを緩めない」）。
→ ⚠ **人間にエディタを1回通してもらう。⚠ 新規クラスを作る回は、これを最初に頼むこと。**
→ ⚠ **確かめ方**：`grep -c "<クラス名>" .godot/global_script_class_cache.cfg`

### ⚠ `ja.csv` の再インポートも、エディタを通すまで走らない

⚠ **行を足しても `localization/ja.ja.translation` が古いままだと、⚠ `tr()` はキー文字列をそのまま返す。**
→ ⚠ **確かめ方**：`ls -la localization/ja.csv localization/ja.ja.translation` で日時を比べる。
→ ⚠ **上の `class_name` と同じ操作で一緒に解ける。⚠ 頼むのは1回でよい。**

### ⚠ `--check-only --script` は Autoload を読まない

⚠ **`Identifier not found: Balance` / `GameManager` / `SceneManager` が出るが、⚠ これは構文エラーではない。**
⚠ **`Parse Error` が0件なら構文は通っている。⚠ 実機を回す前に構文だけ確かめたいときに使える。**

### ⚠ 敵が自分に状態を掛ける前に殺すと、検証が「無音で通る」

⚠ **待たずに撃つと `intervene` が1行も出ないのに `exit=0` で終わる。**

### ⚠ 検証用の状態は、シナリオの中で寿命が切れる長さにする

⚠ **`duration_sec: 30` だと戦闘が先に終わり、⚠ 「消えたのか終わったのか」が区別できない。**

### ⚠ 「敵に付ける効果 × 前後の比較」は1シナリオに1件まで

### ⚠ 静的な突き合わせは実機の代わりにならない

⚠ **ヘッドレスが使える。真似ずに走らせること。**

### ⚠ Windows の bash で `cat >>` すると追記分が CRLF になることがある

⚠ **`ja.csv` への48行の追記では起きなかった**（`od -c` で確認済み）。⚠ **追記したら毎回確かめること。**
⚠ **`grep -c $'\r'` は Git Bash で空パターン扱いになり全行に当たる。⚠ `awk '/\r/{n++}'` で数えること。**

### ⚠ `_ready()` の中では root に `add_child()` も画面遷移もできない

`Parent node is busy ...` で弾かれる。⚠ **`call_deferred` を使う。**

### 正常系に警告を付けない・`print` を増やさない

**出したい記録は `BattleLog` へ。**（⚠ **`tests/` は例外。あちらは `print` が出口**）

### インデントはタブ

`.gd`はタブ。**`.json`も既存ファイルはタブ**（⚠ `stages.json` だけトップレベルが半角スペース2つ）。`ja.csv`はUTF-8（BOMなし）。

---

## 5. 引き継いだ宿題

**⚠ 全件は `PROJECT_STATUS.md`「溜まっている宿題」を見ること。** ここは判断待ちと大きい穴だけ。

### ⚠ 人間の判断待ち

1. ⚠ **`AGENTS.md` の「`ja.csv` は人間が再インポート」は正しかった**（⚠ **`NEXT_STEPS` 側の「自動で走る」が間違いだった。⚠ このファイルでは §4 に直して書いてある。⚠ `AGENTS.md` は直さなくてよい**）
2. ⚠ **射程の段のルールが `EXEC_BATTLE_LINEUP.md` にしか書いていない**（`AGENTS.md` か PLAN に載せるか）
3. ⚠ **`summons.json` を分けたのは設計役の判断**（マスターファイルが5本目）
4. ⚠ **台帳（PLAN 8章）の「`charge` 欄の有無で分岐している」が実コードと違う**（段階5で報告済み・未修正）
5. ⚠ **godot MCP の設定を消すか** ／ **`tests/` の既存9件の棚卸し** ／ **Ziva の `.bak` が7件残っている**
6. ⚠ **W16（知らない欄）を赤に上げるか**（⚠ **既存データでは0件**）

### ⚠ 器の穴（**大きいものだけ**）

7. ⚠ **購読は `host: unit` のみ**（罠が載らない）。⚠ **`host: battle` は誰も読まない**
8. ⚠ **召喚の同時数に上限が無い** ／ ⚠ **召喚はスキルもパッシブも持てない**
9. ⚠ **同じ型が複数体出ると立ち位置が重なる**
10. ⚠ **段が保証するのは味方側だけ**
11. ⚠ **`stack` の5部品のうち上限だけ入れた**（消え方・再付与・閾値は未実装）
12. ⚠ **「死亡時発動」と「他人の蘇生」はまだ書けない**
13. ⚠ **多段の2発目に投射物が出ない**
14. ⚠ **反射は1段だけ** ／ ⚠ **DoT は反射しない**（どちらも PLAN 10-2 の決定どおり）
15. ⚠ **軽減の上限95%はコードの定数** ／ ⚠ **シールドが複数付いたときの吸う順が未定**
16. ⚠ **オーラの範囲が画面に描かれない**（⚠ **効いている相手にマスは出るようになった。⚠ 範囲そのものは `zone` のログでしか追えない**）
17. ⚠ **未実測が2件**：⚠ **追従中に付与者が死んだら座標が止まるか** ／ ⚠ **オーラに `atk_mult_pct` を書いたときに効くか**（⚠ **範囲の毒沼で購読が発火するかは `pool` で確認済み**）

### ⚠ 状態のUIで増えたもの

18. ⚠ **色が3つしかない。⚠ デバフが青**（⚠ **分けるなら器に「良い／悪い」の欄が要る**）
19. ⚠ **状態の残り時間がマスに出ない**（⚠ **16px に数字が入らない**）
20. ⚠ **`ui_status_ch_*` の45行は検証用。⚠ リリース前に消す**
21. ⚠ **スキルボタンが絵になったら、⚠ 左の帯の幅を見直す**（⚠ **人間の指示の前提**）

### 片付け

22. **検証用のものはリリース前に消す**（`stage_dbg_*` ／ `skill_dbg_*` ／ `st_dbg_*` ／ `char_debug_*` ／ `enemy_dbg_*` ／ `summons.json` ／ `tests/debug_boot` ／ ⚠ **`ui_status_ch_*` の45行**）
23. ⚠ **フォルダを増やしたら定数に1行足す**（`CHARACTER_DIRS_REQUIRED` / `ENEMY_DIRS_*`）

---

## 6. 終わったあと

**このファイルを、次のタスクの内容に書き換える。**
⚠ **`debug_boot` の `SCENARIOS` は消さない**（次の回で使い回す）。
