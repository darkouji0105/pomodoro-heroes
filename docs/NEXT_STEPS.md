# 次にやること：**立ち位置をずらす（重なりの解消）**

**このファイルは「次の1タスク」だけを書く。** 終わったら次のタスクの内容に書き換える。全体の状況は`PROJECT_STATUS.md`、ルールは`AGENTS.md`と`CLAUDE.md`、**ゲームの中身は`GAME_DESIGN.md`**、**決定台帳は`docs/01_plan/PLAN_SKILL_TEMPLATE.md`**。

**このファイルだけ読めば着手できるように書いてある。** 過去のタスクを知らない前提で読んでよい。

⚠ **このタスクの選び方だけ、先に人間に確認すること**（§1-0）。器（段階1〜6）は全部載ったので、**次に何をやるかは順番の判断になる。**

---

## 0. 前のタスクは終わっている（**2026-08-21・段階6 ＝ `spawn`**）

**指示書は `docs/02_exec/EXEC_SKILL_SPAWN.md`。** ⚠ **§5・§6（ログ／ファイル）は設計役がヘッドレスで全項目実測済み。§7（画面）は人間が実施。**

⚠ **これで PLAN 19章の段階1〜6が全部載った。** 器の追加は一区切り。

### 0-1. 段階6で入ったもの（**要点だけ。全件は `PROJECT_STATUS.md`**）

| | 入ったもの |
|---|---|
| 効果 | ⚠ **`type: "summon"`**（`unit_id` / `count` / `duration_sec` / `offset_x` の4欄・**全部必須**） |
| 素データ | ⚠ **`resources/balance/master/summons.json`（5本目のマスターファイル）** |
| 配列 | ⚠ **`BattleSession.summon_units`（専用配列）。`is_party_wiped()` / `is_wave_cleared()` は1行も触っていない** |
| 探し方 | ⚠ **`BattleSession.find_unit()` に一本化した**（複製4本を寄せた＝旧宿題20は解消） |
| 記録 | ⚠ **`BattleLog.log_spawn()`（`ev: "spawn"`・`why` は `begin` / `expire` / `owner_death` / `death` / `clear`）** |
| 検証 | ⚠ **`E93`〜`E101` ／ `W13`。`host: "spawn"` は赤になった**（`W6` は欠番） |

### 0-2. ⚠ 段階6の人間の決定（**覆すときは影響範囲が広い**）

1. **座標は JSON で指定**（`offset_x`）。⚠ **符号は「敵に向かう向きが正」＝前衛が正・後衛が負**
2. **召喚は頭数に入らない**（専用配列）
3. **期限切れは死亡ではない。召喚者が死んだら召喚も消える**
4. **発動者は召喚自身**（本体に戻さない）
5. ⚠ **同時数の上限は作らない**（§2-3 の穴）
6. **ウェーブ交代とリトライで消える**
7. **`type: "summon"` が正。`host: "spawn"` は赤**

### 0-3. ⚠ 積み残し（**後回し。忘れないこと**）

| 積み残し | なぜ止めたか |
|---|---|
| **コンボ**（購読の `host: battle` 拡張 ＋ `combo_count`） | ⚠ **2026-08-17、人間の判断で後回し。**「見た目が整わないと検証が難しすぎる」 |
| **`point` の条件（オーラ）** | 真偽が「状態 × ユニットの対」ごとになり、今の `active` では足りない |
| ⚠ **立ち位置が重なる** | ⚠ **このタスク。下を見ること** |

---

## 1. このタスク：**立ち位置をずらす**

### 1-0. ⚠ なぜこれが次なのか（**人間に確認すること**）

⚠ **同じ症状が3回別々の場所から出ている。**

| いつ | 症状 |
|---|---|
| 段階4（`area`） | ⚠ **範囲攻撃の巻き込みが画面から読めない。** 同じ射程のユニットは**同じ x に重なって停まる**ので数字が重なる |
| 段階3（コンボ） | ⚠ **「見た目が整わないと検証が難しすぎる」で人間が後回しにした** |
| 段階6（`spawn`） | ⚠ **召喚の x が既存のユニットと重なりうる** |

⚠ **旧 `NEXT_STEPS` §4 の順番表でも、コンボ（2番）に「見た目が整ってから」と条件が付いていた。** ⚠ **つまり実質、これが先。**

⚠ **ただし「見た目」は広い。** ⚠ **このタスクをどこまでにするかは人間が決めること**（§1-2 の1）。

### 1-1. ⚠ 台帳が決めていること（**これしか決まっていない**）

| | 決定 |
|---|---|
| 2次元化 | ⚠ **PLAN 16章。⚠ 「担当外にするもの」に入っている**（21章の下） |
| `offset` | ⚠ **効果の `offset`（2次元化）はまだ無い。**「敵の団の少し手前で爆発」が書けない |
| `area` の距離 | ⚠ **1次元（`absf(x - x)`）** |
| ユニットの停まる位置 | ⚠ **`距離 <= attack_range` で止まる。狙う相手は死ぬまで変えない** |
| 絵 | ⚠ **`assets/images/` は空。キャラは 64×64 の `ColorRect` 1枚** |

⚠ **「立ち位置をずらす」が2次元化そのものなのか、表示だけずらすのかは決まっていない。** ⚠ **これが §1-2 の本題。**

### 1-2. ⚠ 着手前に決めること（**台帳に無い。人間の判断が要る**）

⚠ **設計役が勝手に決めない。EXEC を書く前に聞くこと。**

1. ⚠ **どこまでやるか**（**表示だけずらす** ／ **戦闘の距離計算も2次元にする** ／ **スプライトやエフェクトまで**）
   ⚠ **距離計算を2次元にすると、`attack_range` / `area` の `radius` / `distance` のスケール変数が全部同時に動く**（バランスが変わる）
2. ⚠ **何をずらすか**（`y` をずらす ／ `x` を微妙にずらす ／ 両方）
   ⚠ **`x` をずらすと当たり判定（射程）が動く。`y` だけなら数値は1つも変わらない**
3. ⚠ **並ぶ順を何で決めるか**（生成順 ／ チーム ／ HP順）。⚠ **順が毎フレーム変わると画面がちらつく**
4. ⚠ **召喚を列に入れるか**（段階6で増えた。⚠ **上限が無いので理論上いくらでも並ぶ**）
5. ⚠ **何体まで並べるか**（画面の高さは 1280。⚠ **溢れたらどうするか**）

### 1-3. ⚠ 検証シナリオ（**`debug_boot` の `SCENARIOS` に足す**）

⚠ **このタスクは「画面」の項目が主になる。** ⚠ **設計役は絵を取れない。**
→ ⚠ **`y` をずらすだけなら、設計役が取れるのは「x が1ミリも変わっていないこと」だけになる。**
⚠ **EXEC を書くときに、ログで取れるものと人間しか見られないものを最初から分けること**（`AGENTS.md`「誰が取るか」）。

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ 既存の x を1ミリも動かさない（**決め次第**）

⚠ **段階1〜6で毎回かけている完了条件の形。** ⚠ **`y` だけずらす決めになったら、`battle_last.jsonl` の `damage` / `heal` の件数が全シナリオで一致すること**（`area` は `narrow`=2 / `wide`=4 / `far`=4 / `heal`=3）。

### 2-2. ⚠ `GROUND_Y` は1箇所ではない

⚠ **`battle_controller.gd` の `GROUND_Y` は、ユニット生成（`_make_unit_view()`）と投射物の始点・終点（`_on_projectile_requested()`）の両方が読んでいる。** ⚠ **片方だけずらすと矢が足元から飛ばない。**

### 2-3. ⚠ 召喚の同時数に上限が無い（**段階6の人間の決定**）

⚠ **CDの短い召喚スキルを1本書くと無限に増える。** ⚠ **立ち位置の列に入れる決めにすると、溢れ方をここで決めることになる。**

### 2-4. ⚠ ドキュメントの「実装済み」を信じない

**ズレが13回起きている。** ⚠ **`grep` で関数の中身を見てから判断する。読んだ結果ドキュメントが違っていたら報告する（勝手に直さない）。**

### 2-5. ⚠ E / W の次番号

⚠ **E101 まで使用済み → E102 から。** ⚠ **W13 まで使用済み → W14 から**（**W6 と W7 は欠番**）。

---

## 3. 調査済みの事実（**`grep`し直さなくてよい**・2026-08-21確認）

| | 事実 |
|---|---|
| ⚠ ログの実体 | `C:/Users/admin/AppData/Roaming/Godot/app_userdata/pomodoro-heroes/logs/battle_last.jsonl`。⚠ **戦闘のたびに上書き** |
| ⚠ 出力パネル | `.../logs/godot.log`。⚠ **保持5本。設計役が走らせると人間のログが1本消えるので、読む前に走らせない** |
| ロード時の正常な出力 | ⚠ **`skills validated: 61 entries, 0 errors, 1 warnings`**（黄1本は `skill_dbg_dot_odd` の端数＝**出るのが正解**）／ `basic attacks validated: 19 entries, 0 errors, 0 warnings` |
| 出る出来事 | `battle_start` / `wave` / `cast` / `damage` / `heal` / `dot` / `react` / `status_add` / `status_end` / `status_clear` / `condition` / `intervene` / `recast` / **`spawn`** / `result` |
| ⚠ 座標の定数 | **`GROUND_Y = 240` ／ 味方 `PARTY_BASE_X=200` `STEP=100` ／ 敵 `ENEMY_BASE_X=900` `STEP=100`**（`battle_controller.gd` の先頭） |
| ⚠ ビューの生成 | ⚠ **`_make_unit_view(unit, parent)` の1本**（味方・敵・召喚の3箇所が通る。段階6で寄せた） |
| ⚠ ユニットを引く | ⚠ **`BattleSession.find_unit()` の1本**（段階6で4本から寄せた）。⚠ **配列を足したらここだけ直す** |
| ⚠ 走査 | ⚠ **`battle_controller._all_units()`（味方 → 敵 → 召喚）。並びを変えない** |
| ⚠ ユニットの停まる位置 | **`距離 <= attack_range` で止まる**。⚠ **狙う相手は死ぬまで変えない** |
| ⚠ 編成 | ⚠ **状態が唯一の正**（`battle_controller.gd`）。⚠ **`stages.json` の `party_id` では決まらない** |
| ⚠ スキル枠 | **`SKILL_SLOT_COUNT` は 2** |
| 検証用ステージ | `stage_dbg_enemy_skill` ／ `_condition` ／ `_intervene` ／ `_passive` ／ `_area` |
| 行数 | `game_manager` 3048 ／ `battle_controller` 1641 ／ `skill_schema` 1268 ／ `status_registry` 1116 ／ `skill_resolver` 727 ／ `master_data_loader` 675 ／ `skill_runtime` 536 ／ `debug_boot` 428 ／ `unit` 368 ／ `battle_session` 133 ／ `unit_view` 119 |

```
battle_controller  … 入力と表示。ノードを触る唯一の層
	  ↓ cast()
SkillRuntime       … 待ち行列。trigger・購読の配布と発火・中断
	  ↓ 効果1件ずつ
SkillResolver      … 1つの効果を確定した対象に当てる。時間も段も知らない
	  ↓ host が none 以外
StatusRegistry     … 状態。寿命はスキルより長い

BattleLog          … 静的クラス。どの層からも呼べる（Autoload ではない）
```

### 3-1. ⚠ 検証の道具（**人間に頼る前にこれを回す**）

```powershell
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=area
```

- ⚠ **`Start-Process` ＋ `-RedirectStandardOutput` / `-RedirectStandardError` で実行する**（直接叩くと1行も返らない）
- ⚠ **読むのは `[System.IO.File]::ReadAllLines(path, UTF8)`**（`Get-Content` は化ける）
- ⚠ **今あるシナリオ：`area` / `recast` / `recast_expire` / `summon` / `summon_wipe` / `training`**（`training` は窓あり専用。⚠ **`--quit-after 300` が要る**）
- ⚠ **シナリオは `SCENARIOS` に1行足す。シーンを増やさない**
- ⚠ **`fire` の行に使える欄：`gap`（間隔の上書き）／`prepare`（`damage_party` / `kill_party`）／`skill: ""`（下ごしらえだけの行）**
- ⚠ **検証用スキルは `skills.json` だけでなく `characters.json` の候補一覧にも足すこと**（`game_manager.gd:2129`）
- ⚠ **数値で区別したいなら `attack_type: "true"`**（`physical` は敵の `def` で `amount: 1` に潰れる）
- ⚠ **足した検証が本当に赤を出すか、データを一時的に壊して確かめる。無音で通る検証が一番怖い**
- ⚠ **人間に渡す前に、全シナリオを1回ずつ回す**

---

## 4. このあと来るもの（**このタスクではやらない**）

| 順 | 実装するもの | なぜその順か |
|---|---|---|
| **次** | **段階3の積み残し**：⚠ **コンボ** ／ **`point` の条件（オーラ）** | ⚠ **立ち位置が片付いてから**（§1-0） |
| 2 | **ダメージの介入点の利用者**（シールド・軽減・反射・貫通%・確定クリティカル） | ⚠ **4つの介入点のうちここだけ利用者ゼロ**（`_step_crit_override` / `_step_reduction` が `pass`） |
| 3 | **バランスの実測** | 構造が出揃ってから |
| — | **PLAN 14-5 の2欄**（召喚が狙われるか／支援対象になるか） | ⚠ **ゾンビ型の召喚を作りたくなったとき** |
| — | **`activation: toggle` ／ 変身 ／ リソース ／ `move`** | 台帳が「別途」にしている |

---

## 5. 罠

### ⚠ 静的な突き合わせは実機の代わりにならない

⚠ **段階4で、ロード時検証を Python で真似て「OK」と言ったが実機では10件の赤が出た。**
→ ⚠ **ヘッドレスが使える。真似ずに走らせること。**

### ⚠ 複製された関数は、配列を足したときに置いていかれる

⚠ **段階6で実際に踏んだ。** `_find_unit()` が4本あり、3本が `summon_units` を知らないまま残った。
⚠ **症状は「`cast` の行だけ出て `damage` が1本も出ない」。エラーは1つも出ない。**
→ ⚠ **`BattleSession.find_unit()` と `battle_controller._all_units()` に寄せてある。新しい配列を足すときはこの2本だけ直す。**

### 関数を足す前に `grep` する

**足す前に `grep -n "func <名前>"`、足したあとにも `grep` で当たったか確認する。**

### ⚠ Windows の bash で `cat >>` すると追記分が CRLF になる

元が LF の JSON に混ざって壊れる。**JSON に追記したら改行コードを確かめる。**

### ⚠ `_ready()` の中では root に `add_child()` も画面遷移もできない

`Parent node is busy ...` で弾かれる。⚠ **`call_deferred` を使う。** ⚠ **遷移の枝が2つある**（`change_scene` と `change_scene_with_data`）。

### 正常系に警告を付けない・`print` を増やさない

**出したい記録は `BattleLog` へ。** コンソールに流さない。

### インデントはタブ

`.gd`はタブ。**`.json`も既存ファイルはタブ**（⚠ `stages.json` だけトップレベルが半角スペース2つ）。`ja.csv`はUTF-8（BOMなし）。

---

## 6. 引き継いだ宿題

**⚠ 全件は `PROJECT_STATUS.md`「溜まっている宿題」を見ること。** ここは判断待ちだけ。

### ⚠ 人間の判断待ち

1. ⚠ **`summons.json` を `enemies.json` と分けたのは設計役の判断**（`EXEC_SKILL_SPAWN.md` §0-1 の1）。⚠ **マスターファイルが5本目になった**
2. ⚠ **台帳（PLAN 8章）の「`charge` 欄の有無で分岐している」が実コードと違う**（段階5で報告済み・未修正）
3. ⚠ **godot MCP の設定を消すか**（使えていない。設定だけ残っている）
4. ⚠ **`tests/` の既存9件の棚卸し**
5. **Ziva が作った `.bak` が7件残っている**
6. ⚠ **`AGENTS.md` の `CHARACTER_GROWTH` の表に `passives` を足すか**

### ⚠ 器の穴（**大きいものだけ**）

7. ⚠ **召喚の同時数に上限が無い**（段階6の決定5）
8. ⚠ **購読は `host: unit` のみ**（コンボ・罠がまだ載らない）
9. ⚠ **ダメージの介入点だけ利用者ゼロ**
10. ⚠ **`stack` の5部品のうち上限だけ入れた**（消え方・再付与・閾値は未実装）
11. ⚠ **効果の中の欄に「知らない欄」の検出が無い**（typo が無音）
12. ⚠ **「死亡時発動」と「他人の蘇生」はまだ書けない**
13. ⚠ **DoT の周期ダメージでは購読が発火しない**
14. **死亡中にCDが回る**
15. ⚠ **`target.range` が未設定** ／ ⚠ **`atk_multiplier` が常に 1.0**
16. ⚠ **多段の2発目に投射物が出ない**

### 片付け

17. **検証用のものはリリース前に消す**（`stage_dbg_*` ／ `skill_dbg_*` ／ `summons.json` ／ ⚠ **`tests/debug_boot`**）
18. ⚠ **フォルダを増やしたら定数に1行足す**（`CHARACTER_DIRS_REQUIRED` / `ENEMY_DIRS_*`）
19. **状態のUIが無い**（F3 パネルと `P` キーだけ）

---

## 7. 終わったあと

**このファイルを、次のタスク（コンボ ／ `point` の条件）の内容に書き換える。**
⚠ **`debug_boot` の `SCENARIOS` は消さない**（次の段階で使い回す）。
