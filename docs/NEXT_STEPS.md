# 次にやること：**⚠⚠ 人間が決める（2026-09-14 に台帳を実コードに合わせた）**

> ⚠⚠ **このファイルは「いまの議題」だけを持つ**（2026-09-21 に分けた。⚠ 人間の決定）。
> ⚠ **§0-UI-Q（進め方の相談・画面を撮れるようになった）→ §0-UI-P（切れ目・分岐）→ §0-UI-O（小さい掃除）の3節。**
> ⚠ **前の回は `docs/03_log/NEXT_STEPS_2026-09.md` と `..._2026-08.md`**（⚠ 一番下に一覧）。

> ⚠⚠ **いま効いている値の正は `docs/DECISIONS.md`**（⚠ 1決定＝1行）。
> ⚠ **このファイルに値を書き写さない。⚠ 番号を参照すること。**
> ⚠ **経緯を知りたいときだけ `docs/01_plan/PLAN_◯◯.md`**（⚠ とっかかり。⚠ 値は古いことがある）。

> ⚠⚠ **2026-09-06。⚠ 人間が順番を決めた。⚠ 言葉のまま：**
> ⚠ **「⚠ Eをした後に　⚠ UI関連を全部しあげてからのほうがいいかも　⚠ ダンジョンは、それを治してる間にイメージを固める」**
> ⚠ **「⚠ UIは、テスト用のページを作ってそこで全部確認できるようにしたい」**
> ⚠ **「⚠ まだたりないＵＩなどをぜんぶかたずけたい　⚠ あとアセットを使わなくていいものは次全部やりたい」**

## 0-UI-Q. ✅ **進め方の相談 ／ ⚠⚠ 画面を撮れるようになった（2026-09-21）**　⚠ §0-UI-P の続き

⚠ 人間が題目に「⚠ いまの仕様駆動の方式についての相談」を指定した回。⚠ **実装は1件だけ**（⚠ 画面を撮る口）。

### 0-UI-Q-1. ⚠⚠ 人間の決定（**覆さない**）

- ⚠⚠ **実機確認は「⚠ 頻度を減らして一気にチェックする」**（人間「⚠ 実機確認が重いが重要でもある　⚠ だから頻度を減らして一気にチェックしたい」）。
  ⚠ **＝1タスクごとに人間へ渡さない。⚠ たまったところで1回まとめて見る回を置く。**
  ⚠⚠ **何回ためるか・誰が「見る回」の合図を出すかは未決**（⚠ 下の Q-5）
- ⚠⚠ **画面を PNG で撮ってよい**（人間「⚠ その実験もいいよ　⚠ 画面とる」）。⚠ **実験は通った**（⚠ 下の Q-3）
- ⚠⚠ **見る回は案2**（人間「⚠ 案2」）。⚠ **「絵で判断できないもの」が3件たまったら渡す**。⚠ 回数では数えない
- ⚠⚠ **撮る画面は役割で選ぶ**（人間「⚠ とる画面を増やしていい　⚠ 役割に応じて何開くか決めて」）。
  ⚠ ①ランの中で判断する ②戦う ③拠点の入口 ④育てる ⑤回す（経済）＝**11枚**。⚠ 同じ役割を2枚撮らない
- ⚠⚠ **台帳は3つに分ける**（人間「⚠ plan はとっかかりだと思う」「⚠ nextsteps はいまの議題だと思う」
  「⚠ それ（＝過去の回）が溜まってるのが問題かも」「⚠ 決定表を作るのはいいと思う」）。
  ⚠ **`PLAN`＝とっかかり ／ `NEXT_STEPS`＝いまの議題 ／ `DECISIONS.md`＝いま効いている値**。
  ⚠⚠ **値を書くのは `DECISIONS.md` だけ。⚠ 本文は番号を参照する**
- ⚠⚠ **着手の前に「変わるはずの数字」を1行書く**（人間「⚠ ja」＝論点C）。
  ⚠ 書くのは**生成・分布・レイアウト・数え方が変わるものだけ**（⚠ 全タスクに義務づけない）
- ⚠⚠ **進め方を実体に合わせる**（人間「⚠ E はもう ziva を使わないから、あってる方式にしたい」）。
  ⚠ **`IMPL_LOG` は廃止**（⚠ 記録は `NEXT_STEPS` の節が果たしている）。
  ⚠ **`EXEC` は「器が作り直しになるもの」だけ**。⚠ `PRE_PLAN` は人間の依頼書・モックとして残る

### 0-UI-Q-2. ⚠ 入ったもの

| コミット | 中身 |
|---|---|
| `1b85998` | ⚠⚠ **`scenario=shot`（窓あり専用）**。⚠ `KIND_SHOT` ＋ 内側のクラス `ShotTaker`。⚠ 出し先は `shot_dir=<パス>`（⚠ 既定 `user://shots`）。⚠ 撮る画面を増やすのは `SCENARIOS["shot"]["shots"]` に1行 |

### 0-UI-Q-3. ⚠⚠ 実測（**画面の絵が取れた**）

- ⚠⚠ **`--headless` を外して回すと、⚠ 絵が本当に取れる**。⚠ `dungeon_map` **1280 x 720**・PNG 93KB。⚠ 赤0
- ⚠ 回した時間 ＝ 1回あたり十数秒（⚠ ヘッドレスと同じくらい）。⚠ **窓が人間の画面に出る**
- ⚠ `layout` をヘッドレスで回して 赤0 ／ 黄1（⚠ 例の `skill_dbg_dot_odd`）＝**既存のシナリオは1本も壊していない**
- ⚠ シナリオの一覧にも `shot` が載った

### 0-UI-Q-4. ⚠⚠ 報告（**この回で踏んだ2件。⚠ どちらも直してある**）

- ⚠⚠ **1枚目は「⚠ 出ないはずの右上の通貨」が写っていた。⚠ これは本番の不具合ではなくハーネスの都合だった。**
  ⚠ `add_child()` で画面を直に足すと、⚠ `dungeon_map.gd:91` の `ResourceHud.set_shown(false)` が
  ⚠ **`ResourceHud` が生まれる前に空振りする**（⚠ ログの順番で分かる）。
  ⚠⚠ **＝撮る画面は必ず `SceneManager` 経由で開くこと。⚠ 直に足すと本番と違う絵が撮れる**
- ⚠⚠ **`CanvasLayer` は `CanvasItem` ではない。** ⚠ デバッグのパネルを `is CanvasItem` で消そうとして
  ⚠ **黙って効かず、2枚目にパネルが写った**。⚠ `is CanvasLayer` の枝を足した
- ⚠ **デバッグのパネル（`tests/debug_overlay.gd`）は既定で出ている**（⚠ `[0]` で消す）。
  ⚠ **画面の右3分の1を覆う**。⚠ 撮るときは `ShotTaker` が消す。⚠ **人間が遊ぶときは出たまま**

### 0-UI-Q-2b. ⚠ 入ったもの（**2回目・台帳と進め方**）

| コミット | 中身 |
|---|---|
| `e95aed1` | ⚠ **撮る画面を役割ごとに11枚へ**。⚠⚠ **赤2本が残っている**（⚠ 下の Q-4b）。⚠ **push していない** |
| （この回） | ⚠⚠ **`docs/DECISIONS.md` を新設**（⚠ 難ダンジョンの決定1〜47 を1枚に）／ ⚠ `AGENTS.md` の進め方を実体に書き直した ／ ⚠ `CLAUDE.md` の表の導線を直した |

### 0-UI-Q-4b. ⚠⚠ 報告（**直していない**）

- ⚠⚠ **`scenario=shot` で 11枚撮ると赤が2本出る**（⚠ 1枚だけなら0本）。
  ⚠ 出どころは `scenes/ui/components/resource_hud.gd:117` の `resized` の無名関数（`Lambda capture ... was freed`）。
  ⚠ **手は2つ試して両方外れた**（⚠ ①演出を黙らせる ②畳む前に間を置く）。⚠ **3手目に進まず止めた**。
  ⚠⚠ **`ResourceHud` は静的に1個だけ持つ作りで自分では消えない。⚠ 誰が消しているのか未特定**。
  ⚠⚠ **人間が普通に遊んでも画面は何十回も替わるので、前から出ていた可能性が高い**
  （⚠ 確かめたかったが、⚠ この回に Godot を6回回したので人間のログは押し出されて消えている）
- ⚠ **絵で見えたこと（直していない）**：⚠ **戦闘＝下のスキルのパネルが画面の下端で切れている**・上下に大きな空白 ／
  ⚠ **装備＝「鍛冶の欠片 0 鍛冶の塊 0 …」が1行にベタ書きで折り返している**

### 0-UI-Q-2c. ⚠ 入ったもの（**3回目・仕様駆動の見直し**）

⚠ 人間の裁き：⚠ 「⚠ claude のみでいい」「⚠ わーくふろーはかきなおし」「⚠ 決定表をカテゴリごとに」「⚠ 3は新しく書いて」。

| 中身 |
|---|
| ⚠⚠ **`CLAUDE.md` / `AGENTS.md` の「画面の絵は取れない」を直した**（⚠ 今日から嘘になっていた）。⚠ 「誰が取るか」の表も**在るか／色・活性**で分けた |
| ⚠⚠ **`docs/WORKFLOW.md` を全部書き直した**（⚠ 4者・9段階 → **2者・10段階**）。⚠ 絵を撮る【7】と「見る回」【10】を足した |
| ⚠⚠ **決定表をカテゴリごとに**（⚠ 7章・52件を追加）：⚠ `UI` / `NAV` / `BT` / `BS` / `GR` / `RUN` / `RS` ＋ 難ダンジョン1〜47 |
| ⚠ **`PROJECT_STATUS.md` の「実装の体制」を新しく書いた**。⚠ 当時の4者は `<details>` に畳んだ。⚠ 「どこを見るか」の表も書き直した |

### 0-UI-Q-4c. ⚠⚠ 報告（**この回の見直しで見つけた**）

- ⚠⚠ **体制の記述が「4者」のまま残っていた**。⚠ `WORKFLOW.md` に**レビュー役（DeepSeek）という3人目**が居た。
  ⚠ `AGENTS.md` と `CLAUDE.md` を直しただけでは足りていなかった
- ⚠⚠ **台帳を月ごとに分けたとき、「覆さない決定」14節を記録の中に閉じ込めていた**（⚠ 同じ日の自分の作業で作った穴）。
  ⚠ **決定表のカテゴリ分けで拾い直した**
- ⚠ **まだ残っている当時の文書**（⚠ もう使わないが消していない）：
  ⚠ `REVIEWER_GUIDE.md`（240行）／ `PROMPT_IMPL.md`（160行）／ `PROMPT_ZIVA_*.md` 3本（351行）／ `IMPL_LOG_TEMPLATE.md`。
  ⚠ **`WORKFLOW.md` と `PROJECT_STATUS.md` に「もう使わない」と書いた**

### 0-UI-Q-5. ⚠ 残っているもの

- ✅ **`e95aed1` は push した**（⚠ 人間「⚠ 1 はい」＝**前からある赤と判断した**）。
  ⚠⚠ **赤2本そのものは残っている**（⚠ 下の Q-4b）。⚠ **原因を追うかは未決**
- ✅ **過去の回を月ごとに分けた**（⚠ 人間「⚠ 2 やって」）。⚠ 3243行 → **227行**。
  ⚠ 移した先は `docs/03_log/NEXT_STEPS_2026-09.md`（2323行）と `..._2026-08.md`（730行）
- ✅ **決定3 を書き直した**（⚠ 人間「⚠ 3 かきなおしていい」）。⚠ `DECISIONS.md`
- ✅ **`PLAN_HARD_DUNGEON.md` §5-9 を実態に直した**（⚠ 人間「⚠ 4 直して」）
- ⚠ **窓を撮る仕掛け**（⚠ 拾いもの・通路のできごと・戦闘の結果・レリック選択・商人）。
  ⚠⚠ **設計役の判断：いまは作らない。⚠ `RunLootWindow` を検査から開くと赤が出る件（未解決）に正面からぶつかるため。
  ⚠ 赤2本の原因を先に潰すほうが安い**（⚠ 人間「⚠ 赤2本はまだいい」なので、⚠ 窓の撮影もセットで後ろへ）
- ⚠⚠ **絵で判断できなかったもの ＝ 2件**（⚠ 戦闘の下パネルが切れている ／ ⚠ 装備の素材のベタ書き）。
  ⚠ **あと1件たまったら「見る回」**
- ⚠ **決定47（層ごとの個別調整）** ＝ ⚠ 難ダンジョンで唯一の未実装。⚠ **`EXEC` を書く側**（⚠ 器が作り直しになる）
- ⚠ **`PROJECT_STATUS.md` の宿題82**（⚠ `PRE_PLAN_UI_DECISIONS.md` の最小サイズが古い）は**行き先が変わった**
  （⚠ 正は `NEXT_STEPS.md` §0-UI-C → ⚠ いまは `docs/03_log/NEXT_STEPS_2026-09.md`）

#### 0-UI-Q-5b. ⚠⚠ 台帳の食い違い（**2026-09-21 に grep で取った。⚠ 直していない**）

⚠ **`PLAN_HARD_DUNGEON.md` の最終更新は 2026-09-06。⚠ それ以降の決定は全部 `NEXT_STEPS.md` にしかない。**

| ⚠ 決定 | ⚠ PLAN §5-9 に書いてあること（**古い**） | ⚠ いま効いていること |
|---|---|---|
| ⚠ **42** | ⚠ 「最上級素材は**5個**、ポーションは3個」 | ⚠ **素材10個**（`bag_material_stack`）・ポーションは1個1枠 |
| ⚠ **44** | ⚠ 「`loot.chest` の **`rolls`**（3）を触る」 | ⚠ `chest`/`boss` の **`count` を 2→1** |
| ⚠ **38** | ⚠ 「⚠ 3〜4が1層あたりか合計かは**未決**」 | ⚠ **区画ごとに 1〜3 ランダム**（⚠ 同じ日に3版）。⚠ PLAN には1つも載っていない |
| ⚠ **45** | ⚠ 「了解」で閉じた | ⚠ `NEXT_STEPS` には**1度も出てこない** |

✅ **4件とも `PLAN_HARD_DUNGEON.md` §5-9 を実態に直した**（2026-09-21・人間「⚠ 4 直して」）。
✅ **`CLAUDE.md` の表の導線も直した**（⚠ 前は「仕様を決めるとき → `PLAN_◯◯.md`」＝**古いほうを読ませていた**）。

---

## 0-UI-P. ✅ **区画の切れ目を消した ／ サイドの道を 1〜3 ランダムに（2026-09-20・2回目）**　⚠ §0-UI-O の続き

⚠ 人間が実機で拾いものの窓が開くことを確認したあとの回。⚠ §0-UI-O の「残り」への裁きも一緒に出た。

### 0-UI-P-1. ⚠⚠ 人間の決定（**覆さない**）

- ⚠⚠ **区画の切れ目の表示は消す**（人間「⚠ 区画の切れ目の表示は消して」）。⚠ **区画そのものは残す**。⚠ 消したのは見た目だけ
- ⚠⚠ **サイドの道は「区画ごとに 1〜3 を振り、⚠ 出口側に寄せる」**
  （人間「⚠ 5個のルート分岐の中に確定で3つサイドの道があるが、⚠ 1から3の中でランダム」＋「⚠ 区画ごとに振る・出口側に寄せる」）。
  ⚠⚠ **§0-UI-N-3i の決定38 の直し（2〜4層・1層だけにしない）を一部覆している**（⚠ 1 を許す）。
  ⚠ 戻すなら `segment_branch_min_layers` を 2 に上げるだけでよい（⚠ コードは触らなくてよい）
- ⚠ `ui_dungeon_chest_*` は**使っているので消さない**（人間「⚠ 使わないなら消していい」→ ⚠ 9本とも現役だった）
- ⚠ `DUNGEON_EDGE_EFFECT_TRAP_BAG` の定数と、⚠ `PLAN_SCENARIO_MAP.md` の `consumables` の行は**消す**（人間「4 は消していい」「5 も消していい」）
- ⚠ 宝箱の `chest_id` `"dungeon_chest"` は**そのまま**（人間「7 はまあいい」）
- ⚠⚠ **`run_loot_window` の置き場所は保留**（人間「⚠ そこでいいが、⚠ ダンジョン用の UI 用のフォルダにするのはどうか」）。
  ⚠ **フォルダの新設は人間の承認が要る**（AGENTS.md）。⚠ まだ作っていない

### 0-UI-P-2. ⚠ 入ったもの

| コミット | 中身 |
|---|---|
| `cca2fa3` | ⚠ **区画の切れ目の表示を消した**（`SEAM_*` の綴り・線・字・`get_seam_count()` ／ `_seam_lines()` ／ `get_dungeon_segment_seams()` ／ `ui_dungeon_segment_seam` ／ 検査の print）。⚠ `set_lines()` と `set_map()` の引数が減った。⚠ ついでに死んだ綴り2件 |
| `fb270db` | ⚠⚠ **サイドの道を 1〜3 ランダムに**。⚠ 出口側に寄せる。⚠ `segment_branch_min/max_layers` を 1/3 に（⚠ 意味も「振り幅」に変えた）。⚠⚠ **振る単位を間違えていた**（下） |
| `313bc83` | ⚠⚠ **直し：振るのは「帯ごと」ではなく「区画ごと」**（⚠ 人間が実機で発見）。⚠ 3つの区画は**横に並んでいる**ので帯に1回だと全部同じ形になる。⚠ `inner_by_layer` を `[層][区画k]` にした |
| `6137f20` | ⚠⚠ **ボスの後にショップを自動で見せる（A案）** ／ ⚠⚠ **一本しかない道に罠を置かない** |

### 0-UI-P-3. ⚠ 実測

- ⚠ `--import` 赤0 ／ `layout` 赤0 ／ `floor` 赤0 ／ `dungeon_battle` 赤0
- ⚠⚠ **`dungeon` を4回**：⚠ 赤0・W35 0 ／ ⚠ 全ルート 216 本 ／ ⚠ ボスに着かない行き止まり 0 ／
  ⚠ **どのルートからも通れないノード 0**（⚠ ここが今回いちばん危なかった枝）
- ⚠⚠ **1階のノード数が 68 / 68 / 74 / 77 と回ごとに変わる**（⚠ 前は毎回 **83** で固定だった）＝ランダムが効いている
- ⚠ `layout`：⚠ 通路の線 シナリオ26本・難ダンジョン97本 ／ ⚠ 一番斜めな線 296 / 291px（⚠ 切れ目を消す前と同じ）
- ⚠⚠ **直しのあと（`313bc83` / `6137f20`）**：⚠ `dungeon` を4回とも 赤0。
  ⚠ **区画の数で割り切れない層 = 2 / 3 / 5 / 3 件**（⚠ 帯ごとに振っていたときは必ず 0 件だった＝これが見分け）／
  ⚠ **一本しかない道に付いた罠 = 0 件**（⚠ 得は 16 / 10 / 11 / 12 件）／ ⚠ 通れないノード 0 ／
  ⚠ 降りたあと「ショップを自動で出したか」= false（⚠ 階ごとに戻る）／
  ⚠ マップの「ショップへ」の行 = 子2個・見える=true・**280 x 38**
- ⚠ `layout`・`floor`・`dungeon_battle`・`economy`・`inventory` 赤0

### 0-UI-P-1b. ⚠⚠ 人間の決定（**2026-09-20・実機を見たあと**）

- ⚠⚠ **区画ごとに違う道になった**（人間・確認済み）。⚠ ただし**最初の実装は間違っていた**（下の P-2 の `313bc83`）
- ⚠⚠ **ボスの後にショップを自動で見せる**（人間「⚠ 地味すぎてわからないので　⚠ 次のフロアに行く前にショップを見せる」＝**A案**）。
  ⚠ 行（`ShopList`）は**残す**（⚠ 一度閉じたあとに自分で寄り直せる）。⚠ 見せるのは**階ごとに1回**
- ⚠⚠ **一本しかない道に罠を作らない**（人間）。⚠ 選んでいない道の減点は避けようが無いため。
  ⚠ **得（宝箱・資源）は一本道でも付く**（⚠ 外すのは罠だけ）
- ⚠ **シナリオの確認は後回し**（人間「⚠ シナリオは難ダンジョンの後で作ればいいので無視」）
- ⚠⚠ **次の回の題目は「いまの仕様駆動の方式についての相談」**（人間）。⚠ 実装ではなく**進め方の相談**

### 0-UI-P-4. ⚠⚠ 報告（**直していない**）

- ⚠⚠ **中の 1 → 2（道が分かれるところ）で危うく「通れないマス」を作るところだった**。
  ⚠ 「中どうしは真っ直ぐ1本」を**ノード数が同じときだけ**に絞って直した。⚠ ここを戻さないこと
- ⚠ **`segment_layers` を 5 から変えると振り幅の上限が実質変わる**（⚠ 中の層数で切り詰めるため）。⚠ 6 にすると 1〜3 がそのまま効く
- ⚠⚠ **振る単位を間違えた**（`fb270db`）。⚠ 3つの区画が**横に並んでいる**ことを読み違えて、⚠ 帯に1回だけ振った。
  ⚠ 人間が実機の1枚で見つけた（⚠ 3層=3 / 4層=6 / 5層=6 ＝ 区画の数で必ず割り切れていた）。
  ⚠ **絵が見られないぶん、⚠ 「割り切れるか」のような数字の合図を検査に入れておくこと**
- ⚠⚠ **ショップは画面側が作られていた**（⚠ 子2個・見える=true・280 x 38）。⚠ 出ていなかったのではなく**地味で見落とされた**。
  ⚠ 人間の言葉「⚠ 地味すぎてわからない」。⚠ **「実装してある」と「気づける」は別**
- ⚠ **実機で見ていない**：⚠ 自動で出るショップ ／ ⚠ 一本道に罠が付かなくなったこと

### 0-UI-P-5. ⚠ 残っているもの

- ⚠⚠ **次の回は実装をしない。⚠ 題目は「いまの仕様駆動の方式についての相談」**（人間）
- ⚠ 実機で見ていないもの（⚠ 上の P-4）
- ⚠ **シナリオ側の実機確認**（⚠ 人間「難ダンジョンの後でいい」＝**いまは見ない**）
- ⚠ **ダンジョン用の UI フォルダを作るか**（⚠ 人間の提案。⚠ 作るなら承認と移すファイルの一覧が要る）

---

## 0-UI-O. ✅ **小さい掃除のまとめ（2026-09-20）**　⚠ §0-UI-N の続き

⚠ 人間が「⚠ D：小さい掃除のまとめ」を選んだ回。⚠ 宿題5件を1本ずつ片付けた。

### 0-UI-O-1. ⚠⚠ 人間の決定（**覆さない**）

- ⚠⚠ **`DungeonEdgeLines` は動かさない ／ マスの文言（たたかう／戦闘）は変えない**。
  ⚠ 09-19（§0-UI-M-1）の決定を**そのまま生かし、宿題の行のほうを消した**（人間「⚠ 決定を残し、宿題の行を消す」）。
  ⚠⚠ **もう宿題ではない。** ⚠ 触るなら人間の合意を取り直すこと
- ⚠⚠ **`DungeonChest` → `RunLootWindow` に改名する**（人間）。⚠ シナリオでも使う窓なので名前を中身に合わせた

### 0-UI-O-2. ⚠ 入ったもの

| コミット | 中身 |
|---|---|
| `62ec621` | ⚠ **使われなくなった翻訳キー4本**：`ui_relic_give` ／ `ui_dungeon_shop_torch_now` ／ `ui_dungeon_edge_event_trap_bag` ／ `ui_dungeon_edge_title_trap_bag`。⚠ 979 → 975 行 |
| `a1e0d4b` | ⚠ **`FLOOR_RUN.consumables` の欄を消した**。⚠ 14-e から誰も読み書きしておらず、AGENTS.md「欄だけ足して実装しない」に触れていた。⚠ `state_keys.gd` / `_empty_floor_run()` / `load_state()` の int() / AGENTS.md の表 |
| `42b7c3c` | ⚠ **`PROJECT_STATUS.md` の「現在地」を 09-16 → 09-20 に**。⚠ 09-17〜09-20 の6行を足した |
| `05cf4eb` | ⚠⚠ **`DungeonChest` → `RunLootWindow`**。⚠ ファイル3本（`.gd` / `.tscn` / `.gd.uid`）＋ 呼ぶ側4本。⚠ uid の値は変えていない。⚠ `CHEST_SCENE` → `LOOT_WINDOW_SCENE`。⚠ 使われていなかった `CHEST_PATH` も消した |

### 0-UI-O-3. ⚠ 実測

- ⚠ `--import` 赤0（⚠ uid_cache.bin を消した直後の1回目だけ黄1＝フォントの fallback の uid。⚠ 2回目は0）
- ⚠ `global_script_class_cache.cfg` に `RunLootWindow` が載った ／ ⚠ `DungeonChest` は全ファイル0件
- ⚠ `dungeon` 赤0 ／ `floor` 赤0（⚠ 黄は `skill_dbg_dot_odd` 1 ＋ わざと弾く2）／ `layout` 赤0 ／ `glyphs` 表 **49件**・NG 0
- ⚠ `layout`：⚠ `run_loot_window.tscn` 最小 **620 x 342**（⚠ 改名の前後で同じ値）

### 0-UI-O-4. ⚠⚠ 報告（**直していない**）

- ⚠⚠ **引き継ぎの「拾いもの **620 x 316**」は実測と合わない。** ⚠ この回は改名の**前も後も 620 x 342**。⚠ 正は 342
- ⚠ **翻訳キーは `ui_dungeon_chest_*` のまま**（5本）。⚠ 窓の名前だけ `RunLootWindow` になった。⚠ 寄せるかは未決
- ⚠ **`GameStateKeys.DUNGEON_EDGE_EFFECT_TRAP_BAG` は残っている**（⚠ 綴りの定数だけ・09-20 の前の回の判断）。⚠ いまは誰も読まない
- ⚠ **`PLAN_SCENARIO_MAP.md` §7（306行目あたり）に `consumables` の行が残っている**（⚠ 台帳なので触っていない）
- ⚠ **`run_loot_window` の置き場所は `scenes/adventure/` のまま**。⚠ AGENTS.md の「2画面以上は `components/`」には合っていないが、⚠ 使う2画面ともダンジョン系。⚠ `DungeonEdgeLines` を動かさない決定と揃えた
- ⚠ 宝箱の `chest_id` `"dungeon_chest"` は**改名していない**（⚠ マスターデータの ID・CLAUDE.md 4番）

### 0-UI-O-5. ⚠ 残っているもの

- ⚠⚠ **実機で見る（⚠ 09-19〜20 のぶん全部）。⚠ ここが一番たまっている**
- ⚠ 実機で見ていない：⚠ 拾いものの窓（⚠ 改名したが中身は1行も変えていない）

---


## ⚠ 前の回はここ（**2026-09-21 に分けた**）

> ⚠⚠ **人間の決定：⚠ 「plan はとっかかり」「nextsteps はいまの議題」「それ（＝過去の回）が溜まってるのが問題かも」。**
> ⚠ **3154行・15節あったので、⚠ 直近3節（§0-UI-Q / §0-UI-P / §0-UI-O）だけ残して月ごとに分けた。**

| どこ | 中身 |
|---|---|
| `docs/03_log/NEXT_STEPS_2026-09.md` | ⚠ **§0-UI-N（09-19）〜 §0-L（09-02）**。⚠ 戦闘画面・倉庫の別窓・難ダンジョンの段階17〜20 |
| `docs/03_log/NEXT_STEPS_2026-08.md` | ⚠ **§0-A（08-31）以降**と、⚠ 当時の参考の章（⚠ 事故りやすい箇所・調査済みの事実・罠・引き継いだ宿題） |

- ⚠⚠ **いま効いている値の正は `docs/DECISIONS.md`。** ⚠ 記録の中の数字は当時のもので、**古いことがある**
- ⚠ **ルールの正は `AGENTS.md` と `CLAUDE.md`**（⚠ 記録の §2〜§6 ではない）
