# 次にやること：**スキル解放と選択**

**このファイルは「次の1タスク」だけを書く。** 終わったら次のタスクの内容に書き換える。全体の状況は`PROJECT_STATUS.md`、ルールは`AGENTS.md`と`CLAUDE.md`、**ゲームの中身は`GAME_DESIGN.md`**。

**このファイルだけ読めば着手できるように書いてある。** 過去のタスクを知らない前提で読んでよい。

`PLAN_IMPLEMENTATION.md` 3章の**3番の残り**（レベルの役割転換のうち、割り振りが終わり、スキルとパッシブが残っている）。

---

## 0. 前のタスクは終わった

**割り振りポイント（ステータスノード）が完了した（2026-08-15）。** 実施結果は`EXEC_LEVEL_ROLE_SHIFT.md` §13。

入ったもの：

- **`character_nodes.json`（新規・180件）。** キャラごと3本の枝×20段、前提条件つき。**固定データ。抽選しない**
- **`stat_node_screen`（新規画面）。** 枝の本数と並び順は`characters.json`の`allocatable_stats`が決める。**軸を決め打ちしていない**
- `GameManager`にノード関数7本（`unlock_stat_node()` / `reset_stat_nodes()` / `get_stat_node_bonus()` ほか）。**`get_effective_stats()`は5項目の足し算になった**
- **`stat_growth_formula`が`"base"`になった。レベルではステータスが伸びない**
- **`save_version`は3。** 旧セーブは捨てた
- `load_state()`に`_resync_growth_stats_from_master()`を追加。**ロードのたびに`stats`をマスター＋式で計算し直す**

**同じ作業をもう一度やらないこと。**

---

## 1. 今どういう状態か（**ここが出発点**）

**レベルの役割転換の表（`GAME_DESIGN.md` 5-2）のうち、1つ目だけが入った。**

| レベル | 何が起きるか | 実装 |
|---|---|---|
| 1レベルごと | 割り振りポイントを1点 | **入った** |
| 5・10・15・20 | **スキル候補が1つ解放**（初期2個＋4個＝6個） | **無い** |
| 20レベルごと | **パッシブが1つ解放**（計5個） | **無い** |

**このタスクは2つ目（スキル）だけをやる。パッシブは次の回。**

### なぜパッシブを分けるか

**パッシブは戦闘に「条件発動」の仕組みが要る**（HP半分以下で…など）。`BattleFormula`は式を4つ持っているだけで、条件を評価する場所が無い。**スキル選択は既にある仕組みの付け替えで済むが、パッシブは戦闘に新しい層を足す。**

---

## 2. ⚠ 調査は済んでいる。`grep`し直さなくてよい

以下は実コードを見て確認した事実（2026-08-15）。

| | 事実 |
|---|---|
| **`select_skill()`は空実装** | `game_manager.gd` **1728行**。`print`1行だけの dummy。**呼び出し元は0件**（`grep`で全ファイル確認済み） |
| **戦闘はマスターを直読みしている** | `battle_controller.gd` **164行**が`char_data.get("skills", [])`を読み、`unit.skill_ids`に入れている。**`growth.skills`は戦闘に一切関係していない。ここを付け替えるのが本題** |
| **`growth.skills`は`{}`のまま** | `_default_growth_for()`が空辞書を入れている。キーの定数`GROWTH_SKILLS`は`state_keys.gd` 123行にある |
| **`skills.json`は6件しか無い** | 1キャラ2個ずつ。**候補6個にするには1キャラ4個ずつ、計12件を足す必要がある** |
| **`skills.json`に`user_character_id`がある** | どのキャラのスキルかは既に表せている。**`unlock_level`は無い。足す必要がある** |
| **スキル画面は存在しない** | `training_screen.gd` 49行の`skill_button`が`placeholder_screen`へ飛ぶだけ（`SCREEN_ID_SKILL = "training_skill"`） |
| **スキルボタンはユニットから作られる** | `battle_controller.gd` **416行**`_build_skill_buttons()`が`_session.party_units`の`skill_ids`を回す。**`unit.skill_ids`さえ正しくすれば戦闘UIは追従する** |
| **`save_version`は3** | `save_manager.gd` 7行。**今回は上げなくてよい見込み**（`growth.skills`は既に空辞書として存在し、中身が増えるだけ）。ただし§5で判断する |
| **レベル上限は研究が決める** | `get_effective_level_cap()`。**Lv20まで上げられるか、デバッグオーバーレイの「研究を全部解放」で先に確かめること** |

### 行数（実装役に切り出せるか）

| ファイル | 行数 |
|---|---|
| `game_manager.gd` | **2542** |
| `battle_controller.gd` | **904** |
| `stat_node_screen.gd`（画面の参考になる） | 194 |

**`game_manager.gd`と`battle_controller.gd`は両方200行を超えている。** 触るなら設計役が全文を書く（`PLAN_IMPLEMENTATION.md` 6章）。**新規のスキル画面だけなら切り出せる可能性がある。**

---

## 3. 仕様は`GAME_DESIGN.md`が正

**ここには仕様を書かない。2箇所に本文があると片方だけ直して食い違う。**

見るのは`GAME_DESIGN.md` **3-2（スキル）** と **5-2（レベル）**。

要点だけ：

- **候補6個から2枠を選んで戦闘に持ち込む**
- 候補の解放：**初期2個 ＋ Lv5・10・15・20 で1個ずつ**
- **スキル1は武器スロット、スキル2はアクセサリースロットに対応する**（ルーンとの紐づき。7章）。**枠の順番に意味がある。入れ替え可能にすること**
- チャージスキルの仕様は既存のまま

---

## 4. 前のタスクで作った、真似できるもの

**ステータスノードとスキル選択は形が似ている。** 「マスターにある候補を、レベルで解放し、状態にはIDだけ持つ」。

| 参考になるもの | どこ |
|---|---|
| 画面の作り | `scenes/guild/stat_node_screen.gd`（194行）。`TransferKeys.CHARACTER_ID`で対象を受け取る |
| 育成画面からの導線 | `training_screen.gd`の`_on_stat_node_pressed()` |
| 「解放できるか」の判定を状態変更と分ける形 | `can_unlock_stat_node()` と `unlock_stat_node()` |
| マスターの遅延ロード | `master_data_loader.gd`末尾の`_ensure_character_nodes_loaded()` |
| **等幅の列** | `stat_node_screen.tscn`の`Scroll`に`horizontal_scroll_mode = 0`。**これが無いと子の`SIZE_EXPAND_FILL`が効かない**（実機で1回踏んだ） |

---

## 5. 未決（**着手時に人間が決める**）

- **スキル12個（3人×4個）の内容。** 今回は器だけにして中身を仮で埋めるかも含めて
- **解放レベルの割り当て。** Lv5/10/15/20 に1個ずつでよいか（`GAME_DESIGN.md` 3-2 のとおりなら確定だが、`skills.json`に`unlock_level`を持たせるか、レベルを`character_nodes.json`のように別ファイルに置くか）
- **`growth.skills`の構造。** `{"slots": [skill_id, skill_id]}` か `{"slot_1": ..., "slot_2": ...}` か
- **未選択のときどうするか。** 初期2個を自動で入れるか、空のまま戦闘に出せるようにするか。**「状態にマスターを複製しない」を守るなら、空のときはマスターの先頭2個にフォールバックする形になる**
- **スキル選択の画面をどこに置くか。** 新規画面（`stat_node_screen`と同じ形）か、育成画面の中か
- **`save_version`を4に上げるか。** キーが増えるだけなら不要。§2の見込みでよいか最終確認する

---

## 6. スコープの候補（**着手時に人間が決める**）

| | 内容 |
|---|---|
| **A：付け替えだけ** | `select_skill()`の実装、`growth.skills`に2枠、**戦闘が`growth.skills`を読むように変える**、選択画面。**スキルは今の6個のまま**（1キャラ2個なので、選ぶ余地が無い状態で仕組みだけ通す） |
| **B：A＋候補12個** | `skills.json`に12件追加、`unlock_level`、解放判定 |

**Aだけだと画面上は何も選べない**（候補2個・枠2個）。**ただし「戦闘がマスター直読み」の付け替えが独立して検証できる。** 前回の割り振りで「器」と「式」を分けたのと同じ判断ができる。

---

## 7. 罠

### ドキュメントの「実装済み」を信じない

**ズレが7回起きている。** `grep`で関数の中身を見てから判断する。読んだ結果ドキュメントが間違っていたら、**勝手に直さず報告する。**

### 編集したら`grep`で当たったことを確認する

「戦闘だけ反映されない」で1タスク溶かした事故がある。**今回はまさにその戦闘を触る。** `battle_controller.gd` 164行の付け替えは、**編集直後に`grep -n "growth" scenes/adventure/battle_controller.gd`で確認すること。**

### `MasterDataLoader`が返す数値は`float`

`int()`で包み忘れるとセーブに`.0`が乗る。**`unlock_level`を足したら`int()`で読むこと。**

### 状態にマスターデータを複製しない

セーブが持つのは**選んだスキルIDだけ**。倍率もCDも解放レベルも`skills.json`から毎回引く。**代償としてスキルIDを改名できない。**

### 状態を変える前に全部の判定を終える

`unlock_stat_node()`と同じ形にする。**途中で状態を触ってから弾くと壊れる。**

### 再描画に`await`を持たせない

`remove_child()`してから`queue_free()`する。

### `.tres`は`@export`の既定値を書き出さない

**実際に効く値は`.gd`側にある。** この罠でこのプロジェクトは既に2件死んでいる。

### インデントはタブ

`.gd`はタブ。`.json`も既存ファイルはタブ。**`ja.csv`はUTF-8（BOMなし）。**

### Godotを起動できない

**動かして確かめられるのは人間だけ。「動きました」と書かない。** 完了条件は「ログ」「セーブファイル」「画面」の3つに分ける。

---

## 8. 検証の道具

**デバッグオーバーレイが全画面の右上に常駐している**（`res://tests/debug_overlay.gd`）。`0`キーで表示切替。

| ボタン | 効果 |
|---|---|
| ゴールド・ジェム・スタミナ | 10万 / 1万 / 上限まで |
| 素材を全種類 | +999（**レベル上げの素材もここ**） |
| 消費アイテムを全種類 | +99 |
| 装備を全種類 1個ずつ | — |
| 研究を全部解放 | **先に「素材を全種類」。レベル上限もこれで上がる** |
| セーブする | — |

戦闘画面は**`F3`でデバッグパネル**（**右上**・速度1〜8倍・`K`敵1体撃破・`L`全滅・`J`味方全員に物理の一撃・`M`同じく魔法・`V`強制勝利・`B`強制敗北・`S`CDリセット）。**ユニット1体につき2行で10軸と実測ダメージが出る。**

⚠ **`F4`は`_unhandled_input`に届かない。** **`F2`・`0`・英字キーは届く。**

---

## 9. このタスクでやらないこと

- **パッシブ**（次の回。戦闘に条件発動の層が要る）
- **ルーン**（`PLAN_IMPLEMENTATION.md` 3章の8番。スキル選択が動いてから）
- **プリセット**（同3章の7番）
- **等級10・`parts`の種類つき**（同3章の4番）
- **バランス調整**（敵HP・スキル倍率。同3章の12番）

---

## 10. 前のタスクから引き継いだ宿題

`PROJECT_STATUS.md`にもあるが、**この回に関係しそうなものだけ**。

- **`EXEC_LEVEL_ROLE_SHIFT.md` §4-4 のドキュメント更新が未了。** `GAME_DESIGN.md` 5-2 / 5-3 は反映済みだが、**14章の未決削除・15章・`DATA_SCHEMA.md`・`PROJECT_STATUS.md`・`PLAN_IMPLEMENTATION.md` 3章のチェックが残っている**
- **`characters.json`の`growth_per_level`が死にデータになった**（`stat_growth_formula`が`"base"`のため）。消すか残すか未決
- **`character_nodes.json`の生成スクリプトはリポジトリに入れていない。** キャラを1人足すと60件増える
- **`adventure_config.tres`に上限3値が書かれていない**（`.gd`の既定値が効いている）
- **検証用のものはリリース前に消す**（デバッグオーバーレイ・0Gスロット・`weapon_debug_blade`）

---

## 11. 終わったあと

**このファイルを、次のタスク（パッシブ）の内容に書き換える。**
