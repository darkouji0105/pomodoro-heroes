# 次にやること：**⚠⚠ 人間が決める（2026-09-14 に台帳を実コードに合わせた）**

> ⚠⚠ **このファイルは「いまの議題」だけを持つ**（2026-09-21 に分けた。⚠ 人間の決定）。
>
> | | 中身 |
> |---|---|
> | ⚠⚠ **「これからの回（仮）」**（⚠ すぐ下） | ⚠ **次に何をするか。⚠ 新しい宿題はここへ入れる** |
> | §0-UI-W / §0-UI-V / §0-UI-U | ⚠ 終わった回（⚠ 直近3節だけ） |
> | `docs/03_log/NEXT_STEPS_<年月>.md` | ⚠ それより前の回 |

> ⚠⚠ **いま効いている値の正は `docs/DECISIONS.md`**（⚠ 1決定＝1行）。
> ⚠ **このファイルに値を書き写さない。⚠ 番号を参照すること。**
> ⚠ **経緯を知りたいときだけ `docs/01_plan/PLAN_◯◯.md`**（⚠ とっかかり。⚠ 値は古いことがある）。

> ⚠⚠ **2026-09-06。⚠ 人間が順番を決めた。⚠ 言葉のまま：**
> ⚠ **「⚠ Eをした後に　⚠ UI関連を全部しあげてからのほうがいいかも　⚠ ダンジョンは、それを治してる間にイメージを固める」**
> ⚠ **「⚠ UIは、テスト用のページを作ってそこで全部確認できるようにしたい」**
> ⚠ **「⚠ まだたりないＵＩなどをぜんぶかたずけたい　⚠ あとアセットを使わなくていいものは次全部やりたい」**

## ⚠⚠ これからの回（**仮**。⚠ 人間がいつでも並べ替えてよい）

> ⚠⚠ **人間の指示（2026-09-21）：⚠ 「⚠ あと残ってる作業をどの回でするか整理したいね　⚠ 前もって次のプランを
> ⚠ 仮として書いてそこに入れていく　⚠ どういう流れで進むか　⚠ いまはだんじょんの UI だけど」。**

### ⚠ 使い方（**きまり**）

1. ⚠⚠ **新しく宿題が出たら、⚠ その回の「残っているもの」ではなく、⚠ **この表のどれかの回に入れる**。
   ⚠ どこにも入らないものだけ「⚠ まだ回に入れていない」へ置く
2. ⚠ **回が終わったら、⚠ その行を消して、⚠ 下に「終わった回」の節（§0-UI-◯）を書く**
3. ⚠⚠ **「見る回」は順番に入れない。⚠ 割り込む**（⚠ 「絵で判断できなかったもの」が3件たまった時点）
4. ⚠ **順番の根拠は人間の言葉**（⚠ 下の「⚠ 並べた理由」）。⚠ 変えたいときは言えばよい

### ⚠ いまの流れ

```
【いま】ダンジョンの UI
   ↓ 回1〜回3
共通の UI と拠点の残り（⚠ アセットが要らないもの）
   ↓ 回4〜回5
難ダンジョンの中身（⚠ 決定47・入口・バランス）
   ↓ 回6
通しプレイ → 数値の調整
   ↓ 回7〜回8
シナリオ（floor_1..5）へ降ろす
   ↓ 回9
リリース前の片付け
```

### ⚠⚠ 進み具合（**2026-09-26 新設**・⚠ 人間「⚠ 何パーセントとかそういう感じで」）

⚠ **回の数で割る**（⚠ 重さは見ない）。⚠ 回が終わったらここの数字を直す。⚠ 回が増えたら分母も増える。

| | 済み／全体 | ％ | 中身 |
|---|---|---|---|
| ⚠⚠ **UI の作り直し** | **6 / 26** | **23%** | ⚠ テーマ1 ／ 共通の部品1 ／ 入口1 ／ 見た目の画面8（戦闘・マップ・レリック・わかれ道・商人・確かめの窓・タイトル・ポモドーロ）／ 組み替え5（育成・持ち物・詰所・掲示板・宝箱）／ 仕組み8（鍛冶・記録・設定・帰還報告書・集中の道具・小窓・特殊効果・ノルマ札）／ ダンジョンの形1 |
| ⚠ **これからの回ぜんぶ** | **6 / 34** | **18%** | ⚠ 上の26 ＋ 回3-b・3-c・4・5・6・7・8・9 |

### ⚠ 表

| 回 | 題目 | 中身 | 終わりの合図 |
|---|---|---|---|
| ⚠⚠ **回UI-4〜**（次） | ⚠ **見た目だけの画面**（⚠ 1回1枚） | ⚠ ~~戦闘~~（✅ 09-26）→ ダンジョン系（~~マップ~~（✅ 09-26）・~~レリック~~（✅ 09-26）・わかれ道・ショップ）→ 確かめの窓（⚠ **並びは `MD-4` のまま**）→ タイトル → ポモドーロ | ⚠ 手本の png と `shot` を並べて見る |
| **回UI-組** | ⚠ **画面の組み替え** | ⚠ 育成（身上書＋4タブ・昇級）→ 持ち物（⚠ 回3-b と一緒）→ 詰所 → 掲示板 → 届いた宝箱 | ⚠ 画面ごと |
| **回UI-仕組み** | ⚠ **新しい仕組みが要る画面**（⚠ **決定を先に出す**） | ⚠ ① 鍛冶（`EQ-5`〜`7`・`11`）② 記録（⚠ 集中の履歴などのデータが無い）③ 設定（`BS-13`）④ 帰還報告書 ⑤ 集中の道具 ⑥ 小窓（⚠ OS の別窓）⑦ 特殊効果（★・⚠ 決定が無い）⑧ ノルマ札 | ⚠ 1つずつ |
| **回D-形** | ⚠⚠ **ダンジョンの形を変える**（決定49） | ⚠ 決定11・26・28・48 を覆す**決定を先に出す**。⚠ 回4（決定47）と回5（入口）は**この回のあとで見直す**（⚠ 層の数え方が変わるため） | ⚠ 決定表が書き換わる |
| ⚠⚠ **回3-b** | ⚠⚠ **装備関連 UI ── ⚠ コンセプトは決まった。⚠ モック待ち** | ⚠⚠ **2026-09-22 に人間が止めた**（⚠ 経緯は §0-UI-V）。⚠⚠ **2026-09-23 にコンセプトが決まった**：⚠ `DECISIONS.md` `CN-1`〜`CN-3`・`BS-9`・`BS-10`・`BS-15`・`BS-20`（⚠ 本線はピッチA ／ 拠点の倉庫をやめる ／ 持ち物の画面）。⚠ 出どころ `docs/03_log/PRE_PLAN_WEAPON_RELIC.md`。⚠ **モックの依頼書は `docs/03_log/PRE_PLAN_INVENTORY_REWORK_MOCK.md`**。⚠ **3列の装備画面は入ったまま**（`3661672`） | ⚠ モックを裁く → ⚠ `EXEC_EQUIPMENT_UI.md` を書き直す。⚠ 武器・鍛冶・遺物は `DECISIONS.md` `EQ-1`〜`EQ-14` に入った（⚠ **ほとんど未実装**＝⚠ 実装の回は別に立てる）。⚠ **`EQ-13` は器が変わる**（⚠ 装備が等級つきで生まれる）＝⚠ 実装の回で `EXEC` を書く |
| **回3-c** | ⚠ **一時通貨の改名**（⚠ 決定45：「遺物片」→「コイン」） | ⚠ `ja.csv` の文字だけ（⚠ **キーは変えない**）＋ ⚠ 絵 `icon_res_shard.svg` をどうするか。⚠ 軽い | ⚠ `grep 遺物片 localization/ja.csv` が 0 件 |
| **回4** | ⚠⚠ **決定47：層ごとの個別調整** | ⚠⚠ **`EXEC` を書く側**（⚠ 器が作り直しになる）。⚠ **着手の前に「いま敵と戦利品がどう決まっているか」を `grep` で取る**（⚠ 台帳 §5-9-3。⚠ 推測で表を作らない） | ⚠ 層ごとに別の表が引ける ／ ⚠ `dungeon` 赤0 |
| **回5** | ⚠ **難ダンジョンの入口**（⚠ 決定11） | ⚠ いまコスト無し＝テストプレイ優先。⚠ **リリース前に必ず入れ直す**。⚠ `ITEM_TYPE_KEY_ITEM`（通行証）を使う | ⚠ 通行証が無いと入れない |
| **回6** | ⚠⚠ **通しプレイ → 数値の調整** | ⚠ 台帳 §6-B の「作ってから測るもの」6件（⚠ MAX HP の目減り量・1フロアの大きさ・ポーションの供給量・一時通貨のカーブ・鞄の初期枠・たいまつの半径と値段）。⚠ **人間が遊ばないと決まらない** | ⚠ 3階を通しで遊べる ／ ⚠ 数字が `DECISIONS.md` に入る |
| **回7** | ⚠ **シナリオ（floor_1..5）の実機確認** | ⚠ 人間「⚠ 難ダンジョンの後でいい」。⚠ 鞄・マップ・レリックは共通部品になっているので**見るだけ**のはず | ⚠ 絵と実機で通る |
| **回8** | ⚠ **段階16：`floor_1..5` のバランス** | ⚠ 決定2「難しいほう → 簡単なほう」。⚠ **難ダンジョンの数字が決まってから落とす** | ⚠ 5フロアが通る |
| **回9** | ⚠ **リリース前の片付け** | ⚠ 宿題77（⚠ `tests/ui_test_page.*` ／ `base_screen.gd` の入口 ／ `debug_boot.gd` の1行 ／ `tests/resource_gain_demo.*`）／ ⚠ `debug_instant`・0Gスロット・`weapon_debug_blade`・ポモドーロの「残り1秒」ボタン ／ ⚠⚠ **`scenario=shot` と `tests/` 一式** ／ ⚠ 当時の文書4本（`REVIEWER_GUIDE` / `PROMPT_IMPL` / `PROMPT_ZIVA_*` / `IMPL_LOG_TEMPLATE`・911行） | ⚠ 検証用が0件 |

### ⚠ 割り込み（**順番に入れない**）

| | いつ | 中身 |
|---|---|---|
| ⚠⚠ **見る回** | ⚠ **「絵で判断できなかったもの」が3件たまったら** | ⚠⚠ **一覧は `docs/HUMAN_CHECK.md` の1枚**。⚠ **いま 0 件**（⚠ 2026-09-23 に回3-b の5件を見ずに捨てた） |
| ⚠ 人間の思いつき | ⚠ いつでも | ⚠ 最優先。⚠ 回の表は後ろへずれる |

### ⚠ まだ回に入れていない（**アセット待ち・別の回が要る**）

| | 中身 | なぜ入れていないか |
|---|---|---|
| ⚠ **設定画面**（音量・ミュート） | ⚠ `SoundConfig` の3つの欄が起動時に当たるだけ。⚠ UI が無い | ⚠⚠ **セーブ構造ごと決める回が要る**（⚠ 決定 `BS-13`「別のファイルにする」だけ在る）。⚠ 重い |
| ⚠ **BGM** | ⚠ `SoundManager` に `play_bgm()` が無い。⚠ バスと音量欄だけ在る | ⚠ **音源待ち** |
| ⚠ **段階13：SDキャラとアニメーション** | — | ⚠ **絵待ち**（⚠ 人間の決定・2026-08-25） |
| ⚠ **段階19-a：絵文字を絵に** | — | ⚠⚠ **NotoSansJP に絵文字は無い**（⚠ 実測済み）。⚠ **絵文字フォントが入るまで着手しない** |
| ⚠ **OS通知を残すか** | ⚠ `window_request_attention()` は実装済み・体験版の暫定 | ⚠ **人間が音を聴いて判断する** |

### ⚠ 並べた理由（**人間の言葉**）

- ⚠ **「⚠ アセットを使わなくていいものは次全部やりたい」**（09-06）＝ ⚠ 絵待ちは後ろへ
- ⚠ **「⚠ UI を決め切ったほうがいいかも」「⚠ 数字なんて後でいい」**（09-05）＝ ⚠ 回1〜3 が先、⚠ 数値は回6
- ⚠ **「⚠ 戦闘関連はさいご」**（09-06）＝ ⚠ 強敵ノード・ボスの種類は回の表に入れていない
- ⚠ **決定2「⚠ 難しいほう（難ダンジョン）→ 簡単なシナリオ」** ＝ ⚠ 回7・回8 は回4〜6 のあと

---

## 0-UI-AD. ✅ **レリック選択（回UI-4 の3枚目）（2026-09-26）**　⚠ §0-UI-AC の続き

- ⚠ 人間の選択：⚠ **手本の形**（⚠ `RUN-10` を覆した＝右の常設の詳細を消した）
- ⚠ 入ったもの：⚠ 紙のカード3枚（⚠ 絵・名前（明朝）・効き目・誰に効くか・「選んだ」の判）／ ⚠ 下の紙に「付ける人」の丸い札と「◯◯に付ける」／ ⚠ Theme に `PaperPanelChosen`・`PaperChoice(Selected)`（⚠ 紙の上の Ghost は字が明るすぎて読めないため）／ ⚠ 翻訳キー3行
- ⚠ `shot` に **`21_relic_chosen`**（⚠ 1人用のカードと人を押した姿）＝ ⚠ **19 → 20枚**
- ⚠ 実測：⚠ `theme`（2回）・`dungeon`・`layout` 赤0・黄1 ／ `floor` 赤0・黄3（平常）／ `shot` 20枚・赤0・黄2
- ⚠ 報告：⚠ 使わなくなった翻訳キー2行（`ui_relic_candidates` / `ui_relic_press_hint`）は残した ／ ⚠ 見出しの「レリックを1つ」（明朝・中央）と手本の効き目の大きな字（「攻撃 +15%」）はまだ（⚠ いまは説明の文）

---

## 0-UI-AC. ✅ **地図の見た目（回UI-4 の2枚目）（2026-09-26）**　⚠ §0-UI-AB の続き

- ⚠ 入ったもの：⚠ 地図を羊皮紙に（⚠ `RunMapView` が紙を敷き、紙のテーマを持つ）／ ⚠ 札・線・ピン・霧を墨の色に ／ ⚠ **直書きの14色を Theme へ移した**（`UI-1` 違反だった）（⚠ 値は `RUN-11`）
- ⚠ シナリオの地図も同じく変わった（⚠ 共有の部品・`RUN-2`）
- ⚠ 実測：⚠ `theme`（2回）・`dungeon`・`layout` 赤0・黄1 ／ `floor` 赤0・黄3（⚠ **変更前も同じ3本**＝わざと弾かせる検査）／ `shot` 19枚・赤0・黄2
- ⚠ 1手目で**霧が紙の外へはみ出した**（⚠ 横 80 固定・上は 0 で止まっていた）→ ⚠ 2手目で紙の余白ちょうどに
- ⚠ **やっていない**：⚠ 手本の3列（左に3人とレリック・右に鞄と「次の出口まで」）／ ⚠ アイコンだけの丸いマス ／ ⚠ 見出しの「33層 ⋂出口まで7」
- ⚠ 報告：⚠ シナリオの地図だけ、⚠ たいまつの光が紙の下端から少し漏れる（⚠ 光の器は紙の外まで広がる）

---

## 0-UI-AB. ✅ **戦闘の見た目（回UI-4 の1枚目）（2026-09-26）**　⚠ §0-UI-AA の続き

- ⚠ 入ったもの：⚠ 丸い駒と縁 ／ 名前の枠 ／ 戦場の地を夜の床に ／ 状態アイコンを HP バーの上へ ／ 浮かぶ数字を 900＋黒い太いふち（⚠ 値は `BT-11`）
- ⚠ 着手の前の1行：⚠ **駒の本体 74 x 64 → 74 x 74**（⚠ 並べ方は幅しか使っていないので並びは変わらない）→ ⚠ そのとおり
- ⚠ 実測：⚠ `theme`（2回）・`area`・`layout` 赤0・黄1 ／ `shot` 19枚・赤0・黄2
- ⚠ **やっていない**：⚠ 手本の「会心」の小さな札（⚠ 新しい文言）／ ⚠ 下のパネルの HP の数字（`55 / 60 [70]`）と丸い顔 ／ ⚠ 見出しの「難ダンジョン 33層」「拠点へ」の並び ／ ⚠ 手本の線画の駒（⚠ 絵待ち・今は絵文字）
- ⚠ 報告：⚠ **駒どうしが重なる**（⚠ 僧侶と弓兵・検証の敵とウルフ）。⚠ 位置は戦闘の計算（`unit.x`）が決めていて、⚠ **前から**（⚠ 四角のころも重なっていた）。⚠ 見た目の回では触らない
- ⚠⚠ **見る回の目安（3件）に届いた**（`HUMAN_CHECK.md`）

---

## 0-UI-AA. ✅ **入口の組み替え（回UI-3）（2026-09-26）**　⚠ §0-UI-Z の続き

⚠ 人間の選択：⚠ **詰所＝編成** ／ ⚠ **記録＝倉庫の図鑑タブ**（⚠ 値は `DECISIONS.md` `NAV-6`・`UI-17`）。

### 0-UI-AA-1. ⚠ 着手の前に書いた1行

> ⚠ **`shot` が 20枚 → 19枚**（`05_guild` が無くなる）。⚠ 帯が拠点・育成・作業場・研究・ショップ・倉庫の6枚に写る

→ ⚠ **そのとおり**（⚠ 19枚・赤0・黄2）。

### 0-UI-AA-2. ⚠ 入ったもの

- ⚠⚠ **`BaseFacilityBar`**（⚠ 施設の並びと行き先の唯一の正）。⚠ 拠点・編成（⚠ 拠点から来たときだけ）・育成・作業場・倉庫・研究・ショップに敷く
- ⚠⚠ **ギルドの画面を消した**（`guild_screen.*`）／ ⚠ 拠点のギルドと編成のボタンを消した ／ ⚠ 5画面の「戻る」をギルド → **本部（拠点）** へ
- ⚠ 倉庫がタブを受け取る（`TransferKeys.WAREHOUSE_TAB`）
- ⚠ 見出しの題を明朝22・左右に◆（`UI-17`）
- ⚠ 検査 `inventory_window` の③を「帯に持ち物・記録」に書き換えた ／ ⚠ `shot` から `05_guild` を外した ／ ⚠ 翻訳キー8行（`ui_facility_*`）

### 0-UI-AA-3. ⚠ 実測

- ⚠ `theme`（2回）・`layout`・`inventory_window`（③④ 全部 true）・`glyphs`・`economy`・`equip_drag` 赤0・黄1 ／ `inventory` 赤0・黄3 ／ `shot` 19枚・赤0・黄2

### 0-UI-AA-4. ⚠ 報告

- ⚠⚠ **育成の一覧は帯のぶん縦が足りない**（⚠ 検証用3人の行で長い・⚠ 一覧がスクロールしない）。⚠ 1手目で「⚠ 上へ伸びて見出しがずれる」を「⚠ 下（帯の裏）へ伸びる」に変えた＝⚠ **最後の検証用の行が帯に隠れる**（⚠ 本物の3人は見える）。⚠ 育成を組み替える回で直す
- ⚠ 題の◆は1手目で**左端に出た**（⚠ 並べ終わる前に描いていた）→ ⚠ 2手目で直った
- ⚠ **見出しの帯（横いっぱい・70）はまだ**（`UI-17`）
- ⚠ 帯の線画アイコンはまだ（⚠ 字だけ）

---

## 0-UI-Z. ✅ **紙の部品（回UI-2）（2026-09-26）**　⚠ §0-UI-Y の続き

### 0-UI-Z-1. ⚠ 着手の前に書いた1行

> ⚠ **`scenario=shot` が 19枚 → 20枚**（⚠ 赤0のまま）

→ ⚠ **そのとおり**（⚠ 20枚・赤0・黄2＝平常値）。

### 0-UI-Z-2. ⚠ 入ったもの（⚠ 値は `DECISIONS.md` `UI-15`・`UI-16`）

- ⚠ 部品6つ：`PaperSheet`（角飾り）／ `SheetHeading`（太い下線・菱形の飾り罫・字間を広げた明朝）／ `PaperTabs`（46／38）／ `LedgerRow`（点線の罫・選んでいる行・使えない行）／ `Stamp`（四角・丸・傾き）／ `FacilityBar`（灯りの線・しおり紐）
- ⚠ 見本は UI テストのページの**一番上**。⚠ `shot` に `20_ui_parts`
- ⚠⚠ **本文の太さを 400 に**（`UI-15`）。⚠ 全画面の字が太くなった
- ⚠ 翻訳キー10行（`ui_uitest_paper*` ／ `ui_stamp_*`）

### 0-UI-Z-3. ⚠ 実測

- ⚠ `theme`（2回）・`glyphs`・`layout`・`economy` 赤0・黄1 ／ `inventory` 赤0・黄3 ／ `shot` 20枚・赤0・黄2
- ⚠ 絵（`20_ui_parts`）：⚠ 紙の上の字が墨 ／ 選んでいる行に左の線 ／ 使えない行が薄い ／ 判が傾く ／ 施設の帯の2つ目にしおり紐

### 0-UI-Z-4. ⚠⚠ 報告

- ⚠⚠ **本文のフォントはずっと Thin（太さ100）で出ていた**。⚠ 可変フォントの既定値が 100 だった（⚠ ファイルの `fvar` を読んだ）。⚠ **09-26 までの全部の絵と実機は Thin**
- ⚠ **紙は傾けられない**（⚠ `Container` が子の回転を0に戻す）。⚠ 傾けたいなら「器の外に置く」か「描画だけ回す」しかない＝⚠ **見送り**（`UI-16`）
- ⚠ この回で作らなかったもの：⚠ 説明の窓・HPバー・確かめの窓（⚠ 使う画面の回で作る）／ ⚠ 見出しの帯（⚠ 回UI-3 へ）
- ⚠ 施設の帯の絵（⚠ 手本の線画アイコン）はまだ無い（⚠ 字だけ）

---

## 0-UI-Y. ✅ **テーマを手本の色へ（回UI-1）（2026-09-26）**　⚠ §0-UI-X の続き

⚠ 人間：紙の上の字は「⚠ あで」（⚠ 紙用のテーマを1枚足す＝`UI-14`）。

### 0-UI-Y-1. ⚠ 着手の前に書いた1行

> ⚠ **`shot` の19枚で、ボタンの地が暗い灰から革の茶に変わるはず**（⚠ 赤0のまま）

→ ⚠ **そのとおり**（⚠ `04_base` の下段7個・`17_modal_confirm` の「いいえ」以外）。⚠ 戦闘（`03_battle`）は専用の色なので変わらない（⚠ 手本も「戦闘だけ例外」）。

### 0-UI-Y-2. ⚠ 入ったもの

- ⚠ `theme_builder.gd`：⚠ 手本の色を `TOKEN_*` に1か所で持ち、⚠ ボタン5階層（⚠ **名前はそのまま**）・字・面・床・HP・状態アイコン（`BT-3`）・focus の縁を差し替え。⚠ ボタンは**高さ44・角丸6**
- ⚠⚠ **紙のテーマ** `theme/paper_theme.tres`（⚠ 字の型10個＋罫）と ⚠ 紙の面 `PaperPanel`（⚠ まだどの画面も使っていない＝回UI-2）
- ⚠⚠ **画面の地 20 枚**：⚠ `Background` が `ColorRect` に**色を直書き**していた（⚠ `UI-1` 違反。⚠ 紫がかった `1a1418` など5色）→ ⚠ 既にある `BackgroundPanel` に付け替えた
- ⚠ 手本のフォルダに `.gdignore`（⚠ `--import` が png 42枚を取り込んでいた）
- ⚠ 検査 `theme` に「⚠ 紙の上の字が本当に墨を引くか」を足した（⚠ 木に入れて聞く）

### 0-UI-Y-3. ⚠ 実測

- ⚠ `theme`：⚠ 紙の上 `Label 2b2118` ／ `MutedLabel・CaptionLabel 6e5a43` ／ `AccentLabel 9c7424`、⚠ 紙の外 `Label eadfca`。⚠ **欠け0・赤0・黄1**
- ⚠ `layout` `glyphs` `economy` 赤0・黄1 ／ `inventory` 赤0・黄3 ／ `shot` **19枚・赤0・黄2**（⚠ どれも平常値）

### 0-UI-Y-4. ⚠ 報告（**直していない**）

- ⚠⚠ **テーマを書き直した直後の1回目の `theme` は赤2**（⚠ 紙の外の字・`PaperPanel`）。⚠ 起動時に読んだ**古い** `main_theme.tres` を同じ回の中で聞くため。⚠ **2回目で0**。⚠ 値を変えた回は2回回すこと
- ⚠ 倉庫のタブ（`TabBar`）はテーマが当たっておらず Godot の既定のまま（⚠ 回UI-2 の紙のタブで置き換わる）
- ⚠ 見出しのフォント（`UI-12`）：⚠ 同じ日に**入った**（⚠ 人間「⚠ とってきて」。⚠ google/fonts から ExtraBold の1本・15MB）。⚠ 見出しの型4つに当てた。⚠⚠ **結果の窓の「3層 波3/3」の数字も明朝になる**（⚠ 手本「数字は明朝にしない」とずれる＝回UI-4 の戦闘で見る）

---

## 0-UI-X. ✅ **UI の作り直し一式を読んで、回に分けた（2026-09-26）**

⚠ 人間：「⚠ これを見ていろいろなおしてほしい」。⚠ 手本は `docs/pomodoro-heroes-ui-docs/docs/ui/`（`UI-10`）。
⚠ **コードは触っていない**（⚠ 読む → 洗い出す → 人間の裁き）。

### 0-UI-X-1. ⚠ 人間の裁き（⚠ 値は `DECISIONS.md`）

⚠ 「1い 3あ 4あ 5い 6い 7あ 8い 9あ」→ `UI-10`〜`UI-12`・`NAV-6`・`NAV-7`・`BT-3`・`MD-4`・決定49。
⚠ 聞き直した2問：⚠ 問2＝**入れる**（`UI-13`）／ ⚠ 問10＝**たたき台の順のまま**。
⚠ あわせて：⚠ **サインのフォントは今は入れない**（`UI-12`）／ ⚠ **絵文字フォント（`segoe-ui-emoji.ttf`）の扱いは「まだ」**＝保留。

### 0-UI-X-2. ⚠ 実測

- ⚠ 手本は **42ファイル**（⚠ 37画面 ＋ 部品の見本5枚）。⚠ プロンプトの「44」は誤り
- ⚠ **手本と合っている**：加護 45／90／180分（`protection_*.tres`）・スキル枠2・10軸・割り振り3軸・パッシブ 20〜100・コイン（決定45）・わかれ道 → ショップ → 次（決定48）
- ⚠ **手本に無い今の画面**：ギルドのカード・ショップ・研究・`floor_map` / `floor_shop`・休憩と振り返り・拾いものの窓
- ⚠ 詰所の控えは手本 **8** ／ いま `PARTY_PRESET_COUNT` **10**

### 0-UI-X-3. ⚠ 画面ごとの種類（⚠ 見＝見た目だけ ／ 組＝組み替え ／ 新＝新しい仕組み）

| 種類 | 画面 |
|---|---|
| 見 | Title ／ LevelUpDone ／ Pomodoro（⚠ ノルマ札の行は新）／ Focus ／ Sign ／ DungeonMap ／ Battle ／ DungeonFork ／ DungeonShop ／ Confirm ／ EmptyStates |
| 組 | QuestBoard ／ Barracks ／ Character・Nodes・Skills・Equip ／ LevelUp ／ PassiveUnlock（⚠ 知らせる画面が無い）／ Belongings（＋Wide）／ Chest ／ DungeonRelic（⚠ `RUN-10` とぶつかる）／ DungeonResult |
| 新 | Main（⚠ 絵待ち）／ RichItem*（⚠ 特殊効果）／ Forge・ForgeResult・Fail（`EQ-5`〜`7`）／ Records ／ FocusSkins・PomoSkin ／ Companion ／ DungeonGate（決定49）／ Settings |
| 参考 | Parts ／ Signals ／ Icons ／ Status ／ Notify |

### 0-UI-X-4. ⚠ 報告（**直していない**）

- ⚠ **図鑑と遺物の置き場**：手本は「記録」、`BS-15`・`EQ-8` は「持ち物」。⚠ **未決**（⚠ 回UI-組で持ち物を作るまでに決める）
- ⚠ `assets/fonts/segoe-ui-emoji.ttf` が git に入っている（⚠ Microsoft のフォント。⚠ 配ってよいライセンスではないはず）

---

## 0-UI-W. ✅ **倉庫の別窓をやめた（回3-b の1手目）（2026-09-23）**　⚠ §0-UI-V の続き

⚠ 人間：「⚠ もう倉庫の別窓はいらないと思う」。⚠ 入口は「⚠ 1ア」（ギルドのカード）、⚠ 範囲は「⚠ B」（⚠ **容量をなくすのは後**・UI はモックのあと）。

### 0-UI-W-1. ⚠ 入ったもの

- ⚠⚠ **`InventoryWindow` を消した**（`scenes/inventory/` ごと）／ ⚠ 右上の「倉庫」ボタンを消した ／ ⚠ OS の別窓からの落とし先探し（`route_screen_drop()` ほか）を消した
- ⚠ **倉庫をふつうの画面に戻した**（⚠ 左上に戻る＝行き先はギルド ／ ⚠ 題「倉庫」）。⚠ **ギルドのカードの5枚目**
- ⚠ 題の帯のぶん縦に 30px はみ出した（`layout` で 902 x 750）→ ⚠ 持ち物のマス目をスクロールの器へ（⚠ 装備画面と同じ形）→ **902 x 234**
- ⚠ 検査 `inventory_window` と `equip_drag` を作り直した（⚠ 名前は残した）／ ⚠ `shot` に **19枚目 `19_warehouse`**

### 0-UI-W-2. ⚠ 実測

- ⚠ `InventoryWindow` の参照 **15 → 0 件**（⚠ 残りはコメントと「無いこと」を見る検査だけ）／ ⚠ ギルドのカード **4 → 5 枚**
- ⚠ `inventory_window` 赤0（⚠ ①〜④ 全部 true）／ ⚠ `equip_drag` 赤0（⚠ 右の持ち物 → 装備マスで装備・持ち物から消える）
- ⚠ `layout` `glyphs` `economy` `drag_cursor` 赤0・黄1 ／ ⚠ `inventory` 赤0・黄3 ／ ⚠ `shot` **19枚**・赤0・黄2（⚠ 平常値）

### 0-UI-W-3. ⚠ 報告（**直していない**）

- ⚠ **拠点の右上の素材の帯が画面の右端にくっついている**（`04_base`）。⚠ 帯は HUD の幅を見ていない（`reserved_height()` だけ）ので、⚠ **今回の変更ではなく前からの可能性が高い**
- ⚠ `AGENTS.md` のフォルダ構造に `scenes/inventory/` が残っている（⚠ フォルダはもう無い）。⚠ `CLAUDE.md` の「いまは18枚」も 19枚になった
- ⚠ **容量（500マス）・ページ送り・枠を買う・並べ替えはまだ残っている**（⚠ 決定 `BS-10` `BS-20`。⚠ 持ち物の画面を作り直す回で消す）

---

## 0-UI-V. ⚠⚠ **装備関連 UI（回3-b）── 途中で止めた（2026-09-22）**　⚠ §0-UI-U の続き

⚠⚠ **人間が実機を見て止めた回。** ⚠ 言葉のまま：
> ⚠ **「がめんがきれてる　⚠ いったん考え直したほうがいいと思う　⚠ コンセプトから
> ⚠ インベントリ性とこの画面があってないんだと思う」**
> ⚠ **「というかインベントリシステムじゃなくてもいいかもね」**

### 0-UI-V-1. ⚠ 入ったもの（**残っている。⚠ 消していない**）

| コミット | 中身 |
|---|---|
| `c1386e0` | ⚠ モックの依頼書（`docs/03_log/PRE_PLAN_EQUIPMENT_UI_MOCK.md`） |
| `7bd98d3` | ⚠ モックを裁いた（⚠ 決定 `BS-15`〜`BS-19`）／ ⚠ `EXEC_EQUIPMENT_UI.md` |
| `3661672` | ⚠⚠ **装備画面を3列に**（⚠ 持ち物を画面の中へ ／ 説明の窓 `ItemActionPanel` ／ 枠へ落として刺す） |
| `e5e127a` | ⚠ 「装飾はまだ使えない」が描き直すたびに積もっていたのを直した |

### 0-UI-V-2. ⚠⚠ 止まっている理由（**コンセプトの食い違い**）

⚠ 2026-09-22 に `docs/00_concept/` を読み直して見つけた。⚠ **推測ではなく引用**。

| | 言っていること |
|---|---|
| ⚠ `CONCEPT.md`（42行） | ⚠ **「ポモドーロをこなすと報酬」「1プレイ＝1ポモドーロサイクル（15〜50分）」「勝敗より継続的な育成」** |
| ⚠ `game_design_doc.md`（128行） | ⚠ **「タルコフのリスクと所有欲」「死亡でアイテム全ロスト」「1ラン＝3階×25層」** |

⚠⚠ **さらに：⚠ `game_design_doc.md` のインベントリ節は全部「ランの中の鞄」の話**
（⚠ 探索アイテムと戦利品が同じ枠で競合 ／ 初期枠は少なめ ／ ショップで拡張）。
⚠⚠ **拠点の500マスはコンセプト文書のどこにも根拠が無い。**
⚠ 出どころは `PLAN_INVENTORY.md`（2026-09-02）の決定1「マインクラフト風のマス目」・決定2・決定8。
⚠ **決定2 は `PLAN_HARD_DUNGEON.md` の「拠点の `INVENTORY` は無限のまま。さわらない」を覆したもの**（⚠ 台帳にそう書いてある）。

### 0-UI-V-3. ⚠ 実測（**この回で取った数字**）

- ⚠ **品は全部で 91 種**（⚠ 装飾61 ／ 素材16 ／ **装備11** ／ ダンジョン専用2 ／ 消耗品1）
- ⚠ **倉庫は 500 マス**（10 × 10 × 5ページ・⚠ 最初から500）／ ⚠ **ランの鞄は 8 枠**
- ⚠ `slot_position`（⚠ マスに置いた位置を覚える欄）は**在るのに呼び出し元が0件**＝⚠ **並べ替えは一度も作られていない**
- ⚠ 装備画面の最小サイズ `593 x 518` → **`876 x 518`**（⚠ 3列にした結果）
- ⚠ `layout` `glyphs` `economy` 赤0・黄1 ／ `inventory` 赤0・黄3 ／ `shot` 18枚・赤0（⚠ 平常値）

### 0-UI-V-4. ⚠ 次にすること

⚠⚠ **コンセプトを先に決める**（⚠ ピッチA か B か）。⚠ **持ち物 UI の案はそのあとでないと選べない。**
⚠ 渡す文書と聞くことは `docs/03_log/NEXT_SESSION_PROMPT.md`。

---

## 0-UI-U. ✅ **拠点・共通 UI の残り（回3）（2026-09-22）**　⚠ §0-UI-T の続き

⚠ 同じ日の2本目。⚠ **人間の決定：⚠ 装飾の枠は「マス＋吹き出し」**（⚠ 4択から選んだ）。

### 0-UI-U-1. ⚠⚠ 人間の決定（**覆さない**）

- ⚠⚠ **値の正は `docs/DECISIONS.md` の `BS-14`**（⚠ 装飾の枠はマス。⚠ 押すと吹き出し）。
  ⚠ 「⚠ 文字の行のまま整える」「⚠ マスにするがボタンは今のまま」は**採らなかった**

### 0-UI-U-2. ⚠ 入ったもの

| コミット | 中身 |
|---|---|
| `ba0e427` | ⚠⚠ **装飾の枠をマスに**（⚠ `PartSlotRow` ＝ホバーの詳細と同じ器）／ ⚠ **押すと吹き出し**（⚠ 刺す・外す(赤)・移動量）／ ⚠ わかれ道の「手ぶらだ」を中央へ ／ ⚠ 宿題79・80 ／ ⚠ `layout` と `shot` に下ごしらえ |

### 0-UI-U-3. ⚠ 実測

- ⚠ **`layout` に検査を足した**：⚠ `PartSlots_weapon = 1/2 ／ [攻撃の宝石①] [（空き）宝石枠]`。
  ⚠ **0 行なら赤**（⚠ 枠が出ないまま気づかないのを止める）
- ⚠ **`scenario=shot` 18枚目**（`18_equipment_parts`）＝ ⚠ **吹き出しを出したまま撮った**。
  ⚠ 「宝石枠 ／ 攻撃の宝石① 攻撃 +4 ／ **外す（赤）**」
- ⚠ `--import` 赤0・黄0 ／ ⚠ `layout` `glyphs` **赤0・黄1** ／ ⚠ `inventory` 赤0・黄3（⚠ 平常値）
- ⚠ `shot` **18枚・赤0・黄2**（⚠ **黄が1本増えた**＝下の報告）

### 0-UI-U-4. ⚠⚠ 報告（**台帳のほうが間違っていた3件**）

⚠ **`grep` で中身を見たら、⚠ 回3 の題目のうち3件は既に済んでいた**（⚠ CLAUDE.md 1番）。

| 台帳の言い分 | 実際 |
|---|---|
| ⚠ **`DANGER` は定義だけで未割り当て** | ⚠ **4箇所で使っている**（⚠ 倉庫の「捨てる」／ 共通モーダルの実行 ／ レリック選択 ／ 脱落したキャラ）。⚠ **今回さらに「外す」が5つ目** |
| ⚠ **`ItemSlot` のツールチップとドロップダウンが二重** | ⚠ **二重にならない**。⚠ `ItemDetailPopup.watch()` が器ごと素のツールチップを止めている（`item_grid.gd:132`）。⚠ 枠を見ている画面は6つとも片方だけ |
| ⚠ **宿題81：戦闘の HUD が測れない（`0 x 0`）** | ⚠ **2026-09-16 に解消済み**（⚠ HUD をコンテナ化。⚠ `shot` の `measure` でも取れている） |

### 0-UI-U-5. ⚠ 報告（**直していない**）

- ⚠⚠ **`[BaseScreen] unlocked screen_id not in navigation: decoration`**（⚠ 黄）。
  ⚠ 下ごしらえで装飾を解放したら出た。⚠⚠ **本番でも段階解放で `decoration` が開くので、⚠ 同じ黄が出るはず**。
  ⚠ 「装飾に入口を作るのか／`decoration` を解放しない作りにするのか」は**決めていない**
- ⚠ **`ui_part_rune_move` は注記に回した**（⚠ ドロップダウンの見出しだった）。⚠ キーは使っている
- ⚠ **吹き出しは外を押しても閉じない**（⚠ 前からある弱点）。⚠ 人間の見る回へ回した

### 0-UI-U-6. ⚠ 残っているもの

- ⚠⚠ **「見る回」の目安に届いた（3件）**。⚠ 一覧は `docs/HUMAN_CHECK.md`
- ⚠ **装飾の枠を空にしたときのマス**（⚠ 全部空きの姿）は絵で撮っていない（⚠ 1つ刺した姿だけ）

---

## 0-UI-T. ✅ **窓を撮る仕掛け（回1）（2026-09-22）**　⚠ §0-UI-S の続き

⚠ 人間が題目に「⚠ 回1」を選んだ回。⚠ **`tests/debug_boot.gd` だけを触った**（⚠ 本番コードは1行も変えていない）。

### 0-UI-T-1. ⚠ 着手の前に書いた1行

> ⚠ **`scenario=shot` の枚数が 11 から 17 に変わるはず**（⚠ 赤0のまま）

→ ⚠ **そのとおりになった**（⚠ 17枚・赤0・黄1＝例の `skill_dbg_dot_odd`）。

### 0-UI-T-2. ⚠ 入ったもの

| コミット | 中身 |
|---|---|
| `82ec663` | ⚠⚠ **窓6枚**（⚠ 拾いもの・戦闘の結果・レリック選択・**わかれ道**・商人・**共通モーダル**）／ ⚠ 下ごしらえ「ボスの先に立たせる」／ ⚠ 画面を開いたあとに窓を出す手（`after`）／ ⚠⚠ **開いた画面が頼んだものと同じかを確かめる守り** |

⚠ 撮り方は2通り。⚠ **どちらも本番の口を通す**（⚠ 窓のシーンを単体で開かない＝後ろが無く幕が透けない）。

- ⚠ `SceneManager` で開くもの（⚠ レリック選択・商人・わかれ道）＝ ⚠ 行に `prepare` を足すだけ
- ⚠ 重ねるもの（⚠ 拾いもの・戦闘の結果・共通モーダル）＝ ⚠ 行に `after` を書く

### 0-UI-T-3. ⚠ 実測（**絵で見た**）

| 枚 | 何が写ったか |
|---|---|
| `12_run_loot_window` | ⚠ マップの上に重なった窓。⚠ 帯「宝箱を見つけた」＋右に注意書き ／ ⚠ 「開ける」＋「閉じる」 ／ ⚠ 鞄 0/8 |
| `13_battle_result` | ⚠ 「戦闘に勝利」／ ⚠ 見本の報酬がチップ9個 ／ ⚠ 「拠点へ」「次へ進む」／ ⚠ 金 +65 |
| `14_relic_select` | ⚠ 候補3マス ／ ⚠ 右は「マスを選ぶと、ここに詳細が出ます」 |
| `15_dungeon_floor_clear` | ⚠⚠ **わかれ道**（決定48）。⚠ 「踏破 フロア 1」／ ⚠ 「さらに潜る」「ここで戻る」 |
| `16_dungeon_shop` | ⚠ 「ボスの間の商人」／ ⚠ 品2行（⚠ どちらも「遺物片が足りない」で不活性） |
| `17_modal_confirm` | ⚠⚠ **共通モーダル**（決定39）。⚠ 帯「確認」／ ⚠ **実行が赤で左**・「いいえ」が右（⚠ `MD-4`・`MD-5` のとおり） |

### 0-UI-T-4. ⚠⚠ この回で踏んだこと（**次に同じ手を使う人へ**）

- ⚠⚠ **`Modal.confirm()` は撮るのに使えない**（⚠ 2手とも外れた）。
  ⚠ ① 直に呼ぶ → ⚠ **`must be called with "await"` でパースエラー**（⚠ 起動そのものが止まる）
  ⚠ ② `Callable` 越しに呼ぶ → ⚠ **実行時に赤**（`Trying to call an async function without "await"`）
  ⚠ ⚠ `await` すると「はい／いいえ」が押されるまで戻らないので**1枚も撮れない**。
  ⚠⚠ **器を返す1段下の口（`Modal._enqueue()`）を呼ぶ**。⚠ 器を自分で `instantiate()` しない
- ⚠⚠ **名前と中身がずれる危険は「守り」で消した。** ⚠ 撮る直前に**開いた画面が `scene` と同じか**を見て、
  ⚠ 違えば**保存しない**。⚠ 09-21 に「マップの絵がわかれ道の名前で保存される」を恐れて足せなかった件は、
  ⚠ これで**足せるようになった**
- ⚠ **`debug_boot`（外側）への参照を窓の撮影役に持たせない。** ⚠ 画面を差し替えた時点で消えているため
  （⚠ マスを探す口は撮影役の中にもう1つ持った）

### 0-UI-T-5. ⚠ 報告（**直していない**）

- ⚠⚠ **わかれ道の「手ぶらだ」だけ左端（x=36）に寄っている**。⚠ 見出し・説明・ボタンは中央。
  ⚠ 人間が 09-21 に見たときは**拾いものが1件あった**ので気づけなかった（⚠ 空のときだけ出る）→ ⚠ **回3 へ入れた**
- ⚠ **まだ撮れない窓が2つ**：⚠ 通路のできごと（⚠ 本番は閉じるまで `await` する形）／
  ⚠ シナリオ側の商人（`floor_shop`・⚠ 器が別）

### 0-UI-T-6. ⚠ リリース前に消すものが増えた

⚠ `scenario=shot` の**窓6枚**も `tests/` 一式と一緒に消える（⚠ 回9）。
⚠ `Modal._enqueue()` を外から呼んでいるのは**ここ1箇所だけ**（⚠ 本番は `confirm()` / `notify()` を通る）。

---

## ⚠ 前の回はここ（**2026-09-21 に分けた**）

> ⚠⚠ **人間の決定：⚠ 「plan はとっかかり」「nextsteps はいまの議題」「それ（＝過去の回）が溜まってるのが問題かも」。**
> ⚠ **3154行・15節あったので、⚠ 直近3節（§0-UI-Q / §0-UI-P / §0-UI-O）だけ残して月ごとに分けた。**

| どこ | 中身 |
|---|---|
| `docs/03_log/NEXT_STEPS_2026-09.md` | ⚠ **§0-UI-N（09-19）〜 §0-L（09-02）**。⚠ 戦闘画面・倉庫の別窓・難ダンジョンの段階17〜20 |
| `docs/03_log/NEXT_STEPS_2026-08.md` | ⚠ **§0-A（08-31）以降**と、⚠ 当時の参考の章（⚠ 事故りやすい箇所・調査済みの事実・罠・引き継いだ宿題） |

- ⚠⚠ **いま効いている値の正は `docs/DECISIONS.md`。** ⚠ 記録の中の数字は当時のもので、**古いことがある**
- ⚠ **ルールの正は `AGENTS.md` と `CLAUDE.md`**（⚠ 記録の §2〜§6 ではない）
