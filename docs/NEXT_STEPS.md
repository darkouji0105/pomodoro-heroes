# 次にやること：**装飾システム**（素材の4段階が入った）

**このファイルは「次の1タスク」だけを書く。** 終わったら次のタスクの内容に書き換える。全体の状況は`PROJECT_STATUS.md`、ルールは`AGENTS.md`と`CLAUDE.md`、**ゲームの中身は`GAME_DESIGN.md`**、**決定台帳は`docs/01_plan/PLAN_SKILL_TEMPLATE.md`**。

**このファイルだけ読めば着手できるように書いてある。** 過去のタスクを知らない前提で読んでよい。

⚠ **今回は「何をやるか」は決まっている**（§1）。⚠ **決まっていないのは中身。⚠ §1-2 を人間に確認してから着手すること。**

---

## 0. 前のタスクは終わっている（**2026-08-22・素材の4段階＋装備の等級10**）

**指示書は `docs/02_exec/EXEC_MATERIAL_TIERS.md`。** ⚠ **§5・§6・§7 は全部通っている**（⚠ **画面も人間が実施済み。⚠ 宿題は残っていない**）。

### 0-1. ⚠ 直近3回で入ったもの（**全件は `PROJECT_STATUS.md`**）

| 回 | 入ったもの |
|---|---|
| **無音の穴** | ⚠ **死亡中はCDを進めない** ／ ⚠ **`atk_mult_pct`** ／ ⚠ **DoT の周期ダメージで購読が発火** ／ ⚠ **「知らない欄」を W16 で出す** |
| **状態のUI** | ⚠ **状態が画面に出る**（味方＝スキルボタンの左に縦・敵＝HPバーの上に横）／ ⚠ **色付きのマス ＋ 漢字1文字** ／ ⚠ **`StatusRegistry.entries_for()` が状態を引く唯一の口** |
| **素材の4段階** | ⚠ **素材が3件 → 12件**（建築・育成・鍛冶 × ①〜④）／ ⚠ **装備の等級上限が 3 → 10** ／ ⚠ **等級帯で要求段階が変わる**（2〜3→① / 4〜6→② / 7〜9→③ / 10→④）／ ⚠ **分解は半分しか返らない**（払った段階ごと）／ ⚠ **`EquipmentConfig` を新設** ／ ⚠ **`E118`（素材IDの改名漏れ）** ／ ⚠ **`kind: "report"` のシナリオ** |

### 0-2. ⚠ 直近の人間の決定（**覆すときは影響範囲が広い**）

1. ⚠ **コンボは作らない**（2026-08-22）
2. ⚠ **`target.range` は触らない**
3. ⚠ **バランスの実測は「だいぶ後」**（⚠ **作業場の作り直しが未着手なので、⚠ 測る対象が作り直しになる。`PROJECT_STATUS.md` 794章**）
4. ⚠ **状態の色は3つだけ。⚠ デバフも青**
5. ⚠ **順番は ①素材の4段階 → ②装飾 → ③作業場**（2026-08-22。⚠ **設計役の推奨は「作業場が先」だったが覆された。⚠ 装飾素材が4段階なので、①を先にやらないとIDの改名になる**）
6. ⚠ **作業場の回（③）は「廃止だけ」**（⚠ **中間素材も作らない。⚠ `recipes.json` は空になる。⚠ 進行中のキューは黙って落とすまま**）
7. ⚠ **鍛冶のコストは式ではなく数値の配列**（⚠ **「1個だけ直したい」が式では書けないため**）
8. ⚠ **分解は半分しか返さない**（⚠ **それまでは全額＝無損失だった**）

---

## 1. ⚠ このタスク：**装飾システム**

⚠ **`GAME_DESIGN.md` 7章。⚠ 宝石・護符・紋章の3種類 ＋ ルーン（別系統）。**

### 1-1. ⚠ なぜ今これか

⚠ **決定5で順番が決まっている。⚠ 装飾が入らないと③（作業場）の目玉である「装飾のランダム製作」が書けない。**
⚠ **①で素材の段階の規則（`<系統>_material_<1..4>`）が確定したので、⚠ 装飾素材を同じ規則で足せる＝改名が起きない。**

### 1-2. ⚠ 着手前に決めること（**設計役が推奨付きで出してから聞くこと**）

⚠ **少なくともこの5つは人間が決める。⚠ 勝手に決めない。**

1. ⚠ **どこまでやるか**（⚠ **3種類全部か、宝石1種類だけ先か。⚠ ルーンは別系統なので分けるか**）
2. ⚠ **装飾素材を足すか**（⚠ **`decor_material_1..4` の4件。⚠ 入手経路と使い道が要る**）
3. ⚠ **ランダムロールをどこで振るか**（⚠ **`GAME_DESIGN` は「刺すときに振れる」。⚠ 刺した結果は `parts` に個体化する**）
4. ⚠ **外したら壊れる仕様を今入れるか**（⚠ **`GAME_DESIGN` 7-6。⚠ 「壊れる確認」のモーダルが要る**）
5. ⚠ **鍛冶屋トークンを今入れるか**（⚠ **出目を最高値に確定させる天井。⚠ 通貨が1本増える**）

---

## 2. ⚠ 事故りやすい箇所

### 2-1. ⚠ ドキュメントの「実装済み」を信じない

**ズレが17回起きている。** ⚠ **`grep` で関数の中身を見てから判断する。⚠ 違っていたら報告する（勝手に直さない）。**

⚠ **直近で実際に見つけたズレ**：
- ⚠ **17件目：`EXEC_STATUS_UI.md` §7 が本番スキルを `力の一撃` / `聖なる光線` と書いていた**（⚠ **実データは `強撃` / `聖光`。⚠ IDと効果は正しかった。⚠ 表示名だけ違った。⚠ EXEC 側に注記を足してある**）

### 2-2. ⚠ 新しい `class_name` は、エディタを1回通すまで認識されない

⚠ **前回1タスク溶けた。⚠ 今回も `EquipmentConfig` で踏んだ。**
→ ⚠ **参照する側（`balance.gd` の `@export` など）を、⚠ 人間がエディタを通すまで書かない。⚠ 実装を前半・後半に割る。**
→ ⚠ **`Node` 型 ＋ `has_method()` で逃げない。⚠ `@export var x: Resource` にも落とさない**（`AGENTS.md`）。
⚠ **確かめ方**：`grep -c "<クラス名>" .godot/global_script_class_cache.cfg`

### 2-3. ⚠ `.tres` をエディタ外でリネームすると `uid` が古いパスを指す

⚠ **`.tscn` の `ext_resource` に `uid` があると、⚠ Godot は `path` より `uid` を優先する。⚠ `.godot` の uid キャッシュが古いままで `Parse Error: referenced non-existent resource` になった。**
→ ⚠ **`uid` を書かず `path` だけにすれば通る**（⚠ **`shop_config.tres` など既存3つがこの形**）。

### 2-4. ⚠ 「同じ形の判定」が散っていたら1本に寄せる

⚠ **この器で3回踏んでいる。** 今ある1本ものは：
- ⚠ **`BattleSession.find_unit()`** ／ **`battle_controller._all_units()`**
- ⚠ **`StatusRegistry._hosts()` / `_applies_to()` / `entries_for()`**
- ⚠ **`GameManager.get_forge_material_tier()`**（⚠ **等級 → 段階。⚠ 装飾もここを通すこと**）
- ⚠ **`GameManager.add_to_inventory()`**（⚠ **装備の個体を作る唯一の口。`CLAUDE.md` 8番**）

### 2-5. ⚠ 画面の完了条件は「その配置で本当に起きるか」を確かめてから書く

⚠ **`EXEC_STATUS_UI.md` §1-1 と `EXEC_MATERIAL_TIERS.md` §1-1 では、⚠ 先に本番データを全部引いてから書いた。⚠ この形を続けること。**
⚠ **ただし今回、⚠ 「3件の前提で組まれた器に12件入れると溢れる」は予想できていたのに直さずに人間へ渡した。⚠ 予想できている落ちは先に直すこと。**

### 2-6. ⚠ E / W の次番号

⚠ **E118 まで使用済み → E119 から。** ⚠ **W17 まで使用済み → W18 から**（**W6 と W7 は欠番**）。

---

## 3. 調査済みの事実（**`grep`し直さなくてよい**・2026-08-22確認）

| | 事実 |
|---|---|
| ⚠ ログの実体 | `C:/Users/admin/AppData/Roaming/Godot/app_userdata/pomodoro-heroes/logs/battle_last.jsonl`。⚠ **戦闘のたびに上書き** |
| ⚠ 出力パネル | `.../logs/godot.log`。⚠ **保持5本。⚠ 読む前に自分でヘッドレスを走らせない** |
| ロード時の正常な出力 | ⚠ **`skills validated: 79 entries, 0 errors, 1 warnings`**（黄1本は `skill_dbg_dot_odd` の端数＝**出るのが正解**）／ `basic attacks validated: 19 entries, 0 errors, 0 warnings` ／ ⚠ **`items validated: 24 entries, 0 errors`** |
| ⚠ **素材** | ⚠ **12件。`construction_material_1..4`（木材/石材/鉄材/金材）／ `training_material_1..4`（修練/鍛錬/練達/極意の証）／ `forging_material_1..4`（鍛冶の欠片/塊/結晶/極）** |
| ⚠ **素材が触れる場所** | ⚠ **`items.json` / `stages.json` の `rewards.materials` / `shop.json` / `research.json` / `recipes.json` / `initial_state_config.tres`。⚠ `.gd` の直書きは `state_keys.gd` の `ITEM_FORGING_MATERIAL_PREFIX` だけ** |
| ⚠ **装備の等級** | ⚠ **1〜10**（`Balance.equipment.max_equipment_grade`）。⚠ **コストは `forge_cost_by_grade`（9個の配列）。⚠ 段階は `forge_material_tier_min_grades = [1,4,7,10]`** |
| ⚠ **刺す枠** | ⚠ **`parts` は長さ2の固定配列**（`PART_SLOT_COUNT = 2`・`null` 込み）。⚠ **等級5で1つ・等級10で2つ開く**（`PART_SLOT_GRADES = [5, 10]`・実機で確認済み）。⚠ **中身は空。⚠ 刺す機能はまだ無い** |
| ⚠ **装飾は0件** | ⚠ **`items.json` に宝石・護符・紋章が1件も無い。⚠ 装飾素材も0件。⚠ 等級5段階も無い** |
| ⚠ **分解** | ⚠ **`dismantle_equipment()` は装備だけ。⚠ 戻りは `{material_id: count}` の Dictionary。⚠ 払った量の半分（切り捨て）** |
| ⚠ 座標の定数 | **`GROUND_Y = 240` ／ 味方 `PARTY_BASE_X=200` `STEP=100` ／ 敵 `ENEMY_BASE_X=900` `STEP=100`** |
| ⚠ **射程の段** | ⚠ **`60`（前衛）／ `180`（中衛）／ `300`（後衛）／ `420`（最後衛）** |
| ⚠ 編成 | ⚠ **状態が唯一の正**（`battle_controller.gd`）。⚠ **`stages.json` の `party_id` では決まらない** |
| ⚠ スキル枠 | **`SKILL_SLOT_COUNT` は 2** |
| ⚠ **本番ステージ** | ⚠ **`stage_1` / `stage_2` / `stage_3` の3本 × 各5ウェーブ。⚠ 5波目が全部ボス** |
| ⚠ **本番の編成** | ⚠ **`party_default` ＝ 僧侶(180) / 弓(300) / 剣士(60)** |
| ⚠ **本番スキルの表示名** | ⚠ **剣士＝`強撃` / `横薙ぎ` ／ 僧侶＝`聖光`**（⚠ **`力の一撃` / `聖なる光線` は存在しない**） |
| ⚠ **画像素材が0件** | ⚠ **`assets/images/` は `.gitkeep` だけ** |
| ⚠ **`F4` のデバッグパネル** | ⚠ **`tests/debug_overlay.gd`。⚠ 「素材を全種類」「装備を全種類 1個ずつ」「研究を全部解放」「セーブする」。⚠ 新規セーブから装備の検証に到達する唯一の経路** |
| 行数 | `game_manager` 3168 ／ `battle_controller` 1712 ／ `status_registry` 1516 ／ `skill_schema` 1490 ／ `debug_boot` 991 ／ `skill_resolver` 858 ／ `master_data_loader` 770 ／ `skill_runtime` 536 ／ `battle_log` 379 ／ `unit` 368 |

```
battle_controller  … 入力と表示。ノードを触る唯一の層
	  ↓ cast()
SkillRuntime       … 待ち行列。trigger・購読の配布と発火・中断
	  ↓ 効果1件ずつ
SkillResolver      … 1つの効果を確定した対象に当てる。時間も段も知らない
	  ↓ host が none 以外
StatusRegistry     … 状態。寿命はスキルより長い

BattleLog          … 静的クラス。どの層からも呼べる（Autoload ではない）
```

### 3-1. ⚠ 検証の道具（**人間に頼る前にこれを回す**）

```powershell
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' `
  --headless --path d:\pomodoro-heroes res://tests/debug_boot.tscn -- scenario=area
```

- ⚠ **`Start-Process` ＋ `-RedirectStandardOutput` / `-RedirectStandardError` で実行する**（直接叩くと1行も返らない）
- ⚠ **読むのは `[System.IO.File]::ReadAllLines(path, UTF8)`**（`Get-Content` は化ける）
- ⚠ **警告とエラーは `-RedirectStandardError` のほう**（⚠ **`push_warning` / `push_error` は stdout に出ない**）
- ⚠ **今あるシナリオ（21本）**：`area` / `recast` / `recast_expire` / `summon` / `summon_wipe` / `lineup` / `mitigate` / `pierce` / `shield` / `reflect` / `reflect_self` / `intervene_legacy` / `aura` / `aura_follow` / `pool` / `atk_mult` / `dot_react` / `status_ui` / `status_ui_over` / **`materials`** / `training`（⚠ **`training` は窓あり専用**）
- ⚠ **`materials` は戦闘を回さない**（`kind: "report"`）。⚠ **素材・等級・鍛冶・分解を `print` で出すだけ。⚠ 戦闘に出ない器を足したときはこの形を使う**
- ⚠ **1本あたり10〜20秒。⚠ 全部回すと4分以上かかるので、⚠ PowerShell の既定タイムアウト（2分）に当たる。分けて回すこと**
- ⚠ **シナリオは `SCENARIOS` に1行足す。シーンを増やさない**
- ⚠ **`fire` の行に使える欄**：`gap` ／ `prepare`（`damage_party` / `kill_party`）／ `skill: ""`
- ⚠ **1行目の `gap` は効かない**（`_last_fire_sec` の初期値が `-999`）。⚠ **待たせたいときは「待つだけの行」を2つ書く**
- ⚠ **同じスキルを続けて撃つときは `gap` を CD 以上にする**
- ⚠ **敵の `hp` は 400。1体に 400 を超えて当てると死んで、次の一撃が別の敵に飛ぶ**
- ⚠ **足した検証が本当に赤を出すか、データを一時的に壊して確かめる。⚠ しかも2箇所で確かめる**（⚠ **1ファイルだけ通っていて他が素通り、を潰すため。⚠ E118 で実際にやった**）
- ⚠ **人間に渡す前に、全シナリオを1回ずつ回す**

---

## 4. 罠（**直近で実際に踏んだものだけ**）

### ⚠ 新しい `class_name` は、エディタを1回通すまで認識されない

⚠ **§2-2。⚠ 2回連続で踏んでいる。**

### ⚠ `.tres` の新規作成は人間の作業で、順番の制約がある

⚠ **`Balance` の割り当て枠は `balance.gd` に `@export` を書かないと Inspector に現れない。**
⚠ **その `@export` は `class_name` が登録されるまで書けない**（⚠ **書くと Autoload が落ちてゲームが起動しない**）。
→ ⚠ **エディタを開いてもらうのは2回。⚠ 人間の実作業は2回目の1回分。⚠ 「1回で足りる」と書かないこと。**

### ⚠ `.tres` をエディタ外でリネームすると `uid` が古いパスを指す

⚠ **§2-3。**

### ⚠ `ja.csv` の再インポートはエディタを通すまで走らない

⚠ **確かめ方**：`ls -la localization/ja.csv localization/ja.ja.translation` で日時を比べる。

### ⚠ `--check-only --script` は Autoload を読まない

⚠ **`Identifier not found: Balance` は構文エラーではない。⚠ `Parse Error` が0件なら通っている。**

### ⚠ 3件の前提で組まれた器に12件入れると溢れる

⚠ **拠点の `MaterialsDisplay` が `HBoxContainer` で、⚠ 横に際限なく広がった**（⚠ **`GridContainer` 4列 ＋ 0を出さない に直した**）。
→ ⚠ **件数を増やす回では、⚠ 既存の器の型を先に見ること。**

### ⚠ 正常系に警告を付けない・`print` を増やさない

**出したい記録は `BattleLog` へ。**（⚠ **`tests/` は例外。あちらは `print` が出口**）

### ⚠ `_ready()` の中では root に `add_child()` も画面遷移もできない

⚠ **`call_deferred` を使う。**

### インデントはタブ

`.gd`はタブ。**`.json`も既存ファイルはタブ**（⚠ `stages.json` だけトップレベルが半角スペース2つ）。`ja.csv`はUTF-8（BOMなし）。

---

## 5. 引き継いだ宿題

**⚠ 全件は `PROJECT_STATUS.md`「溜まっている宿題」を見ること。** ここは判断待ちと大きい穴だけ。

### ⚠ 人間の判断待ち

1. ⚠ **射程の段のルールが `EXEC_BATTLE_LINEUP.md` にしか書いていない**
2. ⚠ **`summons.json` を分けたのは設計役の判断**（マスターファイルが5本目）
3. ⚠ **台帳（PLAN 8章）の「`charge` 欄の有無で分岐している」が実コードと違う**（段階5で報告済み・未修正）
4. ⚠ **godot MCP の設定を消すか** ／ **`tests/` の既存9件の棚卸し** ／ **Ziva の `.bak` が7件残っている**
5. ⚠ **W16（知らない欄）を赤に上げるか**（⚠ **既存データでは0件**）
6. ⚠ **`AGENTS.md` の「`ja.csv` は人間が再インポート」は正しい**（⚠ **直さなくてよい**）

### ⚠ 器の穴（**大きいものだけ**）

7. ⚠ **購読は `host: unit` のみ**（罠が載らない）。⚠ **`host: battle` は誰も読まない**
8. ⚠ **召喚の同時数に上限が無い** ／ ⚠ **召喚はスキルもパッシブも持てない**
9. ⚠ **同じ型が複数体出ると立ち位置が重なる** ／ ⚠ **段が保証するのは味方側だけ**
10. ⚠ **`stack` の5部品のうち上限だけ入れた**（消え方・再付与・閾値は未実装）
11. ⚠ **「死亡時発動」と「他人の蘇生」はまだ書けない**
12. ⚠ **多段の2発目に投射物が出ない**
13. ⚠ **反射は1段だけ** ／ ⚠ **DoT は反射しない**（PLAN 10-2 の決定どおり）
14. ⚠ **軽減の上限95%はコードの定数** ／ ⚠ **シールドが複数付いたときの吸う順が未定**
15. ⚠ **オーラの範囲が画面に描かれない**
16. ⚠ **未実測が2件**：⚠ **追従中に付与者が死んだら座標が止まるか** ／ ⚠ **オーラに `atk_mult_pct` を書いたときに効くか**
17. ⚠ **状態の色が3つしかない**（デバフが青）／ ⚠ **状態の残り時間がマスに出ない**
18. ⚠ **本番スキルに `react` と `host: point` が0件**（⚠ **器はあるがプレイヤーには一度も見えない**）

### ⚠ 素材の4段階で増えたもの

19. ⚠ **等級4〜10の鍛冶コスト7個が勘**（⚠ **`forge_cost_by_grade` の3〜9番目。⚠ バランスの回で直す**）
20. ⚠ **分解の返却率 0.5 が勘**（⚠ **`dismantle_refund_ratio`**）
21. ⚠ **段階④の入口がショップだけ**（⚠ **ステージが3本しか無い。⚠ 増えたら落とす**）
22. ⚠ **素材欄の並びが Dictionary のキー順**（⚠ **`sort_order` ではない**）
23. ⚠ **`Balance` の枠が9本になった**（⚠ **割り当て漏れで全画面が落ちる形が1つ増えた。⚠ `null` のとき赤で直し方を言うようにはしてある**）
24. ⚠ **新規セーブから装備の検証に到達する経路が `F4` しか無い**（⚠ **作業場で作ると30分待ち**）
25. ⚠ **`shop.json` は `daily` しか持っていない**（⚠ **状態には `weekly` / `monthly` がある**）
26. ⚠ **`E118` は `shop.json` と `recipes.json` の枝が未実測**（⚠ **`stages` と `research` では実測済み**）

### 片付け

27. **検証用のものはリリース前に消す**（`stage_dbg_*` ／ `skill_dbg_*` ／ `st_dbg_*` ／ `char_debug_*` ／ `enemy_dbg_*` ／ `summons.json` ／ `tests/debug_boot` ／ `tests/debug_overlay` ／ ⚠ **`ui_status_ch_*` の45行**）
28. ⚠ **フォルダを増やしたら定数に1行足す**（`CHARACTER_DIRS_REQUIRED` / `ENEMY_DIRS_*`）

---

## 6. 終わったあと

**このファイルを、次のタスクの内容に書き換える。**
⚠ **`debug_boot` の `SCENARIOS` は消さない**（次の回で使い回す）。
⚠ **次は ③作業場の廃止**（決定5・決定6）。
