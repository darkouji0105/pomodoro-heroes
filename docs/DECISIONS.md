# 決定表：**いま効いている値だけ**（2026-09-21 新設）

> ⚠⚠ **人間の決定（2026-09-21）：⚠ 台帳の役割を3つに分けた。**
>
> | ファイル | 役割 | 人間の言葉 |
> |---|---|---|
> | `docs/01_plan/PLAN_◯◯.md` | **とっかかり**（⚠ 作る前の設計・⚠ なぜそうしたかの経緯） | ⚠ **「plan はとっかかりだと思う」** |
> | `docs/NEXT_STEPS.md` | **いまの議題**（⚠ 進行中の回） | ⚠ **「nextsteps はいまの議題だと思う」** |
> | **このファイル** | ⚠⚠ **いま効いている値**（⚠ 1決定＝1行） | ⚠ **「決定表を作るのはいいと思う」** |
>
> ⚠⚠ **人間の見立て：⚠ 「⚠ それ（＝過去の回）が溜まってるのが問題かも」。**
> ⚠ **＝`NEXT_STEPS.md` に15回ぶんの節が積まれ、⚠ 1つの決定が3つの節に散っていた。**

---

## このファイルのきまり

1. ⚠⚠ **値を書くのはここだけ。** ⚠ `PLAN` と `NEXT_STEPS` の本文は**番号を参照する**（⚠ 値を書き写さない）。
   ⚠ 理由は `CLAUDE.md` 4番（⚠ **状態にマスターデータを複製しない**）と同じ。⚠ 2か所に書くと必ず食い違う
2. ⚠⚠ **覆したら行を上書きし、⚠ 旧文は「覆した相手」の欄に取り消し線で1行だけ残す。** ⚠ 行を増やさない
3. ⚠ **「どこが持っているか」は実コードを `grep` して書く。** ⚠ ドキュメントから写さない
4. ⚠ **リリース後に ID を改名しない**（`CLAUDE.md` 4番）。⚠ この表の `item_id` / `recipe_id` / ノードID も同じ
5. ⚠ **番号は使い回さない。** ⚠ 取り消した決定の番号も空けたままにする

---

## カテゴリ

| 章 | 記号 | 何の決定か |
|---|---|---|
| 0 | `CN-n` | ⚠⚠ **コンセプト**（⚠ 2026-09-23 に新設。⚠ **ほかの章はこれに従う**） |
| 1 | `UI-n` | **共通・見た目**（Theme・ボタン・絵・窓） |
| 2 | `NAV-n` | **画面と導線**（戻る・遷移・どの画面に出すか） |
| 3 | `BT-n` | **戦闘** |
| 4 | `BS-n` | **拠点・倉庫・装備** |
| 4-2 | `EQ-n` | ⚠⚠ **装備・鍛冶・遺物**（⚠ 2026-09-23 に新設。⚠ **ほとんど未実装**） |
| 5 | `GR-n` | **育成** |
| 6 | `RUN-n` | **ラン共通**（⚠ シナリオと難ダンジョンの両方に効く） |
| 7 | **1〜47** | **難ダンジョン**（⚠ 番号は 2026-09-02 からの通し） |
| 8 | `RS-n` | **資源と演出** |
| 9 | `MD-n` | **窓・モーダル**（⚠ 2026-09-21 に新設） |

⚠ **出どころの `§` は `docs/03_log/NEXT_STEPS_2026-09.md` の節。**

---

## 0. コンセプト（`CN-n`）（**⚠ 2026-09-23・コンセプトの回**）

⚠ **出どころは `docs/03_log/PRE_PLAN_WEAPON_RELIC.md`**（⚠ 人間が別の AI と相談して持ち帰ったもの）の §1〜§3。
⚠ 同じ文書の §4〜§7（⚠ 武器・鍛冶・遺物）は **`EQ-n`（4-2 章）** に入れた（⚠ 同 §0-2 の A・B・C・F を人間が裁いたあと）。

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **CN-1** | ⚠⚠ **本線はピッチA**（⚠ ポモドーロをこなすと報酬がもらえる育成ゲーム）。⚠ ピッチB（⚠ ローグライク）の原則は**Aの内側に置き直す** | — | `docs/00_concept/CONCEPT.md` が本線 ／ `game_design_doc.md` は従 | **09-23** | ⚠ 2本のコンセプト文書が並立していた（⚠ どちらが上か決まっていなかった） |
| **CN-2** | ⚠⚠ **ポモドーロがくれたものは決して失わない。⚠ ゲームの中で拾ったものに限って、安全は犠牲と引き換え** | — | ⚠ 決定7（⚠ 全ロストはダンジョン内で手に入れたものだけ）と同じ線 | **09-23** | ~~`PLAN_INVENTORY.md` §4-1「倉庫が満杯ならポモドーロの報酬を押し戻す」~~ |
| **CN-3** | ⚠ **1プレイ＝休憩1回**（⚠ 1ランではない）。⚠ **区画（5層）で必ず中断・再開できる** | ⚠ 1ランは 75層のまま（決定26） | ⚠ 中断はいま**どのノードでも**できる（`dungeon_map.gd` `floor_map.gd` の「拠点へ」→「続きから」） | **09-23** | ⚠ ピッチB「1ラン＝1プレイ」 |

---

## 1. 共通・見た目（`UI-n`）

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **UI-1** | ⚠⚠ **見た目の値を持つのは2つだけ。⚠ `theme_override_` を書かない** | — | `tools/theme_builder.gd` ／ `theme/main_theme.tres` | 09-07 | ~~シーンに `theme_override_` を書く（69箇所）~~ |
| **UI-2** | ⚠⚠ **ボタンは5階層** | 既定(Secondary) / 真鍮 / Ghost / 赤 / **戻る** | `UiButton.Variant`（⚠ `BACK` は末尾） | 09-14 | ~~4階層~~ |
| **UI-3** | **`SecondaryButton` という variation は作らない**（⚠ 基底 `Button` が Secondary） | — | `theme_builder.gd` | 09-07 | — |
| **UI-4** | **variation の名前は「用途」で付ける。⚠ 大きさで付けない** | — | `theme_builder.gd` | 09-07 | — |
| **UI-5** | **`BackButton` は革の茶＋真鍮の枠** | `4a3526` / `a8791f` | `theme_builder.gd` | 09-14 | ~~青緑（「目立つがマッチしない」で却下）~~ |
| **UI-6** | ⚠⚠ **窓の縁は全部のモーダルに付ける。⚠ 題の帯は任意** | — | `WindowPanel` / `WindowTitlePanel` / `WindowTitleLabel` | **09-21** | ~~倉庫の開封結果だけ（09-18 の前）~~ ／ ~~**帯も全部のモーダルに付ける（09-18）**~~ → ⚠ **`MD-1` へ** |
| **UI-7** | **絵は `IconTextures`（SVG）→ 無ければ `Glyphs`（絵文字）に落ちる** | 線画36枚 | `IconTextures.ICON_SUFFIX` | 09-08 | — |
| **UI-8** | **自前で `_draw()` する部品も値は Theme が持つ** | — | Theme に `TimerRing` / `SetDots` / `ResourceGainEffect` / `ResourceChip` の型 | 09-10 | — |
| **UI-9** | ⚠ **NotoSansJP に絵文字は無い**（⚠ 絵文字フォントが入るまで段階19-a は着手しない） | — | `scenario=glyphs` | — | — |

## 2. 画面と導線（`NAV-n`）

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **NAV-1** | ⚠⚠ **戻るは全画面で左上・文字は「戻る」** | — | `UiButton.Variant.BACK` | 09-14 | ~~戻るの文言は行き先の名前~~ |
| **NAV-2** | ⚠⚠ **例外は窓。⚠ 窓は戻る先を持たないので下に「閉じる」** | 戦闘の結果窓 ／ 拾いもの | `BattleResultView` / `RunLootWindow` | 09-20 | ~~窓も左上に「戻る」~~ |
| **NAV-3** | **画面遷移は `SceneManager` だけ。⚠ `change_scene_to_file()` を直に呼ばない** | — | `autoload/scene_manager.gd` | — | — |
| **NAV-4** | ⚠ **右上の常駐の通貨を消すのは難ダンジョンだけ**（⚠ ＋戦闘・ポモドーロ） | — | 各画面の `_ready()` の `ResourceHud.set_shown(false)` | 09-20 | ~~シナリオでも消す（09-20 の前半）~~ |
| **NAV-5** | ⚠ **拒否仕様**（⚠ 作らない） | 戦闘プレビュー画面 ／ ボス画面 ／ イベント交換所 ／ DLCショップ ／ タイマー装飾の共有 | `AGENTS.md` 拒否仕様 | — | ~~「ダンジョン」の行（決定4 で消した）~~ |

## 3. 戦闘（`BT-n`）

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **BT-1** | ⚠⚠ **戦闘中は右上の資源を全部出さない** | — | `battle_controller.gd:167` | 09-16 | — |
| **BT-2** | **層を出すのはフロアと難ダンジョンのときだけ**（⚠ ステージ直行は「波 n / m」） | — | 戦闘のヘッダ | 09-16 | — |
| **BT-3** | ⚠⚠ **状態異常のチップは「地の色＋白字」の2色で決める** | バフ=`58a7ee` ／ デバフ=`c4534a` ／ 復活=`f0c04a` | Theme | 09-17 | ~~色は3つだけ（赤・緑・青）・2026-08-22~~ |
| **BT-4** | **良い／悪いは付けたときの中身から自動で決める**（⚠ JSON に欄を足さない） | — | `status_registry.gd` | 09-17 | — |
| **BT-5** | **シールドだけの状態はチップを出さない**（⚠ HPバーに継ぎ足してあるため） | — | 戦闘の HUD | 09-17 | — |
| **BT-6** | ⚠⚠ **結果窓は `Modal` を使わず `battle.tscn` の中の専用部品** | — | `BattleResultView` | 09-17 | — |
| **BT-7** | **「拠点へ」はランを残す・確認を出さない**（⚠ 例外＝フロアで負けたときだけ `abandon_floor()`） | — | `battle_controller.gd` | 09-17 | — |
| **BT-8** | ⚠⚠ **敵の行動予告は SP ゲージ**（⚠ ボス以外にも付ける・⚠ クールダウンでは止めない） | ゲージは HP の下 | 敵の HUD | 09-18 | ~~ボスだけ~~ |
| **BT-9** | **スキルはキーでも撃てる**（⚠ QWERTY の順で右から） | — | 戦闘の下部パネル | 09-17 | — |
| **BT-10** | **顔は `CharacterAvatar` を使い回す**（⚠ 部品を2つに増やさない） | 正方形 40 | `CharacterAvatar` | 09-16 | ~~モックの 46 x 40~~ |

## 4. 拠点・倉庫・装備（`BS-n`）

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| ~~**BS-1**~~ | ⚠⚠ ~~**倉庫は OS の別窓**~~ → ⚠⚠ **画面の中に置く**（⚠ **`BS-15` へ**） | — | ⚠ `InventoryWindow` は**消す** | **09-22** | ~~ゲームの中に出す（使い比べて消した）~~ ／ ⚠⚠ **09-15 の自分を覆した** |
| ~~**BS-2**~~ | ⚠⚠ ~~**ギルドの倉庫のカードは消す**~~ → ⚠⚠ **戻す**（⚠ **`BS-15` へ**） | — | `GuildScreen.GUILD_SCENES` | **09-22** | ⚠⚠ **09-15 の自分を覆した** |
| ~~**BS-3**~~ | ~~**出すのは拠点とギルド系だけ**~~ → ⚠ **器ごと消える**（⚠ **`BS-15` へ**） | — | ~~`InventoryWindow.ALLOWED_SCENES`~~ | **09-22** | ⚠ **`BS-1` と一緒に落ちた** |
| **BS-4** | ⚠⚠ **宝箱は拠点のバッジから開ける。⚠ 倉庫の宝箱タブは消す** | 1つずつ／まとめての両方 | `ChestPanel` | 09-15 | — |
| **BS-5** | **宝箱の一覧はモーダルにしない**（⚠ モーダルは同時に1枚。⚠ 結果の窓が出なくなる） | — | `ChestPanel` | 09-15 | — |
| **BS-6** | ⚠⚠ **操作はドラッグ。⚠ マスが「どのマス目から来たか」を運ぶ** | — | `ItemSlot` | 09-15 | — |
| **BS-7** | **組を持たない器（倉庫・鞄・宝箱）は別のマス目から受けない** | — | `ItemGrid` | 09-15 | — |
| **BS-8** | **つまんだ品はカーソルの絵になる** | — | `drag_cursor` | 09-15 | — |
| **BS-9** | ⚠⚠ **装備画面は作り直す**（⚠ 「このキャラに着ける・刺す・鍛える」だけを持つ。⚠ 形は**モック待ち**） | ⚠ いまは3列のまま残っている（`3661672`） | `equipment_screen.tscn` ／ ⚠ 依頼書 `docs/03_log/PRE_PLAN_INVENTORY_REWORK_MOCK.md` | **09-23** | ~~**3列・右＝持ち物（09-22）**~~ |
| **BS-10** | ⚠⚠ **拠点の持ち物はマス目にしない**（⚠ ページ・枠を買う・並べ替えも持たない） | — | ⚠ いまは `InventoryConfig` ／ `INVENTORY_ORDER` ／ `move_inventory_slot()` ／ `expand_inventory()` が残っている（⚠ 消すのは実装の回） | **09-23** | ~~**1ページ 100 マス（10 × 10）× 5（09-08）**~~ |
| **BS-11** | ⚠⚠ **装備の個体は `add_to_inventory()` だけが作る** | — | `game_manager.gd` `_create_equipment_instance()`（⚠ 呼び元1箇所） | — | — |
| **BS-12** | **Linux（Wayland）は対象にしない** | — | — | 09-15 | — |
| **BS-13** | **設定を作るときはゲームのセーブと別のファイルにする** | ⚠ 設定画面はまだ無い | `user://settings.cfg`（⚠ 予定） | 09-15 | — |
| **BS-14** | ⚠⚠ **装飾の枠はマス。⚠ 押すと吹き出し**（⚠ **09-22 に細くした**＝⚠ 一覧は吹き出しに入れず**持ち物が光る**） | ⚠ 幅 236（吹き出し） | `PartSlotRow` ／ `SlotActionPopover` ／ `equipment_screen.gd` | 09-22 | ~~文字の行＋右のボタン＋`OptionButton`~~ |
| **BS-15** | ⚠⚠ **拠点の持ち物は「持ち物の画面」1つで扱う**（⚠ 見出しは **装備／装飾／素材／図鑑**）。⚠ **画面は増やさず、いまの倉庫画面を作り替える** | ⚠ 入口は**未決**（⚠ ギルドのカードか拠点の下段か） | `warehouse_screen`（⚠ いまは `InventoryWindow` の中。⚠ 見出しは 持ち物／素材／図鑑） | **09-23** | ~~**持ち物は装備画面の中に置く（09-22）**~~ ／ ~~`BS-1`・`BS-2`・`BS-3`（09-15）~~ |
| **BS-16** | ⚠⚠ **品の操作は「説明の窓」1つに集める**（⚠ 着ける・外す・刺す・鍛える・段階を上げる・壊す・重ねる・捨てる） | — | ⚠ 新しい共通部品（⚠ 倉庫の詳細を切り出す） | **09-22** | ~~部位の行5本＋右のボタン~~ |
| **BS-17** | ⚠⚠ **閉じた装飾の枠は「次の1つだけ」鍵で出す**（⚠ B案） | ⚠ 枠が開く等級 **3,4,5,5,6,7,8,9**（⚠ `PartConfig`） | `PartSlotRow` ／ `part_config.gd` | **09-22** | ~~**未開放はそもそも表示しない（09-08）**~~ |
| **BS-18** | ⚠⚠ **装飾は「鍛える」ではなく「段階を上げる」**（⚠ 混ぜない） | — | `upgrade_part()` ／ `forge_equipment()` | **09-22** | — |
| **BS-19** | ⚠ **等級の色は変えない**（⚠ 等級4〜6 の青系も含む。⚠ 「青系は使わない」より**見分け**を採る） | ⚠ `IconConfig.grade_colors`（10段） | `icon_config.gd` | **09-22** | — |
| **BS-20** | ⚠⚠ **拠点に容量を持たせない**（⚠ 500マスの倉庫をやめる。⚠ **「入らないなら止める」判定も全部消す**） | ⚠ 上限なし | ⚠ いまは `get_inventory_free_slots()` ／ `_inventory_slots_needed*()` の判定が残っている（⚠ 宝箱・製作・ランの持ち帰り ほか。⚠ **本数は実装の回に `grep` で数える**） | **09-23** | ~~`PLAN_INVENTORY.md` 決定2「拠点の倉庫にも容量」・決定8「500マス」（09-02）~~ ⚠ ＝`PLAN_HARD_DUNGEON.md`「拠点の `INVENTORY` は無限のまま」に戻った |

## 4-2. 装備・鍛冶・遺物（`EQ-n`）（**⚠ 2026-09-23・コンセプトの回**）

⚠ **出どころは `docs/03_log/PRE_PLAN_WEAPON_RELIC.md` §4〜§7 と、⚠ 同 §0-2 の A・B・C・F への人間の裁き**。
⚠⚠ **数字は全部仮置き**（⚠ 決定14）。⚠⚠ **この章はほとんど未実装**（⚠ 「どこが持っているか」は**いまの**実装）。

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **EQ-1** | ⚠⚠ **武器はジャンル共有**（⚠ 剣・弓・杖）。⚠ **誰が持てるかはジャンルで決まる** | ⚠⚠ **キャラを増やす前提**（⚠ 人間「⚠ B２」）。⚠ いまは3人・3ジャンルで1対1＝**取り合いはまだ起きない** | ⚠ いまは制限なし（`get_equip_reject_reason()` にキャラの判定が無い） | **09-23** | — |
| **EQ-2** | ⚠⚠ **武器の縦の並び（木・鉄・鋼）は廃止。⚠ 強さの差は等級だけ** | — | ⚠ いまは `weapon_wooden_sword` / `weapon_iron_sword` / `weapon_steel_sword`（`items.json`） | **09-23** | ~~木・鉄・鋼の上位互換~~ |
| **EQ-3** | ⚠ **各ジャンルに型を3つ**（⚠ タンク・速度・スキル）。⚠ 伸ばす軸と下げる軸を必ず対にする。⚠ **武器は防御を持たない** | ⚠ 型の名前は全ジャンル共通 | — | **09-23** | — |
| **EQ-4** | **装飾の枠の並びは全武器共通**（⚠ 型で変えない） | ⚠ `BS-17` のまま | `part_config.gd` | **09-23** | — |
| **EQ-5** | ⚠⚠ **鍛冶のレベル**：⚠ 上がるほど**装備からもらえるステータスに補正**。⚠ 全ジャンル共通。⚠ **鍛えられる等級をレベルで縛らない** | ⚠ 補正の形・レベルの最大は**未決** | — | **09-23** | — |
| **EQ-6** | ⚠⚠ **鍛えるのは等級5→6 から失敗する**。⚠ **失敗は素材を失うだけ**（⚠ 等級は下がらない） | ⚠ 成功率は**未決** | ⚠ いまは失敗しない（`forge_equipment()`） | **09-23** | ~~必ず成功する~~ |
| **EQ-7** | **確定成功のトークン**（⚠ 1つ使うとその1回は必ず成功）。⚠ **宝箱からたまに出る** | ⚠ 出る率は**未決** | — | **09-23** | — |
| **EQ-8** | ⚠⚠ **遺物**：⚠ 等級5〜10 に1つずつ・**6個**。⚠ 点数は**強化の失敗と装備の分解だけ**で貯まる（⚠ 等級5以上のみ） | ⚠ 軸（仮）：5=攻撃 ／ 6=HP ／ 7=防御（物理・魔法の両方）／ 8=HP回復量 ／ 9=ランの獲得ゴールド量 ／ 10=ランのレアドロップ率 ／ ⚠ 貯まる量は **失敗 ＜ 分解 ＜ 作るのにかかったぶん** ／ ⚠ 1段ごとの伸びは逓減 | ⚠ 分解の口は既にある（`dismantle_equipment()`・払ったぶんの **0.5倍**） | **09-23** | — |
| **EQ-9** | **初回の記録**：⚠ **初めて作った装備で全キャラが上がる**（⚠ 遺物と別勘定） | ⚠ 量と軸は**未決** | — | **09-23** | — |
| **EQ-10** | ⚠ **全キャラを強くする仕組みは4本のまま**（⚠ 研究 ／ 遺物 ／ 初回の記録 ／ 鍛冶のレベル） | — | ⚠ 研究は `stat_boost_all` 8ノード | **09-23** | ⚠ 人間「⚠ 別に手間がかからないからいい」 |
| **EQ-11** | ⚠⚠ **作業場で装備を作れる。⚠ 等級1はいつでも作れる** | ⚠ レシピ・素材は**未決** | ⚠ いまの作業場は**装飾だけ**（`recipes.json` 3件） | **09-23** | ~~装備は作れない（拾う・買うだけ）~~ |
| **EQ-12** | ⚠⚠ **装備が直接落ちるのは難ダンジョンだけ** | — | ⚠ いまは**拠点の宝箱**（`chests.json`・11種ぜんぶ）と `dungeon.json`（1件）から出る | **09-23** | ~~拠点の宝箱からも装備が出る~~ |
| **EQ-13** | ⚠⚠ **装備は等級つきで落ちる。⚠ レアな種類は高い等級でしか落ちない**（⚠ 例：**等級9以上**の状態で落ちてくる）。⚠ **レアな種類は狙って作れない。⚠ 高いコストのランダムな製作でだけ作れる**（⚠ 作業場の装飾の抽選と同じ形） | ⚠ しきい値 **9（例）** ／ ⚠ 抽選のコストと表は**未決** | ⚠⚠ **いまは個体が必ず等級1で生まれる**（`_create_equipment_instance()`）＝⚠ **器が変わる**（⚠ `EXEC` を書く側）。⚠ 抽選の製作は既にある（`recipes.json` の `draw`） | **09-23** | ~~落ちる装備は必ず等級1~~ |
| **EQ-14** | **捨てるのはランの鞄だけ。⚠ 拠点では「壊す」「分解する」に一本化** | — | ⚠ いまは倉庫に「捨てる」がある（`warehouse_screen.gd`） | **09-23** | ~~拠点でも捨てられる~~ |

## 5. 育成（`GR-n`）

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **GR-1** | ⚠⚠ **パッシブは総ポイント（獲得した分）で自動解放。⚠ 振らなくてよい** | **20 / 40 / 60 / 80 / 100** | `passives.json` の `unlock_total_points` ／ `_is_candidate_unlocked()` | 09-14 | ~~振り分け済み pt で開く（⚠ 端数で5個目に届かない振り方があった）~~ |
| **GR-2** | **パッシブの表示はステータスノード画面**（⚠ スキル設定からは消した） | — | `stat_node_screen` | 09-14 | — |
| **GR-3** | **割り振りは 10 軸で作る。⚠ ＋は1回1段** | いまは3軸 | `allocatable_stats` | 09-14 | ~~モックのシールド・SP（⚠ 存在しない軸）~~ |
| **GR-4** | **編成のプリセットは参照方式**（⚠ 編成側にキャラの中身を複製しない） | — | `PARTY_PRESETS` | — | — |

## 6. ラン共通（`RUN-n`）（**⚠ シナリオと難ダンジョンの両方に効く**）

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **RUN-1** | ⚠⚠ **器は別のまま。⚠ `FLOOR_RUN` と `DUNGEON_RUN` を1つにまとめない** | — | 台帳 §7 ／ `GameStateKeys` | — | — |
| **RUN-2** | ⚠ **共有するのは「口」と「部品」だけ** | — | `RunMapView` / `run_relic_select` / `RunLootWindow` / `RunHpBar` / `RunPartyStrip` ／ `*_run_bag*` | 09-19 | ~~`floor_1..5` はさわらない（決定3）~~ |
| **RUN-3** | **マップは2枚のまま。⚠ ショップも2枚のまま触らない** | — | `dungeon_map` / `floor_map` | 09-19 | — |
| **RUN-4** | **`GameManager` の口はレリックだけ種類で受ける**（⚠ マップ・たいまつ・視界・進める先は広げない） | — | `get_run_relic_choices` / `take_run_relic` / `is_run_character_downed` | 09-19 | — |
| **RUN-5** | **翻訳キーは `ui_run_*` に寄せない**（⚠ 部品は翻訳済みの文字を受け取る） | — | — | 09-19 | — |
| **RUN-6** | ⚠⚠ **シナリオのルートにも鞄**（⚠ 難ダンジョンと同じ形） | 枠 **8**（仮） | `FLOOR_RUN.bag` ／ `RUN_KIND_FLOOR` | 09-18 | — |
| **RUN-7** | ⚠⚠ **宝箱は「宝箱のまま鞄へ」運び、中身は拠点で引く** | — | `is_chest_item()` ／ `deliver_floor_bag()` | 09-18 | ~~段階19-b の案A「その場で開ける」~~ |
| **RUN-8** | ⚠⚠ **宝箱の中身は開けるときに抽選する** | `PENDING_CHESTS.rewards` は積む時点で空 | `open_chest()` ／ 積む口は `grant_chest()` の1本 | 09-18 | ~~積むときに振る（人間の決定C）~~ |
| **RUN-9** | **道中の戦利品は「そのフロアのふつうの宝箱の表を1回引く」**（⚠ 表を二重に持たない） | — | `dungeon.json` / `chests.json` | 09-18 | — |
| **RUN-10** | **レリック選択の詳細は右に常設**（⚠ ホバーの枠ではない） | — | `run_relic_select` | 09-19 | — |

---

## 7. 難ダンジョン（決定1〜47）

⚠ **出どころの既定は `docs/01_plan/PLAN_HARD_DUNGEON.md`。** ⚠ それ以外だけ書いてある。

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **1** | 難ダンジョンを実装する | 実装中（段階20まで） | — | 09-02 | ~~2026-08-25 決定2「仕様を書かない」~~ |
| **2** | 順番は「難ダンジョン → 簡単なシナリオ」 | 有効 | — | 09-02 | ~~`PLAN_IMPLEMENTATION` 段階16を先にやる~~ |
| **3** | ⚠⚠ **`floor_1..5` とは器を分けたまま、⚠ 部品と口だけ共有する** | 有効 | `RunMapView` / `run_relic_select` / `RunLootWindow` ／ `*_run_bag*`（`RUN_KIND_FLOOR` / `RUN_KIND_DUNGEON`） | **09-21** | ~~`floor_1..5` は今回さわらない（09-02）~~ |
| **4** | `AGENTS.md` の拒否仕様から「ダンジョン」を消す | 済 | `AGENTS.md` 拒否仕様 | 09-02 | — |
| ~~**5**~~ | ~~マップは平面グリッド~~ | **取り消し** | — | 09-02 | 決定12 が正 |
| **6** | 難ダンジョンの台帳は `PLAN_HARD_DUNGEON.md` | ⚠ **値の正はこの表**（2026-09-21〜）。⚠ 経緯は引き続き PLAN | — | 09-02 | ⚠ 2026-09-21 に一部変わった |
| **7** | 全ロストは「ダンジョン内で手に入れたもの」だけ。**装備は含まない** | 有効 | `GameManager` の鞄の口 | 09-02 | — |
| **8** | 「素の MAX HP」と「ランの MAX HP」は別のもの | 有効 | `get_dungeon_max_hp()` | 09-02 | — |
| **9** | ランを出たら MAX HP は元に戻る | 有効 | ランを捨てる口 | 09-02 | — |
| **10** | 蘇生は「蘇生用ポーション」と「休憩場所」の2本 | 有効 | `items.json` / `rest` ノード | 09-02 | — |
| **11** | 入口のコストは**いまは無し** | ⚠ **暫定。リリース前に必ず入れ直す** | — | 09-02 | — |
| **12** | マップは層構造（下から上へ・引き返さない） | 有効 | `_build_dungeon_map()` | 09-02 | ~~決定5（平面グリッド）~~ |
| **13** | 蘇生の復帰は素の MAX HP の **50%**／HP はそのまた **50%** | 50 / 50 | `DungeonConfig` | 09-02 | — |
| **14** | 数値の未決は仮置きで作り、遊んでから測る | 有効 | `PLAN_HARD_DUNGEON` §6-B | 09-02 | ~~数値を実装の前提条件にする~~ |
| **15** | お預けポイントは作らない。**ボスの直後にショップで「続行／撤退」** | 有効 | `dungeon_shop` | 09-02 | — |
| **16** | ランの一時通貨は**1種類** | ⚠ 名前は決定45 | `DUNGEON_RUN` の通貨 | 09-02 | — |
| **17** | ランの中で使う品は専用の `item_type` に分ける | 有効 | `items.json` | 09-02 | — |
| **18** | 戦闘は毎回「戦闘時 MAX HP」で満タンから始まる | 有効 | `battle_controller` | 09-02 | — |
| **19** | ポーションは2件。回復量は素の MAX HP の **30%** | **30** | `DungeonConfig.potion_heal_pct` | 09-02 | ~~ポーション3件~~ |
| **20** | レリックはシナリオと表を共有（`relics.json` 12件） | 有効 | `relics.json` | 09-04 | — |
| **21** | レリック・ショップは**別画面** | 有効 | `run_relic_select` / `dungeon_shop` | 09-04 | ~~マップの中に並べる~~ |
| **22** | 宝箱のマスは**その場で開く**（拠点へ持ち帰らせない） | 有効 | `open_dungeon_chest()` | 09-04 | — |
| **23** | 通路にボーナスとペナルティを置く（たいまつの価値を上げる） | 有効 | `dungeon.json` `edges` | 09-04 | — |
| **24** | 通路の効果は**5本に1本**に付く | **20%** | `DungeonConfig.edge_effect_chance_pct` | 09-04 | ⚠ 09-20 に2件変更（下の 24b / 24c） |
| **24b** | ⚠ **荷物を落とす罠（`trap_bag`）は作らない** | 廃止 | `dungeon.json` から削除済 | **09-20** | ~~決定24 の5通りのうち1つ~~ |
| **24c** | ⚠⚠ **一本しかない道に罠を置かない**（得は置いてよい） | 有効 | `_make_dungeon_edge()` | **09-20** | ~~どの通路にも罠が付く~~ |
| **25** | たいまつの初期等級と見える層 | 初期等級 **1**（1層先） | `DungeonConfig.torch_initial_grade` | 09-04 | — |
| **26** | 1ラン ＝ **3階 × 25層** | 3 / 25 | `dungeon.json` の層 | 09-05 | ~~フロアは上限なし（未決1-c）~~ |
| **27** | 拾ったものは「拾い待ち」に出して、**何を入れるか選ぶ** | ⚠ **画面は残る**（下の決定40） | `DUNGEON_RUN_PENDING_LOOT` | 09-05 | — |
| **28** | 区画＝**5層** ／ 1階に **3個** ／ 入口は **3択** | 5 / 3 / 3 | `segment_layers` `segment_count` | 09-05 | — |
| ~~**30**~~ | ~~区画の中は一本道~~ | **取り消し** | — | 09-05 | 決定38 が覆した（09-20） |
| **29** | 戦利品も「選ばせる」に揃える | ⚠ **決定40 で一部覆った** | — | 09-05 | — |
| **31** | 通路の宝箱は**その場で開けなければ失う** | 有効 | `dungeon_map` | 09-05 | ~~あとから開ける~~ |
| **32** | マップの上にたいまつの等級を出す | 有効 | `dungeon_map` ヘッダ | 09-05 | — |
| **33** | 素材は**最上位の4段（`material_tier` 4）だけ**落とす | 有効 | `dungeon.json` の5つの表 | 09-05 | ~~2段・3段も落とす~~ |
| **34** | 拾いものの画面は戦闘のあとも**毎回出す** | 有効 | — | 09-05 | — |
| **35** | 鞄が満杯のときは下の鞄を押して捨てて入れ替える | 有効 | `dungeon_map` の鞄の帯 | 09-05 | — |
| **36** | 拾いもの・宝箱は画面遷移をやめ、**マップの上に重ねる** | 有効 | `RunLootWindow.open_as_overlay()` | 09-05 | ~~別画面へ遷移~~ |
| **37** | マップを画面の真ん中に出す | 実装済（ずれ −4px） | `dungeon_map` | 09-19 | — |
| **38** | ⚠⚠ **サイドの道は区画ごとに 1〜3 層つづき、出口側に寄せる** | **min 1 / max 3**・中は2本 | `segment_branch_min_layers` `segment_branch_max_layers` `segment_inner_ways` | **09-20** | ~~決定30（中は一本道）~~ ／ ~~合計4マス（09-20 朝）~~ ／ ~~2〜4層（09-20 昼）~~ |
| **39** | モーダルは窓の形式にする | ⚠ **進行中**（窓の縁と題の帯は全モーダルに入った。⚠ 共通モックを依頼中） | `WindowPanel` ほか ／ `docs/03_log/PRE_PLAN_MODAL_UI_MOCK.md` | 09-06 | — |
| **40** | 窓を閉じたら**入るだけ自動で鞄へ**。結果を見せる | 有効 | `RunLootWindow` | 09-19 | ~~決定27・29（必ず選んで入れる）~~ |
| **41** | マップの鞄から**いつでも捨てられる** | 実装済 | `discard_dungeon_bag_item()` | 09-06 | — |
| **42** | ⚠⚠ **素材は鞄の1枠に 10 個まで重なる**（ポーション・宝箱は1個1枠） | **10** | `DungeonConfig.bag_material_stack` ／ `get_run_bag_stack_limit()` | **09-20** | ~~最上級素材5個・ポーション3個（09-06）~~ |
| **43** | 回復量は**とりあえずそのまま** | **30%**（決定19 と同じつまみ） | `DungeonConfig.potion_heal_pct` | **09-20** | — |
| **44** | ⚠ **難ダンジョンの宝箱の中身は合計3個まで** | `count` **1** × 3回引き | `dungeon.json` の `chest` / `boss` | **09-20** | ~~`rolls` を触る（09-06 の読み）~~ ／ ⚠ **拠点の `chests.json` は触っていない** |
| **45** | ⚠⚠ **一時通貨の表示名を「コイン」に変える**（⚠ 拠点の「遺物」`EQ-8` とぶつかるため。⚠ 人間「⚠ ダンジョンの通貨なので、コインとかでいい」） | ⚠ **「コイン」（仮）** ／ ⚠⚠ **未実装**（⚠ いまは「遺物片」のまま） | `ja.csv` の `ui_dungeon_currency` ほか4行 ／ 絵は `icon_res_shard.svg` ／ ⚠ **キーの名前は変えない**（⚠ 文字だけ） | **09-23** | ~~**表示名「遺物片」・改名しない（09-06）**~~ |
| **46** | 持っているレリックをヘッダに出す | 実装済 | `dungeon_map` ヘッダ | 09-19 | ~~出す口が1つも無い~~ |
| **47** | ⚠⚠ **個別調整の単位は「層ごと」** | ⚠⚠ **未実装** | — | **09-20** | ~~「すべてをばらばらで」＝層・区画・敵の種類の3軸（09-06）~~ |

| **48** | ⚠⚠ **ボスの後は「わかれ道の画面」を1枚出す**（⚠ 続けるか戻るかをそこで選ぶ） | ✅ **入った**（`e32b627`） | `scenes/adventure/dungeon_floor_clear.tscn` ／ `dungeon_map.gd` の `_auto_enter_shop_after_boss()` | **09-21** | ~~ボスの直後にショップを見せ、⚠ そこで続行／撤退を選ぶ（決定15・09-02）~~ ／ ~~ボスの後に**自動で**ショップを見せる（09-20・A案）~~ |
| **48-a** | ⚠ **「ここで戻る」は拠点へ直行。⚠ ただし何を手に入れたか見せる** | ✅ 入った | 同上（⚠ `retreat_from_dungeon()` → 拠点） | 09-21 | — |
| **48-b** | ⚠ **マップの「ショップへ」の行は消す**（⚠ 入口がわかれ道の画面に移るので二重になる） | ✅ 入った（⚠ 続行・撤退のボタンも出さない） | `dungeon_map` の `ShopList` ／ `_update_footer()` | 09-21 | ~~行は残す（09-20）~~ |
| **48-c** | ⚠ **これは拒否仕様の「ボス画面」ではない**（⚠ ボスの演出ではなく、⚠ ボスの**後**の分かれ道） | — | `AGENTS.md` 拒否仕様 | 09-21 | — |

| **48-d** | ⚠⚠ **ショップは飛ばせない**（⚠ 人間「⚠ ただ出ればいいだけだから」） | ⚠ 「さらに潜る」→ ショップ → 次の階 | `TransferKeys.DUNGEON_DESCEND_AFTER_SHOP` | 09-21 | — |
| **48-e** | ⚠⚠ **潜るのはショップを出たあと**（⚠ 先に潜ると店が空になる） | — | `dungeon_shop.gd` の `_on_back_pressed()` | 09-21 | — |
| **48-f** | ⚠ **わかれ道の画面に左上の「戻る」は出さない**（⚠ 人間「⚠ 2 は出さない」） | — | `dungeon_floor_clear.tscn` | 09-21 | ~~戻るは全画面で左上（`NAV-1`）~~ ⚠ ＝**窓と同じ例外** |

> ⚠ **決定47 に着手する前に、⚠ 「いま敵と戦利品がどう決まっているか」を `grep` で取ってから設計を出すこと**
> （⚠ 推測で表を作らない。⚠ `PLAN_HARD_DUNGEON` §5-9-3）。

---

---

## 9. 窓・モーダル（`MD-n`）（**⚠ 2026-09-21・モックを見ての裁き＝決定39**）

⚠ **モックは `docs/03_log/modal_ui_mock.html`。⚠ 依頼書は `docs/03_log/PRE_PLAN_MODAL_UI_MOCK.md`。**

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **MD-1** | ⚠⚠ **題の帯は必須ではない**（⚠ 人間「⚠ 帯は必須じゃない」） | ⚠ **窓ごとに出す／出さない** | `ModalDialog` | **09-21** | ~~**帯は全部のモーダルに付ける（09-18・`UI-6`）**~~ |
| **MD-2** | **ボタンの行は下の中央** | — | `ModalDialog` | 09-21 | ~~帯の右に ✕~~ ／ ~~下の右~~ |
| **MD-3** | ⚠⚠ **窓の幅は4段階の固定** | **300 / 400 / 560 / 720** | `ModalDialog` | 09-21 | ~~最小400で中身に合わせて伸びる~~ |
| **MD-4** | ⚠⚠ **「はい」が左・「いいえ」が右**（⚠ 人間「⚠ はいが左じゃないか」） | — | `ModalDialog` | 09-21 | ~~いいえ｜はい（⚠ モックの A案・B案とも）~~ |
| **MD-5** | ⚠⚠ **取り返しのつかない確認は実行を赤に**（⚠ セーブを消す・品を捨てる・装飾を壊す） | `DangerButton` | `ModalDialog` | 09-21 | ~~安全なほうを真鍮にして実行を Ghost に沈める~~ |
| **MD-6** | ⚠⚠ **暗幕は「なし」も作る**（⚠ 人間「⚠ ないパターンも作る」） | ⚠ **なし / 60% / 72% の3通り** | `ModalDialog` | 09-21 | ~~全部 72% にそろえる~~ ／ ~~60%（拾いものだけ72%）~~ |
| **MD-7** | **出入りの演出は入れない** | — | — | 09-21 | ~~入り160ms／出100ms のフェード~~ |
| **MD-8** | ⚠⚠ **窓が続けて出るときは、⚠ 閉じて 150ms 置いてから次** | **150ms** | `ModalDialog` の待ち行列 | 09-21 | ~~窓を残して中身だけ差し替え~~ |
| **MD-9** | ⚠⚠ **長い本文は2行以上で左ぞろえ・⚠ 本文の中だけスクロール**（⚠ 人間「⚠ スクロールもできるように」） | 最大幅 **560** ／ ⚠ 帯とボタンは動かない | `ModalDialog` | 09-21 | ~~中央ぞろえ・最大480~~ |

> ⚠⚠ **`MD-6` の「どの窓で暗幕を出さないか」は未決**（⚠ 器だけ作る）。
> ⚠ **暗幕が無くても後ろは押せないこと**（⚠ `NAV`／決定「後ろは押せない」）は変えない。

---

## 8. 資源と演出（`RS-n`）

| # | 決定（一行） | いま効いている値 | どこが持っているか | 決めた日 | 覆した相手 |
|---|---|---|---|---|---|
| **RS-1** | ⚠⚠ **増加の演出はリソースの移動に紐づける**（⚠ 画面ごとに呼び出しを書かない） | 増えたときだけ ／ 同じフレームは1回にまとめる | `ResourceGainEffect`（`resource_changed` / `material_changed`） | 09-10 | — |
| **RS-2** | ⚠ **一時通貨は状態の資源ではないので、出来事を知っている側が `play()` を呼ぶ** | — | `dungeon_map` ／ `RunLootWindow` | 09-20 | — |
| **RS-3** | ⚠⚠ **`set_muted()` は `play()` に効かない**（⚠ 効くのは自動で流れる経路だけ） | — | `resource_gain_effect.gd` | 09-20 | — |
| **RS-4** | **演出は一番上のレイヤー。⚠ 窓が開いている間は流さず、閉じるまで待つ** | layer **300**（⚠ 窓は 200） | `ResourceGainEffect` | 09-10 | — |
| **RS-5** | ⚠⚠ **通貨3つはページをまたぐ常駐 HUD。⚠ Autoload は増やさない** | layer **40** | `ResourceHud`（⚠ `SceneManager` が root に1つ作る） | 09-10 | — |
| **RS-6** | **素材16件は拠点だけ**（⚠ 全画面に並べると上の帯が埋まる） | — | 拠点画面 | 09-10 | — |
| **RS-7** | **「戻る」と重なる件は画面側が `reserved_width()` ぶん空けて避ける**（⚠ 画面側に数値を書かない） | — | `ResourceHud.reserved_width()` | 09-10 | — |
| **RS-8** | **資源の色の名前を決める口は1本** | 7つとも既存の色の使い回し | `ResourceBar.color_key_for()` | 09-10 | — |
| **RS-9** | ⚠ **バランスの数値は調整のフェーズでまとめて決める** | — | — | — | — |

---

## ⚠⚠ 食い違い（**2026-09-21 に見つけた。⚠ 直していない**）

| # | 何が食い違っているか | どちらが実態か |
|---|---|---|
| **3** | ✅ **書き直した**（2026-09-21・人間「⚠ 3 かきなおしていい」）。⚠ 「さわらない」→「**器は分けたまま、部品と口だけ共有する**」 | ⚠ **台帳 §7「`FLOOR_RUN` と `DUNGEON_RUN` を1つにまとめない」は生きている**。⚠ 共有したのは部品（`RunMapView` ほか）と口（`*_run_bag*`）だけ |
| **6** | ⚠ 決定6 は「台帳は `PLAN_HARD_DUNGEON.md`」 | ⚠ **2026-09-21 から、値の正はこの表。** ⚠ PLAN は経緯（とっかかり） |
| — | ✅ **`PLAN_HARD_DUNGEON.md` §5-9 を実態に直した**（2026-09-21・人間「⚠ 4 直して」） | ⚠ **それでも値の正はこの表。** ⚠ PLAN 側には「正はこの表」と書いた |

---

## 更新履歴

- **2026-09-23（2回目）… 装備・鍛冶・遺物（`EQ-n`）の章を足した**（⚠ 14件）。⚠ 人間の裁き：⚠ A＝**古いほう（遺物片）を「コイン」に**（⚠ 決定45 を覆した）／ ⚠ B＝**キャラを増やす前提** ／ ⚠ C＝**4本のまま** ／ ⚠ F＝**作業場で装備を作る・直接ドロップは難ダンジョンだけ・等級1はいつでも作れる**。
- **2026-09-23 … コンセプト（`CN-n`）の章を足した**（⚠ 人間が止めた回3-b の続き。⚠ 出どころ `docs/03_log/PRE_PLAN_WEAPON_RELIC.md` §1〜§3）。
  ⚠⚠ **本線はピッチA（`CN-1`）**。⚠ **拠点の倉庫をやめた（`BS-20`）**＝⚠ `PLAN_INVENTORY.md` 決定2・8 を覆した。
  ⚠ `BS-9`（3列）・`BS-10`（100マス）・`BS-15`（持ち物は装備画面の中）を上書きした。
  ⚠ **§4〜§7（武器・鍛冶・遺物）はまだ入れていない**（⚠ 同 §0-2 の A・B・C が未決）。
- **2026-09-21（3回目）… 窓・モーダル（`MD-n`）の章を足した**（⚠ 決定39・⚠ モックを見ての裁き9件）。
  ⚠⚠ **`UI-6` の「題の帯は全部のモーダルに付ける」（09-18）を覆した**（⚠ 人間「⚠ 帯は必須じゃない」）。
  ⚠ **縁は全モーダルのまま。⚠ 変わったのは帯だけ。**
  ⚠ **`MD-6`（どの窓で暗幕を出さないか）は未決＝器だけ作る。**
- **2026-09-21（2回目）… カテゴリごとに分けた**（⚠ 人間「⚠ 決定表をカテゴリごとに」）。
  ⚠ 難ダンジョン（決定1〜47）だけだったところへ、⚠ **7章・52件**を足した：
  ⚠ 共通・見た目（`UI-n`）／ 画面と導線（`NAV-n`）／ 戦闘（`BT-n`）／ 拠点・倉庫・装備（`BS-n`）／
  ⚠ 育成（`GR-n`）／ ラン共通（`RUN-n`）／ 資源と演出（`RS-n`）。
  ⚠ **出どころは `docs/03_log/NEXT_STEPS_2026-09.md` の「覆さない決定」14節**
  （⚠ 分けたときに記録の中へ入ってしまい、⚠ 引けなくなっていた）。
- **2026-09-21 … 新設。** ⚠ 人間の決定「⚠ 決定表を作るのはいいと思う」。
  ⚠ 難ダンジョンの決定1〜47 を `PLAN_HARD_DUNGEON.md`（§1・§8 の更新履歴）と
  `NEXT_STEPS.md`（§0-UI-N・§0-UI-P）から1枚にまとめた。
  ⚠ **値は実コードで確かめたものだけ書いた**（`dungeon_config.gd` の `@export` を `grep`）。
  ⚠ **食い違い3件を見つけたが直していない**（⚠ 上の表）。
